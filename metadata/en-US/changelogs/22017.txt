Product README — pitch + charcoal/orange branding, photorealistic launcher icon. Stable product version — About and GitHub releases use Point & Shoot {x.y.z} (no `-beta.N`). Next `/ship` graduates `0.14.0-beta.22` to 0.14.0. Signed `/ship` APKs — publish refuses debug-key fallback; requires `keystore.properties` or `ANDROID_KEYSTORE_*`.
