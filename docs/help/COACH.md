# Coach (other IDEs)

Same recipe as Cursor `/coach` (`.cursor/commands/coach.md`).

Name the next recommended BUILD_PLAN action in one sentence, then the industry reason. For a feature backlog, use `docs/help/IDEAS.md`.
