# Environment variables (copy to .env if needed — never commit .env)
# Device serials and ADB secrets belong in gitignored scripts/pns_adb_device.env
# (copy from scripts/pns_adb_device.env.example).

# Template update checker upstream (optional override)
# TEMPLATE_UPSTREAM=edwardlthompson/agent-project-bootstrap

# Optional local knobs
# PNS_ADB_SERIAL=
# JAVA_HOME=
