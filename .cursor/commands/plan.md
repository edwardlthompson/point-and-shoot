# Feature or ADR plan (Plan Mode)

Read the active `BUILD_PLAN.md` row and grep `KNOWLEDGE_BASE.md` + `docs/AGENT_REGRESSION_MEMORY.md` for the area.

If the change is trivial, skip planning and execute directly. Otherwise propose 1–3 approaches with mandatory **Critique** before coding.

Do not edit code until the user approves the plan. Major trade-offs → append `docs/adr/`.

Begin now.
