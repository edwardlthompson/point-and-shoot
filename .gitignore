# Gradle / IntelliJ / Android Studio
.gradle/
build/
app/build/
captures/
.idea/
*.iml
local.properties
.kotlin/
_gradle_extract/
gradle-*-bin.zip
_enc_test.txt
**/build/

# Probe exports (keep canonical PROBE_RESULTS.md)
PROBE_RESULTS_*.md
hfr-runs/
perf-runs/
pns-live-logcat.txt

# Local adb addressing (USB serial); copy from scripts/pns_adb_device.env.example
scripts/pns_adb_device.env

# macOS / Windows
.DS_Store
Thumbs.db

# Keystores + signing material (NEVER commit)
*.jks
*.keystore
keystore.properties
keystore.b64

# NDK / CMake build outputs
.cxx/

# Release APK copies at repo root (GitHub release assets only)
Point-and-Shoot_*.apk
dist/

# jadx bundle (downloaded on demand by pns_*_apk_decompile.ps1)
tools/jadx/*
!tools/jadx/README.md

# CycloneDX SBOM artifacts emitted on demand by scripts/pns_sbom.ps1
# (gate verifies in-memory; no need to commit the JSON).
docs/sbom.json
