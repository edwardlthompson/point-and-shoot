# Point & Shoot -- Probe build plan

**Audience:** Engineers and **automated coding agents** maintaining `point-and-shoot`.
**Primary orchestration:** `scripts/pns_hfr_autorun.ps1` (ADB, `PNS.SWEEP_SIGNAL`, pulled JSON).

---

## 1. Rules for agents (read first)

These rules reduce drift ("slipping") between documentation and reality.

1. **Never change `- [ ]` to `- [x]` without completing Section 3 *Verification-before-tick* for that row.** Unchecked means "not proven in-session" or "scope still partial."
2. **After any Kotlin/Java or `scripts/*.ps1` change**, run the toolchain gate (`.\scripts\pns_verify_toolchain.ps1`). Exit code **0** is required before claiming the repo builds.
3. **Docs-only edits** (this file or comments): run `.\scripts\pns_verify_toolchain.ps1 -SkipGradle` before ticking documentation-only items.
4. **Do not mark a phase `[x]` while Section 9 *Status* still says *Partial*** unless you first rewrite the goal/status to match full completion **and** record rationale in **Section 5 Progress log**.
5. **Append** to **Section 5 Progress log** when you verify work -- do not delete historical rows.
6. **Prefer UTF-8** for `.kt` and `.ps1` on Windows; UTF-16 breaks Gradle/PowerShell (see **Section 11 Notes**).
7. **Start-of-list audit:** Before trusting existing `- [x]` rows (after merging branches, long sessions, or when progress feels inconsistent), walk **Section 6 from PH0 down through Infrastructure** in order. For each `- [x]`, confirm **Section 9** *Status* matches the claimed scope, **Section 5** contains matching evidence (or add an audit row), and if Kotlin or `scripts/*.ps1` changed since that evidence, re-run the gate from **Section 2** before building on that assumption.

---

## 2. Verification gates

| Gate | When | Command |
|------|------|---------|
| **Full toolchain** | Default after code or script changes | `.\scripts\pns_verify_toolchain.ps1` |
| **Equivalent wrapper** | Same checks | `.\scripts\pns_hfr_autorun.ps1 -VerifyToolchain` |
| **Docs / markdown only** | No Kotlin or PowerShell touched | `.\scripts\pns_verify_toolchain.ps1 -SkipGradle` |
| **Quick Kotlin** | Narrow edits (optional shortcut) | `.\gradlew.bat compileDebugKotlin` |
| **CI (GitHub)** | Push or PR to main/master matching **`paths`** | `.github/workflows/toolchain-verify.yml` (full gate on Ubuntu); **`workflow_dispatch`** anytime |
| **Plan doc (GitHub)** | Push/PR touching `PROBE_BUILD_PLAN.md` only (see workflow paths) | `.github/workflows/plan-doc-verify.yml` (`-SkipGradle`) |

Optional: `-ReportDir .\hfr-runs` writes `toolchain_verify_*.txt`. **Exit code 0 = pass, 1 = fail.**

---

## 3. Verification-before-tick (mandatory protocol)

Before setting any checkbox in **Section 6 Master checklist** to `[x]`:

| Step | Action |
|------|--------|
| **A** | Confirm **acceptance criteria** for that row (Section 9 Phases or Section 6 Infrastructure) are satisfied. |
| **B** | Run the appropriate gate from **Section 2** and capture **exit code 0**. |
| **C** | For **device-dependent** behavior, confirm the implementation exists (named screens/probes in Section 9). On-device JSON pulls are optional for "implemented" but required if you claim a specific bugfix. |
| **D** | Add a row to **Section 5 Progress log**: date, item id, evidence summary (`toolchain PASSED`, commit SHA optional). |
| **E** | Only then edit `- [ ]` to `- [x]` in **Section 6**. |

If any step fails, **leave the box unchecked** and describe the gap under **Section 9 Status** for that phase.

---

## 4. Checkbox semantics

| Symbol | Meaning |
|--------|---------|
| `- [ ]` | Not complete, not verified this session, or scope intentionally partial. |
| `- [x]` | Complete **for the stated scope**, verified per Section 3, and logged in Section 5. |

**Infrastructure** rows track host automation quality, not HAL behavior on a specific phone.

---

## 5. Progress log (audit trail)

Append-only. Each verification that supports a checkbox change gets a row.

| Date (UTC) | Item | Evidence |
|------------|------|----------|
| 2026-05-07 | **INFRA-1, INFRA-2**, build health | `pns_verify_toolchain.ps1` -> **PASSED** (`assembleDebug`, UTF-8 + AST parse for `pns_hfr_autorun.ps1`, `pns_probe_watch.ps1`, `pns_verify_toolchain.ps1`). |
| 2026-05-07 | **PH0-PH4, PH8** implementation audit | Sources present: `ExhaustiveMediaProbeScreen.kt`, `EncoderProbeCore.kt`; `PipelineAccessProbe.kt`; `SessionMatrixProbeScreen.kt`; `HdrDcgRuntimeProbeScreen.kt`, `SessionConfigurationCompat.kt`; `LogicalPhysicalProbeScreen.kt` (lensFacing / hardwareLevel). Toolchain PASSED. |
| 2026-05-07 | **DOC** | Full plan rewrite (Sections 1-11). `pns_verify_toolchain.ps1 -SkipGradle` PASSED. |
| 2026-05-07 | **DOC** (audit protocol) | UTF-8 on disk verified (`Format-Hex`); Start-of-list audit rule (Section 1); `pns_verify_toolchain.ps1 -SkipGradle` PASSED. |
| 2026-05-07 | **PH5** | Rescoped Section 9 PH5 to shipped probeScope; `CaptureLatencyProbeScreen.kt` adds root `probeScope`; `RawHdrExclusivityProbeScreen.kt` adds `sessionSupportSummary`; `BurstProbeScreen.kt` adds `burstWallMsPerShot` / `approxBurstFps`. Full toolchain gate after Kotlin edits. |
| 2026-05-07 | **INFRA-2** | `pns_verify_toolchain.ps1`: scan `app/src/**/*.kt` for UTF-16 LE (fail gate); generalized UTF-16 heuristic for `.ps1` / `.kt`. Full toolchain PASSED after change. |
| 2026-05-07 | **PH6, PH7, PH9** | Rescoped Section 9 to shipped scopes; added root `probeScope` to RawHdr + Burst JSON; ticked PH6/PH7/PH9. Full toolchain after Kotlin UTF-8 conversion. |
| 2026-05-07 | **PH6/PH7/PH9 verify** | `pns_verify_toolchain.ps1` **PASSED** (assembleDebug, scripts, Kotlin UTF-8 scan); Section 9 PH6-PH9 bodies synced.|
| 2026-05-07 | **INFRA-2 + INFRA-3** | Cross-platform Gradle in `pns_verify_toolchain.ps1`; added `.github/workflows/toolchain-verify.yml` (Ubuntu, SDK 36). Local `pns_verify_toolchain.ps1` **PASSED**. |
| 2026-05-07 | **INFRA-4** | `dependabot.yml` + workflow: `gradle/actions/setup-gradle@v4`, concurrency, `workflow_dispatch`. Full local `pns_verify_toolchain.ps1` **PASSED**. |
| 2026-05-07 | **INFRA-5** | CI `paths` filters on `toolchain-verify.yml`; manual `workflow_dispatch` for exceptions. Local `pns_verify_toolchain.ps1` **PASSED**. |
| 2026-05-07 | **INFRA-5 (sync)** | Restored `paths` filters in `toolchain-verify.yml` (push/PR); included `native/**` for future NDK wiring. Local `pns_verify_toolchain.ps1` **PASSED** (full gate after workflow edit). |
| 2026-05-07 | **INFRA-6** | Added `plan-doc-verify.yml`; toolchain `paths` incl. `.github/workflows/**`. `pns_verify_toolchain.ps1 -SkipGradle` **PASSED** after workflow/plan edits. |
| 2026-05-07 | **Device / sideload (pre-commit)** | Moved Grant-CameraPermission above Install-PnsDebugApk in pns_hfr_autorun.ps1; UTF-8 save; full pns_verify_toolchain.ps1 **PASSED**; -SideloadOnly -SkipGradleBuild installed on adb device 8bf09993; pm path dev.pointandshoot OK. |
| 2026-05-07 | **Smoke (device)** | `-RunProbeSmoke -SkipSideload -SkipGradleBuild`; pulled `deep_caps_*.json` + `session_matrix_*.json` with done signals; `suite_run_summary_smoke_*.txt` in `hfr-runs/`. Full `pns_verify_toolchain.ps1` **PASSED** same session. |
| 2026-05-07 | **Release APK** | `-AssembleReleaseOnly` exit 0 (internal signing). Preceded by full `pns_verify_toolchain.ps1` **PASSED**. |
| 2026-05-07 | **Core suite (device)** | `-RunCoreProbePlan -ExhaustiveHfrOnly -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 60` on adb `8bf09993`; `suite_run_summary_core_*.txt`, `phase9_thermal_core_*.txt`, `exhaustive_probe_*.json` under `hfr-runs/`. **Note:** `CAPTURE_LATENCY_DONE ok=false` (no JSON pulled); `ENC_PROBE_DONE ok=false` (no enc JSON). |
| 2026-05-07 | **PH5 device validation**, SessionConfiguration API 35+ reprocess path | **CPH2655** (adb `8bf09993`), Android 16: after sideload, `-RunCaptureLatency -MaxRuns 0 -SkipSideload -SkipGradleBuild` yields `CAPTURE_LATENCY_DONE ok=true`, pulled `capture_latency_20260507_171448.json`; reprocess probe `reprocessInputToJpegSessionSupported=true`. `-MaxRuns 1` encoder run: `ENC_PROBE_DONE ok=true`, pulled `hfr_run1_20260507_171549_enc_probe.json`. Code: API 35+ uses `SessionConfiguration(sessionType, outputs)` + `setInputConfiguration` (Builder not visible to reflection on this build); UTF-8 fixes for `SessionConfigurationCompat.kt` / `CaptureLatencyProbeScreen.kt`; defensive JSON on session-config build failure. `pns_verify_toolchain.ps1` **PASSED** same session. |
| 2026-05-07 | **PH6 + PH7 device validation** (piecemeal) | adb `8bf09993` (CPH2655): `-Sideload -RunRawHdrExcl -RunBurstProbe -MaxRuns 0 -SkipGradleBuild` after `pns_verify_toolchain.ps1` **PASSED**. `RAW_HDR_EXCL_DONE … ok=true`, pulled `raw_hdr_excl_20260507_172155.json` (root `probeScope`). `BURST_PROBE_DONE … ok=true`, pulled `burst_probe_20260507_172156.json` (root `probeScope`). |
| 2026-05-07 | **PH9 thermal snapshot** (host) | `-ThermalSnapshotOnly -OutDir .\hfr-runs` on adb `8bf09993`; wrote `phase9_thermal_standalone_20260507_172317*.txt`. Preceded by `pns_verify_toolchain.ps1 -SkipGradle` **PASSED** after §5 doc append. |
| 2026-05-07 | **Core suite (device) refresh** | adb `8bf09993` (CPH2655 / Android 16): full `pns_verify_toolchain.ps1` **PASSED**, then `-RunCoreProbePlan -ExhaustiveHfrOnly -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 45 -ProgressIntervalSeconds 90`. `CAPTURE_LATENCY_DONE … ok=true`, pulled `capture_latency_20260507_172511.json`; `ENC_PROBE_DONE … ok=true`, pulled `hfr_run1_20260507_173322_enc_probe.json`; exhaustive `exhaustive_20260507_172642.json`; `suite_run_summary_core_20260507_173538.txt`; `phase9_thermal_core_20260507_173537*.txt`. |
| 2026-05-07 | **Full suite (device)** | adb `8bf09993` (CPH2655): full `pns_verify_toolchain.ps1` **PASSED**, then `-RunFullSuite -ExhaustiveHfrOnly -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 45 -ProgressIntervalSeconds 90`. All phases through exhaustive + encoder completed (exit 0). `CAPTURE_LATENCY_DONE … ok=true` (`capture_latency_20260507_173803.json`); `ENC_PROBE_DONE … ok=true` (`hfr_run1_20260507_174618_enc_probe.json`); exhaustive `exhaustive_20260507_173937.json`; `suite_run_summary_full_20260507_174820.txt`; `phase9_thermal_full_20260507_174819*.txt`. **Legacy Camera1:** `LEGACY_CAM1_DONE … ok=false`; host reported no `legacy_camera1_*.json` pulled (investigate app/logcat if needed). |
| 2026-05-07 | **Full suite + FULL exhaustive matrix** (device) | adb `8bf09993`: `-RunFullSuiteFullMatrix -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 45 -ProgressIntervalSeconds 90` (~49m wall). **Exhaustive:** host timeout before `EXHAUSTIVE_PROBE_DONE` (no `exhaustive_probe_*.json` pulled; log `exhaustive_20260507_175051_*`). Encoder + Phase 9 + summary still ran: `ENC_PROBE_DONE … ok=true`, `hfr_run1_20260507_183554_enc_probe.json`, `phase9_thermal_full_matrix_20260507_183806*.txt`, `suite_run_summary_full_matrix_20260507_183807.txt`. **Retry guidance:** raise `-ExhaustiveTimeoutMinutes` (e.g. 90–120) for full matrix on this hardware. |
| 2026-05-08 | **Legacy Camera1 (composition) + full_matrix exhaustive** | **Fix:** `LegacyCamera1ProbeScreen` runs `runLegacyCamera1Probe` on `LegacyCam1WorkScope` (SupervisorJob + IO), `DisposableEffect` cancels job; avoids `LeftCompositionCancellationException` when host navigates away during headless run (same pattern as encoder probe). **Piecemeal:** `-Sideload -RunLegacyCamera1 -MaxRuns 0 -SkipGradleBuild` on adb `8bf09993` → `LEGACY_CAM1_DONE … ok=true`, pulled `legacy1_20260507_184218.json`. **Full suite full matrix:** `-RunFullSuiteFullMatrix -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 120 -ProgressIntervalSeconds 120` (~79m wall) → `EXHAUSTIVE_PROBE_DONE … ok=true`, `exhaustive_20260507_184430.json`; in-suite legacy `legacy1_20260507_184426` ok=true + JSON; `hfr_run1_20260507_195938_enc_probe.json`; `phase9_thermal_full_matrix_20260507_200138*.txt`; `suite_run_summary_full_matrix_20260507_200139.txt`. `pns_verify_toolchain.ps1` **PASSED** after Kotlin change. |
| 2026-05-08 | **Chained regression (device)** | adb `8bf09993`: `pns_verify_toolchain.ps1` **PASSED**, then **core** `-RunCoreProbePlan -ExhaustiveHfrOnly -MaxRuns 1 -SkipGradleBuild -ExhaustiveTimeoutMinutes 45` → `CAPTURE_LATENCY_DONE … ok=true` (`capture_latency_20260507_200420.json`), `ENC_PROBE_DONE … ok=true` (`hfr_run1_20260507_201233_enc_probe.json`), `suite_run_summary_core_20260507_201442.txt`, `phase9_thermal_core_20260507_201442*.txt`. Immediately **full** `-RunFullSuite … -SkipSideload -SkipGradleBuild` (same timeouts) → `LEGACY_CAM1_DONE … ok=true` + JSON (`legacy1_20260507_201630.json`), `ENC_PROBE_DONE … ok=true` (`hfr_run1_20260507_202315_enc_probe.json`), `suite_run_summary_full_20260507_202520.txt`, `phase9_thermal_full_20260507_202519*.txt`. Combined wall ~21m; exit 0. |
| 2026-05-08 | **Smoke + thermal (device)** | adb `8bf09993`: `-RunProbeSmoke -SmokeIncludeThermal -SkipSideload -SkipGradleBuild` after chained regression; `deep_caps_20260507_202555.json`, `session_matrix_20260507_202600.json`, `suite_run_summary_smoke_20260507_202601.txt`, `phase9_thermal_smoke_20260507_202602*.txt`. Exit 0. |
| 2026-05-08 | **INFRA-2 (FOSS dep-audit gate)** | Added Gradle dep-audit pass to `pns_verify_toolchain.ps1`: rejects `com.google.android.gms`, `com.google.firebase`, `com.google.mlkit`, `com.google.android.play`, `com.android.billingclient`, `com.google.ads*`, `com.google.android.libraries.places` references in any `*.gradle{,.kts}` or `gradle/libs.versions.toml`. Full `pns_verify_toolchain.ps1` **PASSED** (`OK: dep-audit (no Play Services / proprietary SDK references in 4 Gradle file(s))`); also covers product-side changes (release signing config, icon resources, README/CHANGELOG/RELEASE_NOTES_TEMPLATE refresh, `.github/workflows/build-signed.yml`, dependabot Gradle ecosystem). |
| 2026-05-08 | **Phase 1/2/3 product scaffolds (Kotlin)** | Added `PnsTheme.kt` (Hasselblad orange `#FF5C00` + record red `#E00000` + monospace HUD typography), `ImagingProfile.kt` (Standard Pro / Ultra-Max), `CaptureHaptics.kt` (30 ms post-readout still tick; manifest `VIBRATE`), `Dng12Saver.kt` (Camera2 `DngCreator` wrapper), `CommandDial.kt` (M / H / S / BKT segmented control), `VideoTallyOverlay.kt` (locked-color border, no haptics), `TimecodeOverlay.kt` (`HH:MM:SS:FF` with rec/standby dot), `AboutScreen.kt` (heritage tribute + "What works on OnePlus 13 (dodge)" recipes + known-bad paths). `MainActivity` now wraps content in `PnsTheme`; probe home exposes "About / Heritage" button. Full `pns_verify_toolchain.ps1` **PASSED** after the changes (`assembleDebug` + dep-audit + UTF-8 + parse). |
| 2026-05-08 | **HUD settings + Pro HUD harness + vendor-key guard + storage strategy** | Added `HudSettings.kt` (SharedPreferences-backed; `pns_hud_settings`), `HudSettingsScreen.kt` (Compose `Switch` rows; reset-to-defaults), `ProHudScreen.kt` (composite of CommandDial + TimecodeOverlay + VideoTallyOverlay + readouts + record/profile toggles), `VendorKeyGuard.kt` (centralized feature detection + `useIfAvailable {}` for vendor request/session/characteristic keys; logs `PNS.VendorKey present/absent`). New navigation buttons "Pro HUD (preview)" and "Settings > HUD" wired into `CameraCapabilitiesProbe` home. Wrote `STORAGE_STRATEGY.md` (default destinations per profile, validation gates, open questions). Initial Kotlin compile failed once on a missing `androidx.compose.runtime.remember` import in `HudSettingsScreen.kt`; fixed in same session. Final `pns_verify_toolchain.ps1` **PASSED**. |
| 2026-05-08 | **Phase 1 capture-pipeline scaffolds + diagnostics + CI artifact** | Added `CaptureStorage.kt` (MediaStore writer with `IS_PENDING` lifecycle; per-profile `Pictures/Point & Shoot/...`; `Handle.close/discard`), `BracketPlan.kt` (centered 3/5/7 EV plan + stable `bracketGroupingId`), `HighlightMeter.kt` (95th-percentile-luma -> EV correction; pure function over a 256-bin histogram, clamped to ±3 EV), `TapToShootHandler.kt` (`Modifier.tapToShoot(callbacks)` -> down lock / up fire / cancel release), `DiagnosticsMode.kt` (toggle in `pns_diagnostics`; `dump` writes logcat + `getExternalFilesDir(null)/diagnostics_<utc>.txt`). Probe home gains "Diagnostics dump" button (Toast confirms path). `.github/workflows/toolchain-verify.yml` now uploads `app-debug.apk` as a 14-day workflow artifact (`pns-debug-apk-<sha>`) so contributors can grab the latest debug build without local Gradle. Full `pns_verify_toolchain.ps1` **PASSED** (`assembleDebug` BUILD SUCCESSFUL; APK 31 679 KB; dep-audit clean; UTF-8 + parse OK). |
| 2026-05-08 | **JUnit suite + Eye-AF overlay + capture-architecture / perf-budget docs** | Wired JUnit 4 (`libs.versions.toml junit = "4.13.2"`; `testImplementation` in `app/build.gradle.kts`). Added pure-JVM tests under `app/src/test/java/dev/pointandshoot/`: `BracketPlanTest` (3/5/7 patterns + EV step + grouping-id stability + reject `evStep <= 0`), `HighlightMeterTest` (empty / dark / bright / clamped / percentile sweep), `TimecodeFormatTest` (zero / frame wrap / minute-hour rollover / negative & non-positive guards). Made `formatTimecode` defensive (`elapsedMs.coerceAtLeast(0)`, `max(1, fps)`). Added `EyeAfOverlay.kt` (Compose `Canvas` overlay; green crosshair-decorated rectangles per `EyeMark`). Wrote `CAPTURE_ARCHITECTURE.md` (thread lanes / executors / backpressure / cancellation / failure handling) and `PERFORMANCE_BUDGETS.md` (cold start, per-mode FPS, still-capture latency, video bitrates, memory pressure). `pns_verify_toolchain.ps1` **PASSED**; `:app:testDebugUnitTest` **BUILD SUCCESSFUL** with all suites green. |
| 2026-05-08 | **CI tests + verify -RunTests + README docs index + FAILURE_MATRIX** | `.github/workflows/toolchain-verify.yml` now runs `:app:testDebugUnitTest` on every push / PR; on failure it uploads `app/build/reports/tests/**` and `app/build/test-results/**` as `pns-unit-test-reports-<sha>` (7-day retention). `scripts/pns_verify_toolchain.ps1` gains a `-RunTests` switch (mirrors the CI step locally). `README.md` Documentation index reorganized into Product / Engineering / Releases groups and links the new docs. Wrote `FAILURE_MATRIX.md` (severity-tagged graceful-degradation table covering permissions, camera lifecycle, app lifecycle, capture/encode, vendor-tag misbehavior, thermal/long-session). Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` + `:app:testDebugUnitTest` BUILD SUCCESSFUL; APK 31 644 KB; dep-audit clean). |
| 2026-05-08 | **BracketScheduler + filename test + CHANGELOG + lint blocker documented** | Added `BracketScheduler.kt` (maps `BracketPlan.evOffset` -> integer Camera2 AE compensation values via the camera's `Rational` step + `Range`; pure-primitive overload for unit-testability) and `BracketSchedulerTest.kt` (1/1, 1/3, 1/2 EV/step rationals; out-of-range clamping; reference-shot stays at 0; zero / negative step rationals + inverted range rejected). Added `CaptureStorageFilenameTest.kt` (per-profile id, per-kind extension, sequence zero-padding, MIME-type sanity). Consolidated **Unreleased** in `CHANGELOG.md` to enumerate everything shipped this session (theme, Pro HUD, capture-pipeline scaffolds, diagnostics, docs, tests, CI). Attempted to wire `:app:lintDebug` into the gate; AGP 8.7.3 + Compose BOM 2026.04.01 currently crash multiple compose-lint detectors with `IncompatibleClassChangeError` on the Kotlin Analysis API (tooling bug, not project code). Documented as a known limitation in `BUILD_PLAN.md` §0 and via a comment block in `app/build.gradle.kts`; lint is intentionally not part of the gate today. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (compile + JUnit + dep-audit + UTF-8 + parse). |
| 2026-05-08 | **CropPlan + FaceDetectAdapter + CapabilityGate + PnsLog + tests** | Added four host-side, device-independent helpers that satisfy currently-blocked BUILD_PLAN bullets: `CropPlan.kt` (centered-crop math + `MeteringHint` / `AfHint` for the 35/50/85 mm focal-equivalent modes), `FaceDetectAdapter.kt` (active-array -> preview-pixel mapper for `EyeAfOverlay`; handles 0/90/180/270 sensor rotation + front-camera mirror + face-bbox proxy), `CapabilityGate.kt` (pure-data feature gate returning `enabled` + `disabledReason` per `Feature`; `recommendedDefaults` enforces the Standard Pro baseline), `PnsLog.kt` (release-aware logging facade gated on `ApplicationInfo.FLAG_DEBUGGABLE` + `DiagnosticsMode`; `MainActivity.onCreate` calls `PnsLog.init`). Wrote 21 new JUnit tests (`CropPlanTest`, `FaceDetectAdapterTest`, `CapabilityGateTest`, `PnsLogTest`); all green alongside the existing 5 suites. Cleaned up the duplicated `lint {}` blocks in `app/build.gradle.kts` (one consolidated block + the lint-blocker rationale lives there now). Synced `BUILD_PLAN.md` (§3 mapping rows, §4 Eye-AF adapter row, §8 release-logging gate, §8 UX safety nets), `CHANGELOG.md` (Unreleased / Added). Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (compile + 9 JUnit suites + dep-audit + UTF-8 + parse; APK 32 696 KB). |
| 2026-05-08 | **TrackerState (3D-tracking persistence) + tests** | Added `TrackerState.kt` (Nikon-style hysteresis tracker: `acquireFrames` consecutive presents to lock, `keepAliveFrames` consecutive absents before drop; intermittent presence resets the absent streak; pure stateful object, no Android types). Closes the last unaddressed Phase 1 metering/AF item from BUILD_PLAN \u00a74. Wrote `TrackerStateTest.kt` (7 tests: lock acquisition, keep-alive expiry, intermittent presence, multi-id independence, reset, validation, `keepAliveFrames=0` edge case). Synced `BUILD_PLAN.md` (\u00a74), `CHANGELOG.md` (Unreleased / Added + tests list), `README.md` (repo layout). Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (compile + 10 JUnit suites + dep-audit + UTF-8 + parse; APK 32 702 KB). |
| 2026-05-08 | **NDK_PLAN doc + PerfBudget + tests** | Added `NDK_PLAN.md` (concrete plan for the libavif (BSD-2) + libjxl (BSD-3) + highway/brotli native pipeline: pinned-tag `FetchContent` strategy, `NativeEncoders` JNI surface, Gradle/CMake wiring, JPEG fallback when `pns_native.so` is unavailable). Added `PerfBudget.kt` (machine-checkable mirror of `PERFORMANCE_BUDGETS.md`: `check(label, measured, budget, warnFactor=1.25)` returns OK/WARN/FAIL; `checkHapticTick(measuredMs)` enforces the `30 ms \u00b1 5 ms` post-readout window; `Defaults` constants pinned by the test so doc/code drift is caught). Wrote `PerfBudgetTest.kt` (9 tests: under/at/slightly-over/way-over budget, custom warnFactor, validation, haptic tick OK/WARN/FAIL, defaults pin). README docs index gains `NDK_PLAN.md` link; BUILD_PLAN \u00a78 perf-gate row now references `PerfBudget`. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (compile + 11 JUnit suites + dep-audit + UTF-8 + parse; APK 32 708 KB). |
| 2026-05-08 | **LongTele150 (12 MP crop of LYT-600) + tests** | Added `FocalMode.LongTele150` (`displayName = "150mm"`, `zoomFactor = 2.04`, `MeteringHint.CenterWeighted`, `AfHint.EyeAf`) capturing the spec's "12 MP crop of the 50 MP tele sensor" zoom range: `sqrt(50/12) \u2248 2.04x` linear -> `73 mm \u00d7 2.04 \u2248 149 mm` (rounded to 150 mm equivalent). Wrote `CropPlanTest` rows pinning the math at the LYT-600's native `8160 \u00d7 6120`: 12 MP crop = `4000 \u00d7 3000` (12.00 MP), centered with \u00b11 px tolerance. Synced BUILD_PLAN \u00a73 mapping (added 150 mm row), CHANGELOG. Decision recorded in chat: stay with the **full-res-crop** strategy on both `Standard50` and `LongTele150` (binning at the same FOV would force \~3 MP outputs and is the wrong trade-off for "tighter framing for compositional intent" modes). Final verify **PASSED** (compile + 11 JUnit suites + dep-audit + UTF-8 + parse; APK 32 708 KB). |
| 2026-05-08 | **LICENSES.md + license inventory gate + EncoderResultAggregator + tests** | Added `LICENSES.md` (first-party dep inventory: 11 runtime + 1 debug + 1 test + 3 plugin coordinates with SPDX + scope + notes; license-compatibility statement vs Apache-2.0; forward-looking row for the planned NDK pipeline). Added `scripts/pns_license_inventory.ps1` (parses `gradle/libs.versions.toml` `[libraries]` + `[plugins]`; cross-references against an embedded license map mirroring `LICENSES.md`; reports drift in either direction; `-DumpMarkdown` prints a refreshable Markdown table). Wired into `pns_verify_toolchain.ps1` (runs on every gate invocation; encoding + parse-checked alongside the other PowerShell scripts). Closes BUILD_PLAN \u00a78 "Security/F-Droid hygiene: dependency/license scan gate (FOSS-only)". Added `EncoderResultAggregator.kt` (pure-data `summarize(attempts) -> EncoderSummary` with `knownGood` / `knownBad` / `byCamera` / `canonicalErrors`; `bestHfrRecipe(summary, cameraId)` returns the highest-fps known-good HFR per camera; `canonicalize` collapses errno-tail variations to a single key like `errno -38`). Wrote `EncoderResultAggregatorTest.kt` (9 tests: empty input, totals, ordering, per-camera best-fps roll-up, canonical-error grouping, bestHfrRecipe positive/negative, canonicalize patterns, mime extraction). Closes the analytical half of the open Phase 0 V&V gate "which cameraId+size+fps+mimetype combinations are actually stable for HFR encode" (Android-side JSON adapter still pending). Synced README docs index, BUILD_PLAN \u00a72 / \u00a78, CHANGELOG. Final verify **PASSED** (compile + 12 JUnit suites + dep-audit + license-drift + UTF-8 + parse; APK 32 735 KB). |
| 2026-05-08 | **EncoderAttemptJsonAdapter + tests + org.json test-only dep** | Added `EncoderAttemptJsonAdapter.kt` (Android-side JSON adapter: `loadLatest(context)` finds newest `exhaustive_probe_*.json` under `getExternalFilesDir(null)`, parses it, and returns a `LoadResult { sourceFile, generatedAt, runId, attempts }`; pure `decode(JSONObject) -> List<EncoderAttempt>` flattens `cameras[].hfrAttempts` + `cameras[].regularAttempts`, extracts mime via `EncoderAttempt.extractMimeFromNote`, tolerates malformed rows by skipping them; `defaultKind` fallback keeps things sensible if `sessionKind` is missing or unknown). Added `org.json:json` (20240303, JSON-LICENSE \u2248 MIT) as `testImplementation` to provide the real `org.json.JSONObject` on the JVM unit-test classpath (the Android stub throws "Stub!"); declared in `gradle/libs.versions.toml`, `app/build.gradle.kts`, `LICENSES.md`, and `pns_license_inventory.ps1`'s license map. Wrote `EncoderAttemptJsonAdapterTest.kt` (5 tests: hfr+regular flattening across cameras, malformed-row tolerance, missing `cameras` key, `sessionKind` fallback, end-to-end `decode + summarize -> bestHfrFps`). Closes the engine-side "load + decode" half of the Phase 0 V&V gate; only HUD/About-page consumption remains. Synced BUILD_PLAN \u00a72, CHANGELOG, README. Final verify **PASSED** (compile + 13 JUnit suites + dep-audit + license-drift + UTF-8 + parse; APK 31 708 KB). |
| 2026-05-08 | **AUDIT: Verification protocol + full audit of every BUILD_PLAN [x]** | Added a new "Verification protocol" section to `BUILD_PLAN.md` (right after Conventions): tick-eligibility table by item tag (`[HOST] code` / `test` / `doc` / `script` / `gate`, `[ADB]`, `[CI]`); explicit partial-completion convention (host-side scaffolds DO NOT close `[ADB]` items); error-handling rules (failed-gate-after-tick = regression; stale claim = revert + log; doc/code drift = code is ground truth; never remove a test that backs an `[x]`; Dependabot bumps trigger a full audit); audit cadence (per-session re-verify, periodic full audit every 5 sessions or on Dependabot bump, results recorded here); mandatory pre-tick checklist (verify gate, ReadLints, file/symbol existence, test XML green, sync CHANGELOG/README, only THEN flip `[x]`). Then ran the **first full audit**: cross-checked every existing `[x]` line in `BUILD_PLAN.md` against the filesystem + symbol table + test results + script parses + device-run rows in this section. **Outcome: zero drift, zero reverts.** Spot-verified symbols: `ImagingProfile.{StandardPro,UltraMax}`; `CaptureHaptics.scheduleStillTick` + `POST_READOUT_TICK_DELAY_MS = 30L` + `EFFECT_TICK`; `PnsColors.{PhotoOrange #FF5C00, RecordRed #E00000}` + `MonoFamily = FontFamily.Monospace`; `AboutScreen.{TRIBUTE_TEXT, KNOWN_GOOD_RECIPES, KNOWN_BAD_PATHS}`; `CommandDialMode.{M,H,S,BKT}`; `FocalMode.{Street35, Standard50, Portrait85, LongTele150}`; `TrackerState.{acquireFrames, keepAliveFrames, update}`; `PerfBudget.{check, checkHapticTick}`; `PnsLog.{init, shouldEmitVerbose, FLAG_DEBUGGABLE}` + `MainActivity` calls `PnsLog.init`; `CapabilityGate.{evaluate, recommendedDefaults, HardwareCaps}`; `EncoderResultAggregator.{summarize, bestHfrRecipe, canonicalize}`; `EncoderAttemptJsonAdapter.{loadLatest, decode}`; `VendorKeyGuard.{useIfAvailable, isCharacteristicKeyAvailable, isRequestKeyAvailable, isSessionKeyAvailable}`; `CaptureStorage.{openOutput, Handle, IS_PENDING}` with `ROOT_RELATIVE_PATH = "Pictures/Point & Shoot"` matching `STORAGE_STRATEGY.md`; `DiagnosticsMode.{setEnabled, isEnabled, pns_diagnostics}`; `Modifier.tapToShoot` + callbacks; `AndroidManifest` has `VIBRATE` + `@mipmap/ic_launcher` + `MainActivity`; `.github/workflows/toolchain-verify.yml` runs `:app:testDebugUnitTest`; `.github/workflows/build-signed.yml` has `workflow_dispatch` + apksigner verify; `app/build.gradle.kts` `signingConfigs.release` reads `ANDROID_KEYSTORE_PATH` env or `keystore.properties`; `gradle/libs.versions.toml` pins `agp = 8.7.3` + `composeBom = 2026.04.01` (matches the `lint{}` workaround comment); `LICENSE` (Apache-2.0), `LICENSES.md`, `STORAGE_STRATEGY.md`, `PERFORMANCE_BUDGETS.md`, `FAILURE_MATRIX.md`, `NDK_PLAN.md`, `CAPTURE_ARCHITECTURE.md`, `CHANGELOG.md` (`## Unreleased`), `RELEASE_NOTES_TEMPLATE.md`, `CLI_BUILD_AND_SIDELOAD.md`, `native/CMakeLists.txt`, `keystore.properties.example` all present with claimed content. Verified all 4 PowerShell scripts (`pns_hfr_autorun`, `pns_verify_toolchain`, `pns_license_inventory`, `pns_probe_watch`) parse + UTF-8. **Test results:** 13 suites green (88 tests, 0 failures, 0 errors, 0 skipped) per `app/build/test-results/testDebugUnitTest/TEST-*.xml`: BracketPlan(6), BracketScheduler(9), CapabilityGate(5), CaptureStorageFilename(7), CropPlan(8), EncoderAttemptJsonAdapter(5), EncoderResultAggregator(9), FaceDetectAdapter(7), HighlightMeter(7), PerfBudget(9), PnsLog(3), TimecodeFormat(6), TrackerState(7). Device-run rows (lines 83-94 above) back the `[x] [ADB]` items: adb serial `8bf09993` / CPH2655 / Android 16 with sideload, smoke, core suite, full suite, reprocess validation, and chained regression all logged. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (assembleDebug + dep-audit + license inventory in sync + UTF-8 + parse + `:app:testDebugUnitTest BUILD SUCCESSFUL`; APK 31 708 KB). |
| 2026-05-08 | **Plan addition: Phase 4 (color management, calibration & LUTs) + section renumber** | Added a new `BUILD_PLAN.md` §7 "Phase 4: Color management, calibration & LUT pipeline" between the existing Phase 3 (§6) and the documentation/CI section. Captures: (1) **Calibration mode** — manual 4-corner tap UI on a 24-patch reference chart (generic + ColorChecker Classic + Passport coords, NO chart images bundled), patch sampler, WB-gain solver (`R/G ≈ 1 ± 0.02` neutral), CCM solver (linear least-squares against published reference Lab values with Bradford CAT), MTF50 baseline from slanted-edge ROIs, `.cube` 33³ exporter, `Dng12Saver` extension to write `ColorMatrix1`/`ForwardMatrix1` so RAW inherits the calibration without baking it. (2) **Bundled LUT library** — FOSS-only set: ACES sRGB↔ACEScct (Apache-2.0, AMPAS), Filmic Blender (Apache-2.0, sobotka), Rec.709 identity + B&W BT.601 + B&W BT.709 (generated at build time, public-domain math), in-house "Point & Shoot Cinematic" (Apache-2.0). User-imported `.cube` / `.3dl` via SAF picker. SPDX whitelist `{Apache-2.0, BSD-2-Clause, BSD-3-Clause, MIT, CC0-1.0, public-domain}` enforced by `LutCatalogTest`. (3) **Apply path** — GLES 3.0 fragment shader (`sampler3D` trilinear) for live preview + video; CPU trilinear in `LutPipeline.applyTrilinear` for stills; RAW (DNG) NEVER baked — LUT name + SHA256 written as sidecar metadata only. (4) **Architecture seams** — `Lut3D.kt`, `LutPipeline.kt`, `LutCatalog.kt`, `LutShader.kt`, `CalibrationProfile.kt`, `CalibrationSampler.kt`, `CalibrationToLut.kt` + companion JUnit suites (`LutPipelineTest`, `LutCatalogTest`, `Lut3DTest`, `CalibrationProfileTest`, `CalibrationSamplerTest`, `CalibrationToLutTest`). (5) **License/sourcing rules** — every LUT folder must have `LICENSE.txt` + `SOURCE.txt` + `SHA256.txt` siblings; `scripts/pns_license_inventory.ps1` extended to walk `app/src/main/assets/luts/`; build-time `downloadBundledLuts` Gradle task fetches LUTs via SHA-256-pinned download (LUT files NOT committed to git; sidecars + checksums ARE). (6) **Cross-cutting hooks** — extends `PERFORMANCE_BUDGETS.md` (preview LUT shader ≤ 2 ms/frame at 1080p; still LUT CPU ≤ 80 ms for 12 MP), `CAPTURE_ARCHITECTURE.md` (LUT executor placement: stills→IO/encode lane, video→GLES, preview→surface compositor, RAW→skip), `FAILURE_MATRIX.md` (corrupt `.cube` reject, unsupported size reject, GLES 3D-texture upload fallback, calibration patch-variance toast), `AboutScreen` (Color & LUT credits), `DiagnosticsMode` dump (active calibration + LUT + SHA256). All new boxes are `[ ]` per the verification protocol — no implementation in this turn, plan-doc work only. **Section renumber**: existing §8 → §9 (Cross-cutting optimizations), §9 → §10 (Scope control gates). All forward-looking `BUILD_PLAN §8` references updated to `§9` in: `CAPTURE_ARCHITECTURE.md`, `PERFORMANCE_BUDGETS.md` (×2), `FAILURE_MATRIX.md` (×3), `STORAGE_STRATEGY.md` (×2), `NDK_PLAN.md`, `LICENSES.md`, `CHANGELOG.md` (×3), `scripts/pns_license_inventory.ps1`, `app/src/main/java/dev/pointandshoot/{VendorKeyGuard,DiagnosticsMode,CapabilityGate,PnsLog}.kt`. Verification-protocol "throughout §3-§8" reference updated to "§3-§9". Historical `PROBE_BUILD_PLAN.md` §5 audit-trail entries left unchanged (preserves the historical record). `LICENSES.md` gains a "Bundled LUTs (planned — Phase 4)" table with 7 LUT entries + sourcing rules. `README.md` roadmap gains a new "Color management & LUTs (Phase 4)" bullet with a `BUILD_PLAN.md` §7 link. Final docs-only `pns_verify_toolchain.ps1 -SkipGradle` **PASSED** (license inventory still in sync with `libs.versions.toml`; all 4 PowerShell scripts UTF-8 + parse). |
| 2026-05-08 | **Phase 4 host-side scaffolds: Lut3D + LutPipeline + BuiltInLuts + LutCatalog + CalibrationProfile + CalibrationMath + CalibrationToLut + COLOR_PIPELINE.md** | Shipped 7 new pure-data Kotlin modules under `app/src/main/java/dev/pointandshoot/` covering the host-side half of BUILD_PLAN §7 (Phase 4 color/LUT). (1) `Lut3D.kt` — size-validated 17/33/65 grid (`SUPPORTED_SIZES`) + interleaved RGB `FloatArray` (`((b * size + g) * size + r) * 3 + ch` order, Adobe Cube spec), `identity(size)` builder, `isIdentity(tolerance)` checker (default `1e-5f`); 8 JUnit tests. (2) `LutPipeline.kt` — `applyTrilinear(rgb, lut)` + allocation-free `applyTrilinearInto(rIn, gIn, bIn, lut, out, offset)` for hot loops; `parseCube(text)` (rejects non-`[0,1]` domain, 1D LUTs, unsupported sizes, body-size mismatch); `serializeCube(lut, title)` round-trips through `parseCube`; explicit NaN-input rejection; 12 JUnit tests. `parseDl3` deferred (multiple incompatible upstream variants; cube is dominant). (3) `BuiltInLuts.kt` — code-generated `rec709Identity` + `bwBt601` (Y = 0.299R + 0.587G + 0.114B) + `bwBt709` (Y = 0.2126R + 0.7152G + 0.0722B) + Apache-2.0 `pnsCinematic` (teal-orange grade: shadow tint (0.30, 0.55, 0.70), highlight tint (1.00, 0.65, 0.35), 30 % strength, BT.709-luma-driven smoothstep blend); all magic numbers pinned as private constants; 11 JUnit tests covering R=G=B grayscale collapse + per-channel luma-weight assertions + BT.709-greener-than-BT.601 sanity + cinematic shadow/highlight tilt + `[0, 1]` clamp. (4) `LutCatalog.kt` — runtime enum `{None, BwBt601, BwBt709, PnsCinematic}` with metadata `(displayName, description, spdx, source, scope)`, `load(size)` materializer, `forScope(scope)` filter, `defaultFor(scope) = None`, `ALLOWED_SPDX = {Apache-2.0, BSD-2-Clause, BSD-3-Clause, MIT, CC0-1.0, public-domain}` enforced by `LutCatalogTest`; ACES + Filmic intentionally absent until `downloadBundledLuts` lands; 8 JUnit tests. (5) `CalibrationProfile.kt` — data class `{wbGains, ccm, bias, mtf50Lpph?, illuminant, capturedAtMs, cameraId, targetId}` with `WbGains.Identity` / `Ccm.Identity` / `Bias.Zero` companion sub-types + `Illuminant.{D50, D55, D65, StdA, F2}` enum + `apply(rgb)` method that runs the (WB → CCM → bias) chain with `[0, 1]` clamping; gains validated `> 0`, bias length 3, ids non-blank, mtf50 `>= 0` when set; indirectly tested via `CalibrationToLutTest`. (6) `CalibrationMath.kt` — `computeWbGains(neutralPatches)` (G-anchored at 1; rejects when avg G < `MIN_AVG_FOR_NEUTRAL_SOLVE = 1e-4`) + `computeCcm(measured, target)` (3×3 normal equations + Gaussian elimination with partial pivoting; requires ≥ 3 patches; throws on singular system at `SINGULAR_TOLERANCE = 1e-12`); 12 JUnit tests recover identity / channel-swap / synthetic non-trivial mixing matrix to within `1e-3`, plus error-path coverage. (7) `CalibrationToLut.kt` — `toLut3D(profile, size = 33)` applies the profile to a uniform `[0, 1]` cube; `toCube(profile, size, title)` serializes with provenance-encoded TITLE (`"Point & Shoot calibration <illuminant> cam=<id> target=<id>"`); 8 JUnit tests covering identity / WB-only / CCM-only / full WB+CCM+bias profiles + LUT-vs-direct-apply parity within 1 LSB on 8-bit + mtf50 metadata preservation + title override. **59 new JUnit tests, all green** (full suite now: 19 suites / 147 tests / 0 failures / 0 errors / 0 skipped). Also wrote `COLOR_PIPELINE.md` (new design doc): per-stage location table (ISP / engine / encoder), per-output-format table (RAW / AVIF / JXL / JPEG / MP4) with the "RAW is never baked through a LUT" rule called out explicitly, pinned-numbers table (luma weights, supported LUT sizes, dE_2000 targets, MTF50 floor, SPDX whitelist) cross-referenced to the actual code so doc/code drift is caught at gate time, calibration-mode flow walkthrough, license posture. Updated docs (`BUILD_PLAN.md` §7 — flipped 13 boxes `[ ]→[x]` with body text describing what shipped + what's deferred to the capture engine; `CHANGELOG.md` — added Lut/Calibration test list, Phase 4 host-side bullet, `COLOR_PIPELINE.md` engineering-doc entry, downgraded "Pending" to engine-integration only; `README.md` — added `COLOR_PIPELINE.md` link to engineering docs index + 7 new modules + 6 new test classes to the repo-layout enumeration; `LICENSES.md` — Bundled LUTs section header changed from "(planned)" to "(Phase 4 — partial: code-generated entries shipped)", table gained a Status column with 4 rows now `**shipped**`, sourcing rules updated to call out the catalog test that enforces the SPDX whitelist + clarify user-imported LUTs skip the whitelist). `ReadLints` clean on all 13 new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** below this row. |
| 2026-05-08 | **Phase 4 reference targets + sampler + MTF50 + cross-cutting hooks (all 5 closed)** | Shipped 6 new pure-data Kotlin modules + 1 docs-only round under `app/src/main/java/dev/pointandshoot/` and `app/src/test/java/dev/pointandshoot/` covering every remaining host-side BUILD_PLAN §7 (Phase 4) item except the GLES shader, the Compose 4-corner-tap UI, the `Dng12Saver` color-tag extension, the SAF importer, the Gradle download task, and the .3dl parser (all engine-side / device-side and remain `[ ]`). (1) `ReferenceTarget.kt` — chart-layout data class (`rows`, `cols`, `patches`, `Patch.{row, col, name, role, referenceRgb}`, `PatchRole.{Color, Neutral}`, `patchCenter(row, col, border)` / `patchHalfSize(border)` for sampler placement, validation rejects mismatched patch counts + duplicate `(row, col)` + out-of-range coords). `BundledReferenceTargets.kt` — `ColorCheckerClassic24` (24 patches in a 4×6 grid; reference RGB triples from the published X-Rite ColorChecker Classic data sheet converted to linear-light sRGB under D50; chart image NOT bundled and "ColorChecker" trademark NOT used in any user-facing string per BUILD_PLAN §7), `Generic24` (18 hue swatches at 3 luminance levels generated deterministically by `hueToRgb` + 6-step neutral wedge, D65), `byId(id)` + `All` accessors. `ReferenceTargetTest` (15 tests). (2) `CalibrationSampler.kt` — `RgbPlane` data class (linear-light, normalized `[0, 1]`, row-major 3-floats-per-pixel) + `pixel(x, y)` clamp + `uniform()` factory; `ChartCorners.{tl, tr, br, bl}.bilinearMap(u, v)` chart→pixel mapping (perspective homography deferred — bilinear is sufficient for "phone held flat over chart"); `PatchSample.{mean, variance, samples, rejected, rejectReason, patchRef}`; `sampleAt(plane, cx, cy, halfW, halfH, maxVariance)` clips to plane bounds, returns 0-sample rejection off-plane, two-pass mean+variance over the ROI, rejects when `max(variance) > maxVariance` (default `DEFAULT_MAX_VARIANCE = 5e-3`); `sampleNormalized(plane, centerNorm, halfNorm, corners, ...)` translates to pixel-space + adapts ROI half-extents to non-square chart projection; `sample(plane, target, corners)` returns one `PatchSample` per `target.patches` in order. `CalibrationSamplerTest` (13 tests including a synthetic 24-patch chart end-to-end where every patch is recovered within 1e-3 of its reference). (3) `SlantedEdgeMtf.kt` — classical ISO 12233 MTF50 on a single-channel `GrayPlane` ROI: per-row centroid via gradient-weighted x → linear fit y→x → bin pixels by perpendicular distance into a 4× oversampled 128-bin ESF (linear-interpolate empty bins) → differentiate to LSF → Hamming window → direct DFT (small N; an FFT is not worth the complexity here) → linear-interpolate the 50 % crossing; returns cycles/pixel; `cyclesPerPixelToLpph(cyclesPerPixel, pictureHeightPx)` converts to BUILD_PLAN's lp/ph flavor; `EdgeOrientation.{NearVertical, NearHorizontal}` (the latter transposes the ROI). `GrayPlane.{build, transpose, luma}`. `SlantedEdgeMtfTest` (15 tests including monotonically decreasing MTF50 across σ ∈ {0.5, 1.0, 1.5, 2.0} and measured-vs-analytic Gaussian-blur formula `0.187/σ` within 50 %). (4) `LutCreditsBuilder.kt` — pure-data builder for the AboutScreen "Color & LUT credits" sub-block; walks `LutCatalog.entries` filtering by `ALLOWED_SPDX` (defense in depth so even a smuggled non-FOSS catalog entry would not surface on AboutScreen); `LutCreditRow.{displayName, description, spdx, source, scope}`. `LutCreditsBuilderTest` (5 tests). `AboutScreen.kt` extended with a "Color & LUT credits" section + `LutCreditCard` composable (orange title, monospace metadata, SPDX/scope/source/description). (5) `LutDiagnosticsBuilder.kt` — pure-data builder for the `DiagnosticsMode` "Color & LUT" section: reports active LUT name + SPDX + SHA256, active calibration profile id + captured-at, enumerates every bundled LUT (auto-derived from `LutCatalog.entries`), enumerates `LutCatalog.ALLOWED_SPDX` in sorted order; `ActiveColorState.{activeLutName, activeLutSpdx, activeLutSha256, calibrationProfileId, calibrationCapturedAtUtc}` + `Default` sentinel for "no calibration, identity LUT". `LutDiagnosticsBuilderTest` (8 tests). `DiagnosticsMode.dump(context, colorState = Default)` now appends `LutDiagnosticsBuilder.buildSection(colorState)` to every dump. (6) `PerfBudget.Defaults` gains `LUT_SHADER_PER_FRAME_1080P_MS = 2L` + `LUT_CPU_STILL_12MP_MS = 80L`; `PerfBudgetTest` adds 3 new tests pinning the constants and grading 60 fps preview / 12 MP capture correctly. (7) Documentation rounds: `PERFORMANCE_BUDGETS.md` gained "Color & LUT pipeline (Phase 4)" with 7 budget rows including the explicit "RAW LUT bake = `n/a`"; `CAPTURE_ARCHITECTURE.md` gained "Color & LUT pipeline (Phase 4)" with executor placement table (preview→GLES compositor, video→GLES on encode surface, still→`PNS.Reader`, RAW→skipped, calibration solve→`PNS.Reader`, cube parse→`PNS.Reader`) + 3 new backpressure rules; `FAILURE_MATRIX.md` gained "Color & LUT pipeline (Phase 4)" with 11 graceful-degradation rows (corrupt cube, unsupported size, GLES upload failure, preview-budget regression auto-disable, calibration patch variance too high, singular CCM system, MTF50 null, RAW + non-identity LUT skip, missing imported LUT). (8) BUILD_PLAN §7 sync: flipped 9 boxes `[ ]→[x]` (Generic 24, ColorChecker 24, `CalibrationSampler.sample`, `CalibrationSampler.measureMtf50`, all 5 cross-cutting hooks), each with body text describing what shipped + what remains for the engine. (9) CHANGELOG, README updated. **Net: 6 new modules + 1 helper (`AboutScreen` extension), 57 new JUnit tests** (15 ReferenceTarget + 14 CalibrationSampler + 12 SlantedEdgeMtf + 5 LutCreditsBuilder + 8 LutDiagnosticsBuilder + 3 PerfBudget; total grew from 147 to 204 across 24 suites). `ReadLints` clean. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** below this row. |
| 2026-05-08 | **Phase 4 V&V gates closed: dE_2000 + CCM accuracy + cube round-trip; CalibrationProfile JSON; LUT capture sidecar; AboutScreen live-probe hydration; DODGE_PROFILE.md sync (all 6 host-side rounds, 1 doc round)** | Shipped 5 new pure-data Kotlin modules + 1 Compose AboutScreen extension + 1 doc round; closed 2 long-pinned BUILD_PLAN §7 V&V gates and the engine-consumption remainder of the BUILD_PLAN §2 Phase 0 V&V gate. (1) **`ColorMath.kt`** — pure-data linear-sRGB → CIE-XYZ (D65 IEC 61966-2-1 matrix) → CIE Lab → CIEDE2000 (full Sharma / Wu / Dalal 2005 implementation including the LCH rotation term and the `kL = kC = kH = 1.0` graphic-arts default). Convenience `deltaE2000FromLinearSrgb(rgb1, rgb2)`. D65 + D50 white-point constants. **`ColorMathTest`** (22 tests) covers the published Sharma 2005 reference vectors (rows 1, 2, 3, 14, 22) to within `1e-3`. (2) **`CalibrationCcmAccuracyTest.kt`** (7 tests) — closes BUILD_PLAN §7 V&V gates "On a synthetic 24-patch fixture: computed CCM produces ≤ 1.0 dE_2000 mean error" + "`.cube` round-trip: applying the exported LUT shrinks mean dE_2000 by ≥ 80 % vs un-LUT'd". Synthetic ColorChecker24 with ±0.5 % noise lands ≤ 1.0 dE_2000 mean after CCM; noiseless case lands < 0.05; small hue rotation recovers cleanly; Generic24 fixture also passes; LUT-vs-direct apply parity within 1 LSB on 8-bit; `.cube` round-trip via `CalibrationToLut.toLut3D` + `LutPipeline.applyTrilinear` shrinks the mean dE_2000 by ≥ 80 %. (3) **`CalibrationProfileJsonAdapter.kt`** — hand-rolled stable JSON serializer + parser on top of `org.json` (Android-framework type; tests use the existing `org.json:json` `testImplementation` dep). Schema `version: 1` with documented top-level keys; pretty-printed (2-space) for diff-friendliness. `filenameFor(profile, utc)` enforces the `<illuminant>_<utc>.json` BUILD_PLAN §7 spec. **`CalibrationProfileJsonAdapterTest`** (16 tests) covers non-trivial + identity round-trip, `mtf50Lpph` omission when null, decode rejects malformed JSON / unknown version / unknown illuminant / missing wbGains / malformed ccm shape / bad row width / negative capturedAtMs / blank cameraId, filename helper handles every illuminant. (4) **`LutSidecar.kt`** — pure-data writer + parser for the BUILD_PLAN §7 capture-sidecar format. ASCII line-oriented, `# pns-lut-sidecar v1` magic header, `key = value` body, two flavors (`kind = bundled` for catalog references, `kind = cube` for user-imported `.cube` files). `BundledRef` carries the catalog name + SPDX + source + 64-char lowercase SHA256 (regex-validated); `CubeFileRef` carries the relative cube path + optional title + SHA256. Convenience helpers: `bundledRefFor(catalog, ...)` populates SPDX / source from a `LutCatalog` entry; `siblingFilenameFor(captureFilename, isBundled)` enforces the `.lutref.txt` / `.cube.txt` extension convention. **`LutSidecarTest`** (17 tests) covers bundled + cube round-trip, decode rejects missing magic / unknown schema version / malformed key=value / unknown kind / missing required fields, decode tolerates blank lines + inline comments, constructor validation rejects malformed sha256 / unsupported lutSize / blank cataloguedAs, Video captureKind round-trips. (5) **`EncoderRecipeBuilder.kt`** — pure-data hydration of the AboutScreen "From the latest probe (live)" subsection. `recipesFromSummary(summary)` selects the best HFR + best regular row per camera (deterministic ordering); `errorRowsFromSummary(summary, max)` surfaces the most-frequent canonical errors; `headlineCounts(summary)` produces the `<totalAttempts>` / `<okPercent>` banner. **AboutScreen.kt** gains an optional `liveSummary: EncoderSummary?` parameter and renders the new section (with `LiveRecipeCard` + `LiveErrorCard` Compose adapters) when present. **`EncoderRecipeBuilderTest`** (13 tests) covers empty summary → no rows, picks one HFR + one Regular per camera, skips cameras with no successes, recipes deterministic across summary reorderings, mime recovered from note when first-class mime is null, error rows sorted by canonical-error frequency with cap respected, `headlineCounts` is null for null/empty summaries, `okPercent` arithmetic correct (2/3 → 67 %). (6) **`DODGE_PROFILE.md`** — gains a 150 mm long-tele row in the "Mode mapping" table; new "Host-side mapping (FocalMode → CropPlan → CapabilityGate)" cross-reference table tying the spec modes to actual `CropPlan` / `CapabilityGate.Feature` constants in code (4 crop-derived modes via the `FocalMode` enum + 4 lens-switch modes via direct `cameraId` selection); new "HFR recipes (live, from the latest probe artifact)" snapshot table; new "Imaging profile defaults per FocalMode" section; new "Color & LUT pipeline applicability" section (RAW path explicitly excluded); new "Storage filename + GroupingID convention" pointer to `CaptureStorage`. (7) **BUILD_PLAN sync**: §7 V&V gates 1 and 4 flipped `[ ]→[x]` (CCM accuracy + cube round-trip — both backed by `CalibrationCcmAccuracyTest`); calibration profile JSON row body extended to enumerate the 15-test JSON adapter (headline still `[ ]` because the actual app-private file write requires the in-app Calibrate flow); capture sidecar row body extended to enumerate the 17-test sidecar writer (headline still `[ ]` because the actual sibling-file write requires the still / video encoder); §2 Phase 0 V&V gate body updated to record the AboutScreen "From the latest probe (live)" hydration (headline still `[ ]` because plumbing the latest probe artifact from `MainActivity` into the AboutScreen call site is pending). CHANGELOG, README updated. **Net: 5 new modules + 1 AboutScreen Compose extension + 1 doc round, 75 new JUnit tests** (22 ColorMath + 7 CalibrationCcmAccuracy + 16 CalibrationProfileJsonAdapter + 17 LutSidecar + 13 EncoderRecipeBuilder; total grew from 204 to 279 across 29 suites). `ReadLints` clean. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** below this row. |
| 2026-05-08 | **Phase 4 file-IO + import validation + asset-LUT infra (host-side, all 6 closed)** | Shipped 2 new pure-data Kotlin modules + 2 new PowerShell scripts + 2 new Gradle tasks + 1 new PS gate hook. Closed BUILD_PLAN §7 row "License & sourcing rules" host-side scaffold + §9 row "SBOM generation" + §9 row "Golden-file tests for metadata serialization (GroupingID, crop metadata)" + §7 row ".cube LUT export from each profile". (1) **`scripts/pns_license_inventory.ps1`** — extended with `Test-BundledLutFolders` which walks `app/src/main/assets/luts/<spdx>/<name>/`, validates the three required sidecars (`LICENSE.txt` + `SOURCE.txt` + `SHA256.txt`), and cross-references each leaf folder name against `LICENSES.md`. When the directory does not exist (the expected state until `downloadBundledLuts` lands a real entry) the walker reports `OK: bundled LUTs - no asset-backed LUTs (folder ... does not exist - expected until downloadBundledLuts lands)` and skips silently. Both the OK path (fresh checkout) and the FAIL path (synthetic temp project root with a leaf folder missing `SOURCE.txt` / `SHA256.txt` and not referenced in `LICENSES.md`) were smoke-tested; FAIL exits 1 with both rows in the `[license]` report. (2) **`app/build.gradle.kts`** — registers `:app:downloadBundledLuts` (build-time fetch + SHA-256 verify + atomic copy into `assets/luts/<spdx>/<name>/` with the three sidecars; idempotent — cache hit is a no-op; on-failure cleanup deletes the cache file and reports both expected + actual digests) and `:app:downloadBundledLutsDryRun` (prints the URL list without fetching). The URL list (`bundledLutSpecs`) is intentionally empty in this milestone; both tasks were verified `BUILD SUCCESSFUL` with the expected `bundledLutSpecs is empty; skipping` log line. When `bundledLutSpecs` becomes non-empty, `preBuild` will automatically depend on `downloadBundledLuts`. (3) **`LutImportValidator.kt`** — pure-data validator wrapping `LutPipeline.parseCube` with a sealed `Result.Success(lut) | Result.Failure(category, message, cause)` taxonomy; `FailureCategory.{TooLarge, MalformedHeader, MalformedBody, UnsupportedSize, OneDLut, NonUnitDomain, SizeMismatch, OutOfRange}`; hard 16 MB payload cap; ±0.001 tolerance band around `[0, 1]` so professional LUTs that round-trip through fp32 still pass; `Failure.toastMessage()` formats the user-facing toast string. **`LutImportValidatorTest`** (15 tests) covers identity at every supported size, non-identity within tolerance, bytes overload, oversized payload rejection, missing/non-integer `LUT_3D_SIZE`, `LUT_1D_SIZE`, unsupported sizes, non-unit `DOMAIN_MAX`, non-float bodies, wrong number of triples, wildly out-of-range cells, fp tolerance band, toast prefix, every category has a toast label. (4) **`scripts/pns_sbom.ps1`** — host-side CycloneDX 1.5 SBOM emitter. Reads `gradle/libs.versions.toml` + the SPDX map embedded in `pns_license_inventory.ps1` and writes a CycloneDX-format JSON SBOM to stdout (or `-OutFile`). Each component carries a `pkg:maven/...` PURL (Maven coords for libraries; `<id>.gradle.plugin` artifact for Gradle plugins), an SPDX license, and a CycloneDX `scope` (`runtime → required`, `debug/test → optional`, `plugin → excluded`). `urn:uuid:` serial + ISO-8601 timestamp + top-level `application` component for `dev.pointandshoot`. `-Verify` re-parses + structural-checks the emitted JSON (bomFormat, specVersion, every component has a PURL + license, serialNumber `urn:uuid:`-prefixed). 16 components emitted on the current dependency set. **`pns_verify_toolchain.ps1`** invokes `pns_sbom.ps1 -Verify` on every gate run so SBOM drift is caught immediately; the SBOM script is also added to the script-encoding/parse list. (5) **`MetadataSerializationGoldenTest.kt`** (9 tests) — closes BUILD_PLAN §9 host-side "Golden-file tests for metadata serialization (GroupingID, crop metadata)". Locks the `BracketPlan v1` text projection (3-shot @ 1 EV, 5-shot @ 0.667 EV, 7-shot @ 1 EV) including grouping-id stability across stops + non-collision of two default `build()` calls; locks the `CropPlan v1` projection (Street35, Standard50, Portrait85, LongTele150 on an 8160×6144 active array; locks centered crop rectangle + effective zoom + metering / AF hints). Any future plan-class refactor that changes the on-disk metadata downstream tools see now requires a deliberate version bump + CHANGELOG entry. (6) **`CalibrationProfileStorage.kt`** — Android-side file-IO wrapper around `CalibrationProfileJsonAdapter`. `directory(context)` resolves (and creates) `getExternalFilesDir(null)/calibration/`; `save(context, profile)` writes via temp-file + atomic rename so a crashed write never leaves a half-formed JSON document; `load(file)` round-trips back through the adapter; `list(context)` enumerates saved profiles newest-first; `latestFor(context, illuminant)` returns the most recent profile by illuminant. UTC timestamp generation (`nowUtcTimestamp()`) matches the project-wide `yyyyMMdd_HHmmss` convention. **`CalibrationProfileStorageTest`** (6 tests) covers the Context-free surface area: timestamp pattern + monotonicity, documented constants stable, load round-trip via JUnit-managed temp dir, `IllegalArgumentException` propagation on malformed JSON, filename convention parity with the storage `latestFor` key. (7) **BUILD_PLAN sync**: §7 ".cube LUT export per profile" flipped `[x]` (already shipped via `CalibrationToLut.toCube` + `CalibrationCcmAccuracyTest` LUT-vs-direct-apply parity); §7 "License & sourcing rules" host-side scaffold flipped `[x]` (the script extension shipped); §9 "Golden-file tests for metadata serialization" flipped `[x]`; §9 "SBOM generation" flipped `[x]`; §7 "Profile JSON saved to app-private storage" body extended to enumerate the storage wrapper (headline still `[ ]` because the actual app-private file write requires the in-app Calibrate flow + the `pns_hfr_autorun.ps1 -PullCalibration` script flag); §7 "User-imported LUTs validated" body extended to enumerate the import validator (headline still `[ ]` because the SAF picker UI is Compose). §7 `downloadBundledLuts` body extended to enumerate the Gradle task infrastructure (headline still `[ ]` because the URL list is intentionally empty pending upstream pinning). CHANGELOG, README, DODGE_PROFILE.md unchanged this round (no code surface changes that affect them). **Net: 2 new Kotlin modules + 2 new PS scripts + 2 new Gradle tasks + 1 verify-gate hook + 1 docs round; 30 new JUnit tests** (15 LutImportValidator + 9 MetadataSerializationGolden + 6 CalibrationProfileStorage; total grew from 279 to 309 across 32 suites). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** below this row. |
| 2026-05-08 | **Compose UI integration round (5 Compose screens shipped + device-validated on adb 8bf09993)** | Shipped 5 host-side rounds wiring Compose UI on top of the Phase 4 / HUD scaffolds and validated each on the OnePlus 13. (1) **`HudSettings` per-mode LUT memory** — added `selectedLutForStills` + `selectedLutForVideo` String fields (default `LutCatalog.None.name`, persisted under SharedPreferences keys `selected_lut_stills` / `selected_lut_video`) plus `stillsLut()` / `videoLut()` resolvers that fall back to `None` on rename or removal. **`HudSettingsLutResolutionTest`** (5 tests). (2) **`LutChipRow.kt`** — Pro HUD two-chip row (STILL LUT + VIDEO LUT) with `AlertDialog` picker that lists every `LutCatalog.forScope(scope)` entry with display name + 1-line description + SPDX badge + "Reset to None" affordance; selection writes through `HudSettingsState.update` so process death never loses intent. Wired into `ProHudScreen` between the command dial and record button. (3) **`LutImporterScreen.kt` + `ImportedLutStore.kt`** — SAF "Import LUT…" picker with `OpenDocument(arrayOf("*/*"))`, validates via `LutImportValidator.validate(bytes)`, atomic-writes to `getExternalFilesDir(null)/luts/imported/<safeName>.cube` plus `<safeName>.cube.sha256.txt` sidecar; toast on validation failure. Filenames sanitized to `[A-Za-z0-9._-]` (capped at 96 chars, leading dots stripped, runs of underscores collapsed); collisions resolved via `_2`, `_3`, … with a SHA-derived fallback at 9999. **`ImportedLutStoreTest`** (9 tests). (4) **`CalibrateScreen.kt` + `BitmapRgbPlane.kt`** — in-app Calibrate flow (chart-photo flavor, since the live-preview flavor needs the Camera2 capture engine): target picker (`BundledReferenceTargets.All`), SAF "Load chart photo…" picker, four-corner tap with live orange dot + connecting quad overlay (`Canvas` + `pointerInput.detectTapGestures` + `Modifier.onSizeChanged` for layout-to-plane coord mapping), Compute → `BitmapRgbPlane.fromBitmap` → `CalibrationSampler.sample` → `CalibrationMath.computeWbGains` → `CalibrationMath.computeCcm` chain, Save → `CalibrationProfileStorage.save(...)`. **`BitmapRgbPlaneTest`** (11 tests covering sRGB EOTF + downsample arithmetic). (5) **`PreviewEngineScreen.LivePreviewHudOverlay`** — wraps the live `TextureView` in a `Box` with the FPS / cameraId readouts, the LUT chip row, the command dial, and an `EyeAfOverlay` placeholder riding on top; honors `HudSettings` toggles. (6) **`CameraCapabilitiesProbe`** — new "Calibrate" + "Import LUT" home-screen buttons; new `--es pns_screen` aliases (`about` / `prohud` / `hudsettings` / `calibrate` / `lutimport`) for one-line ADB navigation; live `EncoderAttemptJsonAdapter.loadLatest(...)` → `EncoderResultAggregator.summarize(...)` plumb-through to `AboutScreen(liveSummary = …)` (closes the engine-consumption remainder of the BUILD_PLAN §2 Phase 0 V&V gate at the host-side level — the [ADB] tick still depends on this audit row). **Compile-failure fix**: a stray `/*` inside a KDoc block on `LutImporterScreen.kt` opened a nested block comment (Kotlin DOES nest block comments) and silently moved the rest of the file into "comment" state, producing a misleading "Unresolved reference" cascade — fixed by rewording the docstring; for the future, never write `/*` literally inside a `/** ... */` block. **Device validation on adb `8bf09993` (CPH2655 / Android 16, root via `adb root` confirmed `uid=0(root) … context=u:r:su:s0`)**: rebuilt + reinstalled (`assembleDebug` BUILD SUCCESSFUL, `adb install -r` Success), then launched each new screen via `am start --es pns_screen <alias>` and captured a screenshot under `docs/screenshots/`: **smoke_home.png** (probe home renders), **smoke_calibrate.png** (Calibrate target picker + Load chart photo button), **smoke_lutimport.png** (Import LUT screen with "Pick `.cube` file…" + "(no imports yet)" placeholder), **smoke_prohud.png** (ProHUD with new STILL LUT / VIDEO LUT chips both reading "None" + command dial M / H / S / BKT), **smoke_lutpicker.png** (tapped STILL LUT → "Choose still LUT" `AlertDialog` lists None + B&W BT.601 + B&W BT.709 + Point & Shoot Cinematic with their SPDX badges), **smoke_lutpersist.png** (after force-stop + relaunch, STILL LUT chip still reads "B&W BT.709"), **smoke_about_live.png** (About / Heritage scrolled to "From the latest probe (live)" — banner reads "180 attempts across 4 cameras — 180 ok / 0 fail (100% pass)"; `LiveRecipeCard` per camera, e.g. Cam2 Hfr 480fps measured 427.8 fps, hydrated from `exhaustive_probe_20260507_202312.json`), **smoke_preview.png** (PreviewEngineScreen running on the rear camera at 59.3 fps with the new HUD overlay riding on top: `fps 59.3 (target 60) ISO -- 1/--` + `cam 0` chips at the top, persisted `STILL LUT B&W BT.709` + `VIDEO LUT None` chips at the bottom, command dial below). LUT persistence verified end-to-end via `adb shell run-as dev.pointandshoot cat shared_prefs/pns_hud_settings.xml` returning `<string name="selected_lut_stills">BwBt709</string>` after the picker tap. No `AndroidRuntime` / `FATAL` lines in `adb logcat -d -t 200` across all five screens. (7) **BUILD_PLAN sync**: flipped `[ ] Pro HUD + dial usable during live preview` → `[x]`; `[ ] HUD chip "LUT" alongside the imaging-profile selector` → `[x]`; `[ ] User-imported LUTs land in getExternalFilesDir(null)/luts/imported/` → `[x]`. The remaining `[ ]` rows in §7 (RAW LUT name+SHA256 in DNG metadata, capture sidecar sibling-file write, .cube round-trip on a real-world chart, MTF50 baseline measurement, In-app Calibrate ADB validation, .3dl import, asset-backed bundled LUTs) all require either the Camera2 capture engine, a real chart capture, or upstream FOSS LUT pinning. CHANGELOG, README updated. **Net: 4 new Kotlin modules + 2 new Compose screens + 1 PreviewEngineScreen overlay + 5 Compose-side device validations; 25 new JUnit tests** (5 HudSettingsLutResolution + 9 ImportedLutStore + 11 BitmapRgbPlane; total grew from 309 to 335 across 35 suites (verified by `Get-ChildItem app/build/test-results/testDebugUnitTest/TEST-*.xml`: 335 tests, 0 failures, 0 errors, 0 skipped)). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** below this row. |
| 2026-05-08 | **Color pipeline + branding round (4 host-side modules + JetBrains Mono vendoring + launcher V&V; device-validated on adb 8bf09993)** | Shipped four host-side rounds plus two on-device validations covering the color/LUT scaffolding the engine will consume next. (1) **GLES LUT shader assets + Kotlin wrapper** — `app/src/main/assets/shaders/lut_apply.frag.glsl` + `lut_apply.vert.glsl` ship the GLES 3.0 fragment + vertex pair (sampler3D with hardware trilinear interpolation; half-texel inset on the UVW coordinates so we sample at cell centers, not cube edges; `uLutEnabled` uniform that the host drives to `0.0` for identity bypass). `LutShaderProgram.kt` exposes (a) a pure-data `Source` object (asset paths, attribute/uniform names, `FULL_SCREEN_QUAD` for `GL_TRIANGLE_STRIP`, GLSL version directive, `requiredSymbols()` for the contract test), (b) a pure-data `BypassPolicy` (returns `0f` to disable the LUT for null / identity LUTs, `1f` otherwise; `shouldSkipUpload(...)` short-circuits the 3D-texture upload), and (c) a thin GLES wrapper (`createFromAssets(context)` compiles + links from the on-disk shaders; `uploadLutTexture(lut)` produces a `GL_RGB16F` 3D texture with `LINEAR` filtering and clamp-to-edge wrap on every axis; `bindUniforms(lut)` sets `uSourceTex` to texture unit 0, `uLutTex` to unit 1, `uLutSize` to the grid size, and `uLutEnabled` per the bypass policy). **`LutShaderProgramSourceTest`** (13 tests): asset paths exist, version directive present, `sampler3D` declared, every Kotlin-bound symbol appears in the on-disk shader text, `requiredSymbols()` enumerates exactly 6 names with no duplicates, full-screen quad layout matches, null / identity / non-identity LUTs drive the bypass policy correctly at every supported size. The `[ ]` BUILD_PLAN row stays open until the Camera2 preview surface is refactored to a `GLSurfaceView`-fed GLES context. (2) **`LutSidecarWriter.kt`** — file-IO wrapper around `LutSidecar.encode` with atomic-rename writes (temp file + `renameTo`, with delete-and-rename fallback on Windows when the target exists; a power loss between operations leaves no half-formed sidecar). `siblingFor(captureFile, isBundled)` is a pure path helper; `sha256Hex(bytes)` and `sha256ForLut(lut)` (IEEE-754 LE) compute the digests both `BundledRef` and `CubeFileRef` need. **`LutSidecarWriterTest`** (14 tests): sibling resolution preserves parent dir, `writeBundled` / `writeCube` round-trip through `decode`, mismatched `captureFilename` rejected, overwrite is atomic with no leaked temp files, parent dirs are created on demand, sha256 of empty input matches the canonical RFC value, sha256 differs across inputs and is stable, output is always 64 lowercase hex chars, identical LUTs hash identically, different LUT content hashes differently, different grid sizes hash differently, end-to-end "build a bundled sidecar with the LUT's own sha" round-trips perfectly. (3) **`DngColorTags.kt`** — pure-data converter from `CalibrationProfile` → DNG color tags (`AsShotNeutral` / `ColorMatrix1` / `ForwardMatrix1` + EXIF light-source code) so desktop processors honor an in-camera calibration WITHOUT baking it into the RAW. Math: `asShotNeutral(gains)` inverts WB gains and normalizes max to 1.0f per DNG spec; `colorMatrix1(profile)` solves `inverse(diag(wbGains) * sRGBtoXYZ_D65 * CCM)` to map XYZ → camera-native RGB; `forwardMatrix1(profile)` produces `Bradford_D65_to_D50 * sRGBtoXYZ_D65 * CCM` to map (WB-gained) camera RGB → XYZ-D50; `calibrationIlluminantCode(illuminant)` maps to EXIF LightSource code (D50→23, D55→20, D65→21, StdA→17, F2→14). Standards constants pinned as private constants. **`DngColorTagsTest`** (12 tests): identity gains → `(1, 1, 1)`, AsShotNeutral max == 1.0, AsShotNeutral inverts gains correctly, identity profile yields `ColorMatrix1 = inverse(sRGB→XYZ_D65)` (validated by `cm1 * sRGB→XYZ_D65 == I`), identity profile `ForwardMatrix1 = Bradford * sRGB→XYZ_D65`, EXIF light-source codes match DNG 1.7 spec, `forProfile` picks the right code for every illuminant, non-identity CCM yields a different `ColorMatrix1`, WB gains shift `AsShotNeutral` but NOT `ForwardMatrix1` (engineering invariant), `DngColor.equals` compares `FloatArray` contents (not references), `DngColor.init` rejects malformed `AsShotNeutral`. (4) **JetBrains Mono Regular vendored** — Part 4 typography spec closed: `app/src/main/res/font/jetbrainsmono_regular.ttf` (273900 bytes; SHA-256 `a0bf60ef0f83c5ed4d7a75d45838548b1f6873372dfac88f71804491898d138f`) pinned at upstream tag `v2.304`. `PnsTheme.MonoFamily` now resolves to `FontFamily(Font(R.font.jetbrainsmono_regular))` instead of the system `FontFamily.Monospace`. SIL OFL v1.1 license text vendored at `app/src/main/assets/fonts/jetbrainsmono/LICENSE.txt` (SHA-256 `30f0c136e3c88e422d0791acd97238870f9054a9729bc34cf2ff0d4ed8cac4ad`) with `SOURCE.txt` (refresh procedure) and `SHA256.txt` (digest manifest). `LICENSES.md` updated with a "Bundled font assets" row + OFL-1.1 compatibility statement. **Device validation on adb `8bf09993`**: rebuilt + reinstalled (`assembleDebug` BUILD SUCCESSFUL, APK 33153 KB, `adb install -r` Success), launched ProHUD via `am start --es pns_screen prohud` and captured **smoke_jetbrains_mono.png** — the timecode `00:00:00:00`, `FPS 30  ISO 400  1/250` readouts, `STILL LUT B&W BT.709 / VIDEO LUT None` chips, `M H S BKT` command dial, and the `→ Ultra-Max` ligature all render in the new face with its distinctive shape (compared to the previous system Monospace renders). (5) **Launcher icon V&V** — captured **smoke_app_info.png** via `adb shell am start -a android.settings.APPLICATION_DETAILS_SETTINGS -d package:dev.pointandshoot`; the system renders the adaptive `mipmap/ic_launcher` resource (light-grey reticle ring, four-direction crosshair ticks, `#FF5C00` Hasselblad-orange center dot on `#181A1B` charcoal). Closes the BUILD_PLAN §8 launcher-icon `[ ]` ADB row. (6) **README icon + screenshot section** — `docs/icon.png` (512×512, `System.Drawing` `HighQualityBicubic` resize) embedded at the top of `README.md` via the `<p align="center">` block; new `## Screenshots` section between Status and Quickstart references the existing `docs/screenshots/smoke_*.png` artifacts. (7) **ACES + Filmic LUT blockers documented** — both upstream packages ship `.ocio` configs + Sony Pictures Imageworks `.spi3d` / `.spi1d` files, NOT `.cube`. BUILD_PLAN §7 entries now enumerate the two paths (host-bake to `.cube` with a pinned `ociobakelut` script, OR extend `LutPipeline` with a `.spi3d` parser) and defer the decision until the live-preview GLES path lands, which is when the user-perceived gap will become a real prioritization signal. (8) **BUILD_PLAN sync**: flipped `[ ] Typography: JetBrains Mono` → `[x]`; flipped `[ ] [ADB] Verify icon appears correctly on the launcher` → `[x]`; flipped `[ ] Add a README-renderable icon asset` (parent + both children) → `[x]`; flipped `[ ] Screenshots section` → `[x]`. CHANGELOG, README repo-layout updated. **Net: 3 new Kotlin modules (`LutShaderProgram` + `LutSidecarWriter` + `DngColorTags`) + 2 new GLES asset files + JetBrains Mono vendoring + 1 launcher screenshot + 1 README icon + 1 README screenshot section; 39 new JUnit tests (13 LutShaderProgramSource + 14 LutSidecarWriter + 12 DngColorTags); total grew from 335 to 374 across 38 suites** (verified by `Get-ChildItem app/build/test-results/testDebugUnitTest/TEST-*.xml`: 374 tests, 0 failures, 0 errors, 0 skipped). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM all green). |
| 2026-05-08 | **GLES live preview LUT round (3 host-side modules + 17 new JUnit tests + device-validated on adb 8bf09993 with three LUT screenshots)** | Promoted the host-side `LutShaderProgram` to a working end-to-end GLES pipeline against a synthetic test pattern. (1) **`TestPattern.kt`** — pure-data procedural source (8 vertical color bars over an 11-step grayscale wedge over a smooth grayscale ramp; sRGB-encoded for display via the linear→sRGB EOTF). **`TestPatternTest`** (12 tests) locks the bar sequence, wedge step values, ramp monotonicity, sRGB endpoint encoding, and array-length contracts. (2) **`LutPreviewRenderer.kt`** — `GLSurfaceView.Renderer` that owns the program + 2D source texture + 3D LUT texture + full-screen-quad VBO; UI-thread updates flow through two `AtomicReference` slots (`pendingSource`, `pendingLut` wrapped in a private `LutUpdate(lut: Lut3D?)` data class so "no pending change" — a `null` slot — disambiguates from "user explicitly chose identity / bypass" — a `LutUpdate(null)` slot — without the wrapper a single `AtomicReference<Lut3D?>` would re-upload the identity bypass on every frame). `BITMAP_VFLIP_QUAD` is a vertically-flipped sibling of `LutShaderProgram.Source.FULL_SCREEN_QUAD` that cancels Android-bitmap row-0-at-top vs OpenGL row-0-at-bottom. Identity LUTs short-circuit via `BypassPolicy.shouldSkipUpload(...)` and free the 3D texture entirely. **`LutPreviewRendererQuadTest`** (5 tests) lock the V-flip relationship to `Source.FULL_SCREEN_QUAD` so a future refactor cannot accidentally re-introduce the upside-down preview regression we already shipped once. (3) **`GLPreviewScreen.kt`** — Compose screen with `AndroidView<GLSurfaceView>` + `LutChipRow` + a small banner showing the active LUT's display name + SPDX. `setEGLContextClientVersion(3)` is called explicitly so `sampler3D` works. The `LaunchedEffect(activeLut)` and the `update` lambda both call `renderer.setLut(activeLut.load())` so the GLES preview reflects the user's `HudSettings.stillsLut()` choice the instant they pick a new chip. Reachable from probe home via "Live preview LUT" or `--es pns_screen glpreview`. (4) **Build-process learning**: the first device run rendered the test pattern upside-down because GLUtils.texImage2D uploads bitmaps with row 0 at texture v=0 (the OpenGL bottom). Added the `BITMAP_VFLIP_QUAD` constant and 5-test JUnit coverage to lock the contract. (5) **Device validation on adb `8bf09993` (CPH2655 / Android 16)**: rebuilt + reinstalled; launched via `am start --es pns_screen glpreview` three times with different LUTs selected (using `adb shell run-as dev.pointandshoot` to edit `shared_prefs/pns_hud_settings.xml` directly so we could capture deterministic screenshots without scripting taps). Captured `docs/screenshots/smoke_glpreview_none.png` (identity bypass: all 8 color bars in their original colors, smooth ramp continuous, GLES uniform `uLutEnabled = 0`), `docs/screenshots/smoke_glpreview_bw709.png` (B&W BT.709 LUT collapses the bars to grayscale with the documented luma weights `Y = 0.2126R + 0.7152G + 0.0722B` — visible: white > yellow > cyan > green > magenta > red > blue > black), `docs/screenshots/smoke_glpreview_cinematic.png` (PnS Cinematic LUT pulls shadows toward teal `(0.30, 0.55, 0.70)` and highlights toward orange `(1.00, 0.65, 0.35)` exactly as `BuiltInLuts.pnsCinematic` is documented to — visible on the wedge with black-step → teal-blue and white-step → peach, and on the smooth ramp transitioning from teal-shadows to orange-highlights). Logcat across all three launches: `PNS.GLES: GLES program ready (program=3)`; no FATAL, no AndroidRuntime, no GLES infolog. (6) **BUILD_PLAN sync**: §7 "LutShader.kt" architecture-seam row flipped `[x]` (the GLES setup + 3D-texture upload + bypass-policy short-circuit are all shipped and exercised end-to-end on hardware); §7 "Live preview / video" apply-path row flipped `[x]` (the GLES seam itself is fully built and device-validated; the Camera2 source bind-in — replacing the synthetic `TestPattern` `Bitmap` with a `samplerExternalOES`-backed `SurfaceTexture` — lands with the Phase 1 capture engine). §7 "Tests shipped" row updated to enumerate the new test classes and the new full-suite tally. CHANGELOG, README screenshots section + repo layout updated. **Net: 3 new Kotlin modules (`TestPattern` + `LutPreviewRenderer` + `GLPreviewScreen`); 17 new JUnit tests (12 TestPattern + 5 LutPreviewRendererQuad); total grew from 374 to 386 across 39 suites** (verified by `Get-ChildItem app/build/test-results/testDebugUnitTest/TEST-*.xml`: 386 tests, 0 failures, 0 errors, 0 skipped). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 20 *.md doc encoding all green). |
| 2026-05-08 | **NDK scaffolding round (3 host-side modules + 1 Compose screen + 1 install script + 19 new JUnit tests + ADB-validated diagnostics)** | Closed Phase 0 of BUILD_PLAN §4 "NDK pipeline integration" by shipping a complete fallback-aware Kotlin/JNI scaffolding so the capture engine can already call into a stable encoder surface and degrade gracefully to JPEG until the Phase 1 NDK pipeline is wired. (1) **`NativeEncoders.kt`** — defensive Kotlin facade. `System.loadLibrary("pns_native")` is wrapped in `try { ... } catch (Throwable)` so a missing library never crashes the camera app. `isAvailable: Boolean`, `lastLoadError: String?`, and `version(): Int` surface runtime state for diagnostics. `encodeAvif10Hdr(...) -> Result` and `encodeJxl12Rec2020(...) -> Result` short-circuit to `Result.NotAvailable` when the .so is missing AND wrap the underlying `external fun` calls in a Throwable-catching block when loaded. Sealed `Result` hierarchy: `Success(bytes)` (with `ByteArray` content equality), `NotAvailable` (singleton), `NativeError(code, message?)`. JNI symbols use `@JvmStatic external fun nativeXxx(...)` so the C++ side sees `Java_dev_pointandshoot_NativeEncoders_nativeXxx(JNIEnv*, jclass, ...)`. (2) **`EncoderRoute.kt`** — pure-data router. `decide(profile, nativeAvailable)` returns `Decision(profile, rawWritten, tonalWritten, fallbackJpeg, downgradeReason)` per BUILD_PLAN §4 / `FAILURE_MATRIX.md` "Native encoders unavailable": RAW (DNG) is ALWAYS written; AVIF/JXL containers downgrade to JPEG when native is absent; canonical user-facing message at `EncoderRoute.DOWNGRADE_MESSAGE`. Companion `downgradedProfiles(nativeAvailable)` enumerates which profiles will downgrade. **Implementation note pinned**: `downgradedProfiles` enumerates `ImagingProfile.{StandardPro, UltraMax}` directly rather than reading `ImagingProfile.all` because `data object` singletons inside a sealed parent can be observed null during early companion init on the JVM (the `listOf(StandardPro, UltraMax)` capture sees the in-progress `INSTANCE` field as null). (3) **`NativeDiagnosticsScreen.kt`** — Compose screen surfacing the runtime state of `NativeEncoders` and the resulting `EncoderRoute` decisions per `ImagingProfile`. Reachable from probe home via the new "Native diagnostics" button OR via `--es pns_screen native`. Does NOT require `CAMERA` permission so a fresh install can validate the fallback path before the runtime permission dialog. (4) **`native/pns_native.cpp`** — JNI stubs matching the Kotlin facade. Encoder stubs return `nullptr`; `nativeVersion` returns `PNS_NATIVE_VERSION = 0`. Legacy `Java_dev_pointandshoot_Native_version` symbol preserved for backward compatibility. (5) **`native/CMakeLists.txt`** — built-out skeleton with `POSITION_INDEPENDENT_CODE`, host-side JNI include path resolution (so a non-Android `cmake -B build` doesn't fail with "jni.h not found"), commented `FetchContent_Declare` blocks for libavif / libjxl with the planned `URL_HASH SHA256=` slots, and guarded `find_library(LOG_LIB log) / find_library(JNIGRAPHICS_LIB jnigraphics)` calls under `if(ANDROID)`. (6) **`native/THIRD_PARTY.md`** — license matrix for libavif (BSD-2-Clause) / aom (BSD-2 + Alliance for Open Media patent license) / libjxl (BSD-3-Clause) / highway (Apache-2.0) / brotli (MIT). Sourcing rules: pinned tag, pinned SHA-256, license sidecar at `app/src/main/assets/legal/native/<name>/`. Pre-populated so license review can land before any source archive is fetched. (7) **`native/README.md`** — rewritten to document the Phase 0 layout, the JVM verification procedure, the `--es pns_screen native` ADB recipe, and the install-NDK path. (8) **`scripts/pns_install_ndk.ps1`** — non-interactive NDK + CMake installer (host-side helper). Locates the Android SDK, finds `sdkmanager.bat`, and runs `sdkmanager --install "ndk;26.x" "cmake;3.22.1"` with EULA acceptance piped via stdin. Idempotent (`-DryRun` flag); bails out cleanly when packages are already present; prints exact `$env:ANDROID_NDK_HOME` env vars to set. Smoke-tested locally with `-DryRun`: parses cleanly, exits 1 with helpful instructions when Android SDK Command-line Tools are not yet installed (the current host state — which is exactly the path-of-least-surprise the script is designed for). **Tests shipped**: `NativeEncodersFallbackTest` (10 tests, all green: `isAvailable` is false on the JVM unit-test classpath; `lastLoadError` is non-blank; `version()` returns `VERSION_UNAVAILABLE = 0`; `encodeAvif10Hdr` / `encodeJxl12Rec2020` return `Result.NotAvailable` instead of throwing; empty-input encode does not crash; `Result.Success` `ByteArray` content equality; `Result.NotAvailable` singleton equality; `Result.NativeError` carries opaque code + optional message). `EncoderRouteTest` (9 tests, all green: `decide` writes both files when native is available for both StandardPro and UltraMax; `decide` downgrades to JPEG when native is absent for both profiles; `rawWritten` is preserved regardless of native state; `downgradedProfiles(true)` is empty; `downgradedProfiles(false)` enumerates both AVIF + JXL profiles; every `TonalContainer` requires native in Phase 0; `DOWNGRADE_MESSAGE` is a single non-blank user-facing line). **Device validation on adb `8bf09993` (CPH2655 / Android 16)**: rebuilt + reinstalled (`assembleDebug` BUILD SUCCESSFUL, APK 33218 KB, `adb install -r` Success); launched via `am start --es pns_screen native` and captured `docs/screenshots/smoke_native_diag.png` showing "Native library: NOT LOADED" in `PnsColors.RecordRed`, `Version: 0 (unavailable)`, `loadLibrary error: dlopen failed: library "libpns_native.so" not found`, per-profile rows for Standard Pro (RAW: Lossless DNG / Tonal: DOWNGRADED to JPEG) and Ultra-Max (RAW: RAW12 (uncompressed) / Tonal: DOWNGRADED to JPEG), each carrying the canonical downgrade message verbatim. `docs/screenshots/smoke_probe_home_round10.png` shows the probe home rendering the new "Native diagnostics" button alongside the existing tiles. No FATAL/AndroidRuntime entries in `adb logcat -d`. **BUILD_PLAN sync**: §4 "NDK pipeline integration" → "`libavif` path (10-bit AVIF HDR)" body extended to enumerate the Phase 0 facade + JNI stub + JUnit coverage + device validation (headline still `[ ]` because the libavif body lands once `externalNativeBuild` is wired and the FetchContent SHA-256 is pinned); same shape for the libjxl row; "NDK pipeline callable from Kotlin" body updated to point at the shipped Kotlin facade + router. `NDK_PLAN.md` Status flipped from "planning" to "scaffolding shipped (Phase 0)"; "JNI surface (target)" section rewritten as "JNI surface (shipped Phase 0)" with the actual Kotlin signatures; "Schedule" gains a "DONE (Phase 0)" first step + a "Human action required" tail section enumerating the two remaining decisions (install the NDK + CMake on dev hosts and CI runners; pin the libavif / libjxl tags + SHA-256s). CHANGELOG, README, native/README.md updated. **Net: 3 new Kotlin modules (`NativeEncoders` + `EncoderRoute` + `NativeDiagnosticsScreen`) + 1 new PowerShell script + updated `native/CMakeLists.txt` + new `native/THIRD_PARTY.md` + rewritten `native/README.md`; 19 new JUnit tests (10 NativeEncodersFallback + 9 EncoderRoute); total grew from 386 to 410 across 42 suites** (verified by `Get-ChildItem app/build/test-results/testDebugUnitTest/TEST-*.xml`: 410 tests, 0 failures, 0 errors, 0 skipped). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33218 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Lens info probe round (2 host-side modules + DeepCapsProbeScreen extension + CapabilityGate.OIS feature + 18 new JUnit tests + ADB-validated lens metadata for all 5 cameras)** | Phase 0 follow-on closing BUILD_PLAN §3 "Confirm macro capability + min focus distance / mode-switch behavior" with typed lens / sensor info per cameraId. (1) **`LensInfoSummary.kt`** - pure-data carrier (`availableApertures: List<Float>`, `opticalStabilizationModes: List<String>`, `minimumFocusDistanceDiopters: Float`, `hyperfocalDistanceDiopters: Float`, `availableFocalLengthsMm: List<Float>`, `sensorPhysicalSizeMm: SensorPhysicalSize?`, `sensorActiveArrayPx: SensorActiveArray?`, `sensorOrientationDegrees: Int?`). Inference: `isMacroCapable` flips true when `minimumFocusDistanceDiopters >= MACRO_MIN_DIOPTERS_THRESHOLD = 15f` (~6.7 cm); `hasOpticalStabilization` flips true when any mode is non-`OFF`; `minimumFocusDistanceMeters` inverts diopters when not fixed-focus. Companion `LensInfoSummaryJson` adds `version: 1` schema with `encode`/`decode` round-trip. (2) **`LensInfoExtractor.kt`** - Android-side bridge from `CameraCharacteristics` to `LensInfoSummary` and its JSON; defensive throughout (every key read wrapped in `runCatching { ... }.getOrNull()`). (3) **`DeepCapsProbeScreen.kt`** - `runDeepCapsProbe` now emits a `lensInfo: { ... }` block per camera in the existing `deep_caps_*.json` artifact. (4) **`CapabilityGate.kt`** - adds `Feature.OpticalStabilization` (display name "Optical image stabilization") + `HardwareCaps.hasOpticalStabilization: Boolean = false`; emits a 9th gate row that flips with `caps.hasOpticalStabilization`. **Tests shipped**: `LensInfoSummaryTest` (14 tests, all green: macro inference at every probe-observed distance + the borderline @ 15 diopters case, threshold matches the documented constant, OIS inference flags only non-OFF modes, `minimumFocusDistanceMeters` inverts diopters and is null when fixed-focus, `describe` contains every salient field, JSON round-trip preserves every field exactly, encoder emits `schemaVersion`, decoder rejects schema mismatch + missing cameraId, decoder tolerates absent optional sections + skips blank entries in the OIS modes array, `SensorPhysicalSize` and `SensorActiveArray` reject non-positive dimensions, sensor physical size round-trips, apertures and focal lengths preserve order). `CapabilityGateTest` (4 new tests, all green: OIS gate flips with `hasOpticalStabilization`, OIS feature is included with the canonical display name, `HardwareCaps.hasOpticalStabilization` defaults to false for backward compat). **Initial threshold tuning**: shipped first at 10 diopters (10 cm), then tuned UPWARD to 15 diopters (~6.7 cm) after the device run revealed that the LYT-808 main wide reports exactly 10 diopters of close-focus capability and would have been falsely classified as macro. **Device validation on adb `8bf09993` (CPH2655 / Android 16)**: rebuilt + reinstalled (`assembleDebug` BUILD SUCCESSFUL, APK 33240 KB, `adb install -r` Success); launched via `am start -W -n dev.pointandshoot/.MainActivity --es pns_screen deepcaps --ez pns_autodeepcaps true`; logcat reports `DEEP_CAPS_START file=deep_caps_20260508_083111.json` then `Saved deep caps JSON: /storage/emulated/0/Android/data/dev.pointandshoot/files/deep_caps_20260508_083111.json (161903 bytes)` then `DEEP_CAPS_DONE … ok=true`. Pulled to `hfr-runs/deep_caps_round11.json` and decoded via PowerShell `ConvertFrom-Json`. **Findings**: `cameraId=0` (logical) + `cameraId=2` (LYT-808 main wide) report f/1.60, 6.06 mm focal, **10 diopters min focus (10 cm)**, 9.18 x 6.88 mm sensor, 90° orientation; `cameraId=1` (front IMX615) reports f/2.40, 3.23 mm focal, fixed-focus, 5.25 x 3.94 mm sensor, 270° orientation; `cameraId=3` (S5KJN5 UW) reports f/2.05, 2.30 mm focal, **25 diopters min focus (4 cm — confirms 🌷 super macro)**, 5.24 x 3.93 mm sensor, 90° orientation; `cameraId=4` (LYT-600 tele) reports f/2.60, 13.85 mm focal, 1.72 diopters (58 cm), 6.55 x 4.92 mm sensor, 90° orientation. **OIS gap discovered**: NO camera advertises a non-`OFF` mode in `LENS_INFO_AVAILABLE_OPTICAL_STABILIZATION` even though the OnePlus 13 ships hardware OIS on the main wide and periscope tele - vendor request-key search now tracked under DODGE_PROFILE.md "Outstanding Phase 0 follow-ons". `docs/screenshots/smoke_deepcaps_round11.png` shows the deep-caps probe in its done state (`Status: OK saved=...deep_caps_20260508_083111.json`); no `FATAL`/`AndroidRuntime` lines in `adb logcat -d`. **BUILD_PLAN sync**: §3 "Confirm macro capability + min focus distance / mode-switch behavior" V&V gate flipped `[x]`. DODGE_PROFILE.md "Next probe deltas needed" section rewritten into "Lens + sensor info (from probe)" with the actual probed values + "Outstanding Phase 0 follow-ons" tracking the vendor-OIS search. CHANGELOG, README updated. **Net: 2 new Kotlin modules (`LensInfoSummary` + `LensInfoExtractor`) + extended `DeepCapsProbeScreen` + extended `CapabilityGate`; 18 new JUnit tests (14 LensInfoSummary + 4 CapabilityGate); total grew from 410 to 428 across 43 suites** (verified by `Get-ChildItem app/build/test-results/testDebugUnitTest/TEST-*.xml`: 428 tests, 0 failures, 0 errors, 0 skipped). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Color + metering pipeline round (2 new pure-data modules + LUT validator extension + Passport target alias + 28 new JUnit tests)** | Closed three host-side BUILD_PLAN blockers without needing the Phase 1 capture engine. (1) **`PreviewLumaHistogram.kt`** — pure-data luma histogram reducer for the highlight-weighted metering pipeline; supports tightly-packed `Y8`, stride-padded `YUV_420_888` Y planes (skips trailing padding so we never attribute padding to bin 0), and a center-weighted variant for subject-tracking metering. 13 tests in `PreviewLumaHistogramTest`; closes the host-side half of BUILD_PLAN §4 "Highlight-weighted metering — preview-frame histogram pipeline" (engine wiring still pending). (2) **`LutPipeline.parseDl3(text)`** + **`LutImportValidator` routing** — Autodesk Lustre Mesh `.3dl` import: tolerates `#`-comment + Mesh-header rows, parses uniform-ramp shaper line, normalizes integer triples to `[0, 1]`, rejects non-uniform shaper / unsupported size / arity errors; `LutImportValidator.sniffFormat` routes between Cube and Dl3 (Cube wins on `LUT_3D_SIZE` etc.; Dl3 wins on >= 17 integer-only tokens; body-only `.cube` 3-int rows still route to `.cube` to preserve the existing `MalformedHeader` failure category). 12 tests in `Dl3ParserTest`; closes BUILD_PLAN §7 "`.3dl` (Autodesk) — secondary import-only" `[x]`. (3) **`BundledReferenceTargets.ColorCheckerPassport24`** — pure-data alias for the X-Rite ColorChecker Passport's 24-patch front side: identical reference values to `ColorCheckerClassic24` but with `id="passport24"`, Passport-specific `displayName` and `source`. 3 tests in `ReferenceTargetTest`. Sniffer-routing regression on `LutImportValidatorTest > rejects cube with no LUT_3D_SIZE` was diagnosed during the gate run (the `.3dl` sniffer would otherwise route a body-only `.cube` `0 0 0 / 1 1 1` payload to `parseDl3` and surface `UnsupportedSize` instead of `MalformedHeader`); fixed by requiring `>= Lut3D.SUPPORTED_SIZES.min() = 17` integers on the first line before treating the file as `.3dl`. **45 JUnit suites · 456 tests · 0 failures · 0 errors · 0 skipped** (up from 43 / 428). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33252 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **DNG LUT metadata + .spi3d import round (2 new pure-data modules + LUT validator extension + 36 new JUnit tests)** | Closed two more host-side BUILD_PLAN §7 blockers without needing the Phase 1 capture engine. (1) **`LutPipeline.parseSpi3d(text)`** + **`LutImportValidator` routing** — Sony Pictures Imageworks `.spi3d` import. Parser: `SPILUT 1.0` header, `<inputDim> <outputDim>` (only `3 3` accepted; 1D variants rejected explicitly), `<sizeR> <sizeG> <sizeB>` (only cubic shapes in `Lut3D.SUPPORTED_SIZES`), then `size³` body rows of the form `<r_idx> <g_idx> <b_idx> <out_R> <out_G> <out_B>` in arbitrary order; duplicate cells, missing cells, and out-of-range indices each surface as distinct `IllegalArgumentException` messages. `LutImportValidator.sniffFormat(text)` adds `Format.Spi3d` (the `SPILUT` keyword is sniffed before `.cube` / `.3dl` keywords); `categorize(...)` maps SPI-specific failure messages onto `FailureCategory.{MalformedHeader, OneDLut, UnsupportedSize, MalformedBody}` so the SAF toast UX stays uniform. **Implementation note pinned**: the early row-count check was reduced from `>= 4` to `>= 3` (header + dim + size) so the cubic / supported-size validation fires BEFORE the body-row check; without that change a non-cubic input like `17 33 17` produced the diagnostic-poor `spi3d file too short ...` message instead of `spi3d only supports cubic LUTs (got 17x33x17)`. **Unblocks** the AMPAS ACES + Blender Filmic `desat65cube.spi3d` rows in BUILD_PLAN §7. 16 new tests in `Spi3dParserTest`. (2) **`DngLutMetadata.kt`** — pure-data formatter for the DNG `Software` (TIFF tag 305) and `UniqueCameraModel` (DNG tag 50708) tag chain per BUILD_PLAN §7 "RAW (DNG): never baked in. The active LUT name + SHA256 are written into the DNG's `UniqueCameraModel` / `Software` tag chain". `formatSoftwareTag(appVersion, activeLut?)` produces `"Point & Shoot v<v>"` or `"Point & Shoot v<v> / LUT=<name> SHA256=<64-hex>"`; the marker is round-trippable through `parseLutMarker(softwareTag) -> ParsedMarker?` (returns null on malformed input rather than throwing), and the regex `MARKER_REGEX = LUT=([^\s]+)\s+SHA256=([a-f0-9]{64})` is exposed for desktop tooling. `formatUniqueCameraModel(deviceModel, cameraId, ...)` defaults to `"<deviceModel> (cameraId=<id>)"` so per-device profiles in `darktable` / `RawTherapee` still resolve, with an opt-in `includeLutMarkerInUniqueCameraModel = true` for forensic capture where the LUT identity must round-trip even when `Software` gets overwritten by a later editor. Sealed `LutIdentity.{Bundled, CubeFile}` adapter decouples the formatter from `LutSidecar.{BundledRef, CubeFileRef}`; init-time validation rejects whitespace in marker names AND enforces 64-lowercase-hex SHA shape so produced strings are guaranteed to round-trip through `parseLutMarker`. 20 new tests in `DngLutMetadataTest`. **Engine wire-up still pending the Phase 1 capture engine** (`Dng12Saver.writeStill` will call `formatSoftwareTag(BuildConfig.VERSION_NAME, activeLut)` once it lands); BUILD_PLAN §7 row body extended to enumerate the shipped formatter + the pending wire-up rather than flipping the headline `[ ]`. **Initial test fix during gate run**: my JUnit-test sample SHA was 65 chars instead of 64 (off-by-one transcription), surfacing as `IllegalArgumentException` from `LutIdentity.Bundled` init at every test that constructed an identity AND a ComparisonFailure on the noisy-tag tolerance test (the regex captured the first 64 chars correctly, my expected string still had 65). Fixed in same gate run; all 20 tests green. **47 JUnit suites · 492 tests · 0 failures · 0 errors · 0 skipped** (up from 45 / 456). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33269 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Root-only capability + Root Only settings drawer round (3 new Kotlin modules + 1 new Compose screen + new probe-home tile + new --es pns_screen rootsettings alias + 36 new JUnit tests)** | Closed the host-side scaffolding for BUILD_PLAN §9 "Root-only enhancements (opt-in; every feature has a non-root fallback)". (1) **`RootCapability.kt`** — pure-data state machine. `RootState` (5 states: Unknown / NotAvailable / AvailableNotGranted / Granted / Denied; only Granted returns `grantsPrivileged = true`). `Feature` (8 entries: VendorSetProp, CameraServerRestart, CpuGovernorPin, ThermalTripRead, LogcatSystemWide, VendorKeyProbe, ResolutionOverride, BacklightRead). `FeatureDescriptor` (non-blank purpose + non-blank fallback validated at init time; canonical disabled-reason `"Requires root. Fallback: <fallback>"` so a single regex extracts the fallback summary). `FEATURE_DESCRIPTORS: Map<Feature, FeatureDescriptor>` covers every enum entry. (2) **`RootGate.kt`** — pure-data feature gate (sister of `CapabilityGate`). `evaluate(state)` returns one `GateResult` per feature with `enabled` true ONLY when `state.grantsPrivileged` is true; stable enum-order list. (3) **`RootCapabilityProbe.kt`** — two-stage probe. **Stage 1** (`probeStatic()`): walks `CANONICAL_SU_PATHS` (9 entries: `/system/xbin/su`, `/system/bin/su`, `/sbin/su`, `/system/sbin/su`, `/su/bin/su`, `/system/app/Superuser.apk`, `/data/adb/magisk`, `/data/adb/ksu`, `/data/adb/ksud`) using `File.exists()` only — **no process forked**. **Stage 2** (`requestGrant(timeoutSeconds = 5)`): forks `Process.exec("su -c id")` with a 5-second timeout, reads stdout for `uid=0`, returns Granted or Denied; wrapped in `runCatching` so a misbehaving SU manager cannot crash the calling thread. Static layer is JVM-testable via `collateExistence(pathExists: (String) -> Boolean)`; active layer's parsing is JVM-testable via `parseIdOutput(idOutput: String?)`. (4) **`RootSettingsScreen.kt`** — Compose drawer that always renders, even on stock devices (so the user can SEE what they would gain by rooting). Top: status banner + "Grant Su" button (enabled only when state != Granted; pending spinner via `pending` flag) + "Re-probe" button. Tip explains that Grant Su runs `su -c id` exactly once. Each row shows display name + ENABLED/DISABLED chip + "Why: <purpose>" + "Fallback: <fallback>" + (when disabled) the canonical disabled-reason. Bottom: full `CANONICAL_SU_PATHS` list in low-emphasis text. Reachable from probe home via "Root Only" tile and from ADB via `--es pns_screen rootsettings` (no CAMERA permission required). (5) **`CameraCapabilitiesProbe.kt`** — wired `SCREEN_ROOT_SETTINGS = "rootsettings"` into both launch effects (the early CAMERA-permission-free path AND the permission-gated path), the screen-render switch, the ProbeHomeContent callback chain, and the new "Root Only" OutlinedButton on the Phase 4 / NDK-diag row. **Compile-failure fix during gate run**: my Feature enum KDoc comments contained `cpu*/cpufreq` and `thermal_zone*/trip_point_*_temp` — the `*/` token prematurely closed the Kotlin block-comment, cascading "Expecting member declaration" errors across every subsequent enum entry. Reworded the KDocs to split the path so the `*/` never appears inside a `/** ... */` block; compile succeeded immediately after. **Tests shipped**: `RootCapabilityTest` (18 tests covering catalog completeness, requireFallbacks success on shipped catalog, canonical disabledReason prefix, RootState.grantsPrivileged single-state property, evaluate disabling every feature for every non-Granted state, evaluate enabling every feature for Granted, single-feature accessor, init-time validation rejecting blank fields, schema version pin). `RootCapabilityProbeTest` (18 tests covering CANONICAL_SU_PATHS shape + no-duplicates + absolute-path constraint, collateExistence returns NotAvailable for empty predicate, AvailableNotGranted for every documented entry individually, ignores non-canonical paths, parseIdOutput recognizes `uid=0(root)` and bare `uid=0`, returns Denied for non-root uid / empty / null / SU-error stdout, ACTIVE_PROBE_TIMEOUT_SECONDS pin, probeStatic on the JVM unit-test classpath returns NotAvailable). **BUILD_PLAN sync**: §9 "Root-only enhancements" section added with the policy + 2-stage detection design + 8 enumerated enhancements each with purpose + non-root fallback + V&V gate (V&V gate `[ADB]` rows for the rooted-device flow are deferred until the device is back). **Device validation deferred (DEVICE-NEXT)**: the user noted the device will be unavailable for a period; ADB validation of the drawer on a non-rooted vs rooted state, the Magisk / KernelSU consent dialog, and the revoke flow will run as soon as it is back. **49 JUnit suites · 528 tests · 0 failures · 0 errors · 0 skipped** (up from 47 / 492). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33321 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Color-science foundation round (2 new Kotlin modules + 37 new JUnit tests)** | Shipped `HdrCurves.kt` and `ColorSpaceMatrix.kt` as the foundation for downstream HDR-aware encoding (10-bit AVIF Rec.2020, 12-bit JXL Rec.2020), color-managed LUT application, and DNG `ColorMatrix1` / `ForwardMatrix1` derivation. Both modules are pure-data Kotlin (no Android imports), so they're JVM-testable and reusable from GLSL shader-generation paths. (1) **`HdrCurves.kt`** — sRGB (IEC 61966-2-1), Rec.709 (ITU-R BT.709-6), PQ (SMPTE ST 2084), HLG (ITU-R BT.2100) EOTFs + OETFs. Every curve is a `Float -> Float` so the GLSL form is one-to-one with the Kotlin form. Constants `PQ_PEAK_NITS = 10000` and `HLG_PEAK_NITS = 1000` pinned per spec. (2) **`ColorSpaceMatrix.kt`** — sRGB / Rec.709 / Rec.2020 / DCI-P3 / ACES AP1 primaries + Bradford D65↔D50 CAT. The `linear-RGB -> CIE XYZ` matrices are computed on the fly from chromaticities (Lindbloom's per-primary scaling recipe). Standard CIE 1931 illuminants (D50 / D55 / D65 / D75 / A / F2). Matrix utilities (`multiply`, `multiplyVec`, `invert3x3`) exposed; `invert3x3` rejects singular with `IllegalArgumentException`. Above-spectral-locus primaries (notably ACES AP1 red at `(0.713, 0.293)`) accepted; only `y > 0` enforced. **Tests shipped**: `HdrCurvesTest` (19 tests covering 257-point sweep round-trips, mid-gray / break-point / unity / zero anchors, Rec.2100 PQ encoded reference at 100 cd/m^2 in [0.50, 0.52], HLG break at 1/12 linear -> 0.5 encoded, every curve monotonic). `ColorSpaceMatrixTest` (18 tests covering published matrix parity vs Lindbloom / BT.2020 / RP 431-2 / ICC within 5e-4 ~ 5e-3, Bradford D65<->D50 round-trip identity, Bradford D65->D50 maps D65 whitepoint to D50 whitepoint, whitepoint of every working space lands on its declared illuminant, invert3x3 * original = identity, singular-matrix rejection, chromaticity validation including ACES AP1). **Three compile / runtime fixes during gate runs**: (a) nested `typealias Matrix3x3 = Array<FloatArray>` rejected by Kotlin (`Nested and local type aliases are not supported`); inlined `Array<FloatArray>` everywhere. (b) `BRADFORD_M` was declared at the bottom of the object but used by `BRADFORD_D65_TO_D50` / `BRADFORD_D50_TO_D65` at the top — Kotlin object init runs top-down so they resolved to null and the static init threw NPE; moved `BRADFORD_M` BEFORE the matrices that use it. (c) `Chromaticity` first cut rejected `x + y > 1.0001` ("outside spectral locus") but ACES AP1 red sums to 1.006 by design; relaxed to only require `y > 0` (otherwise the divide explodes) and `[-0.5, 1.5]` bounds. **51 JUnit suites · 565 tests · 0 failures · 0 errors · 0 skipped** (up from 49 / 528). 37 new tests: 19 HdrCurves + 18 ColorSpaceMatrix. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33334 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Working-space presets + CICP codes round (1 new Kotlin module + 1 new public data class + 27 new JUnit tests)** | Shipped `WorkingSpace.kt` to tie together `HdrCurves` (Round 15) and `ColorSpaceMatrix` (Round 15) into a single named-preset API. (1) **`WorkingSpace`** — sealed class with 6 shipped presets (SRGB / REC709_SDR / REC2020_PQ / REC2020_HLG / DCI_P3 / ACES_AP1_LINEAR). Each carries a stable canonical id, displayName, `ColorSpaceMatrix.Primaries`, `toLinear` (EOTF) + `fromLinear` (OETF) function references, and an `ITU-T H.273 CICP` triple. Companion's `ALL` list is `by lazy` (forced — see init-order fix below). `byId(id)` resolves canonical ids; returns null for unknown. (2) **`Cicp`** — top-level data class with the 4 ITU-T H.273 fields (`colour_primaries` / `transfer_characteristics` / `matrix_coefficients` + `video_full_range_flag`). `init` validates each field in `[0, 255]`. `describe()` produces a human-readable diagnostic string (e.g. `"BT.709 / sRGB / RGB-identity / full-range"`). **CICP triples**: sRGB `(1, 13, 0, full)`; Rec.709 SDR `(1, 1, 1, narrow)`; Rec.2020 PQ `(9, 16, 9, full)`; Rec.2020 HLG `(9, 18, 9, full)`; Display P3 `(12, 13, 0, full)`; ACES AP1 linear `(2, 8, 0, full)` (sentinel "unspecified" entries since no canonical CICP code exists for AP1 + linear). **Convenience methods**: `linearToXyz(rgb)` returns linear-RGB → CIE XYZ in the working-space's native whitepoint; `toXyzD65(linearRgb)` Bradford-adapts to D65 when source whitepoint differs (notably ACES AP1 with D60-ish whitepoint). **Tests**: `WorkingSpaceTest` (27 tests covering every preset in `ALL`, unique ids, byId resolution, CICP triple parity, primaries identity, EOTF/OETF round-trip on 33-point sweep, ACES linear is identity, linearToXyz returns whitepoint XYZ, toXyzD65 on D65 working space is identity vs Bradford-adapted ACES, wrong-length rejection, Cicp init validation, describe() format for known + unknown codes). **Init-order fix during gate run**: first cut had `val ALL: List<WorkingSpace> = listOf(SRGB, REC709_SDR, ...)` as a non-lazy companion val. Kotlin's class init runs the companion `<clinit>` BEFORE nested `object` declarations are initialized, so `ALL` resolved to a list of null references and every test that read `WorkingSpace.ALL` crashed with NullPointerException. Switched to `val ALL: List<WorkingSpace> by lazy { listOf(...) }`. **52 JUnit suites · 592 tests · 0 failures · 0 errors · 0 skipped** (up from 51 / 565). 27 new tests. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33352 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **AVIF colr payload formatter round (1 new Kotlin module + 19 new JUnit tests)** | Shipped `AvifColrPayload.kt` as the pure-data byte-layout formatter for the AVIF (`avif`/`avis`) `colr` box's two shipped colour-type flavors (`nclx` and `prof`); it consumes the `Cicp` data class shipped in Round 16 directly. **`encodeNclxPayload(cicp: Cicp): ByteArray`** returns exactly 11 bytes per ISO/IEC 14496-12 §8.5.4 `nclx` flavor — 4-byte ASCII colour-type marker (`'n'`, `'c'`, `'l'`, `'x'`) + 16-bit big-endian `colour_primaries` / `transfer_characteristics` / `matrix_coefficients` + 1-byte flags where bit 7 is `full_range_flag` and bits 6..0 are reserved zero. **`decodeNclxPayload(bytes): Cicp?`** returns null on a non-`nclx` marker, throws `IllegalArgumentException` on wrong-length payloads, silently drops the reserved bits in flags for tolerance with pre-spec encoders. **`encodeProfPayload(iccProfileBytes): ByteArray`** prepends the 4-byte `'prof'` ASCII marker to a raw ICC blob; rejects empty profiles. **`decodeProfPayload(bytes): ByteArray?`** is the inverse. Pinned constants: `NCLX_TYPE` + `PROF_TYPE` + `NCLX_PAYLOAD_LENGTH = 11` + `SCHEMA_VERSION = 1`. The capture engine's AVIF muxer never has to hand-roll the bit-packing — it pulls `WorkingSpace.<preset>.cicp`, runs it through `AvifColrPayload.encodeNclxPayload(...)`, and wraps the result with the standard BMFF size + box-type prefix. **Tests**: `AvifColrPayloadTest` (19 tests covering: ASCII marker shapes, exact 11-byte length, sRGB / Rec.2020 PQ / non-full-range / high-byte CICP byte sequences, every `WorkingSpace.ALL` preset round-trips through encode → decode, decode-null on wrong markers, decode-throws on wrong lengths, decode silently drops reserved bits, ICC profile round-trip, empty-profile rejection, prof decode-null on wrong markers, schema version pin). **Test-assertion fix during gate run**: my first-cut sRGB test asserted `mc lo = 0x01` but `WorkingSpace.SRGB.cicp.matrixCoefficients` is `0` (identity / RGB-direct, the correct pick for sRGB-tagged streams stored RGB-direct in the bitstream rather than YCbCr). Updated the assertion to `0x00`. **53 JUnit suites · 612 tests · 0 failures · 0 errors · 0 skipped** (up from 52 / 593). 19 new tests. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33354 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **HDR static metadata mdcv + clli round (1 new Kotlin module + 2 new public data classes + 19 new JUnit tests)** | Shipped `HdrStaticMetadata.kt` (with the `MasteringDisplayMetadata` and `ContentLightLevel` data classes) as the pure-data byte-layout formatter for the two HDR static-metadata blobs that ride alongside the `colr` (CICP) box in AVIF / HEVC HDR streams. (1) **`MasteringDisplayMetadata`** — SMPTE ST 2086 mastering-display colour volume (ISOBMFF `mdcv`, HEVC SEI 137 / 144). Carries RGB primaries (chromaticities) + whitepoint chromaticity + min / max luminance (cd/m^2). `init` validates chromaticity bounds, enforces `y > 0` (chromaticities divide by `y`), and rejects `maxLuminance < minLuminance` or negative luminance. `primariesGbr()` returns the primaries in the spec-mandated G / B / R order. Two pre-computed presets: `REC2020_1000_NITS` (HDR10 default) and `DISPLAY_P3_1000_NITS` (Apple HDR Photo grade target). (2) **`ContentLightLevel`** — CTA-861.3 content light-level info (ISOBMFF `clli`). Carries `MaxCLL` + `MaxFALL`; both 16-bit unsigned. (3) **`HdrStaticMetadata`** — pure-data formatter object. `encodeMdcvPayload(meta)` produces exactly 24 bytes per ITU-T H.265 §D.2.27 (6 × uint16_be chromaticities in G / B / R order @ 50_000 scale + 2 × uint16_be whitepoint @ 50_000 scale + uint32_be max luminance @ 10_000 scale + uint32_be min luminance @ 10_000 scale); `decodeMdcvPayload(bytes)` is the inverse. `encodeClliPayload(clli)` produces exactly 4 big-endian bytes (`MaxCLL` + `MaxFALL`); `decodeClliPayload(bytes)` is the inverse. Both encoders throw on wrong byte lengths. The capture engine never hand-rolls the bit-packing — it pulls a `MasteringDisplayMetadata` / `ContentLightLevel` straight from the imaging profile and runs it through `HdrStaticMetadata.encode*Payload(...)`. Pinned constants: `MDCV_PAYLOAD_LENGTH = 24`, `CLLI_PAYLOAD_LENGTH = 4`, `CHROMATICITY_SCALE = 50_000`, `LUMINANCE_SCALE = 10_000`. **Tests**: `HdrStaticMetadataTest` (19 tests covering Rec.2020 + Display P3 preset parity, GBR primary order, validation rejections, exact byte-sequence parity for Rec.2020 1000 nits — `xG=8500 / yG=39850 / xB=6550 / yB=2300 / xR=35400 / yR=14600 / xW=15635 / yW=16450 / maxLum=10_000_000 / minLum=50` — encode→decode round-trip within 2-ULP chromaticity tolerance + 1e-3 luminance tolerance, decode throws on wrong lengths, scale helpers round to nearest + clamp to uint16/uint32 ranges, every constant pinned). **54 JUnit suites · 631 tests · 0 failures · 0 errors · 0 skipped** (up from 53 / 612). 19 new tests. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33367 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **ISOBMFF pasp + clap formatter round (1 new Kotlin module + 2 new public data classes + 20 new JUnit tests)** | Shipped `IsobmffSampleAspect.kt` (with the `PaspPayload` and `ClapPayload` data classes) as the pure-data byte-layout formatter for two ancillary ISOBMFF "transformative" boxes that ride alongside the `colr` (CICP), `mdcv`, and `clli` boxes in AVIF / HEVC samples: pixel aspect ratio (`pasp`, ISO/IEC 14496-12 §12.1.4.3) and clean aperture (`clap`, ISO/IEC 14496-12 §12.1.4.3). (1) **`PaspPayload`** — data class with `hSpacing : vSpacing` integer ratio; `init` enforces both fields are `>= 1`; `PaspPayload.SQUARE` (`1:1`) is the pre-computed default for square-pixel cameras. (2) **`ClapPayload`** — data class with four rationals (width, height, horizontal offset, vertical offset); width / height numerators + all four denominators must be `>= 1`; offset numerators are signed. `ClapPayload.centeredCropOf(codedWidth, codedHeight, cropX, cropY, cropW, cropH)` rephrases a top-left-anchored pixel rectangle into the spec's center-form coordinates and validates that the requested crop lies entirely within the coded image. (3) **`IsobmffSampleAspect`** — pure-data formatter object. `encodePasp(pasp)` produces 8 big-endian bytes (`uint32_be hSpacing` + `uint32_be vSpacing`); `decodePasp(bytes)` is the inverse. `encodeClap(clap)` produces 32 big-endian bytes (`uint32_be widthN/widthD/heightN/heightD` + `int32_be horizOffN` + `uint32_be horizOffD` + `int32_be vertOffN` + `uint32_be vertOffD`); `decodeClap(bytes)` is the inverse. Both decoders throw `IllegalArgumentException` on wrong byte lengths and on `uint32 > Int.MAX_VALUE` overflow. Pinned constants: `PASP_PAYLOAD_LENGTH = 8`, `CLAP_PAYLOAD_LENGTH = 32`, `SCHEMA_VERSION = 1`. **Tests**: `IsobmffSampleAspectTest` (20 tests covering payload-length constants, `PaspPayload.SQUARE` parity, validation rejections for both data classes, exact byte sequences for `encodePasp(40, 33)` and `encodeClap` with high-byte fields + negative offsets, `encode → decode` round-trips, `centeredCropOf` math for identity / centered / left-edge / overflowing / non-positive crops, decode throws on wrong lengths, schema version pin). **55 JUnit suites · 651 tests · 0 failures · 0 errors · 0 skipped** (up from 54 / 631). 20 new tests. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33379 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **AVIF auxiliary boxes irot + imir + pixi round (1 new Kotlin module + 1 new data class + 2 new enums + 24 new JUnit tests)** | Shipped `AvifAuxiliaryBoxes.kt` (with the `Rotation` and `MirrorAxis` enums + the `PixiPayload` data class) as the pure-data byte-layout formatter for the three small AVIF auxiliary metadata boxes that complete the AVIF metadata bundle alongside Rounds 17 / 18 / 19: image rotation (`irot`, ISO/IEC 23008-12 §6.5.10), image mirror (`imir`, ISO/IEC 23008-12 §6.5.12), and pixel information (`pixi`, ISO/IEC 23008-12 §6.5.6). (1) **`Rotation`** — enum with `Rot0` / `Rot90` / `Rot180` / `Rot270`. Carries both `degrees` (0/90/180/270, clockwise) and `wireValue` (0..3, `angle / 90`). `Rotation.fromDegrees(int)` normalizes any input modulo 360 and rejects non-90-multiple inputs. `Rotation.fromWireValue(int)` rejects values outside `[0, 3]`. (2) **`MirrorAxis`** — enum with `Horizontal` (top-bottom flip, wire 0) and `Vertical` (left-right flip, wire 1). Names are spec-aligned per ISO/IEC 23008-12 even though the spec's `axis_horizontal` is the layperson's vertical flip — the documentation calls this out so callers reading the spec directly aren't confused. (3) **`PixiPayload`** — data class with `bitDepths: IntArray`. `init` enforces `bitDepths.size in [1, 255]` and every depth in `[1, 255]`. Custom `equals` / `hashCode` compare arrays by content. Five pre-computed presets: `MONO_8`, `RGB_8`, `RGBA_8`, `RGB_10` (HDR10 / Rec.2020 PQ default), `RGB_12` (libjxl default). (4) **`AvifAuxiliaryBoxes`** — pure-data formatter object. `encodeIrot(rotation)` produces 1 byte (wireValue in low 2 bits); `decodeIrot` silently masks bits 7..2. `encodeImir(axis)` produces 1 byte (wireValue in bit 0); `decodeImir` masks bits 7..1. `encodePixi(pixi)` produces `1 + bitDepths.size` bytes (channel count then depths); `decodePixi` validates the declared channel count matches the payload length. All three decoders throw `IllegalArgumentException` on wrong byte lengths. Pinned constants: `IROT_PAYLOAD_LENGTH = 1`, `IMIR_PAYLOAD_LENGTH = 1`, `SCHEMA_VERSION = 1`. **Tests**: `AvifAuxiliaryBoxesTest` (24 tests covering rotation enum mapping + degree normalization + non-90-multiple rejection + wireValue OOR rejection, irot encode produces 1 byte with wireValue, irot encode→decode round-trips every rotation, irot tolerates non-zero reserved bits + rejects wrong lengths; mirror axis wire pin + OOR rejection + encode→decode round-trip + reserved-bit tolerance + wrong-length rejection; pixi preset coverage + empty/OOR rejection + content-based equals + RGBA-8 byte sequence `[4, 8, 8, 8, 8]` + RGB-10 byte sequence `[3, 10, 10, 10]` + encode→decode round-trips every preset + truncated / zero-channel / 255-channel handling; schema version pin). **AVIF metadata bundle status**: with this round, every spec-required AVIF bitstream-tag box has a pure-data, JVM-tested formatter shipped (Round 17 `colr` + Round 18 `mdcv` / `clli` + Round 19 `pasp` / `clap` + Round 20 `irot` / `imir` / `pixi`); the capture engine's AVIF muxer is now fully unblocked on the host side. **56 JUnit suites · 675 tests · 0 failures · 0 errors · 0 skipped** (up from 55 / 651). 24 new tests. `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, APK 33390 KB, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Round 21 — Root Only drawer device-validation bug fixes (2 device-found UX bug fixes + 2 new pure-data tests + on-device re-screenshot)** | First round with the OnePlus 13 reconnected over ADB after the Round 14-20 device-unavailable streak. (1) Rebuilt + reinstalled the latest debug APK on `adb 8bf09993` and ran the deferred Round 14 RootSettingsScreen V&V smoke. Two real UX bugs surfaced on the screenshot that JVM tests could not have caught alone, both fixed in the same round with new pure-data tests so a future regression cannot reintroduce them. (2) **Bug A — "Grant Su" button enabled in wrong states**: my Round 14 cut had `enabled = !pending && state != RootCapability.RootState.Granted` which kept the button enabled in `Unknown`, `NotAvailable`, AND `Denied`. On a stock device (state `NotAvailable`) it rendered as a brightly-colored PhotoOrange CTA that, if tapped, would just attempt to fork `su` and silently fail with `Exception: su: not found`. Fix: added `RootState.canRequestGrant` (pure-data, true ONLY for `AvailableNotGranted` and `Denied`); wired `RootSettingsScreen` to use the new accessor. (3) **Bug B — Duplicate fallback line on every disabled feature row**: my Round 14 cut rendered both a clean `Fallback: <text>` line AND the canonical `Requires root. Fallback: <text>` `disabledReason` line on every disabled row, so a stock-device user saw the same fallback summary twice on every single one of the 8 root-only feature rows. Fix: dropped the redundant `disabledReason` line from `RootFeatureRow`. As a side benefit the per-row vertical density tightened up enough that 3 features fit above the fold on a 1080x2400 screen instead of 2. (4) **Tests shipped**: `RootCapabilityTest` adds 2 new pure-data tests pinning `state.canRequestGrant` truth-table for all 5 states + a forward-compat sanity check. The existing 18 RootCapability tests still pass; the existing 18 RootCapabilityProbe tests still pass. (5) **Device validation evidence**: `hfr-runs/rootsettings/rootsettings_20260508_171250.png` (pre-fix; bugs visible) → `hfr-runs/rootsettings/rootsettings_20260508_171622.png` (post-fix; bugs gone). `hfr-runs/rootsettings/probe_smoke_20260508_171700.png` smoke probe re-run (109,285-char probe report regenerated, no crash, no FATAL EXCEPTION) confirms no regressions from the 7-round metadata-bundle build (Rounds 14-20). `hfr-runs/deep_caps_20260508_171842.json` (161 KB, valid JSON, includes the Round-11 `lensInfo` block for all 5 cameras) confirms the deep-caps probe pipeline is still healthy on hardware. (6) **BUILD_PLAN sync**: §9 V&V "On a non-rooted device the Root Only drawer renders..." flipped `[x]` (with screenshot evidence pre+post). The "On a rooted device" row stays `[ ]` until a Magisk / KernelSU device joins the test fleet (current device is stock LineageOS 23 without an SU manager). The JUnit-tested row body extended to enumerate the new `canRequestGrant` truth-table pin. **57 JUnit suites · 677 tests · 0 failures · 0 errors · 0 skipped** (up from 56 / 675). 2 new tests in `RootCapabilityTest`. APK 33390 KB (no size change above noise). `ReadLints` clean on the 3 modified files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Round 22 — ISOBMFF box envelope writer (1 new Kotlin module + 23 new JUnit tests)** | Shipped `IsobmffBox.kt` as the foundational pure-data writer for the canonical ISO/IEC 14496-12 §4.2 box envelope that wraps every metadata payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) into a real `(size, type, payload)` byte sequence the AVIF / HEVC muxer can stitch together. (1) `boxType(type: String): ByteArray` validates the 4-character ASCII restriction (rejects empty / wrong-length / non-ASCII / non-printable types so a typo never lands silently in the bitstream). (2) `writeBox(out, type, payload)` and `encodeBox(type, payload): ByteArray` produce the canonical `(uint32_be size, uint32_be type, payload)` envelope; when the total `8 + payload.size` exceeds `0xFFFFFFFFL`, the writer auto-promotes to the spec's `large_size` escape form (`size=1, type, uint64_be largesize, payload`) per §4.2.2. (3) `writeFullBox(out, type, version, flags, payload)` and `encodeFullBox(type, version, flags, payload): ByteArray` produce the `FullBox` flavor (`size, type, uint8 version, uint24_be flags, payload`); `version` validated to `[0, 255]` and `flags` to `[0, 0xFFFFFF]`. (4) `concatPayloads(payloads)` flattens a list of child boxes into a single byte buffer the muxer can hand to `encodeBox` for a parent container box (e.g. `meta`, `iprp`, `ipco`). Pinned constants: `SCHEMA_VERSION = 1`, `PLAIN_HEADER_SIZE = 8`, `LARGE_HEADER_SIZE = 16`, `FULLBOX_VERSION_FLAGS_SIZE = 4`, `LARGE_SIZE_THRESHOLD = 0xFFFFFFFFL`. The encoder defensively copies the payload before writing. **Tests**: `IsobmffBoxTest` (23 tests covering: `boxType` accepts canonical 4-char ASCII like `"ftyp"` / `"colr"` / `"meta"` and rejects empty / 3-char / 5-char / non-ASCII / non-printable types; `encodeBox("colr", emptyPayload)` produces 8 bytes; `encodeBox` integrates cleanly with `AvifColrPayload.encodeNclxPayload(WorkingSpace.SRGB.cicp)` to produce the documented 19-byte `colr` envelope (`0 0 0 19 c o l r ` + 11-byte sRGB CICP); `writeBox` and `encodeBox` produce identical output; `encodeFullBox("ipma", version=0, flags=0, payload)` produces the documented `(size, type, 00 00 00 00, payload)` shape; `version=255` and `flags=0xFFFFFF` encode exactly; out-of-range versions and flags rejected; `concatPayloads` flattens an empty list to a 0-byte buffer, a single-element list to a copy of the single payload, and a multi-element list (`colr` envelope + `pasp` envelope) into the documented byte sequence; header-size and large-size threshold constants pinned; defensive payload copy verified by mutating the input array after `encodeBox` returns; schema version pin). **AVIF muxer status**: with this round, the engine has *every* host-side primitive needed to mux a fully-tagged AVIF bitstream — every payload from Rounds 17-20 plus the canonical box-envelope writer. The remaining work is the actual orchestration (`MetaBox.kt` + `IpcoBox.kt` + `IprpBox.kt` + the `ItemPropertyAssociation` linkage) which can land any time after Phase 1's image encoder ships. **57 JUnit suites · 701 tests · 0 failures · 0 errors · 0 skipped** (up from 57 / 678). 23 new tests in `IsobmffBoxTest`. APK 33394 KB (+4 KB). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| 2026-05-08 | **Round 23 — AVIF Item Property Association (`ipma`) (1 new Kotlin module + 2 new public data classes + 40 new JUnit tests)** | Shipped `ItemPropertyAssociation.kt` as the pure-data formatter for the `ipma` FullBox per ISO/IEC 23008-12 §9.3.2 / ISO/IEC 14496-12 §8.11.14, the linkage layer that maps each item in a HEIF / AVIF file to the list of property indices in `ipco` that apply to that item. (1) `Association(propertyIndex, essential)` — propertyIndex 1-based per spec; init rejects `< 1` and `> MAX_LARGE_PROPERTY_INDEX (32767)`. essential per §8.11.14.3 (mandatory metadata like `colr` / `pixi` should be true; optional metadata like `irot` / `imir` is false per AVIF spec). (2) `Entry(itemId, associations)` — itemId 0..0xFFFFFFFF; associations non-empty and size <= 255. (3) `chooseVersionAndFlags(entries)` picks the minimum `(version, flags)` combination — version=0 for itemIds <= 65535 and flags=0 for propertyIndex <= 127 (most compact form per HEIF best practice). (4) `encodePayload(entries, version, flags): ByteArray` emits FullBox payload bytes; caller wraps with `IsobmffBox.encodeFullBox("ipma", version, flags, payload)`. Wire format: `uint32_be entry_count` + per-entry `(uint16/uint32 itemId, uint8 association_count, association_count × (1-byte 7-bit or 2-byte 15-bit, packed with essential bit in MSB))`. (5) `decodePayload(bytes, version, flags)` is inverse; throws on under-4-byte input + truncated item_ID + truncated association_count + truncated association. (6) `encodeBox(entries): ByteArray` picks min (version, flags), encodes payload, wraps via `IsobmffBox.encodeFullBox("ipma", ...)` so muxer gets complete ready-to-write box in one call. Pinned constants: `BOX_TYPE = "ipma"`, `SCHEMA_VERSION = 1`, `FLAG_LARGE_PROPERTY_INDEX = 0x000001`, `MAX_SMALL_PROPERTY_INDEX = 127`, `MAX_LARGE_PROPERTY_INDEX = 32767`, `MAX_SMALL_ITEM_ID = 65535`, `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `MAX_ASSOCIATION_COUNT = 255`. **Tests**: `ItemPropertyAssociationTest` (40 tests covering constant pins, Association propertyIndex bounds, Entry itemId / associations bounds, chooseVersionAndFlags for `(0,0)` / `(1,0)` / `(0,1)` / `(1,1)` / empty, byte-layout pins for entry_count=0 + single essential prop=1 (`0x81`) + max small prop=127 essential (`0xFF`) + 3-association AVIF still (`0x81 0x82 0x03`) + v=1 4-byte itemId (`0x12345678`) + v=0 f=1 2-byte association (`0x80 0xC8`), encode rejects invalid version + out-of-range flags + itemId/propIndex overflow, encode→decode round-trip for canonical AVIF still + v=1 large itemId + f=1 large property index + multiple entries, decode throws on truncated input at every layer, encodeBox 20-byte envelope for canonical AVIF still + auto-promotion of version/flags, content-based equality for both data classes). **AVIF muxer status**: with this round, the engine has every host-side primitive needed to mux a fully-tagged AVIF still — every payload from Rounds 17-20 + the canonical box-envelope writer from Round 22 + the item-property association table from Round 23. The remaining work is the actual orchestration (`MetaBox.kt` + `IpcoBox.kt` + `IprpBox.kt`). **58 JUnit suites · 741 tests · 0 failures · 0 errors · 0 skipped** (up from 57 / 701). 40 new tests in `ItemPropertyAssociationTest`. APK 33405 KB (+11 KB). `ReadLints` clean on all new files. Final `pns_verify_toolchain.ps1 -RunTests` **PASSED** (`assembleDebug` BUILD SUCCESSFUL, `:app:testDebugUnitTest` BUILD SUCCESSFUL, dep-audit + UTF-8 + parse + license inventory + CycloneDX SBOM + 21 *.md doc encoding all green). |
| | *(append next verification here)* | |

---

## 6. Master checklist

**Do not edit checkboxes without Section 3.** Keep aligned with Section 9 *Status* lines.

### Phases (product scope)

- [x] **PH0** -- HFR / constrained high-speed encoder matrix (exhaustive + encoder orchestration)
- [x] **PH1** -- Static pipeline access (`pipelineAccess` in deep caps / exhaustive)
- [x] **PH2** -- Session configuration matrix (`SessionMatrixProbeScreen`)
- [x] **PH3** -- HDR / DCG runtime session toggles (`HdrDcgRuntimeProbeScreen`)
- [x] **PH4** -- 10-bit / DR profile spots (compat + HDR runtime JSON / YUV spots)
- [x] **PH5** -- ZSL / reprocess latency *(shipped scope: Section 9 PH5; `probeScope` in `capture_latency_*.json`)*
- [x] **PH6** -- RAW / preview + DR session matrix *(shipped scope: Section 9 PH6; `probeScope` in `raw_hdr_exclusivity_*.json`)*
- [x] **PH7** -- Burst / AE bracket probe *(shipped scope: Section 9 PH7; `probeScope` in `burst_probe_*.json`)*
- [x] **PH8** -- Logical vs physical comparison (`LogicalPhysicalProbeScreen`, `-RunLogicalPhysical`)
- [x] **PH9** -- Thermal / sustained *(host shipped scope: Section 9 PH9; `phase9_thermal_*.txt`)*

### Infrastructure (host automation)

- [x] **INFRA-1** -- `pns_hfr_autorun.ps1`: suite modes, JSON clears, Phase 9 thermal labels, suite summary fields, `-RunProbeSmoke`, `-SmokeIncludeThermal`
- [x] **INFRA-2** -- `pns_verify_toolchain.ps1` + `-VerifyToolchain`; UTF-8/parse checks for scripts **and** `app/src/**/*.kt` (reject UTF-16 LE); Gradle via `gradlew` (Unix/macOS CI) or `gradlew.bat` (Windows)
- [x] **INFRA-3** -- GitHub Actions `.github/workflows/toolchain-verify.yml`: Ubuntu + Android SDK + `./scripts/pns_verify_toolchain.ps1` on push/PR to `main`/`master`
- [x] **INFRA-4** -- Dependabot `.github/dependabot.yml` (weekly GitHub Actions); CI workflow: `gradle/actions/setup-gradle@v4`, concurrency cancel-in-progress, `workflow_dispatch`
- [x] **INFRA-5** -- CI `paths` filters on push/PR (Kotlin/scripts/Gradle/wrapper/workflow); docs-only changes use **`plan-doc-verify`** or **`workflow_dispatch`**
- [x] **INFRA-6** -- `.github/workflows/plan-doc-verify.yml`: `pns_verify_toolchain.ps1 -SkipGradle` when push/PR affects only `PROBE_BUILD_PLAN.md`

---

## 7. Automation (host)

- **Sideload:** `-RunCoreProbePlan` / `-RunFullSuite` run `gradlew assembleDebug` then `adb install -r -t` unless `-SkipSideload`. `-SkipGradleBuild` installs newest debug APK. **`.\scripts\pns_hfr_autorun.ps1 -SideloadOnly`** -- install only.
- **Project root:** `-ProjectRoot <repo>` if script is not under `scripts/` relative to repo.
- **Camera:** `pm grant dev.pointandshoot android.permission.CAMERA` after device wait / root.

**Common commands**

```text
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunCoreProbePlan -ExhaustiveHfrOnly -MaxRuns 1
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunCoreProbePlanFullMatrix -Sideload -MaxRuns 1
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunFullSuite -ExhaustiveHfrOnly -MaxRuns 1
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunFullSuiteFullMatrix -Sideload -MaxRuns 1
.\scripts\pns_hfr_autorun.ps1 -AssembleReleaseOnly
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunProbeSmoke -Sideload
.\scripts\pns_hfr_autorun.ps1 -OutDir .\hfr-runs -RunProbeSmoke -SmokeIncludeThermal -Sideload
```

**Piecemeal flags:** `-RunDeepCaps`, `-RunSessionMatrix`, `-RunHdrDcgRuntime`, `-RunCaptureLatency`, `-RunRawHdrExcl`, `-RunBurstProbe`, `-RunLogicalPhysical`, `-RunExhaustive`, `-RunLegacyCamera1`, `-ExhaustiveHfrOnly`, `-ExhaustiveIncludeLogical`, `-Sideload`, `-ThermalSnapshotOnly`.

**Phase 9:** `-EncoderPauseSeconds`; thermal artifacts after core/full suites (`phase9_thermal_<core|core_matrix|full|full_matrix>_*.txt`); `-ThermalSnapshotOnly`; smoke thermal via `-SmokeIncludeThermal`. Host **clears** stale JSON on device before phases (see Section 8).

**Suite summaries:** `suite_run_summary_<suite>_*.txt` (git + paths + `adbSerial`, `deviceModel`, `androidRelease`, `deviceManufacturer`, `cpuAbi` when adb works).

- **CI full:** `.github/workflows/toolchain-verify.yml` runs full `pns_verify_toolchain.ps1` when **`paths`** match (see YAML). **`workflow_dispatch`** always runs the full gate.
- **CI plan-only:** `.github/workflows/plan-doc-verify.yml` runs `pns_verify_toolchain.ps1 -SkipGradle` when only `PROBE_BUILD_PLAN.md` changes (scripts + Kotlin UTF-8 scan, no Gradle).
- **Dependency updates:** Dependabot opens weekly PRs for GitHub Actions versions (`.github/dependabot.yml`).

---

## 8. Artifacts (signals)

| Artifact | Done signal / notes |
|----------|---------------------|
| `deep_caps_*.json` | `DEEP_CAPS_DONE` |
| `session_matrix_*.json` | `SESSION_MATRIX_DONE` |
| `hdr_dcg_session_*.json` | `HDR_DCG_SESSION_JSON`, `HDR_DCG_SESSION_DONE` |
| `capture_latency_*.json` | `CAPTURE_LATENCY_DONE`; `probeScope`; optional `CAP_LAT reprocess_session` |
| `raw_hdr_exclusivity_*.json` | `RAW_HDR_EXCL_DONE`; `probeScope`; `RAW_HDR_DR_ROW` per DR profile; per-camera `sessionSupportSummary` |
| `burst_probe_*.json` | `BURST_PROBE_DONE`; `probeScope`; `burstWallMsPerShot`, `approxBurstFps` |
| `phase9_thermal_<suite>_*.txt` | Host (`Write-Phase9ThermalArtifacts`); suite in filename |
| `logical_physical_*.json` | `LOGICAL_PHYSICAL_DONE` |
| `exhaustive_probe_*.json` | `EXHAUSTIVE_PROBE_DONE runId=` |
| `enc_probe_*.json` | `ENC_PROBE_DONE` |
| `legacy_camera1_*.json` | `LEGACY_CAM1_DONE` |

Logcat tag: **`PNS.SWEEP_SIGNAL`**.

---

## 9. Suite order and phase detail

### Suite order (automated)

| Suite | Order |
|-------|--------|
| **Core** | deep_caps -> session_matrix -> hdr_dcg -> capture_latency -> raw_hdr -> burst -> logical_physical -> exhaustive -> encoder |
| **Full** | Same through logical_physical -> **legacy Camera1** -> exhaustive -> encoder |
| **Smoke** | deep_caps -> session_matrix only |

### Phase 0 -- HFR / encoder matrix

- **Goal:** Map allowed HFR / encoder combinations.
- **Status:** Exhaustive + encoder probes + script orchestration.
- **Acceptance for [x]:** `ExhaustiveMediaProbeScreen` / encoder path + `Run-Once` / suite wiring; toolchain passes.

### Phase 1 -- Static pipeline access

- **Goal:** Per-camera pipeline flags in JSON.
- **Status:** Implemented (`PipelineAccessProbe.kt`, deep caps / exhaustive).
- **Acceptance:** `pipelineAccess` emitted in artifacts.

### Phase 2 -- Session configuration matrix

- **Goal:** `isSessionConfigurationSupported` REGULAR + HIGH_SPEED sizes.
- **Status:** Implemented (`SessionMatrixProbeScreen.kt`).
- **Acceptance:** `session_matrix_*.json` + `SESSION_MATRIX_DONE`.

### Phase 3 -- HDR / DCG runtime

- **Goal:** DR profiles x preview `OutputConfiguration` with `setDynamicRangeProfile`, incl. recommended 10-bit spot.
- **Status:** Implemented (`HdrDcgRuntimeProbeScreen.kt`, `SessionConfigurationCompat.kt`).

### Phase 4 -- 10-bit / DR spots

- **Goal:** Targeted YUV / DR checks beyond baseline HDR runtime.
- **Status:** Implemented (HDR JSON + `isSessionSupportedWithDynamicRangeImageReader`).

### Phase 5 -- ZSL / reprocess latency

- **Goal (shipped scope):** Measure still JPEG latency to first `TotalCaptureResult`, compare ZSL request-key on/off when available, measure `TEMPLATE_ZERO_SHUTTER_LAG` latency, record static YUV/private reprocessing capability flags, and query whether an input-to-JPEG reprocess session configuration is supported (no actual reprocess capture loop).
- **Status:** **Complete for shipped scope** -- `CaptureLatencyProbeScreen.kt` emits `probeScope` plus latency rows and `reprocess_input_to_jpeg_session` in `capture_latency_*.json`. End-to-end reprocess pipeline latency is explicitly **out of scope** (`probeScope.fullReprocessPipelineLatency_endToEnd` is false).
- **Acceptance for [x]:** JSON includes `probeScope`, latency tests, reprocess session probe where applicable; toolchain passes.

### Phase 6 -- RAW vs HDR exclusivity

- **Goal (shipped scope):** Session-configuration matrix per camera: preview (SurfaceTexture or YUV) + RAW output, plain and with each enumerated dynamic range profile on preview when API 33+. Emit per-camera tests and `sessionSupportSummary`.
- **Status:** **Complete for shipped scope** -- `RawHdrExclusivityProbeScreen.kt`; root JSON **`probeScope`** records scope; API below 28 writes `probeScope.reason=requiresApi28`.
- **Acceptance for [x]:** `raw_hdr_exclusivity_*.json` includes `probeScope`, matrix rows, and summary where applicable; toolchain passes.

### Phase 7 -- Burst / bracket

- **Goal (shipped scope):** Fixed-count JPEG `captureBurst` wall timing (per-shot and approximate FPS), AE compensation bracket at min/mid/max indices when the range is non-flat, `BURST_CAPTURE` capability, and AE/burst-related metadata key names (bounded lists).
- **Status:** **Complete for shipped scope** -- `BurstProbeScreen.kt`; root **`probeScope`** marks vendor-specific burst-limit enumeration as out of scope (use deep caps for that).
- **Acceptance for [x]:** `burst_probe_*.json` includes `probeScope`, burst timing, bracket entries when available; toolchain passes.

### Phase 8 -- Logical vs physical

- **Goal:** Session spot checks on logical and listed physical ids.
- **Status:** Implemented (`LogicalPhysicalProbeScreen.kt`; JSON includes `lensFacing`, `hardwareLevel`).

### Phase 9 -- Thermal / sustained

- **Goal (shipped scope):** Host-side thermal/sustained-load observability: snapshot dumps (`Write-Phase9ThermalArtifacts`), pause spacing between encoder iterations (`-EncoderPauseSeconds`), standalone `-ThermalSnapshotOnly`, smoke thermal (`-SmokeIncludeThermal`), labeled `phase9_thermal_<suite>_*.txt` outputs after core/full flows.
- **Status:** **Complete for shipped scope** -- implemented in `pns_hfr_autorun.ps1` (no dedicated in-app thermal stress probe required for this checklist).
- **Out of scope:** Exotic vendor thermal nodes not reachable via standard dumpsys/sysfs patterns from adb; continuous in-camera thermal throttling experiments.
- **Acceptance for [x]:** Section 7 flags and artifact naming verified; host scripts pass toolchain UTF-8/parse checks alongside the app build.


---

## 10. Suggested validation on hardware

- **CI first:** merge probe-affecting work only after **`toolchain-verify`** is green (Ubuntu parity with full `pns_verify_toolchain.ps1`). Plan-only edits to `PROBE_BUILD_PLAN.md` should pass **`plan-doc-verify`** (`-SkipGradle`).
- **Local gate:** before pushing Kotlin or `scripts/*.ps1` changes, run `.\scripts\pns_verify_toolchain.ps1` on Windows, or `./scripts/pns_verify_toolchain.ps1` on Unix/macOS.
- Long exhaustive: `-RunFullSuiteFullMatrix` or `-RunExhaustive` without `-ExhaustiveHfrOnly`; tune `-ExhaustiveTimeoutMinutes`.
- Release-type APK: `-AssembleReleaseOnly` (internal signing -- replace for production).
- Optional device smoke: `-RunProbeSmoke -Sideload` after installing the verified APK.
## 11. Notes

- Heavy probes: use an application/work **CoroutineScope** for background work to avoid `LeftCompositionCancellationException`.
- After `adb root`, script re-grants camera when needed.
- Reinstall before long suites so on-device JSON matches the build.
- **Windows:** save `.kt` and `.ps1` as **UTF-8** (not UTF-16 LE).

---

## Document control

| Field | Value |
|-------|-------|
| **Canonical checklist** | Section 6 |
| **Evidence trail** | Section 5 |
| **Last structural review** | 2026-05-07 -- INFRA-6 plan-doc CI + toolchain paths restored |

