# Changelog

All notable changes to **Point & Shoot** are documented here. The project adheres to [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) conventions and [Semantic Versioning](https://semver.org/) once tagged releases begin.

## Unreleased

### Added

- **Android Auto Backup** — `AndroidManifest` **`allowBackup=true`** with **`pns_backup_rules.xml`** / **`pns_data_extraction_rules.xml`** allow-listing app **SharedPreferences** (preview chrome, HUD, welcome flow, diagnostics, vendor highlight AE, root capability snapshot, composition guides, **UI hints**) so settings can restore after reinstall when the platform backup path runs.

- **Preview flash modes** — `PreviewFlashMode` (`Off` / `Auto` / `On` / `Torch`) persisted in **`PreviewChromePreferences`**; **`PreviewFlashPolicy`** wires **`CONTROL_AE_MODE`** (via existing auto AE program) plus **`FLASH_MODE`** on preview repeating requests and **`FLASH_MODE_SINGLE`** on single RAW stills; AE bracket bursts force flash **off**. **7×7** grid: merged **Extra shutters** quick tile (tap + volume) + **Flash** tile (short-press cycle, long-press menu); **`PNS.ChromeUx`** `quickActions=` includes **`extraShutter`**, **`flash`**. **`PreviewFlashPolicyTest`** covers AE mapping + cycle.

- **Variable flash / torch strength (API 33+)** — when the HAL advertises **`FLASH_STRENGTH_LEVEL`** plus **`FLASH_INFO_STRENGTH_DEFAULT_LEVEL`** / **`FLASH_INFO_STRENGTH_MAXIMUM_LEVEL`**, **`PreviewFlashPolicy`** sets **`FLASH_STRENGTH_LEVEL`** on preview torch (including **On**→torch fallback) and on still **`FLASH_MODE_SINGLE`**. Uses the active **`CameraDevice`** session only (no parallel **`CameraManager.turnOnTorchWithStrengthLevel`** while the camera is open).

- **Material 3 snackbar host** — **`MainActivity`** hosts **`SnackbarHost`** and provides **`LocalPnsSnackbarHostState`** so **`PreviewEngineScreen`** can show non-blocking errors and hints without adding preview chrome bands.

- **Immersive gesture hint** — **`PnsUiHintsStore`** (**`pns_ui_hints`**, included in Auto Backup rules) records a one-time snackbar after the first normal preview session (skipped when preview ADB automation extras are active).

- **`PnsUserFacingErrors`** — classifies common still/bracket failure **`Throwable`** messages into short user-facing lines; covered by **`PnsUserFacingErrorsTest`**.

- **Sprint 4.4 (`CONTROL_AUTOFRAMING`)** — `PreviewAutoFraming` sets **`CONTROL_AUTOFRAMING`** ON for preview repeating when **API ≥ 35**, characteristics report **`CONTROL_AUTOFRAMING_AVAILABLE`**, and **Settings → HUD → Camera2 auto-framing** is enabled (default off).

- **Sprint 4.4 (stabilization + post-RAW boost)** — `PreviewStabilization` applies **OIS** / optional preview **EIS** from `HudSettings` (Settings → HUD: lens OIS default on, preview EIS default off; EIS skipped for HFR targets ≥120 fps and for still captures). `PreviewPostRawSensitivity` applies **`CONTROL_POST_RAW_SENSITIVITY_BOOST`** at the midpoint of the advertised range when the HUD toggle is on (default off) and manual ISO/shutter are not active. Wired into preview repeating, RAW/bracket stills, and OEM macro session-parameter template (`PreviewEngineScreen`).

- **Sprint 4.4 (HDR / 10-bit preview session, host)** — **`PreviewHdrSessionSupport`** picks a **`DynamicRangeProfiles`** ID (recommended 10-bit first on API 34+, then sorted fallbacks) only when **`CameraDevice.isSessionConfigurationSupported`** accepts the real multi-output list (**`SessionConfigurationCompat.isMultiOutputSessionSupportedWithDynamicRangeOnPreview`**). **`outputConfigurationsWithOptionalStreamUseCases`** applies **`OutputConfiguration.setDynamicRangeProfile`** on the preview surface; **`PreviewController`** retries session create without HDR and/or without stream hints on failure. **Settings → HUD → HDR / 10-bit preview session** (default off). Log: **`PNS.AdbValidation`** **`previewSessionDynamicRange profile=…`**. JVM: **`PreviewHdrSessionSupportTest`**.

- **Sprint 4.4 (Camera2 session / readout)** — Stream use-case hints on preview + macro **`SessionConfiguration`** (`Camera2SessionCompat` + `PreviewEngineScreen`); logical-camera **Phy** readout from **`LOGICAL_MULTI_CAMERA_ACTIVE_PHYSICAL_ID`**; **`PreviewAeAntibanding`** on OEM macro session-parameter template; RAW still **`lastStatus`** progress strings.

- **Sprint 4.4 (Camera2 extensions + reprocess probe export)** — **`CameraExtensionSupport`** (markdown in **`buildProbeReport`**); **`CameraExtensionSessionSmokeRunner`** + **`CameraExtensionSmokeScreen`**; **`pns_screen=cameraextsmoke`** (**`PNS_SCREEN_CAMERA_EXT_SMOKE`**, optional **`pns_preview_camera_id`**); **`PreviewReprocessStillHints`** section in probe export; **`CapabilityGate`** **`Feature.CameraExtensions`** / **`Feature.ReprocessSession`**. JVM: **`CameraExtensionSupportTest`**.

- **Milestone 10.1–10.2 (host)** — Probe Markdown export: **RAW12 / RAW10 / RAW_SENSOR** stream tables + per-camera **`rawPickEffective=`**; **HFR rollup** line; **4s** cooperative scan budget with **`degraded=true`** truncation; embedded **`DeviceCameraCapabilityCache`** JSON (`schemaVersion`, `fingerprintSha256Prefix`, per-id JPEG/RAW sizes, HFR maxima). **`RawCaptureSupport.pickRawOutput`** now prefers **RAW12 → RAW10 → RAW_SENSOR**. Pure **`FocalSlotAvailability`** (**≥12 MP** gate for digital eq. slots) + **`FocalSlotAvailabilityTest`**. **`RawCaptureSupportTest`** covers **`rawPickEffectiveLabel`** + empty pick maps (JVM stubs cannot construct **`android.util.Size`** for full ordering tests).

- **Milestone 10.1 / 10.8 / 10.9 (host)** — Shallow probe build runs on **`Dispatchers.Default`**; engineering hub **Probe snapshot** shows **`Shallow scan: …ms cameras=N degraded=…`**; **`DODGE_PROFILE.md`** master routing table; **`docs/camera2_reference_qr_barcode_appendix.md`** stub linking the static **`CAMERA2_KEYS`** catalog.

- **ADB automation — shallow scan hub validate** — **`scripts/pns_shallow_scan_hub_validate.ps1`** cold-starts **`pns_screen=probehub`**, asserts **`PNS.ProbeHub`** + **`PNS.Probe`** log lines (ring-buffer-safe logcat filter), writes **`shallow_scan_hub_validate.json`**; integrated into **`pns_automation_smoke.ps1`** (optional **`-SkipShallowScanHubValidate`**); **`pns_probe_append_section5.ps1`** accepts schema **`pns.shallow_scan_hub_validate.v1`**.

### Fixed

- **Scripted RAW / bracket ADB — preview FPS seed** — **`PreviewEngineScreen`**: when **`adbSequentialRawStills`** or bracket automation is active, seed **`selectedFps`** to **60** (not **90**) so **`controller.setDesired`** matches the sub-120 RAW path on cold start; reduces HAL **`captureRawStill`** timeouts on some OEMs.

- **Scripted RAW still (ADB / H dial)** — **`PreviewEngineScreen`**: when **`automationSuppressFacePipeline`** is true (sequential RAW / bracket automation), **do not** attach the YUV analysis **`ImageReader`** for H-dial metering, histogram, or zebra alone; only attach when the face HUD still needs YUV. Prevents **`wantYuv=true`** + **`suppressFacePipeline=true`** (extra **`PNS.PreviewSessionCtx`** restarts, HAL **`captureRawStill`** timeouts, **`ERROR_CAMERA_DEVICE`** on some OEMs).

- **Capture bisect #3 (imaging profile init)** — **`PreviewEngineScreen`**: drop Milestone 9 **`runCatching`** / **`StandardPro` fallback** and singleton **`listOf`** touch in **`imagingProfile`** **`remember`**; keep **`SideEffect`** → **`setImagingProfileForStreams`** so JPEG companion stream shape still tracks the HUD profile.

- **Capture bisect #5 (post-RAW sensitivity on still)** — **`PreviewEngineScreen`**: omit **`PreviewPostRawSensitivity.applyIfCompatible`** on RAW still and bracket **`TEMPLATE_STILL_CAPTURE`** builders (preview repeating unchanged) to bisect OEM HAL timeouts.

- **Capture bisect #4a (REGULAR session stream-use-case hints)** — **`PreviewEngineScreen`**: pass **`streamHints = false`** into **`createRegularCaptureSessionWithRetries`** so **`OutputConfiguration.setStreamUseCase`** is never applied on the normal preview+RAW(+JPEG) session path (macro session path already omitted hints).

- **Scripted RAW still — post-`stopRepeating` settle** — **`PreviewEngineScreen`**: when **`adbValidationShotLabel`** is set, wait at least **420 ms** (vs **160 ms**) before **`sess.capture`** so logical-camera HALs can settle after **`stopRepeating`**; logs **`PNS.AdbValidation`** **`captureRawStill afterStopRepeatingDebounceMs=…`**.

- **`pns_photo_capture_verify.ps1`** — optional **`-SweepCameraIds`**: cold-starts preview with **`pns_preview_camera_id`** for **`(default),0,1,2,3`** until success or all seeds fail; per-seed artifacts under **`seed_*`**. Logcat: prefer **`--pid=`** from **`pidof`**, then tag filter, then broad **`-t 12000`** pull filtered in-host for **PNS** / **`captureRawStill`**. **`pns_capture_pipeline_verify.ps1`** forwards **`-SweepCameraIds`**.

- **Capture bisect #4b (`maybeRestart` 0 ms debounce) — rejected** — Setting **`MAYBE_RESTART_DEBOUNCE_MS = 0L`** caused **`CameraAccessException` / `CAMERA_DISCONNECTED`** during **`onConfigured` → `startRepeating`** on CPH2655; tree keeps **48 ms**.

- **Photo mode preview FPS** — **`PreviewEngineScreen`** now passes **`selectedFps`** into **`PreviewController.setDesired`** in Photo mode (previously forced **120**), so the FPS sheet / readout applies and **DNG** can run when preview is below **120 fps** (RAW **`ImageReader`** session).

- **Highlight (H) metering at default 120 fps** — **`CommandDialMode.H`** now triggers an automatic cap to the highest quick FPS **below 120** (same requirement as the YUV **`ImageReader`** path in **`PreviewController.createSession`**), so highlight-weighted metering stays wired after honoring **`selectedFps`** for Photo.

### Changed

- **`WelcomePermissionsScreen`** — when a **required** runtime permission is still off, shows **Open app settings** (`ACTION_APPLICATION_DETAILS_SETTINGS`); camera step adds explicit **Don\u2019t allow** recovery copy. **Optional** mic/location steps gain **Skip (optional)** (advance without granting).

- **`DebugMenuScreen`** (engineering hub) — camera-permission banner adds **Open app settings**; probe destination rows set a combined **`contentDescription`** for screen readers; **Probe snapshot** shows the **Shallow scan** timing line after **`buildProbeReport`** completes.

- **`CalibrateScreen`** / **`LutImporterScreen`** — removed **Toast** duplicates where inline status / result text already conveys the same outcome (**`BUILD_PLAN`** single-channel UX).

- **7×7 quick settings** — **`contentDescription`** now includes **on/off**, self-timer seconds, or **current flash mode** for TalkBack.

- **`PreviewEngineScreen`** — **Geotag:** when **fine location** is revoked while **Save location in files** was on, clearing the pref shows a **snackbar** (“Location off — new photos won't be geotagged.”). **Other:** DND policy reminder, DNG + bracket capture failures, volume-key capture hints, bottom-tray DNG FPS guard, and FPS sheet root-only targets use **snackbars** (via **`pnsShowSnackbar`**) instead of toasts where applicable; still uses **Toast** for **`ACTION_IMAGE_CAPTURE`** early companion-JPEG failure before **`finish`**.

- **`PreviewChromePreferences` / preview engine** — ADB **`pns_preview_self_timer_sec`** seeds the in-session timer via **`applySessionOnly`** (no **`SharedPreferences` write**), so automation and **`pns_chrome_ux_gate.ps1`** no longer leave a **3 s** timer persisted; disk default stays **0** (off). Default **`previewFlashMode`** remains **Auto** in the data class and **`load()`** defaults.

- **`GalleryThumbnail`** — **`openMediaWithSystemResolver`**: if **`ACTION_VIEW`** fails for the resolved MIME type, retry **`VIEW`** with **`*/*`**, then **`ACTION_SEND`** via **`createChooser`** so users can still share or open the capture; clearer Toast only when nothing matches.

- **`CameraCapabilitiesProbe`** — probe hub **Markdown** export (**`CreateDocument`**) shows a **Toast** when the destination cannot be opened or the write fails (still logs **`Export failed`** for diagnostics).

- **Sprint 4.4 (partial)** — **`PreviewAeAntibanding`** on preview + RAW still + bracket when advertised. Tap-to-focus: one-shot **`sess.capture`** with **`CONTROL_AF_TRIGGER_START`** + **`CONTROL_AE_PRECAPTURE_TRIGGER_START`** (skipped on high-speed constrained preview). **`PreviewStillCaptureHints`**: **`CONTROL_ENABLE_ZSL`** when JPEG still + key advertised + no manual sensor overrides; **`JPEG_ORIENTATION`** + optional **`JPEG_GPS_LOCATION`** on still **`CaptureRequest`** (single + bracket). **`StillCaptureMetadata`**: **`TAG_USER_COMMENT`** gains focus distance / lens state / AF state / rolling-shutter skew when **`CaptureResult`** provides them. **`PreviewStillCaptureHintsTest`** (orientation normalization).

- **`scripts/pns_pull_dcim_captures.ps1`** — **`adb pull`** of **`/sdcard/DCIM/Point & Shoot`** (ampersand-safe path) to **`hfr-runs/pull_dcim_*`** or **`-OutDir`**; **`pns_adb_device.env`** / **`-Serial`**. Supports **Sprint 7.3 / Milestone H.1** desktop validation (**`STORAGE_STRATEGY.md`**). Documented in **`BUILD_PLAN.md`** (global toolkit + Sprint **7.3** / **H.1**), **`CLI_BUILD_AND_SIDELOAD.md`**, **`PROBE_BUILD_PLAN.md` §2**, **`pns_adb_device.env.example`**, **`.cursor/rules/adb-device-env.mdc`**. **`STORAGE_STRATEGY.md`** open question **DCIM vs Pictures** corrected to match **`CaptureStorage`**.

- **`scripts/pns_adb_preview_validate.ps1` (`-ChromeUxPack`)** — **`chrome_ux_smoke.json`** now asserts **`flashQsGrid7Ok`** ( **`quickActions=`** includes **`flash`**) and **`flashPreviewHardwareOk`** (**`PNS.ChromeUx`** **`flashPreviewHardware=true|false`** once per preview session). **`scripts/pns_chrome_ux_gate.ps1`** and **`pns_probe_append_section5.ps1`** include the same fields in **`chrome_ux_gate.json`** / §5 rows.

- **`PreviewEngineScreen`** — ADB self-timer seed logs **`PNS.ChromeUx`** **`selfTimerSec=`** with the **normalized** intent value (avoids stale **`chromePrefs.current`** same-frame read). **`PreviewController`**: one-shot **`flashPreviewHardware`** ChromeUx line after first preview request assembly.

- **`scripts/pns_gradlew.ps1`** — **Windows PowerShell 5.1:** Gradle tasks like **`:app:assembleDebug`** were bound to a trailing **`[string]$JdkHome`** parameter instead of reaching **`gradlew.bat`**. The script now parses only an explicit **`-JdkHome <path>`** pair from **`$args`** and forwards the rest to Gradle (no **`[CmdletBinding]`** so extra args are not rejected).

- **`scripts/pns_adb_preview_validate.ps1` (`Write-MediaStorePnsProbe`)** — **Ampersand-safe** `adb shell` for **`DCIM/Point & Shoot`** (single-quoted paths); lists **`Ultra-Max/`**; **`find`** fallback for **`pns_*.{dng,jxl,avif}`**; **`mediastore_probe.json`** adds **`mediaTailPnsDisplayNameHits`**. Fixes false **`dcimHasPnsCapture=false`** when **`&`** was parsed as shell background.

- **`scripts/pns_probe_append_section5.ps1`** — **§5** mediastore row includes **`mediaTailPnsDisplayNameHits`** when present.

- **`BUILD_PLAN.md` (Milestone 7 / Sprint 7.3)** — Split **MediaStore**: **[ADB]** DCIM probe **closed** (evidence **`hfr-runs/mediastore_probe_retest_20260511`**); **[MIXED]** gallery/desktop remains **Milestone H**.

- **`STORAGE_STRATEGY.md`** — Validation gate text documents the **`&`** / **`adb shell`** pitfall and new **`mediastore_probe`** fields (gallery scroll remains human **§9**).

- **`BUILD_PLAN.md` (Milestone 7 / Sprint 7.3)** — **[MIXED]** backpressure row **closed** on **`CAPTURE_ARCHITECTURE.md`** acceptance gates + **`perf-runs/reader_backpressure_validate_raw_and_bkt3.md`** + **`PROBE_BUILD_PLAN.md` §5** ( **`raw_still_x10`** + **`bracket_bkt3`** logs, all counts **0**).

- **`CAPTURE_ARCHITECTURE.md`** — Sprint **7.3 acceptance gates** table + **`-Command`** example for dual **`‑LogPath`** rollups.

- **`PERFORMANCE_BUDGETS.md`** — Validation gates: **`[x]`** reader backpressure rollup vs bracket / overflow rows.

- **`PROBE_BUILD_PLAN.md` §5** — Row **Sprint 7.3 — backpressure acceptance gates (pass)**.

- **`BUILD_PLAN.md` (Milestone 7 / Sprint 7.1)** — **Perfetto** **[MIXED]** baseline closed on first fleet **light** trace (**`scripts/pns_capture_perfetto_light.ps1`**, **`PROBE_BUILD_PLAN.md` §5** **2026-05-11**); optional deeper **SurfaceFlinger** / **GPU** pbtxt configs called out as backlog. **[ADB]** **`gfxinfo`** baseline remains via **`scripts/pns_capture_gfxinfo_baseline.py`**.

- **`PERFORMANCE_BUDGETS.md`** — § **Perfetto & frame jank**: **Option C** (Windows + device **`/system/bin/perfetto`** light mode, **`adb root`** note for **CPH2655**), validation-gate pairing text for **`perf_*.md`** + **`perfetto_*.perfetto-trace`**, purpose paragraph aligned with **§5** evidence.

- **`scripts/pns_hfr_autorun.ps1` (`-PerfReport`)** — Perfetto section now points at **`pns_capture_perfetto_light.ps1`** instead of a static **not captured** line.

- **`PROBE_BUILD_PLAN.md` §5** — Row for **Sprint 7.1** **Perfetto** light trace + paired **`perf_*.md`**; host-doc row **2026-05-11** no longer claims trace **TBD**.

- **`scripts/pns_adb_device.env.example`** + **`.cursor/rules/adb-device-env.mdc`** — Document **`PNS_ADB_SERIAL`** (USB serial from **`adb devices`**) and **`-Serial`** overrides on **`pns_milestone6_gate.ps1`**, **`pns_adb_preview_validate.ps1`**, **`pns_hfr_autorun.ps1 -PerfReport`**, **`pns_capture_perfetto_light.ps1`**, …). Expanded the list of scripts that read the env file when **`-Serial`** is omitted.

- **`scripts/pns_milestone6_gate.ps1`** — Optional **`-Serial`** forwarded to **`pns_adb_preview_validate.ps1`** so a specific device can be selected when **`pns_adb_device.env`** is unset or you need to override **`PNS_ADB_SERIAL`**.

- **`scripts/pns_automation_smoke.ps1`** — **`-AppendSection5`**: **`mediastore_probe.json`** uses **`pns_probe_append_section5 -PassOnly`** only when **`dcimHasPnsCapture`** is true so **§5** still gets a row when the probe reports empty DCIM (OEM indexing path).

- **`scripts/*.ps1` (Windows PS 5.1)** — Replaced Unicode em dashes with ASCII **` - `** in strings/comments and escaped leading **`[`** in **`Write-Host "`…`** messages (`` `[ ``) so **`[System.Management.Automation.Language.Parser]::ParseFile`** succeeds when files are UTF-8 without BOM. Avoids accidental double UTF-8 BOM on save.

- **`scripts/pns_verify_toolchain.ps1`** — Discovers every **`scripts/*.ps1`** for UTF-8 + parse checks (no manual list drift); scans **`app/src/test/java/**/*.kt`** for UTF-16 LE the same way as **`app/src/main/java`**.

- **`PerfBudget.Defaults`** — **`STILL_IMAGE_READER_MAX_IMAGES`** pins still / metering / YUV **`ImageReader` `maxImages`** (**4**); **`ENCODE_LANE_DRAIN_WAIT_MS`** pins BKT pre-bracket **`ioExecutor`** drain (**200 ms**). **`PreviewEngineScreen`** uses both; **`CAPTURE_ARCHITECTURE.md`**, **`PERFORMANCE_BUDGETS.md`**, **`BUILD_PLAN.md`** Sprint **7.3** aligned. **`.github/workflows/build-signed.yml`** runs **`pns_verify_toolchain.ps1 -RunTests`** before **`assembleRelease`** (parity with **`toolchain-verify.yml`**).

- **Capture / encode lane** — **`PreviewController`** still / metering **`ImageReader`** instances use **`PerfBudget.Defaults.STILL_IMAGE_READER_MAX_IMAGES`** (`maxImages`; aligned with **`CAPTURE_ARCHITECTURE.md`**); superseded or post-process-discarded RAW/JPEG frames log **`PNS.Reader`** **`drop oldest`** (still vs bracket channels) for **`pns_hfr_autorun.ps1 -PerfReport`** and scripted greps. **BKT** starts only after **`ioExecutor`** drains (**`PerfBudget.Defaults.ENCODE_LANE_DRAIN_WAIT_MS`** noop wait) plus best-effort reader queue discard; timeout → **`PNS.AdbValidation`** **`encode_lane_busy`** and Toast **"Engine busy - retry"**.

- **Preview finder** — **`TexturePreviewFit`** uses **center-crop** (`max` scale) for **`TextureView.setTransform`** and **`mapBufferToView`** / **`mapViewToBuffer`** so the live image fills the view (BUILD_PLAN “no side pillarbars”); **`applyPreviewTextureTransform`** uses **`desiredSurfaceSize ?: currentSurfaceSize`** to match Compose sizing. Default **`staticPreviewRotationDeg`** for **new** prefs is **90°** (clockwise spin baseline — fleet still tunes via **Spin**).

- **Preview chrome layout** — **CommandDial** (**M / H / S / BKT**) is **always visible** in the bottom tray **right of shutters** (no Tune FAB popout). **7×7** adds wired quick toggles: horizon, Eye AF, video tally, max brightness, preview DND, tap-to-capture, volume keys. **`BUILD_PLAN.md`** — mandatory **device screenshots** for UI changes.

- **`BUILD_PLAN.md` (Milestones 6–7)** — Sprint **6.3** LUT FPS budget **[ADB]** closed against existing **`milestone6_gate.json`** §5 evidence; encoding/export follow-ups called out as backlog (not 6.3 gates). Sprint **7.2** failure-matrix **`pns_failure_matrix_smoke.ps1`** + **7.4** **`CapabilityGate`** UX marked **[HOST]** complete.

- **`BUILD_PLAN.md` (Milestone 6)** — Kickoff block for **Color, calibration & LUT pipeline**: sprint order **6.1→6.2→6.3**; explicit note that **`DngCreator`** exposes **no public API for DNG tag 50708** (`UniqueCameraModel`); **`DngLutMetadata`** / **`Dng12Saver`** KDoc aligned.

- **`BUILD_PLAN.md` (Milestone 5)** — Sprint **5.5** preview sizing description aligned with **cover-fit** + clipped finder (**`TexturePreviewFit.smallestCoveringAxisAlignedRectWithAspect`**); Sprint **5.3** closed (**Snap AF**, **`HardwareCapsSnapshot`**, **[MIXED] Super Macro gate**). Evidence: **`PROBE_BUILD_PLAN.md` §5** ( **`hfr-runs/adb_preview_validate_20260510_061124/`** , **`super_macro_gate.json`** **`pass: true`** ) + **`pns_verify_toolchain.ps1 -RunTests`**.

- **`scripts/pns_probe_append_section5.ps1`** — Appends **§5** from **`milestone6_gate.json`**, **`mediastore_probe.json`**, **`chrome_ux_gate.json`** (**`pns.chrome_ux_gate.v1`**), **`super_macro_gate.json`**, or **`failure_matrix_smoke.json`** (**`pns.failure_matrix_smoke.v1`**, schema or fingerprint). **`scripts/pns_chrome_ux_gate.ps1`** now emits **`schema`** on **`chrome_ux_gate.json`**. **`scripts/pns_failure_matrix_smoke.ps1`** writes **`schema`** on **`failure_matrix_smoke.json`**; optional **`-AppendSection5`** / **`-ProbePlan`** after **`pass: true`**. **`scripts/pns_automation_smoke.ps1`**: optional **`-RunFullAdbPreviewValidate`** + **`-RequireMediaStoreDcim`**; optional **`-AppendSection5`** / **`-ProbePlan`** (all-green smoke only) runs **`pns_probe_append_section5.ps1 -PassOnly`** for gate JSON under that run’s **`outDir`** (**`chrome_ux_gate`** only when adb was authorized). Successful probe append ends with **`exit 0`** so callers are not polluted by a non-zero **`adb get-serialno`** **`LASTEXITCODE`** when adb is offline.

### Added

- **`scripts/pns_capture_gfxinfo_baseline.py`** — After force-stop + **`dumpsys gfxinfo … reset`**, cold-starts **`MainActivity`** preview (`pns_screen=preview`), settles, then writes **`perf-runs/gfxinfo_<stamp>_serial-<adb>.txt`**. **`--serial`** or **`scripts/pns_adb_device.env`** (`PNS_ADB_SERIAL`). First fleet artifact: **`perf-runs/gfxinfo_20260510_232327_serial-8bf09993.txt`** (**§5** **2026-05-11**).

- **`scripts/pns_analyze_reader_backpressure.ps1`** — Sprint **7.3** host rollup: scans **`logcat_*.txt`** for **`PNS.Reader`** **`drop oldest`** (**`queue=`** / **`channel=`**) and **`encode_lane_busy`** / encode-lane drain timeouts; **`-LogDir`** is recursive and skips **`*_app_pid.txt`**. Samples: **`perf-runs/reader_backpressure_smoke_20260511_030304.md`**, **`perf-runs/reader_backpressure_validate_raw_and_bkt3.md`** (**§5** **2026-05-11**).

- **`scripts/pns_capture_perfetto_light.ps1`** — Device **`/system/bin/perfetto`** **light** mode (**`-t`**, **`-a dev.pointandshoot`**, default **gfx**/**view**/**sched**), writes under **`/data/misc/perfetto-traces/profiling/`**, **`adb pull`** to **`perf-runs/perfetto_<utc>_serial-<adb>.perfetto-trace`**. Runs **`adb root`** by default (many OEMs need it for the write path). Optional **`-AlsoPerfReport`**. First fleet pull: **`perf-runs/perfetto_20260511_033005_serial-8bf09993.perfetto-trace`** (**§5** **2026-05-11**).

- **Milestone 9 chrome gate** — **`PreviewChromeGrid7x7`** now logs **`PNS.ChromeUx`** **`grid7=layout`** (with **`settingsAt`**, **`quickActions`**, and existing **`quickGrid`** tail) so **`pns_chrome_ux_gate.ps1`** can assert **`grid7Ok`** against logcat.

- **`scripts/pns_root_capability_adb.ps1`** — USB ADB **transport root** probe: optional **`adb root`**, captures **`adb shell id`** (expect **`uid=0(root)`** when adbd is root), best-effort **`adb shell su -c id`**, writes **`root_capability_adb.json`** (**`schema`**: **`pns.root_capability_adb.v1`**). **`pns_probe_append_section5.ps1`** fingerprints **`pns.root_capability_adb.v1`** for **§5** rows.

- **`scripts/pns_hfr_autorun.ps1 -PerfReport`** — Writes **`perf-runs/perf_<stamp>.md`**: compares **`am start -W`** cold start to **800 ms** and **`dumpsys meminfo`** PSS to **180 MB** (see **`PerfBudget.Defaults`** / **`PERFORMANCE_BUDGETS.md`**); counts **`PNS.Reader`** **`drop oldest`** lines in a log tail. Optional **`-Serial`** and **`scripts/pns_adb_device.env`** (`PNS_ADB_SERIAL`, USB serial). Pair with **`scripts/pns_capture_perfetto_light.ps1`** (or Studio / desktop **perfetto**) for Sprint **7.1** traces (**`PERFORMANCE_BUDGETS.md`**).

- **`scripts/pns_device_screencap.ps1`** — Writes **`adb exec-out screencap -p`** PNGs by streaming **`Process.StandardOutput`** to disk (PowerShell **`Set-Content`** / naive pipelines corrupt binary captures on Windows PS 5.x).

- **`scripts/pns_chrome_ux_gate.ps1`** — **`Save-LogcatTail`** uses a **250k** mixed ring tail plus tag-filtered **`PNS.ChromeUx`** / **`PNS.AdbValidation`** supplement so noisy OEM HAL spam does not evict **`seedOk`** / **`grid7=`** lines before grep.

- **Milestone 9 — self-timer (Sprint 9.11)** — **`PreviewChromePreferences.selfTimerDelaySec`** (**0 / 3 / 5 / 10**); grid timer icon cycles + cold-start **`PNS.ChromeUx`** **`selfTimerSec=`**; countdown overlay + **`triggerStillCapture()`** for volume-up still, tap-to-shoot, bottom shutter, **Save DNG** (**BKT** unchanged). **`--ei pns_preview_self_timer_sec`** (`EXTRA_PNS_PREVIEW_SELF_TIMER_SEC`) seeds prefs from **`am start`**; **`pns_chrome_ux_gate.ps1`** passes **`3`** and asserts **`selfTimerOk`**. **`pns_adb_preview_validate.ps1 -ChromeUxPack`** writes **`chrome_ux_smoke.json`**. **`PreviewChromePreferencesTest`**.

- **Milestone 9 (started)** — **`pickCameraIdFromM23Resolve`** + **`PickCameraIdFromM23ResolveTest`**: preview cold-start prefers **`resolveFocalMmSlot(M23)`** wide camera instead of raw **`cameraIdList` order**; logs **`PNS.ChromeUx`** **`seedOk slot=M23`** when aligned. **`rememberSystemInsetsDp`** merges display cutout + system bars; **`PNS.ChromeUx`** **`safeInsetsTopPx`** for gate scripts. **`PreviewForegroundDndEffect`** + **`dndWhileInPreview`** + **`InterruptionFilterHold`**: notification-policy DND in preview (nests with recording DND); **`PNS.ChromeUx`** **`dndPreview=`**. **`PreviewReadoutStrip`** / **`PreviewReadoutFormat`**: live ISO / shutter / AWB / FPS from repeating **`CaptureResult`**; **`PNS.ChromeUx`** **`readout=live`** or **`readout=fallback`**. **`PreviewBottomCaptureTray`** dual shutters (orange still / red record, inactive dimmed left, **`dualShutter=visible`**). **7×7 chrome grid**: Settings at **`[6,6]`**, **`ChromeGridSlotSpec`** expand vs quick actions — row **2** LUT cycle / flash+timer stubs / histogram toggle + **`grid7=layout`** **`PNS.ChromeUx`**. **Tune** FAB **mode dial popout** over finder + **`RAW`/`RAW+`** readout badge via **`previewUsesJpegCompanion()`** + **`readoutCapture=`** log; chrome gate JSON **`modeDialPopoutOk`** / **`readoutCaptureOk`**. **`scripts/pns_chrome_ux_gate.ps1`** asserts **`seedOk`** + **`safeInsetsTopPx`** + **`dndPreview=`** + **`readout=`** + **`dualShutter=`** + **`grid7=`** on device; **`BUILD_PLAN.md`** Milestone 9.

- **`CapabilityGateBridge.kt`** + **HUD Settings** — Shared formatting for **`CapabilityGate`** lines from live **`HardwareCapsSnapshot`**; **Settings > HUD** lists **`ok` / `off`** plus truncated disabled reasons ( **`CAMERA`** permission gate). **`scripts/pns_failure_matrix_smoke.ps1`** — Milestone 7 smoke (**preview granted** + **CAMERA revoked** preview) → **`failure_matrix_smoke.json`**. **`CapabilityGateBridgeTest`**.

- **Sprint 5.3 — Snap AF + live capability gates + Super Macro ADB gate** — **`PreviewEngineScreen`**: street **Snap** dial sets infinity-style AF when not tap-metering (`applyStreetSnapAf`). **`HardwareCapsSnapshot`** + **`PreviewController.hardwareCaps()`** feed **`CapabilityGate`**; **Developer menu** lists gate results from live **`CameraCharacteristics`**. **`AboutScreen`**: Snap / Ricoh heritage blurb. **`VendorKeyGuard`**: **`trySetVendorSessionEnable`**, **`trySetVendorRequestEnable`**, shared **`trySetVendorEnableOnBuilder`**, expanded legacy **`set(Key,*)`** probes ( **`byte[]`** — validated on OnePlus 13 for **`com.oplus.macro.closeup.enable`**). **`SessionConfiguration.setSessionParameters`** path for Super Macro ADB probe when API ≥ **33** + ultra-wide camera matches **`BackCameraRoleResolver.ultraWide`**. Extras **`pns_preview_camera_id`** / **`pns_preview_super_macro_probe`**; **`pns_adb_preview_validate.ps1`** **`sprint53_super_macro_vv`** + **`super_macro_gate.json`**; **`scripts/pns_super_macro_gate.ps1`** (**`-RequireSuperMacroPass`**).

- **Preview / level / orientation pipeline overhaul + diagnostic probe (BUILD_PLAN UX, host-only)** — Three independent rendering paths for the live preview that the user can verify on-device without a debugger:
  - **Preview pipeline distortion fix** — `PreviewMainViewport` rewrites the central viewport in `BoxWithConstraints` so the inner `TextureView` is **always mounted** (the earlier "gate on `previewBufferSize`" caused the camera to never start since the surface needed for the camera is produced by the TextureView) and is **sized to the camera buffer's native aspect ratio** once known. The platform's default "stretch SurfaceTexture to view" then renders the buffer letterbox-fit with no distortion — the old `TextureView.setTransform` matrix becomes identity in the steady state. An `OnLayoutChangeListener` re-applies the transform on every size change so it's robust against the rare devices where `AndroidView`'s `update` block doesn't run after a resize.
  - **Static preview rotation** — `PreviewChromePreferences.staticPreviewRotationDeg` (0 / 90 / 180 / 270) is fed to a `graphicsLayer { rotationZ }` on the rotated content box, with the pre-rotation footprint swap-aware so the rotated buffer fills the parent without overflow. Toggle via the new "Spin & probe" rail block. Default `0`. Honors the user's "preview should not auto-rotate as the phone tilts" requirement: the value is **fixed at runtime**, the preview never moves with the device.
  - **Self-contained smooth horizon line** — `HorizonLevelOverlay` now reads its own gravity sensor data, computes a continuous roll angle from `atan2(-ax, ay)`, and rotates the canvas via `drawScope.rotate(horizonDrawAngleCwDeg(displayDeg, rollDeg))` so the bar **smoothly tracks world-horizontal** as the user tilts the phone. Cardinal-anchored level detection (`computeRollDeltaFromCardinal`) — green when within ±1.5° of any 90° step. Bar is drawn outside the static-rotation content box so static rotation never affects the horizon.
  - **Continuous chrome rotation state** — `rememberDeviceUiRotationState()` (new) returns snapped + continuous chrome rotation + raw `physicalDeg`; `rememberDeviceUiRotationDegrees()` (existing) is now a thin wrapper. The smooth value is fed to `OrientationProbeOverlay` as a debug-only readout; chrome itself still uses the snapped cardinal so icons never wobble at small tilts.
  - **`OrientationProbeOverlay` diagnostic** — Compact top-right panel (toggle: "Spin & probe" → Probe overlay) showing `display`, `sensor` kind + `ax/ay/az`, continuous `physicalDeg`, snapped + smooth chrome rotation, buffer dimensions + aspect, view dimensions + aspect, camera `SENSOR_ORIENTATION`, plus a **gravity-driven triangle** that uses the same `(360 − displayDeg − physicalDeg)` formula as the chrome rails (with the continuous physicalDeg) so it acts as a ground-truth reference: tip should always point at world-up. **On by default** while the preview pipeline is being stabilised.
  - **`PreviewController.sensorOrientationDegrees()`** new accessor exposes `CameraCharacteristics.SENSOR_ORIENTATION` to the probe.
  - **`PnsIcons.Spin`** new curved-arrow glyph for the "Spin & probe" rail block.
  - **`computeRollAngleDegrees(ax, ay)`** helper (gated at planar gravity ≥ 30 % of g) feeds the continuous physical roll into both the smooth chrome state and the probe.
  - **Tests** — `DeviceUiRotationTest` adds `roll is null when planar gravity is too small` / `roll matches snap function for cardinals` / `roll smoothly interpolates between cardinals`. `HorizonLevelOverlayTest` covers `horizonDrawAngleCwDeg` / `computeRollDeltaFromCardinal` cardinal pinning.

- **BUILD_PLAN §4 — NDK JNI stubs packaged (`externalNativeBuild`)** — `app/build.gradle.kts`: **`ndkVersion`**, **`ndk.abiFilters`** (`arm64-v8a`, `x86_64`), **`externalNativeBuild.cmake`** → `native/CMakeLists.txt`; debug APK includes **`lib/*/libpns_native.so`**. **`scripts/pns_verify_toolchain.ps1`** asserts the shared object is present inside **`app-debug.apk`**. **`.github/workflows/toolchain-verify.yml`** installs **`ndk;26.3.11579264`** + **`cmake;3.22.1`** before Gradle. Real **libavif** / **libjxl** encode bodies remain **`FetchContent`** follow-up (`NDK_PLAN.md`).

- **Preview chrome — Sony Photography Pro–style blocks + portrait (BUILD_PLAN UX)** — **`MainActivity`**: **`fullSensor`** + **`configChanges`**. **`PreviewEngineScreen`**: landscape **Row** (narrow left / preview / wide right) vs portrait **Column** (preview top ~55%, bottom **Row** left strip + scrollable block rail); **`rememberPreviewChromeTwistDegrees`** (landscape **`ROTATION_270`**, portrait **`ROTATION_180`**); **`ShortcutBlock`** groups (Target FPS, Focal, Guides, Mode, Preview & keys, Capture & tools); **`PreviewChromePreferences.showOnScreenShutter`** + orange **`FloatingActionButton`** still capture on preview (toggle **Shuttr btn**); **`PreviewMainViewport`** / **`PreviewOnScreenShutter`**. **`TexturePreviewFit`** twist helpers; **`TexturePreviewFitTest`**.

- **BUILD_PLAN §4 — 3D tracking + BKT bracket (host)** — **`FaceTrackingSupport`** + **`TrackerState`** in **`processFaceStatistics`** → **`EyeMark.trackingLocked`**; **`captureBracketBurst`** ( **`BracketPlan`** / **`BracketScheduler`**, per-shot AE comp); **`CaptureStorage.openOutput(..., filenameSuffix)`**; preview UI bracket buttons + volume-up when dial **BKT**.

- **BUILD_PLAN §4 — Eye-AF preview pipeline (host)** — **`PreviewEngineScreen.PreviewController`**: face detect mode on repeating request; **`processFaceStatistics`** from **`STATISTICS_FACES`**; **`FaceDetectAdapter`** → **`EyeAfOverlay`** (buffer→view **`TexturePreviewFit.mapBufferToView`**); HUD **`showEyeAfOverlay`** toggles **`setHudFaceOverlayEnabled`**. **`TexturePreviewFitTest`**.

- **BUILD_PLAN §4 — highlight-weighted metering (host)** — **`HighlightMeterSupport.kt`** (`pickYuv420AnalysisSize`, `evToCompensationIndex`). **`PreviewEngineScreen`**: **`LaunchedEffect(commandDialMode)`** → **`PreviewController.setCommandDialMode`**; when **`CommandDialMode.H`** and **≤119 fps**, session adds **`YUV_420_888`** analysis **`ImageReader`**, throttled Y-plane histogram → **`HighlightMeter`** → AE compensation updates (`CONTROL_AE_EXPOSURE_COMPENSATION`). **`HighlightMeterSupportTest`** (null-map guard).

- **BUILD_PLAN §4 — RAW DNG from live preview (host integration)** — When preview runs below **120 fps**, **`PreviewEngineScreen.PreviewController`** adds a RAW **`ImageReader`** surface (prefer **`ImageFormat.RAW12`** via **`RawCaptureSupport.pickRawOutput`**), exposes **`captureRawStill`** → **`CaptureStorage.openOutput`** + **`Dng12Saver.save`** with **`ImagingProfile.toDngCaptureKind()`**, posts **`CaptureHaptics.scheduleStillTick()`** after capture completion, and surfaces **Save DNG** + volume-up (RAW disabled at **≥120 fps** / HFR session). Supporting **`RawCaptureSupport.kt`** (orientation + display rotation).

### Changed

- **ADB preview automation (`scripts/pns_adb_preview_validate.ps1`)** — Safer log capture when **`logcat --pid`** is thin or adb flakes mid-run; optional **`logcat_*_app_pid.txt`** fragment; larger tail fallback. **`PreviewEngineScreen`**: automation suppress flag set **before** camera starts; face-detect off + no FPS spam during scripted RAW/bracket; **`CameraCapabilitiesProbe`** skips huge probe **`Log.i`** markdown when preview extras request automation (**dial / raw count / bracket**).

- **BUILD_PLAN §4 wrap-up** — Added **§4 milestone closure** (host-complete vs deferred **ADB**/desktop/NDK); bracket UI **BKT ×3 / ×5 / ×7** on preview rail (volume-up remains **×5**).

- **BUILD_PLAN §3 Part 2 (35 / 50 / 85 / 150 mm digital crops)** — Shipped **`SensorCropGeometry`** (`SCALER_CROP_REGION` from `CropPlan` + `SENSOR_INFO_ACTIVE_ARRAY_SIZE`), **`DngDefaultUserCropRatios`** (normalized crop vs active array for DNG/tooling parity), JUnit **`SensorCropGeometryTest`** + **`DngDefaultUserCropRatiosTest`**. **`PreviewEngineScreen`**: **Eq. crop** chips on **cameraId `2`** (23 / 35 / 50 mm) and **`4`** (73 / 85 / 150 mm); **`PreviewController`** sets **`CaptureRequest.SCALER_CROP_REGION`**, **`CONTROL_AE_REGIONS`** center patch for **50mm**, logs crop; digital crop auto-disabled at **≥120 fps** (HFR path). **`Dng12Saver`** KDoc links crop metadata story. **`DODGE_PROFILE.md`** master table + mode mapping updated.

- **Calibration host pull (BUILD_PLAN §7)** — **`pns_hfr_autorun.ps1 -PullCalibration`** pulls `…/files/calibration` on device to **`<OutDir>/calibration/`** (default **`.\hfr-runs\calibration\`**). BUILD_PLAN §7: profile JSON + serializer/storage + pull marked **`[x]`**; **target picker** (`CalibrateScreen` **`TargetPickerRow`**) marked **`[x]`**. Docs/KDoc aligned (`CalibrationProfileJsonAdapter`, `CalibrationProfileStorage`, `COLOR_PIPELINE.md`). Logged in **`PROBE_BUILD_PLAN.md` §5**.

- **Phase 0 V&V (BUILD_PLAN §2)** — Marked the **durable machine-readable probe artifact** parent deliverable `[x]` (all child probes + `pns_hfr_autorun.ps1` pull path were already complete). Marked **`[ADB]` HFR encode validation** `[x]`: device evidence is **`docs/screenshots/smoke_about_live.png`** (About → **From the latest probe (live)**) plus `PROBE_BUILD_PLAN.md` §5 row dated **2026-05-09** (adb **`8bf09993`**).

- **Part 2 dodge profile (BUILD_PLAN §3)** — Completed the **`DODGE_PROFILE.md` deliverable**: added **§ Spec ↔ Camera2 mapping (master table)** (per-mode `cameraId`, logical **`physicalCameraIds`**, RAW/HDR/OIS/macro constraints, digital-crop vs lens-switch). Updated **§ HFR recipes** wording to reference live exhaustive JSON / About. Marked BUILD_PLAN deliverable + lens-switch focal rows **`[x]`** (15 / 23 / 73 / 21 mm); crop-heavy rows **35 / 50 / 85 / 150 mm** stay **`[ ]`** until Phase 1 `SCALER_CROP_REGION` + DNG metadata wiring. Marked **`[HOST]` “decisions trace to PROBE_RESULTS / DODGE_PROFILE”** `[x]` (`AboutScreen` already cites both).

### Added

- **HEVC Codec Configuration (`hvcC`) round — primitive for HEIC / HEIF still encoding alongside AVIF (BUILD_PLAN §7, 1 new Kotlin module + 2 new public data classes + 2 companion presets + 52 new JUnit tests)** — Round 41 ships `HevcCodecConfiguration.kt` as the pure-data formatter for the HEVC Codec Configuration Box per ISO/IEC 14496-15 §8.3.3.1. In the HEIF / HEIC item world, `hvcC` rides as an ItemProperty inside `ipco` (Round 24) and is associated to every HEVC image item (`item_type = "hvc1"`) via `ipma` (Round 23) as **essential** per ISO/IEC 23008-12 §10.2 — without `hvcC`, no decoder can find the SPS / PPS / VPS NAL units needed to bootstrap the HEVC frame decoder. This is the HEVC counterpart to AV1's `av1C` (Round 33). Pure-data Kotlin (no Android imports), JVM-testable.
  - `HevcCodecConfiguration.NalUnitArray(arrayCompleteness, nalUnitType, nalUnits)` data class — pure-data carrier for one NAL unit array entry per spec table; `init` validates `nalUnitType in [0, 63]` (6-bit field), non-empty nalUnits ≤ 65535 entries, every NAL ≤ 65535 bytes. Custom `equals`/`hashCode` compare `nalUnits` by content (so `NalUnitArray(...)` works correctly in lists and HashSets even though `nalUnits` is `List<ByteArray>`).
  - `HevcCodecConfiguration.Config(generalProfileSpace, generalTierFlag, generalProfileIdc, generalProfileCompatibilityFlags, generalConstraintIndicatorFlags, generalLevelIdc, minSpatialSegmentationIdc, parallelismType, chromaFormat, bitDepthLumaMinus8, bitDepthChromaMinus8, avgFrameRate, constantFrameRate, numTemporalLayers, temporalIdNested, lengthSizeMinusOne, nalUnitArrays)` data class with full validation across every spec field. Two pre-computed companion presets:
    - `Config.DEFAULT_8BIT_MAIN_STILL` — HEIF Main Still Picture profile (`profile_idc = 3`, level 5.0, YUV 4:2:0 8-bit). Profile compatibility flag bit 3 set per H.265 §A.3.4.
    - `Config.DEFAULT_10BIT_MAIN_10` — HEIF Main 10 profile (`profile_idc = 2`, level 5.1, YUV 4:2:0 10-bit). The HDR10 still default. Profile compatibility flag bit 2 set per H.265 §A.3.2.
  - `HevcCodecConfiguration.payloadSize(config)` — computes the byte-exact wire size of the HEVCDecoderConfigurationRecord without doing the actual encode (handy for muxers planning `iloc` extents). Empty config = 23 bytes; each NAL array adds 3 bytes (array header) + per-NAL 2 bytes (length prefix) + payload.
  - `HevcCodecConfiguration.encodePayload(config)` emits the body of the `hvcC` box per ISO/IEC 14496-15 §8.3.3.1: 23-byte fixed prefix (`configurationVersion = 1` + bit-packed profile/tier/level + 48-bit constraint flags + bit-depth + frame-rate fields + numOfArrays) followed by N variable-length NAL unit arrays (each: 1 byte completeness/type + 2-byte numNalus + per-NAL 2-byte length + raw NAL bytes).
  - `HevcCodecConfiguration.decodePayload(bytes)` is the inverse — rejects under-23-byte input, wrong `configurationVersion`, truncated NAL array headers, and truncated NAL bodies.
  - `HevcCodecConfiguration.encodeBox(config)` wraps `encodePayload(config)` via `IsobmffBox.encodeBox("hvcC", payload)` for a complete plain-Box envelope (NOT a FullBox; `hvcC` is a plain Box per spec). Empty MAIN_STILL = 31 bytes total (8-byte box header + 23-byte payload).
  - **NAL unit type pins** (per ITU-T H.265 §7.4.2.2): `NAL_UNIT_TYPE_VPS = 32`, `NAL_UNIT_TYPE_SPS = 33`, `NAL_UNIT_TYPE_PPS = 34`, `NAL_UNIT_TYPE_PREFIX_SEI = 39`, `NAL_UNIT_TYPE_SUFFIX_SEI = 40`.
  - **Profile IDC pins** (per H.265 Annex A): `PROFILE_IDC_MAIN = 1`, `PROFILE_IDC_MAIN_10 = 2`, `PROFILE_IDC_MAIN_STILL_PICTURE = 3`.
  - **Chroma format pins**: `CHROMA_FORMAT_MONOCHROME = 0`, `CHROMA_FORMAT_YUV420 = 1`, `CHROMA_FORMAT_YUV422 = 2`, `CHROMA_FORMAT_YUV444 = 3`.
  - **Bit-width maxima** (each 2-bit / 3-bit / 5-bit / 12-bit field's spec-allowed maximum): `MAX_GENERAL_PROFILE_SPACE = 3`, `MAX_GENERAL_PROFILE_IDC = 31`, `MAX_MIN_SPATIAL_SEGMENTATION_IDC = 4095`, `MAX_PARALLELISM_TYPE = 3`, `MAX_CHROMA_FORMAT = 3`, `MAX_BIT_DEPTH_MINUS_8 = 7`, `MAX_CONSTANT_FRAME_RATE = 3`, `MAX_NUM_TEMPORAL_LAYERS = 7`, `MAX_LENGTH_SIZE_MINUS_ONE = 3`, `MAX_NAL_UNIT_TYPE = 63`.
  - **Pinned constants in HevcCodecConfiguration**: `BOX_TYPE = "hvcC"`, `SCHEMA_VERSION = 1`, `CONFIGURATION_VERSION = 1`, `FIXED_PAYLOAD_PREFIX = 23`, `NAL_ARRAY_HEADER_BYTES = 3`, `NAL_LENGTH_FIELD_BYTES = 2`.
  - **One byte-arithmetic test fix during gate run**: my first cut of the `encodePayload DEFAULT_8BIT_MAIN_STILL canonical 23-byte layout` byte-pin asserted `0x0F` for byte 21 but the correct packing of `cfr=0|temporalLayers=1|nested=0|lengthSize=3` is `00 + 001 + 0 + 11 = 0b00001011 = 0x0B`. Fixed inline.
  - **Tests shipped**: `HevcCodecConfigurationTest` (52 tests covering: every constant pin (10); `NalUnitArray` validation rejecting above-63 / negative nalUnitType / empty nalUnits / empty NAL bytes / above-uint16 NAL length + content equals (6); `Config` validation rejecting above-3 generalProfileSpace / above-31 generalProfileIdc / above-uint32 generalProfileCompatibilityFlags / above-48-bit constraintIndicatorFlags / above-7 bitDepthLumaMinus8 / above-3 chromaFormat / above-uint16 avgFrameRate / above-3 lengthSizeMinusOne / above-255 nalUnitArrays (9); `payloadSize` empty config = 23 bytes + counts 3-byte array header plus per-NAL 2-byte length plus body (2); `encodePayload` byte-layout pins for DEFAULT_8BIT_MAIN_STILL canonical 23-byte + DEFAULT_10BIT_MAIN_10 (profile_idc=2, level=153, bit-depth bytes `0xFA`) + high-tier flag in bit 5 of byte 1 + generalProfileSpace in high 2 bits + all-bits-set 48-bit constraint flags + max compat flags filling uint32 + byte 21 packing 6-byte field + min_spatial_segmentation packing into 12 bits across bytes 13-14 + appends NAL unit arrays after fixed prefix + arrayCompleteness=false + multi-NAL array (11); `encodeBox` empty MAIN_STILL is 31 bytes (size BE = 0x1F, type "hvcC") + grows by NAL array bytes (2); `decodePayload` round-trips DEFAULT_8BIT_MAIN_STILL + DEFAULT_10BIT_MAIN_10 + canonical HEIF still with VPS/SPS/PPS + multi-NAL arrays + high-tier max-flag config (5); `decodePayload` rejections under-23-byte / wrong configurationVersion / truncated NAL array header / truncated NAL body (4); preset field accessors for DEFAULT_8BIT_MAIN_STILL + DEFAULT_10BIT_MAIN_10 (2); `ItemInfoEntry.ITEM_TYPE_HVC1 = "hvc1"` cross-check (1)).
  - **75 JUnit suites · 1266 tests · 0 failures · 0 errors · 0 skipped** (up from 74 / 1214). 52 new tests in `HevcCodecConfigurationTest`. APK 33750 KB.
  - **HEIC emit path status (NEW)**: with this round, the engine has every host-side primitive needed to mux a fully-tagged HEIC still file alongside AVIF. The remaining work is a HEIC-flavored muxer (`HeicStillMuxer.kt` analogous to `AvifStillMuxer.kt` from Round 32) that wires `hvcC` into `ipco` instead of `av1C` and emits the HEIC `ftyp` brand. The actual HEVC encoder integration is a Phase 1 capture-engine task (Android's MediaCodec already provides HEVC encoding hardware-accelerated; the muxer just consumes the encoder output).

- **AVIF Image Overlay (`iovl` derived-image content) round — primitive for sticker / multi-layer / fill-canvas compositions (BUILD_PLAN §7, 1 new Kotlin module + 2 new public data classes + 49 new JUnit tests)** — Round 40 ships `AvifImageOverlay.kt` as the pure-data formatter for the `iovl` derived-image content per ISO/IEC 23008-12 §6.6.2.4. The `iovl` derived-item type composites N source image items at signed `(x, y)` offsets onto a 16-bit RGBA fill canvas, used by HEIC for sticker overlays and required for any AVIF still that wants to ship a watermark / sticker / multi-layer composition / depth-padded centered-crop without re-encoding the primary image. Pure-data Kotlin (no Android imports), JVM-testable.
  - `AvifImageOverlay.Reference(horizontalOffset, verticalOffset)` — pure-data carrier for one `(h, v)` source-item offset pair; `init` validates `[Int.MIN_VALUE, Int.MAX_VALUE]` (the int32 boundary). The pair's index in `Payload.references` dictates which source item it applies to (the order matches the `iref` `dimg`'s `to_item_ID` list).
  - `AvifImageOverlay.Payload(canvasFillR, canvasFillG, canvasFillB, canvasFillA, outputWidth, outputHeight, references)` — pure-data carrier for one image-overlay recipe; `init` validates canvas channels in `[0, 65535]`, dims in `[1, 0xFFFFFFFF]`. References default to `emptyList()` (a degenerate iovl with just the fill canvas is spec-allowed).
  - `AvifImageOverlay.chooseFlags(payload)` — picks `0` (16-bit dims + int16 ref offsets, 4 bytes per ref) when both canvas dims fit uint16 AND every ref offset fits int16; `FLAG_FIELD_LENGTH_32 = 0x01` (32-bit dims + int32 ref offsets, 8 bytes per ref) otherwise. Auto-promotion is per-payload, not per-field.
  - `AvifImageOverlay.payloadSize(payload, flags)` — computes the on-wire size byte-exact (handy for muxers planning `iloc` extents before they have the bytes). Empty 16-bit payload = 14 bytes; empty 32-bit = 18; each ref adds 4 (16-bit) or 8 (32-bit).
  - `AvifImageOverlay.encodePayload(payload, flags = chooseFlags(payload))` emits the wire-format content blob (auto-picks the minimum field length; caller can force the 32-bit form by passing `flags = FLAG_FIELD_LENGTH_32`). Wire layout: `version (uint8 = 0) + flags (uint8) + canvas_fill_value[4] (always 4 × uint16 RGBA = 8 bytes) + output_width / output_height (uint16 BE typical, uint32 BE when above 65535) + reference_count × (horizontal_offset, vertical_offset) SIGNED two's-complement pairs (int16 typical, int32 when offsets exceed [-32768, 32767])`. Critically, `reference_count` is NOT stored in the iovl content; it comes from the matching `iref` `dimg` reference's count, so the per-reference offset table is bare. The 16-bit form rejects encoding values that would truncate.
  - `AvifImageOverlay.decodePayload(bytes, referenceCount)` is the inverse — caller must supply `referenceCount` from the matching `iref` `dimg`. Rejects negative referenceCount, under-14-byte input, wrong version byte, and truncation against the declared field length.
  - **Pinned constants in AvifImageOverlay**: `SCHEMA_VERSION = 1`, `ITEM_TYPE = "iovl"`, `VERSION = 0`, `FLAG_FIELD_LENGTH_32 = 0x01`, `CANVAS_FILL_CHANNELS = 4`, `CANVAS_FILL_BYTES_PER_CHANNEL = 2`, `CANVAS_FILL_TOTAL_BYTES = 8`, `MIN_PAYLOAD_SIZE_16 = 14`, `MIN_PAYLOAD_SIZE_32 = 18`, `REFERENCE_BYTES_16 = 4`, `REFERENCE_BYTES_32 = 8`, `MAX_OUTPUT_DIMENSION_16 = 65535`, `MAX_OUTPUT_DIMENSION_32 = 0xFFFFFFFF`, `MAX_REFERENCE_OFFSET_16 = 32767`, `MIN_REFERENCE_OFFSET_16 = -32768`, `MAX_REFERENCE_OFFSET_32 = 2147483647`, `MIN_REFERENCE_OFFSET_32 = -2147483648`, `MAX_CANVAS_FILL_VALUE = 65535`.
  - **Tests shipped**: `AvifImageOverlayTest` (49 tests covering: every constant pin (18 tests); `Reference` validation rejecting above-int32 horizontalOffset / below-int32 verticalOffset, accepting int32 boundaries (3); `Payload` validation rejecting negative canvasFillR / above-uint16 canvasFillG / zero outputWidth / above-uint32 outputHeight, accepting canonical sticker overlay, defaulting references to empty (5); `chooseFlags` returns 0 for compact 16-bit case (canonical 1920×1080 with both refs fitting int16); promotes when outputWidth overflows int16 / when ref horizontalOffset overflows int16 / when ref verticalOffset is below int16 min (4); `payloadSize` empty-16 = 14 / empty-32 = 18 / 2-ref-16 = 22 / 2-ref-32 = 34 (4); `encodePayload` byte-layout pins for empty 16-bit canonical 14-byte layout, 2-ref 16-bit canonical sticker overlay (1920×1080 with refs at (0,0) and (100,50) — 22-byte byte-exact), 16-bit signed negative offsets in two's-complement (last 4 bytes for h=-1 / v=-32768 = 0xFF FF 80 00), above-uint16 width auto-promotes to 18-byte 32-bit base, 32-bit refs auto-promote when offset overflows int16 (h=32768 / v=-1 → 26-byte total with 8-byte tail 00 00 80 00 FF FF FF FF), force-32 flag emits 18-byte base for small canvas, rejects 16-bit flag when ref offset overflows (7); `decodePayload` round-trips empty 16-bit / 2-ref 16-bit signed offsets / 32-bit signed offsets at int32 boundaries (3); `decodePayload` rejects negative referenceCount / under-14-byte input / wrong version byte / truncated body when reference_count is non-zero (4); `ItemInfoEntry.ITEM_TYPE_IOVL` matches `AvifImageOverlay.ITEM_TYPE` cross-check (1)).
  - **74 JUnit suites · 1214 tests · 0 failures · 0 errors · 0 skipped** (up from 73 / 1164). 49 new tests in `AvifImageOverlayTest`. APK 33625 KB (+13 KB).
  - **AVIF emit path status (UPDATED)**: with this round, the muxer has every primitive needed to declare overlay-composited stills. A future muxer extension can wire `iovl` items via an `infe` entry of type `"iovl"`, an `iref` `dimg` reference from the iovl item to N source items in z-order (back-to-front), and an `iloc` extent for the iovl content blob (the `AvifImageOverlay.encodePayload(...)` bytes go in `mdat`).

### Fixed

- **`scripts/pns_verify_toolchain.ps1` dep-audit silently scanned every file in the tree (Round 40)** — `Get-ChildItem -LiteralPath ... -Recurse -Include *.gradle, *.gradle.kts` is a known Windows PowerShell trap: `-Include` is silently ignored when `-LiteralPath` lacks a wildcard, so the cmdlet returned every file in the project (markdown / logcat dumps / scripts / etc.) and the dep-audit was reporting bogus matches against documentation files and against the verify script itself. Replaced the `-Include` argument with an explicit `Where-Object` extension filter (`$_.Name -like '*.gradle' -or $_.Name -like '*.gradle.kts'`) plus the existing build/cache exclusion. The audit count dropped from `848 Gradle file(s)` (bogus) to `4 Gradle file(s)` (correct: project `build.gradle.kts` + `settings.gradle.kts` + `app/build.gradle.kts` + `gradle/libs.versions.toml`). No behavior change in the FOSS audit's result for legitimate Gradle inputs — the audit was previously working but emitting overwhelming noise; now it ALSO emits only the relevant Gradle files.

- **AVIF Image Grid (`grid` derived-image content) round — primitive for tiled high-res stills (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 3 new ItemInfoEntry constants + 43 new JUnit tests)** — Round 39 ships `AvifImageGrid.kt` as the pure-data formatter for the `grid` derived-image content per ISO/IEC 23008-12 §6.6.2.3. The `grid` derived-item type lets a HEIF / AVIF container stitch together an `(rows × columns)` array of equally-sized tile image items into one logical image — used by Apple HEIC for every HEIC still and required for AV1 stills that exceed AV1 Level 6.0's `7680` max coded width (e.g. the OnePlus 13's `8192 × 6144` main wide). Pure-data Kotlin (no Android imports), JVM-testable.
  - `AvifImageGrid.Payload(rows, columns, outputWidth, outputHeight)` data class — pure-data carrier; `init` validates `rows in [1, 256]`, `columns in [1, 256]`, `outputWidth / outputHeight in [1, 0xFFFFFFFF]`.
  - `AvifImageGrid.chooseFlags(payload)` — picks the minimum `flags` byte that can encode the canvas dimensions: `0` for sub-65535 px (the 16-bit field-length form, 8 bytes total), `FLAG_FIELD_LENGTH_32 = 0x01` when at least one dimension overflows 16 bits (the 32-bit form, 12 bytes total).
  - `AvifImageGrid.encodePayload(payload, flags = chooseFlags(payload))` emits the wire-format content blob (auto-picks the minimum field length; caller can force the 32-bit form by passing `flags = FLAG_FIELD_LENGTH_32`). Wire layout: `version (uint8 = 0) + flags (uint8) + rows_minus_one (uint8) + columns_minus_one (uint8) + output_width (uint16 or uint32 BE) + output_height (uint16 or uint32 BE)`.
  - `AvifImageGrid.decodePayload(bytes)` is the inverse — rejects under-8-byte input, wrong version byte, and 32-bit-flag declarations on under-12-byte buffers.
  - **Pinned constants in AvifImageGrid**: `BOX_TYPE = "grid"` (well, `ITEM_TYPE = "grid"` since this is a content blob, not a box), `SCHEMA_VERSION = 1`, `VERSION = 0`, `FLAG_FIELD_LENGTH_32 = 0x01`, `PAYLOAD_SIZE_16 = 8`, `PAYLOAD_SIZE_32 = 12`, `MAX_ROWS = 256`, `MAX_COLUMNS = 256`, `MAX_OUTPUT_DIMENSION_16 = 65535`, `MAX_OUTPUT_DIMENSION_32 = 0xFFFFFFFF`.
  - **New ItemInfoEntry item-type constants**: `ITEM_TYPE_GRID = "grid"` (this round), `ITEM_TYPE_IOVL = "iovl"` (overlay), `ITEM_TYPE_IDEN = "iden"` (identity transform). The two latter constants are forward-looking placeholders; only `grid` has a content-formatter shipped this round.
  - **Tests shipped**: `AvifImageGridTest` (43 tests covering: every constant pin (10 tests); `Payload` validation rejections (zero rows, above-256 rows, zero columns, above-256 columns, zero outputWidth, negative outputHeight, above-uint32 outputWidth) and acceptance (canonical 2×2 8K grid, maxed 256×256 grid); `chooseFlags` returns 0 for sub-65535 / 65535 boundary; returns FLAG_FIELD_LENGTH_32 above-uint16 width / above-uint16 height; `encodePayload` byte-layout pins for 1×1 1×1 (8-byte canonical), 2×2 8192×6144 (OnePlus-13-class 8K still, 8-byte payload `00 00 01 01 20 00 18 00`), 256×256 1×1 max grid (rows/cols=0xFF), 1×1 65535×65535 boundary, above-uint16 width auto-promotes to 12-byte 32-bit form (`00 01 03 03 00 01 86 A0 00 00 C3 50` for 100000×50000), max-uint32 dims produces 12-byte all-FF tail, rejects 16-bit flag with above-uint16 dim, force-32 flag emits 12-byte payload even for small dims; `decodePayload` round-trips canonical 2×2 8K + 1×1 degenerate + 256×256 max + 32-bit-field grid + max-uint32 dims grid; `decodePayload` rejections (under-8-byte, wrong version byte, 32-bit flag on 8-byte buffer); `decodePayload` tolerates trailing bytes; `ItemInfoEntry.ITEM_TYPE_GRID` matches `AvifImageGrid.ITEM_TYPE`; `ItemInfoEntry.ITEM_TYPE_IOVL` and `ITEM_TYPE_IDEN` constant pins).
  - **73 JUnit suites · 1164 tests · 0 failures · 0 errors · 0 skipped** (up from 72 / 1121). 43 new tests in `AvifImageGridTest`. APK 33612 KB.
  - **AVIF emit path status (UPDATED)**: with this round, the muxer has the primitive needed to declare grid-tiled stills. A future muxer extension can wire `grid` items via an `infe` entry of type `"grid"`, an `iref` `dimg` reference from the grid item to N tile items in row-major order, and an `iloc` extent for the grid content blob (the `AvifImageGrid.encodePayload(...)` bytes go in `mdat`).

- **Alpha auxiliary image items integrated into AvifStillMuxer (BUILD_PLAN §7, AvifStillMuxer extension + 1 new `Av1CodecConfiguration.Config` companion preset + 23 new JUnit tests)** — Round 38 wires alpha-channel auxiliary image items into `AvifStillMuxer.encode(...)` using the `auxC` primitive shipped in Round 37 and the `iref` primitive shipped in Round 34. With this round, the muxer can produce AVIF stills with full alpha (transparency) — the standard wire form for any AVIF reader (`Lr / Photoshop / Chrome / Firefox / darktable`) to extract a 4-channel image.
  - `AvifStillMuxer.Input` gains three new fields: `alphaBitstream: ByteArray? = null`, `alphaConfiguration: Av1CodecConfiguration.Config? = null`, `alphaBitDepths: IntArray = BIT_DEPTHS_ALPHA_8`. `Input.init` requires `alphaBitstream.isNotEmpty()` when set, requires `alphaConfiguration` non-null iff `alphaBitstream` non-null (both-or-neither), validates `alphaBitDepths.size == 1` (alpha is monochrome), and validates `alphaBitDepths[0] in [1, 255]`.
  - When `alphaBitstream` is non-null, the muxer (1) emits a fourth `infe` entry in `iinf` with `itemId = 4` and `itemType = "av01"` for the alpha image, (2) emits an `iref` `auxl` reference from the alpha item to the primary AV1 image item (per AVIF spec §3.4), (3) emits a fourth `iloc` extent pointing to the alpha bitstream byte range inside `mdat`, (4) appends three new ItemProperties to `ipco` — alpha-only `pixi` (single-channel, typically 8-bit), alpha-only `av1C` (the alpha bitstream's own AV1 sequence header), and `auxC` declaring `aux_type = "urn:mpeg:mpegB:cicp:systems:auxiliary:alpha"` — and (5) emits a SECOND `ipma` Entry binding `ALPHA_ITEM_ID` to a shared `ispe` (primary's, since AVIF spec §4 mandates dimensions match) plus the three alpha-only properties. All four alpha associations are marked `essential = true` per AVIF spec §3.4 (the decoder MUST fail if it can't honor any of these).
  - The full `mdat` layout now becomes `[AV1 primary] [exif_tiff_header_offset (4 BE) | EXIF TIFF block]? [XMP packet]? [AV1 alpha]?` — entries appear in that fixed deterministic order regardless of which optional items are set. The two-pass offset planning still works because we know each item's length up-front.
  - `Av1CodecConfiguration.Config.DEFAULT_8BIT_MONOCHROME` companion preset added — canonical 8-bit monochrome AV1 still config (`seq_profile = 0`, `seq_level_idx_0 = 8`, `monochrome = true`, `highBitdepth = false`). This is the typical alpha-channel auxiliary image bitstream config for AVIF spec §3.4.
  - **Pinned new constants in AvifStillMuxer**: `ALPHA_ITEM_ID = 4`, `BIT_DEPTHS_ALPHA_8 = [8]`, `BIT_DEPTHS_ALPHA_10 = [10]`, `BIT_DEPTHS_ALPHA_12 = [12]`.
  - **Tests shipped**: 23 new tests in `AvifStillMuxerTest` covering: `ALPHA_ITEM_ID = 4` distinct from `PRIMARY / EXIF / XMP`; three `BIT_DEPTHS_ALPHA_*` array pins; `Input` validation rejections (empty `alphaBitstream` when set, `alphaBitstream` without `alphaConfiguration`, `alphaConfiguration` without `alphaBitstream`, multi-channel `alphaBitDepths`); alpha set produces two `iinf` entries both `av01` (primary + alpha); alpha set emits `iref` `auxl` sub-box with `from = ALPHA_ITEM_ID`, `count = 1`, `to = PRIMARY_ITEM_ID`; alpha set appends `pixi`/`av1C`/`auxC` to `ipco` (3 new entries; `ispe` is shared); alpha set emits a SECOND `ipma` Entry binding `ALPHA_ITEM_ID` with exactly 4 associations (ispe + pixi + av1C + auxC); all alpha associations marked `essential` (high bit set in association byte); alpha set places alpha bytes at end of `mdat` (no prefix); alpha `iloc` has two extents with alpha at `av1_offset + av1_size`; alpha set keeps `ftyp / meta / mdat` top-level structure; alpha set ends `mdat` exactly at the alpha end (no trailing bytes); alpha null does NOT emit `auxC` property (regression guard); alpha null vs alpha set produce different outputs with size growing by at least the alpha bitstream size; EXIF + XMP + alpha all set lays out `mdat` as `AV1 → EXIF (with 4-byte zero prefix) → XMP → ALPHA` byte-exact; EXIF + XMP + alpha all set produces four `iinf` entries; EXIF + XMP + alpha all set produces `iref` with three sub-boxes in `(cdsc, cdsc, auxl)` order; `Av1CodecConfiguration.Config.DEFAULT_8BIT_MONOCHROME` field accessors (`monochrome = true`, `seqProfile = 0`, `seqLevelIdx0 = 8`, `highBitdepth = false`).
  - **72 JUnit suites · 1121 tests · 0 failures · 0 errors · 0 skipped** (up from 72 / 1098). 23 new tests in `AvifStillMuxerTest`. APK 33498 KB (+5 KB).
  - **AVIF emit path status (UPDATED — host side now ships full AVIF spec coverage)**: with this round, the host-side AVIF still emit path supports embedded EXIF (Round 35) + XMP (Round 36) metadata items AND alpha-channel auxiliary image items (Round 38) in addition to the spec-mandatory bitstream-tag boxes from Rounds 17-33. The remaining work to ship AVIF stills end-to-end on device is now PURELY the AV1 image encoder pipeline (Phase 1, NDK `libdav1d` / `libaom` integration); the muxer takes the encoder's primary + alpha `av1Bitstream` + `Av1CodecConfiguration.Config` pair and produces a complete, decode-ready AVIF still with alpha in one call.

- **AVIF Auxiliary Type Property (`auxC`) round — primitive for alpha-channel auxiliary image items (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 25 new JUnit tests)** — pure-data formatter for the `auxC` ItemFullProperty per ISO/IEC 23008-12 §6.5.8 and AVIF spec §3.4. `auxC` is associated to image items that carry "auxiliary" image data — alpha mattes, depth maps, or other ancillary single-channel image data tied to a primary image item via an `iref` `auxl` reference. The property declares what the auxiliary channel actually means. AVIF spec §3.4 marks `auxC` as **essential** when associated to an alpha image item.
  - `AvifAuxiliaryTypeProperty.Payload(auxType, auxSubtype = EMPTY_AUX_SUBTYPE)` — pure-data carrier; `init` validates non-empty `auxType` and rejects embedded NUL; custom `equals` / `hashCode` compare `auxSubtype` by content. Two pre-computed companion presets: `Payload.ALPHA` (canonical AVIF alpha URN) and `Payload.DEPTH` (HEIF depth-map URN per §6.5.8).
  - `encodePayload(payload)` emits the FullBox payload — `aux_type (NUL-terminated UTF-8) + aux_subtype[]`. When `aux_subtype` is empty (the spec-default for AVIF alpha), the FullBox payload is exactly `aux_type` plus its trailing NUL byte. Includes a defensive UTF-8-roundtrip NUL check beyond the `Payload.init` precondition.
  - `decodePayload(bytes)` is the inverse — rejects empty payload, missing NUL terminator, and zero-length `auxType`.
  - `encodeBox(payload)` produces a complete mux-ready FullBox via `IsobmffBox.encodeFullBox("auxC", 0, 0, payload)`. The canonical AVIF alpha box is exactly 56 bytes (8 header + 4 version+flags + 43-byte URN + 1-byte NUL).
  - `encodeAlphaBox()` is the AVIF alpha shortcut.
  - **Pinned constants**: `BOX_TYPE = "auxC"`, `SCHEMA_VERSION = 1`, `VERSION = 0`, `FLAGS = 0`, `AUX_TYPE_ALPHA = "urn:mpeg:mpegB:cicp:systems:auxiliary:alpha"`, `AUX_TYPE_DEPTH = "urn:mpeg:mpegB:cicp:systems:auxiliary:depth"`, `EMPTY_AUX_SUBTYPE = ByteArray(0)`.
  - **Tests shipped**: `AvifAuxiliaryTypePropertyTest` (25 tests covering: every constant pin; `Payload` validation rejections (empty `auxType`, embedded NUL); `Payload.equals` / `hashCode` content comparison; `Payload.ALPHA` / `Payload.DEPTH` companion parity; `encodePayload` byte-layout pins for AVIF alpha (44-byte payload — URN ASCII + NUL), HEIF depth, and custom `auxType` + 3-byte subtype; `decodePayload` round-trips ALPHA + DEPTH + custom subtype; `decodePayload` rejections (empty, no NUL, NUL-at-offset-0); `encodeBox` 56-byte canonical AVIF alpha envelope with every field offset pinned (`size BE = 0x38 / type "auxC" / version+flags = 0x00000000 / payload = URN + NUL`); `encodeAlphaBox` byte-exact match to `encodeBox(Payload.ALPHA)`; `encodeBox` HEIF depth canonical envelope; `encodeBox` custom 23-byte total).
  - **72 JUnit suites · 1098 tests · 0 failures · 0 errors · 0 skipped** (up from 71 / 1073). 25 new tests in `AvifAuxiliaryTypePropertyTest`. APK 33493 KB (+5 KB).
  - **AVIF emit path status (UPDATED)**: with this round, the muxer has the primitive needed for alpha-channel auxiliary image items. Round 38 will wire alpha aux items into `AvifStillMuxer` (alpha image item with its own `ispe`/`pixi`/`colr`/`av1C`/`auxC` properties + `iref` `auxl` reference + a separate AV1 monochrome bitstream encoded by the alpha encoder pipeline).

- **XMP metadata items integrated into AvifStillMuxer (BUILD_PLAN §7, AvifStillMuxer extension + 13 new JUnit tests)** — Round 36 extends Round 35's EXIF wire-up to support optional XMP packet embedding alongside (or instead of) EXIF. With this round, the muxer supports the full triumvirate of camera-app metadata embedding for AVIF stills: EXIF (Round 35), XMP (Round 36), and per-item ItemProperties (Rounds 17-33).
  - `AvifStillMuxer.Input` gains `xmpPayload: ByteArray? = null`. When non-null, the muxer (1) emits an `infe` mime entry in `iinf` with `itemId = 3`, `itemType = "mime"`, `content_type = "application/rdf+xml"` (per HEIF spec §11.6.2 / AVIF spec §3.6); (2) emits an additional `iref` `cdsc` sub-box from the XMP item to the primary AV1 image — when both EXIF and XMP are present, the SAME `iref` FullBox carries TWO `cdsc` sub-boxes (one per metadata item), in `(EXIF, XMP)` order; (3) emits an additional `iloc` extent pointing to the XMP byte range inside `mdat`; (4) lays out `mdat` as `[AV1 bitstream] [exif_tiff_header_offset (4 BE) | EXIF TIFF block]? [XMP packet]?` — a fixed deterministic order regardless of which optional metadata blobs are set.
  - Unlike EXIF, XMP carries NO 4-byte offset prefix — the XMP packet (the `<?xpacket begin=...?>` … `<?xpacket end=...?>` byte sequence per ISO 16684-1) lands raw in `mdat`. Caller is responsible for the packet being well-formed XMP — the muxer does not parse it.
  - `Input.init` gains `require(xmpPayload.isNotEmpty())` when non-null so a zero-byte XMP packet fails fast.
  - **Pinned new constants**: `XMP_ITEM_ID = 3`, `XMP_MIME_TYPE = "application/rdf+xml"`.
  - **Tests shipped**: 13 new tests in `AvifStillMuxerTest` covering: `Input` rejects empty `xmpPayload`; `XMP_ITEM_ID = 3` distinct from `PRIMARY_ITEM_ID` AND `EXIF_ITEM_ID`; `XMP_MIME_TYPE = "application/rdf+xml"` pin; xmp set produces two `iinf` entries (`av01` + `mime` with the `application/rdf+xml` content_type substring); xmp set emits `iref` `cdsc` sub-box with `from = XMP_ITEM_ID`, `count = 1`, `to = PRIMARY_ITEM_ID`; xmp set places XMP bytes immediately after AV1 in `mdat` with NO prefix; xmp `iloc` has two extents with XMP at `av1_offset + av1_size`; EXIF + XMP set produces three `iinf` entries; EXIF + XMP set emits `iref` with two `cdsc` sub-boxes in `(EXIF, XMP)` order; EXIF + XMP set lays out `mdat` as `AV1 → EXIF (with 4-byte zero prefix) → XMP` byte-exact; xmp null does NOT emit `iref` box (regression guard); xmp set keeps `ftyp / meta / mdat` top-level structure; xmp null vs xmp set produce different outputs.
  - **71 JUnit suites · 1073 tests · 0 failures · 0 errors · 0 skipped** (up from 71 / 1060). 13 new tests in `AvifStillMuxerTest`. APK 33488 KB (+2 KB).
  - **AVIF emit path status (UPDATED)**: with this round, the host-side AVIF still emit path supports embedded EXIF + XMP metadata in addition to all the spec-mandatory bitstream-tag boxes from Rounds 17-33. Future rounds can ship auxiliary alpha image items (`auxl` + `auxC`) and derived/grid items.

- **EXIF metadata items integrated into AvifStillMuxer (BUILD_PLAN §7, AvifStillMuxer extension + 13 new JUnit tests)** — Round 35 wires optional EXIF metadata-item handling into `AvifStillMuxer.encode(...)` using the `ItemReferenceBox` (`iref`) primitive shipped in Round 34. Closes the host-side EXIF emit gap noted in Round 32 / Round 34. With this round, the muxer can produce AVIF stills with embedded EXIF (camera make/model, capture timestamp, GPS location, orientation, lens info, exposure, ISO, etc.) that any compliant AVIF/HEIF reader (`Lr / darktable / RawTherapee / exiftool`) can extract.
  - `AvifStillMuxer.Input` gains `exifPayload: ByteArray? = null`. When non-null, the muxer (1) emits a second `infe` entry in `iinf` with `itemId = 2` and `itemType = "Exif"` (per HEIF spec §11.4 / AVIF spec §3.5), (2) emits an `iref` `cdsc` reference from the EXIF item (subject) to the primary AV1 image item (object), (3) emits a second `iloc` extent pointing to the EXIF byte range inside `mdat`, and (4) prepends the spec-mandated 4-byte big-endian `exif_tiff_header_offset = 0` (per HEIF §11.4.4) to the EXIF TIFF block before it lands in `mdat`. The mdat layout becomes `[AV1 bitstream][exif_tiff_header_offset (4 BE bytes) | EXIF TIFF block]` (AV1 first so the primary image's `iloc` offset stays at the smallest value). Caller is responsible for the TIFF block being well-formed EXIF — the muxer does not parse it.
  - `Input.init` gains `require(exifPayload.isNotEmpty())` when non-null so a zero-byte EXIF blob fails fast.
  - The two-pass offset-planning strategy (placeholder `iloc` with non-zero offsets matching final field widths → real `iloc` with computed offsets → `realIloc.size == placeholderIloc.size` invariant) still works for the two-extent case; the muxer just carries an extra placeholder offset for the EXIF item.
  - `ipma` stays bound to only the primary image item — the EXIF item is just a data carrier and needs no `ItemProperty` associations. So `ipma.entry_count` remains `1` even when EXIF is present, and the EXIF item carries no properties at all.
  - **Pinned new constants**: `EXIF_ITEM_ID = 2`, `EXIF_TIFF_HEADER_OFFSET_PREFIX_SIZE = 4`.
  - **Tests shipped**: 13 new tests in `AvifStillMuxerTest` covering: `Input` rejects empty `exifPayload` when set; `EXIF_ITEM_ID = 2` pin distinct from `PRIMARY_ITEM_ID`; `EXIF_TIFF_HEADER_OFFSET_PREFIX_SIZE = 4` pin; exif null produces single `iinf` entry; exif null does NOT emit `iref` box (regression guard); exif set produces two `iinf` entries with both `"av01"` and `"Exif"` substrings present; exif set emits `iref` `cdsc` sub-box with `from_item_ID = EXIF_ITEM_ID = 2`, `reference_count = 1`, `to_item_ID = PRIMARY_ITEM_ID = 1`; exif set places EXIF bytes immediately after AV1 in `mdat` with the 4-byte zero `exif_tiff_header_offset` prefix; exif `iloc` has two extents with the second extent (EXIF) at offset = `av1_offset + av1_size`; exif set keeps `ipma` binding properties only to primary item (entry_count = 1, first itemId = `PRIMARY_ITEM_ID`); exif set keeps `ftyp / meta / mdat` top-level structure; exif set ends `mdat` exactly at the EXIF end (no trailing bytes); exif null vs exif set produce different outputs with size growing by at least the exif payload size.
  - **71 JUnit suites · 1060 tests · 0 failures · 0 errors · 0 skipped** (up from 71 / 1047). 13 new tests in `AvifStillMuxerTest`. APK 33486 KB (+2 KB).
  - **AVIF emit path status (UPDATED)**: with this round, the host-side AVIF still emit path supports embedded EXIF metadata in addition to all the spec-mandatory bitstream-tag boxes from Rounds 17-33. Future rounds can ship XMP item handling (`iref` `cdsc` from `mime` item with `content_type = "application/rdf+xml"`, same pattern) and auxiliary alpha image items (`auxl` + `auxC`).

- **ISOBMFF Item Reference Box (`iref`) round — primitive for EXIF cdsc + alpha auxl + thumbnail thmb + derived dimg (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 32 new JUnit tests)** — pure-data formatter for the ISOBMFF / HEIF / AVIF Item Reference Box per ISO/IEC 14496-12 §8.11.12. `iref` is a `FullBox('iref', version, 0)` whose payload is an ordered sequence of `SingleItemTypeReferenceBox` sub-boxes; each sub-box's box-type is a 4-byte ASCII reference-type code (e.g. `"cdsc"`, `"auxl"`, `"thmb"`, `"dimg"`) and the sub-box payload is `from_item_ID + reference_count + reference_count × to_item_ID`. So `iref` is a "list of typed item references" linking `from_item` to one-or-more `to_items` under a typed reason. Common uses: (1) **`cdsc`** (Content Describes) — an item describes another item; required for EXIF / XMP metadata items per HEIF spec §11.4 / §11.6 and AVIF spec §3.5; (2) **`auxl`** (Auxiliary Image) — an auxiliary image (alpha / depth) auxiliary to its primary image; required by AVIF spec §3.4 for alpha channels (the alpha image item carries an `auxC` property whose `aux_type` is `"urn:mpeg:mpegB:cicp:systems:auxiliary:alpha"`); (3) **`thmb`** (Thumbnail) — smaller preview of primary; (4) **`dimg`** (Derived Image) — a derived image depends on its source items (grid / iden compositions). With this round, the muxer has the primitive needed to (a) embed EXIF metadata items linked to the primary AV1 image (Round 35) and (b) embed alpha aux items linked via `auxl` (a future round once the alpha encoder ships).
  - `ItemReferenceBox.Reference(referenceType, fromItemId, toItemIds)` data class — pure-data carrier for one `SingleItemTypeReferenceBox`. `init` validates the `referenceType` is exactly 4 ASCII characters with codepoints in `[0x20, 0x7E]` (printable), `fromItemId in [0, 0xFFFFFFFF]`, `toItemIds` is non-empty with `size <= 0xFFFFFFFF`, every toItemId in `[0, 0xFFFFFFFF]`.
  - `ItemReferenceBox.chooseVersion(references): Int` — picks the minimum FullBox version that can encode every reference in the list without truncation: `0` when every itemId fits in 16 bits AND every reference_count fits in 16 bits, `1` when at least one of those values overflows 16 bits.
  - `ItemReferenceBox.encodePayload(references, version): ByteArray` — emits the FullBox payload (the bytes after the 4-byte version+flags slot). Field width is determined by `version`: `version = 0` uses 2-byte fromItemID / reference_count / toItemID; `version = 1` uses 4-byte. Validates all bounds against the chosen version and throws `IllegalArgumentException` on overflow. Caller wraps with `IsobmffBox.encodeFullBox("iref", version, 0, payload)`.
  - `ItemReferenceBox.encodeBox(references): ByteArray` — convenience that auto-picks the version (via `chooseVersion`) and wraps with `IsobmffBox.encodeFullBox` to produce a complete, mux-ready `iref` FullBox in one call.
  - **Pinned constants**: `BOX_TYPE = "iref"`, `SCHEMA_VERSION = 1`, `REFERENCE_TYPE_CDSC = "cdsc"`, `REFERENCE_TYPE_AUXL = "auxl"`, `REFERENCE_TYPE_THMB = "thmb"`, `REFERENCE_TYPE_DIMG = "dimg"`, `MAX_SMALL_ITEM_ID = 65535`, `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `MAX_SMALL_REFERENCE_COUNT = 65535`, `MAX_LARGE_REFERENCE_COUNT = 0xFFFFFFFF`.
  - **Tests shipped**: `ItemReferenceBoxTest` (32 tests covering: every constant pin; `Reference` validation rejects non-4-char / non-printable-ASCII referenceType, empty toItemIds, negative fromItemId, fromItemId / toItemId beyond `MAX_LARGE_ITEM_ID`, accepts canonical AVIF EXIF cdsc shape; `chooseVersion` returns 0 for empty list / all-16-bit; returns 1 for fromItemId / toItemId beyond 16 bits; `encodePayload` empty-list 0 bytes, canonical cdsc 14-byte byte-layout pin, multi-target dimg 20-byte byte-layout pin, multi-reference concatenation in stable order, v=1 4-byte-fields layout pin, v=0 rejects 16-bit overflows, encodePayload rejects invalid version; `encodeBox` empty 12-byte FullBox envelope, canonical EXIF cdsc 26-byte envelope with field offsets pinned, auto-promotes to v=1 for itemId beyond 16 bits (32-byte envelope), auxl alpha aux pattern, EXIF + alpha bundle 40-byte envelope, order-preservation across references).
  - **71 JUnit suites · 1047 tests · 0 failures · 0 errors · 0 skipped** (up from 70 / 1015). 32 new tests in `ItemReferenceBoxTest`. APK 33484 KB (+7 KB).
  - **AVIF emit path status (UPDATED)**: the muxer now has every primitive needed for full HEIF / AVIF item-reference plumbing. Round 35 will wire EXIF item handling into `AvifStillMuxer` (a new `infe` entry of type `"Exif"`, an EXIF `mdat` segment, an `iloc` extent for the EXIF item, and an `iref` `cdsc` reference from the EXIF item to the primary AV1 image item). Future rounds can ship alpha-auxiliary image items (`auxl` + `auxC` property) and derived/thumbnail items.

- **AVIF AV1 Codec Configuration (`av1C`) round — closes spec-mandatory gap in AvifStillMuxer (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 34 new JUnit tests + 1 muxer-integration test)** — pure-data formatter for the AV1 Codec Configuration Box (`av1C`) per AV1-ISOBMFF Specification §2.3.1. In the AVIF / HEIF item world, `av1C` rides as an ItemProperty inside `ipco` (Round 24) and is associated to every AV1 image item via `ipma` (Round 23) as **essential** per AVIF spec §2.2.1. **`AvifStillMuxer` (Round 32) was missing this primitive at ship time** — Round 33 closes that gap. Without `av1C`, no decoder can find the AV1 sequence header needed to bootstrap the AV1 frame decoder.
  - `Av1CodecConfiguration.Config` data class — pure-data carrier for the structured `av1C` fields (`seqProfile`, `seqLevelIdx0`, `seqTier0`, `highBitdepth`, `twelveBit`, `monochrome`, `chromaSubsamplingX`, `chromaSubsamplingY`, `chromaSamplePosition`, `initialPresentationDelayMinusOne`, `configOBUs`). `init` validates `seqProfile in [0, 1, 2]`, `seqLevelIdx0 in [0, 31]`, `chromaSamplePosition in [0, 3]`, requires `highBitdepth = true` when `twelveBit = true`, and validates `initialPresentationDelayMinusOne in [0, 15]` when set. `equals/hashCode` compare `configOBUs` by content (ByteArray semantics). Two pre-computed presets: `DEFAULT_8BIT_YUV420` (8-bit YUV 4:2:0, Level 4.0) and `DEFAULT_10BIT_YUV420` (10-bit YUV 4:2:0, Level 5.1, the HDR10 default).
  - `Av1CodecConfiguration.encodePayload(config: Config): ByteArray` — emits the 4-byte fixed prefix + variable `configOBUs`. Bit packing per spec: byte 0 = `marker (1 bit) | version (7 bits)`; byte 1 = `seq_profile (3 bits) | seq_level_idx_0 (5 bits)`; byte 2 = `seq_tier_0 (1) | high_bitdepth (1) | twelve_bit (1) | monochrome (1) | chroma_subsampling_x (1) | chroma_subsampling_y (1) | chroma_sample_position (2)`; byte 3 = `reserved (3) | initial_presentation_delay_present (1) | initial_presentation_delay_minus_one (4) OR reserved (4)`.
  - `Av1CodecConfiguration.encodeBox(config: Config): ByteArray` — wraps the payload with `IsobmffBox.encodeBox("av1C", payload)` for a complete 12-byte (8 header + 4 payload) mux-ready box (plus any `configOBUs`).
  - `Av1CodecConfiguration.decodePayload(bytes: ByteArray): Config` — inverse of `encodePayload`; throws `IllegalArgumentException` on under-4-byte input, wrong marker, or wrong version.
  - **Pinned constants**: `BOX_TYPE = "av1C"`, `SCHEMA_VERSION = 1`, `MARKER = 1`, `VERSION = 1`, `FIXED_PAYLOAD_PREFIX = 4`, `MIN_PAYLOAD_SIZE = 4`, `ALLOWED_SEQ_PROFILES = [0, 1, 2]`, `MAX_SEQ_LEVEL_IDX = 31`, `MAX_CHROMA_SAMPLE_POSITION = 3`, `MAX_INITIAL_PRESENTATION_DELAY_MINUS_ONE = 15`.
  - **`AvifStillMuxer` integration**: `Input` now requires an `av1Configuration: Av1CodecConfiguration.Config` field; the muxer's `encode(...)` wires `av1C` into `ipco` between `colr` and the optional auxiliary properties. So `ipco` now contains exactly 4 mandatory entries — `ispe / pixi / colr / av1C` — when no auxiliaries are set (was 3), growing to 10 when every auxiliary is enabled (was 9). `encodeSrgbStill(...)` defaults to `Av1CodecConfiguration.Config.DEFAULT_8BIT_YUV420`.
  - **One byte-arithmetic fix during gate run**: my first cut of the 12-bit YUV 4:4:4 byte-layout test asserted `0xCD` for `byte 1 = (seq_profile=2 << 5) | seq_level_idx_0=13`, but `2 << 5 = 0x40` (not `0xC0`), so the right value is `0x4D`. Fixed inline.
  - **Tests shipped**: `Av1CodecConfigurationTest` (34 tests covering: every constant pin; `Config` validation rejects out-of-range seqProfile / seqLevelIdx0 / chromaSamplePosition / twelveBit-without-highBitdepth / initialPresentationDelayMinusOne; `encodePayload` byte-layout pins for `DEFAULT_8BIT_YUV420` 4-byte payload (`0x81 0x08 0x0C 0x00`), `DEFAULT_10BIT_YUV420` 4-byte payload (`0x81 0x0D 0x4C 0x00`), 12-bit YUV 4:4:4 high-tier (`0x81 0x4D 0xE2 0x00`), monochrome flag in byte 2 (`0x1C`), initialPresentationDelay surfaces in byte 3 (`0x17` for `ipd=7`), `configOBUs` appended after fixed prefix; `encodeBox` 12-byte canonical envelope for `DEFAULT_8BIT_YUV420` with field offsets pinned (size BE = 0x0C / type "av1C" / 4-byte payload); `encodeBox` grows by `configOBUs.size`; `decodePayload` round-trips 8-bit / 10-bit / 12-bit YUV 4:4:4 / monochrome with IPD and OBUs presets; `decodePayload` returns null IPD when not present; `decodePayload` rejects under-4-byte input + wrong marker + wrong version; `Config` equality compares `configOBUs` by content; `DEFAULT_8BIT_YUV420` + `DEFAULT_10BIT_YUV420` field accessors). Plus 1 new test in `AvifStillMuxerTest` (`ipco av1C box round-trips back to the input config`) and updates to existing muxer tests reflecting the new property in `ipco`.
  - **70 JUnit suites · 1015 tests · 0 failures · 0 errors · 0 skipped** (up from 69 / 980). 35 new tests (34 in `Av1CodecConfigurationTest` + 1 in `AvifStillMuxerTest`). APK 33477 KB (+11 KB).
  - **AVIF emit path status (UPDATED — fully spec-compliant)**: with this round, the host-side AVIF still emit path is now AVIF-spec-compliant per §2.2.1; every primary AV1 image item emits the mandatory `av1C` ItemProperty alongside `ispe` / `pixi` / `colr`. The remaining work to actually ship AVIF stills end-to-end on device is now PURELY the AV1 image encoder pipeline (Phase 1, NDK `libdav1d` / `libaom` integration); the muxer takes the encoder's `av1Bitstream` + `Av1CodecConfiguration.Config` and produces a complete, decode-ready AVIF still file in one call.

- **AVIF still muxer integration round — host-side AVIF still emit path is a single function call (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 26 new JUnit tests)** — pure-data integration round that ties together every host-side primitive shipped in Rounds 17-31 into a single-call API. `AvifStillMuxer.kt` turns a working-space tag, canvas size, per-channel bit-depth list, and AV1 image bitstream into a complete, mux-ready AVIF still file (`ftyp` + `meta` + `mdat`). With this round, the host side of the AVIF emit pipeline is a black box from the camera engine's perspective.
  - `AvifStillMuxer.Input(widthPx, heightPx, bitDepths, cicp, av1Bitstream, rotation?, mirror?, pasp?, clap?, mdcv?, clli?)` — pure-data carrier; `init` rejects zero / empty fields. Optional fields (`rotation` / `mirror` / `pasp` / `clap` / `mdcv` / `clli`) surface in `ipco` only when set, so callers don't pay for properties they don't need.
  - `AvifStillMuxer.encode(input): ByteArray` — produces the canonical AVIF still per AVIF spec §4 by composing every host-side primitive (Rounds 17-31) into a single `ByteArray`. The implementation uses **upfront analytical sizing** to resolve the `iloc`-offset circular dependency: build a placeholder `iloc` with nonzero offsets that match the final field widths (4-byte for AVIF stills < 4 GB), compute `meta.size` once, derive the real AV1 offset = `ftyp.size + meta.size + mdat_header_size`, rebuild `iloc` with the real offsets, and `check(...)` that the size didn't change (sanity guard against field-size boundary crossings).
  - `AvifStillMuxer.encodeSrgbStill(widthPx, heightPx, av1Bitstream): ByteArray` — convenience for the most common camera-app case (8-bit RGB sRGB SDR).
  - **Pinned constants**: `SCHEMA_VERSION = 1`, `PRIMARY_ITEM_ID = 1`, `BIT_DEPTHS_RGB_8` / `BIT_DEPTHS_RGB_10` / `BIT_DEPTHS_RGB_12` / `BIT_DEPTHS_MONO_8`.
  - **Tests shipped**: `AvifStillMuxerTest` (26 tests covering: every constant pin; `Input` validation rejections (zero width / height, empty bitDepths, empty av1Bitstream); top-level `(ftyp, meta, mdat)` sequence; canonical AVIF `ftyp` byte-exact prefix; total file size = `ftyp + meta + mdat`; mdat ends at end of file (no trailing bytes); mdat payload is the AV1 bitstream byte-for-byte; ipco contains exactly 3 mandatory properties (`ispe / pixi / colr`) when no auxiliaries are set; ipco appends `irot` / `imir` / `pasp` / `mdcv` + `clli` only when those fields are set; full 9-property bundle when every auxiliary is set (`ispe / pixi / colr / irot / imir / pasp / clap / mdcv / clli`); iloc offset matches actual AV1 byte position in file (round-trips the parser back to the bytes); different widths / different bitstreams produce different outputs; `encodeSrgbStill` byte-exact equal to explicit `encode(Input(...))`; full HDR10 (Rec.2020 PQ 10-bit + mdcv + clli) integration smoke).
  - **69 JUnit suites · 980 tests · 0 failures · 0 errors · 0 skipped** (up from 68 / 954). 26 new tests in `AvifStillMuxerTest`. APK 33466 KB (+9 KB).
  - **AVIF emit path status (UPDATED)**: with this round, the host side of the AVIF emit pipeline is a single function call. The remaining work is the AV1 image encoder pipeline (Phase 1, NDK `libdav1d` / `libaom` integration) — once that ships, the camera app can call `AvifStillMuxer.encode(...)` and write the result straight to a `File`. Future rounds can extend the muxer with EXIF item handling (requires `iref` "cdsc" reference box) and auxiliary alpha image items (requires `iref` "auxl" reference + `auxC` property).

- **AVIF Image Spatial Extents (`ispe`) FullBox round — completes the AVIF property bundle (BUILD_PLAN §7, 1 new Kotlin module + 22 new JUnit tests)** — pure-data formatter for the `ispe` FullBox per ISO/IEC 23008-12 §6.5.3. `ispe` declares image item width and height in pixels and is **mandatory** for AVIF stills (AVIF spec §2.3) and HEIF images (ISO/IEC 23008-12 §7.3) — without `ispe` a decoder cannot know the canvas size of the encoded image before decoding. With this round, the AVIF property bundle that lands in `ipco` (Round 24) is feature-complete: `colr` + `pixi` + `pasp` + `clap` + `irot` + `imir` + `ispe` + (optionally) `mdcv` / `clli`. Wire format: `FullBox('ispe', 0, 0)` with payload `image_width (uint32_be) + image_height (uint32_be)` (exactly 8 bytes); total mux-ready box is 20 bytes.
  - `ImageSpatialExtents.encodePayload(widthPx: Long, heightPx: Long): ByteArray` — produces the FullBox payload; validates dimensions are in `[1, 0xFFFFFFFF]`. Caller wraps with `IsobmffBox.encodeFullBox("ispe", 0, 0, payload)`.
  - `ImageSpatialExtents.decodePayload(bytes: ByteArray): Pair<Long, Long>` — inverse of `encodePayload`; throws `IllegalArgumentException` on wrong length.
  - `ImageSpatialExtents.encodeBox(widthPx: Long, heightPx: Long): ByteArray` — convenience that wraps with `IsobmffBox.encodeFullBox` for a complete 20-byte mux-ready box in one call.
  - `ImageSpatialExtents.encodeBox(widthPx: Int, heightPx: Int): ByteArray` — `Int` overload for the common case (sensor dimensions fit easily in `Int`); rejects zero / negative inputs.
  - **Pinned constants**: `BOX_TYPE = "ispe"`, `SCHEMA_VERSION = 1`, `PAYLOAD_SIZE = 8`, `MAX_DIMENSION = 0xFFFFFFFFL`.
  - **Tests shipped**: `ImageSpatialExtentsTest` (22 tests covering: every constant pin; `encodePayload` 1×1 + canonical 4096×3072 + max-dimension all-FF + high-byte width byte-layout pins; rejects zero / negative width or height; rejects width or height above `MAX_DIMENSION`; `decodePayload` round-trips canonical AVIF still + MAX_DIMENSION boundary; `decodePayload` rejects 7-byte and 9-byte input; `encodeBox` 4096×3072 produces 20-byte canonical envelope with all field offsets pinned (size BE = 0x14 / type "ispe" / v=0+f=0 / payload); `Int` overload matches `Long` overload; `Int` overload rejects zero width/height).
  - **68 JUnit suites · 954 tests · 0 failures · 0 errors · 0 skipped** (up from 67 / 932). 22 new tests in `ImageSpatialExtentsTest`. APK 33457 KB.
  - **AVIF muxer status (UPDATED)**: the AVIF property bundle is now feature-complete on host. The next module is the AVIF muxer integration that combines `FileTypeBox` (Round 30) + `MetaBox` (Round 29) + `MediaDataBox` (Round 30) into a single `AvifStillMuxer.encode(...)` API.

- **ISOBMFF File Type Box (`ftyp`) + Media Data Box (`mdat`) round — host-side AVIF still emit path COMPLETE end-to-end (BUILD_PLAN §7, 2 new Kotlin modules + 29 new JUnit tests)** — pure-data formatters for the two final ISOBMFF top-level primitives needed for a self-contained AVIF still file. **With this round, every host-side primitive needed to write a valid AVIF still file from scratch ships** — `ftyp` for the file's brand declaration (the very first box in every ISO base media file), `meta` for the metadata bundle (Round 29), and `mdat` for the bulk image bytes. The remaining work is PURELY the AV1 image encoder pipeline (Phase 1 capture engine, NDK `libdav1d` / `libaom` integration). Closes the host-side half of BUILD_PLAN §7 "AVIF / HEIF / JXL bitstream container":
  - `FileTypeBox.encodePayload(majorBrand, minorVersion, compatibleBrands): ByteArray` — produces the box payload per §4.3.2 (`major_brand (4 ASCII) + minor_version (uint32_be) + compatible_brands (N × 4 ASCII)`). Validates that `majorBrand` and every entry in `compatibleBrands` is exactly 4 printable-ASCII characters and that `minorVersion` fits in `uint32`. `ftyp` is a regular Box, NOT a FullBox, so there is no version+flags slot.
  - `FileTypeBox.encodeBox(...)` — wraps the payload with `IsobmffBox.encodeBox("ftyp", payload)`.
  - `FileTypeBox.encodeAvifStillBox()` — AVIF still-image shortcut for `major_brand = "avif"`, `minor_version = 0`, `compatible_brands = ["avif", "mif1", "miaf"]` (HEIF brand per ISO/IEC 23008-12 §10.2.1 + MIAF per ISO/IEC 23000-22). Produces a 28-byte canonical envelope.
  - **Brand constants shipped**: `BRAND_AVIF = "avif"` / `BRAND_AVIS = "avis"` / `BRAND_MIF1 = "mif1"` / `BRAND_MIAF = "miaf"` / `BRAND_MA1A = "MA1A"` (AVIF MIAF Profile A1 still) / `BRAND_MA1B = "MA1B"` / `BRAND_ISOM = "isom"` (ISOBMFF generic base) / `BRAND_HEIC = "heic"` (HEIF major brand for HEVC stills). `BRAND_LENGTH = 4`.
  - `MediaDataBox.encodeBox(data: ByteArray)` and `MediaDataBox.encodeBox(datas: List<ByteArray>)` — produce the complete `(header + payload)` box per §8.1.1. Payload is opaque per spec — the parser does not look inside, only knows where the box ends. The list form is equivalent to `encodeBox(IsobmffBox.concatPayloads(datas))` and lets the muxer glue together (e.g. AV1 image bitstream + EXIF metadata blob) in one go.
  - `MediaDataBox.headerSize(payloadSize: Long): Int` — exposes the on-wire header size (8 for the canonical plain header per Round 22, 16 when the box would otherwise overflow the `uint32` size field). Critical for the muxer's offset planning: for `construction_method = FILE_OFFSET`, the `iloc` table (Round 28) must carry absolute file offsets into `mdat`, which means the muxer needs to know the `mdat` header size BEFORE laying out the file. This helper makes that calculation explicit and JUnit-testable without needing to actually allocate a giant payload.
  - **Pinned constants**: `FileTypeBox`: `BOX_TYPE = "ftyp"`, `SCHEMA_VERSION = 1`, `BRAND_LENGTH = 4` + 8 brand-name constants. `MediaDataBox`: `BOX_TYPE = "mdat"`, `SCHEMA_VERSION = 1`, `PLAIN_HEADER_SIZE = 8` (= `IsobmffBox.PLAIN_HEADER_SIZE`), `LARGE_HEADER_SIZE = 16` (= `IsobmffBox.LARGE_HEADER_SIZE`).
  - **Tests shipped**: `FileTypeBoxTest` (15 tests covering: every constant pin; `encodePayload` empty compatible brands → 8-byte payload; AVIF still canonical layout (`avif` / `0` / `[avif, mif1, miaf]` → 20-byte payload with documented byte sequence at offsets 0..4 / 4..8 / 8..12 / 12..16 / 16..20); high-byte minorVersion (`0x12345678`) byte-layout pin; max minorVersion (`0xFFFFFFFF`) encoding; rejects 3-char / 5-char / empty / non-printable-ASCII majorBrand; rejects non-4-char compatible brand; rejects negative minorVersion; rejects minorVersion > uint32; `encodeBox` AVIF still produces 28-byte canonical envelope with all field offsets pinned; `encodeAvifStillBox` byte-exact equal to `encodeBox("avif", 0, [avif, mif1, miaf])`). `MediaDataBoxTest` (14 tests covering: every constant pin including parity with `IsobmffBox` constants; `headerSize` returns 8 for empty payload, 8 for typical 17 KB AVIF, 8 just under the threshold (`LARGE_SIZE_THRESHOLD - PLAIN_HEADER_SIZE`), 16 just over the threshold; rejects negative payloadSize; `encodeBox(empty)` produces 8-byte header-only box `(0 0 0 8 m d a t)`; `encodeBox(payload)` produces 13-byte `(size, "mdat", payload)` byte sequence; `encodeBox` defensively copies payload (mutating source after encode leaves the encoded box unchanged at offsets 8..12); `encodeBox(emptyList())` produces 8-byte header-only box; `encodeBox(list)` concatenates list payloads in stable order; single-element-list parity with direct `encodeBox(payload)`).
  - **67 JUnit suites · 932 tests · 0 failures · 0 errors · 0 skipped** (up from 65 / 903). 29 new tests (15 `FileTypeBoxTest` + 14 `MediaDataBoxTest`). APK 33454 KB (+4 KB).
  - **AVIF still emit path (NEW: COMPLETE end-to-end on host)**: `FileTypeBox.encodeAvifStillBox()` + `MetaBox` (Round 29) + `MediaDataBox.encodeBox(av1ImageBytes)` are the three top-level boxes a parser sees in a self-contained AVIF still. The `meta` box references byte ranges inside `mdat` via `iloc` (Round 28) for items declared in `iinf` (Round 26); each item's properties live in `iprp` (Round 24) linked via `ipma` (Round 23) using property indices that point at children of `ipco` (Round 24) — every byte from `colr` / `mdcv` / `clli` / `pasp` / `clap` / `irot` / `imir` / `pixi` (Rounds 17-20). The remaining work to actually emit AVIF stills end-to-end on device is now PURELY the AV1 image encoder pipeline (Phase 1 capture engine work, NDK `libdav1d` / `libaom` integration).

- **AVIF / HEIF top-level Meta Box (`meta`) round — host-side AVIF muxer end-to-end COMPLETE (BUILD_PLAN §7, 1 new Kotlin module + 1 new public Builder class + 22 new JUnit tests)** — pure-data composer for the top-level `meta` FullBox per ISO/IEC 14496-12 §8.11.1, the root container the entire HEIF / AVIF item-based file format hangs off of. `meta` is a `FullBox('meta', version = 0, flags = 0)` whose payload is an ordered concatenation of child boxes describing the items in the file. Per §8.11.1.2, `hdlr` (Round 27) MUST be the first child; the remaining children are optional, appear at most once each, and the spec orders them as `pitm` (Round 25), `iinf` (Round 26), `iloc` (Round 28), `iref`, `iprp` (Rounds 23-24). For the canonical AVIF still, the minimum is `hdlr + pitm + iinf + iloc + iprp`; once that fits inside `meta`, the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete. **With this round, the host-side AVIF muxer is end-to-end complete** — every primitive needed to mux a fully-tagged AVIF still ships, including the top-level orchestration. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEIF top-level meta box":
  - `MetaBox.encodePayload(handlerBox, primaryItemBox?, itemInfoBox?, itemLocationBox?, itemReferenceBox?, itemPropertiesBox?): ByteArray` — produces the FullBox payload by concatenating the children in the §8.11.1 canonical order. Any null argument is omitted from the wire. The caller wraps with `IsobmffBox.encodeFullBox("meta", 0, 0, payload)` (or uses `encodeBox(...)` below).
  - `MetaBox.encodePayloadOrdered(children: List<ByteArray>): ByteArray` — lower-level form that preserves caller ordering. Validates that `hdlr` is the first child (per §8.11.1.2) and that no child type appears more than once, but otherwise leaves ordering up to the caller. Useful for nonstandard `meta` payloads that the canonical helper does not cover.
  - `MetaBox.encodeBox(handlerBox, ...)` — convenience that produces the complete, mux-ready `meta` box (header + payload) in one call by wrapping the canonical payload with `IsobmffBox.encodeFullBox`.
  - `MetaBox.Builder` — fluent builder for incremental composition. `setHandler(box)` is mandatory; the rest are optional; `build()` validates `hdlr` presence (`IllegalStateException` otherwise), applies the §8.11.1 ordering, and returns the complete mux-ready `meta` box. The builder **defensively copies inputs** so a caller mutating the source buffer afterward cannot corrupt the encoded box. All setters return `this` for fluent chaining.
  - **Pinned constants**: `BOX_TYPE = "meta"`, `SCHEMA_VERSION = 1`, `MIN_CHILD_BOX_SIZE = 8` (the canonical ISOBMFF plain-box header size; rejects malformed children at build time).
  - **One Kotlin lint fix during gate run**: my first cut of the test name `\`end-to-end: full AVIF still meta box round-trips through every R22-R28 module\`` failed the Kotlin compiler with `Name contains illegal characters: :.` (Kotlin rejects `:` in backtick-quoted identifiers). Renamed to `end-to-end full AVIF still meta box round-trips through every R22-R28 module`. Same family of issue as the `[]` rejection in Round 25 — Kotlin's identifier escapes are stricter than the spec implies.
  - **Tests shipped**: `MetaBoxTest` (22 tests covering: `BOX_TYPE` / `SCHEMA_VERSION` / `MIN_CHILD_BOX_SIZE` constant pins; `encodePayload` with only hdlr equals hdlr bytes; `encodePayload` concatenates 4-child canonical AVIF still in `(hdlr, pitm, iinf, iloc)` order with sum-of-children byte-count parity; `encodePayload` omits null children correctly; `encodePayloadOrdered` rejects empty list / too-short child / non-`hdlr` first child / duplicate child type; `encodePayloadOrdered` preserves caller ordering even when non-canonical; `encodeBox` of hdlr-only meta produces 45-byte canonical envelope `(size=45, type='meta', version=0, flags=0, payload=hdlr)`; `encodeBox` for full canonical AVIF still has expected size = `12 + hdlr + pitm + iinf + iloc`; `encodeBox` returns a value distinct from the payload (12-byte FullBox header prefix); `Builder` requires handler before build (`IllegalStateException`); `Builder.build()` matches `encodeBox(...)` for hdlr-only and full-AVIF cases; `Builder` defensively copies inputs (mutating source after `setHandler` does not affect built box); `Builder` setters are fluent (return `this`); **end-to-end** integration test that builds the full AVIF metadata stack — `iprp` from a `colr` + `pixi` + `ipma` chain via `IsobmffItemProperties.Builder` (Round 24) + `ItemPropertyAssociation.encodeBox` (Round 23) + `IsobmffBox.encodeBox` / `encodeFullBox` (Round 22) — and confirms every byte of the final `meta` box is `(FullBox header) + (hdlr + pitm + iinf + iloc + iprp)` in canonical order).
  - **65 JUnit suites · 903 tests · 0 failures · 0 errors · 0 skipped** (up from 64 / 881). 22 new tests in `MetaBoxTest`. APK 33450 KB (+5 KB).
  - **AVIF muxer status (UPDATED — host-side layer COMPLETE end-to-end)**: with this round, the engine has a single-call path from the `WorkingSpace` preset all the way to a fully-tagged `meta` FullBox. The remaining work is the `mdat` orchestration (`MdatBox.kt` for the bulk image bytes + the major-brand `ftyp` box) which lands when the image encoder ships in Phase 1.

- **AVIF / HEIF Item Location Box (`iloc`) round (BUILD_PLAN §7, 1 new Kotlin module + 3 new public data classes + 1 new public enum + 37 new JUnit tests)** — pure-data formatter for the `iloc` FullBox per ISO/IEC 14496-12 §8.11.3, the table that maps every item declared in `iinf` to the byte ranges in `mdat` (or in the `idat` box, or in another item) where its encoded data lives. Without `iloc`, no decoder can find the AV1 / EXIF / etc. bitstream bytes in a HEIF / AVIF still — `iloc` is the bridge between the property / info catalog and the actual encoded image bytes. **With this round, every host-side primitive needed to mux a fully-tagged AVIF still ships** — the next module is the top-level `meta` FullBox itself, after which the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEIF item location":
  - `ItemLocationBox.Extent(offset, length, index = null)` — pure-data carrier for one byte range. `init` rejects negative offset / length / index. `length = 0` is accepted (some implementations zero-pad).
  - `ItemLocationBox.Item(itemId, constructionMethod = FILE_OFFSET, dataReferenceIndex = 0, baseOffset = 0, extents)` — pure-data carrier for one row of the `iloc` table. `init` validates itemId in `[0, MAX_LARGE_ITEM_ID]`, dataReferenceIndex in `[0, 65535]`, baseOffset >= 0, extents non-empty and size <= 65535.
  - `ItemLocationBox.ConstructionMethod` enum — `FILE_OFFSET` (0; bytes in `mdat`), `IDAT_OFFSET` (1; bytes in the `meta`-resident `idat`), `ITEM_OFFSET` (2; bytes in another item, referenced by `extent_index`). For canonical AVIF stills (single mdat-backed image item) the engine emits `version = 0` and this field is absent from the wire entirely.
  - `ItemLocationBox.FieldSizes(offsetSize, lengthSize, baseOffsetSize, indexSize)` — field-size selection in bytes. Each value is one of `[0, 4, 8]` per §8.11.3 (Point & Shoot supports the canonical sizes; arbitrary widths in `[0, 7]` exist in spec but are not interoperable). A value of `0` means "the corresponding field is omitted from the bitstream".
  - `ItemLocationBox.chooseFieldSizes(items): FieldSizes` — picks the minimum widths that can encode every offset / length / base_offset / index across `items` without truncation. Each width is `0` if the corresponding field is always zero (the canonical "no base_offset, no index" AVIF still case), `4` when the largest value fits in a `uint32`, or `8` when it does not.
  - `ItemLocationBox.chooseVersion(items, fieldSizes): Int` — picks the minimum FullBox version: `0` for canonical AVIF stills (16-bit `item_count` + 16-bit `item_ID`, no `construction_method` field), `1` when at least one item has a non-`FILE_OFFSET` construction method or `indexSize > 0` while every itemId still fits in 16 bits, `2` when any itemId or item count exceeds 16 bits.
  - `ItemLocationBox.encodePayload(items, version, fieldSizes): ByteArray` — emits ONLY the FullBox payload (the bytes after the 4-byte version+flags slot); the caller wraps with `IsobmffBox.encodeFullBox("iloc", version, 0, payload)`. Validates that v=0 cannot carry `indexSize > 0` or non-`FILE_OFFSET` construction methods, that every itemId fits the chosen version's `item_ID` field, and that every offset / length / base_offset / index value fits its declared field size.
  - `ItemLocationBox.encodeBox(items): ByteArray` — convenience that picks the minimum field sizes via `chooseFieldSizes`, the minimum version via `chooseVersion`, encodes the payload, and wraps with `IsobmffBox.encodeFullBox` so the muxer gets a complete, mux-ready `iloc` box (header + payload) in one call. For the canonical single-image AVIF still (`itemId = 1`, `offset = 0x12000`, `length = 0x4500`) this produces a 30-byte total box (8 header + 4 v+f + 18 payload).
  - **Pinned constants**: `BOX_TYPE = "iloc"`, `SCHEMA_VERSION = 1`, `MAX_SMALL_ITEM_ID = 65535`, `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `MAX_SMALL_ITEM_COUNT = 65535`, `MAX_LARGE_ITEM_COUNT = 0xFFFFFFFF`, `MAX_EXTENT_COUNT = 65535`, `MAX_DATA_REFERENCE_INDEX = 65535`, `MAX_CONSTRUCTION_METHOD = 15`, `ALLOWED_FIELD_SIZES = [0, 4, 8]`.
  - **Tests shipped**: `ItemLocationBoxTest` (37 tests covering: every constant pin; `ConstructionMethod` wire values 0/1/2; `Extent` rejects negative offset / length / index + accepts zero-length; `Item` rejects negative itemId, itemId > MAX_LARGE, empty extents, out-of-range dataReferenceIndex (negative + 0x10000), negative baseOffset; `FieldSizes` rejects values not in `[0, 4, 8]` for any of its four fields; `chooseFieldSizes` picks 4-4-0-0 for canonical AVIF still + promotes offsetSize to 8 for offsets > 4 GB + promotes lengthSize to 8 for lengths > 4 GB + promotes baseOffsetSize to 4 for nonzero baseOffsets; `chooseVersion` picks 0 for canonical AVIF + 1 for `IDAT_OFFSET` + 2 for itemId > 65535 + stays at 0 at boundary itemId 65535; `encodePayload` v=0 canonical AVIF still produces an 18-byte payload with the documented byte-by-byte layout (`packed_sizes 0x44 0x00 / item_count 0x00 0x01 / item_ID 0x00 0x01 / data_ref_index 0x00 0x00 / extent_count 0x00 0x01 / extent_offset 0x00 0x01 0x20 0x00 / extent_length 0x00 0x00 0x45 0x00`); `encodePayload` v=0 rejects indexSize>0 and non-`FILE_OFFSET` construction methods; `encodePayload` v=1 surfaces 12-bit reserved + 4-bit construction_method packed in uint16 producing a 20-byte payload with the documented layout; `encodePayload` v=2 emits 32-bit `item_count` and `item_ID` producing a 24-byte payload with the documented layout; rejects invalid version (-1 / 3); rejects offset overflow at field size 4; rejects nonzero baseOffset when baseOffsetSize is 0; rejects itemId > MAX_SMALL when v=0; supports multiple extents per item with correct `extent_count` and per-extent sizing; `encodeBox` produces canonical 30-byte AVIF envelope with all field offsets pinned (size BE = 0x1E / type "iloc" / version=0+flags=0 / packed_sizes 0x44 0x00 / item_count / item_ID); `encodeBox` auto-promotes to v=2 for itemId 0x10000 producing a 36-byte envelope; auto-promotes to v=1 for `IDAT_OFFSET` producing a 32-byte envelope; emits offset_size=8 for offsets beyond 4 GB producing a 34-byte envelope with packed_sizes `0x84 0x00`; two-item `(av01 + Exif)` AVIF still preserves order (reversing the input changes the encoded box).
  - **64 JUnit suites · 881 tests · 0 failures · 0 errors · 0 skipped** (up from 63 / 844). 37 new tests in `ItemLocationBoxTest`. APK 33445 KB (+19 KB).
  - **AVIF muxer status (UPDATED — host-side layer COMPLETE)**: with this round, every host-side primitive needed to mux a fully-tagged AVIF still ships — every property payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) + the canonical box-envelope writer from Round 22 + the item-property association from Round 23 + the property container & parent boxes from Round 24 + the primary-item declaration from Round 25 + the item-info table from Round 26 + the handler-reference box from Round 27 + the item-location box from Round 28. The next module in the muxer is the top-level `meta` FullBox itself (which wraps `hdlr` + `pitm` + `iinf` + `iloc` + `iprp`); after that, the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete.

- **AVIF / HEIF Handler Reference Box (`hdlr`) round (BUILD_PLAN §7, 1 new Kotlin module + 14 new JUnit tests)** — pure-data formatter for the `hdlr` FullBox per ISO/IEC 14496-12 §8.4.3, the box that declares the *purpose* of the container it lives in. For a HEIF / AVIF still file, `hdlr` is the **first** child box of the top-level `meta` FullBox and carries `handler_type = "pict"` (still-picture container per ISO/IEC 23008-12 §6.2). Without this box, decoders won't know to scan `meta` for image items. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEIF handler reference":
  - `HandlerReferenceBox.encodePayload(handlerType, name = "")` — emits ONLY the FullBox payload (the bytes after the 4-byte version+flags slot). Wire format: `pre_defined (uint32_be = 0) + handler_type (4 ASCII bytes) + reserved[3] (3 × uint32_be = 0) + name (NUL-terminated UTF-8)`. Fixed prefix is `4 + 4 + 12 = 20` bytes; the trailing NUL is always emitted (an empty name encodes as a single zero byte). Validates that `handlerType` is exactly 4 printable-ASCII characters and that `name` does not contain NUL (NUL is the wire terminator). Caller wraps with `IsobmffBox.encodeFullBox("hdlr", 0, 0, payload)`.
  - `HandlerReferenceBox.encodeBox(handlerType, name = "")` — convenience that produces the complete mux-ready FullBox in one call (`size + type + version=0 + flags=0 + payload`); minimum total box size is 33 bytes (8 header + 4 v+f + 20 prefix + 1 NUL). `encodePictBox(name = "")` is the AVIF / HEIF still-image shortcut for `handler_type = "pict"`.
  - **Handler-type constants shipped**: `HANDLER_TYPE_PICT = "pict"` (HEIF / AVIF still-image `meta` containers), `HANDLER_TYPE_VIDE = "vide"` (MP4 video tracks), `HANDLER_TYPE_SOUN = "soun"` (MP4 audio tracks), `HANDLER_TYPE_META = "meta"` (timed metadata tracks), `HANDLER_TYPE_AUXV = "auxv"` (auxiliary video, e.g. AVIF auxiliary alpha layer per ISO/IEC 23008-12 §7.3 / §7.5).
  - **Pinned constants**: `BOX_TYPE = "hdlr"`, `SCHEMA_VERSION = 1`, `FIXED_PAYLOAD_PREFIX = 20`.
  - **Tests shipped**: `HandlerReferenceBoxTest` (14 tests covering: `BOX_TYPE` / `SCHEMA_VERSION` / `FIXED_PAYLOAD_PREFIX` constant pins; every handler-type constant pin; `encodePayload("pict")` produces a 21-byte payload (20 prefix + 1 NUL) with `pre_defined=0` at offset 0..4, `handler_type="pict"` at offset 4..8, `reserved[3]=0` at offset 8..20, `name` NUL at offset 20; `encodePayload("vide", "VideoHandler")` produces a 33-byte payload with the documented offsets and the UTF-8 name at 20..32 plus NUL at 32; `encodePayload("pict", "Photo")` round-trips the multi-byte UTF-8 case; `encodePayload` rejects 3-char / 5-char / empty handlerType; rejects non-printable-ASCII handlerType (`"pi\u0001t"`); rejects NUL in `name`; `encodeBox("pict")` produces the canonical 33-byte FullBox envelope with size BE=33, type="hdlr", version=0+flags=0, and every field offset pinned (pre_defined at 12..16, handler_type at 16..20, reserved at 20..32, name NUL at 32); `encodePictBox()` is byte-exact equal to `encodeBox("pict")`; `encodePictBox("Photo")` produces a 38-byte envelope with the name at 32..37 and NUL at 37; `encodeBox("auxv")` produces the AVIF auxiliary-handler envelope).
  - **63 JUnit suites · 844 tests · 0 failures · 0 errors · 0 skipped** (up from 62 / 830). 14 new tests in `HandlerReferenceBoxTest`. APK 33426 KB (+3 KB).
  - **AVIF muxer status (UPDATED)**: with this round, the engine has all four `meta` FullBox children **except `iloc`** — `hdlr` (Round 27) + `pitm` (Round 25) + `iinf` / `infe` (Round 26) + `iprp` / `ipco` / `ipma` (Rounds 23-24), riding on top of the property payloads from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) and the canonical box-envelope writer from Round 22. The next round closes the muxer by shipping `iloc` (Item Location; the table of byte offsets into `mdat`), after which the top-level `meta` FullBox + `mdat` orchestration completes the host-side AVIF still-image emit path.

- **AVIF Item Information Entry (`infe`) + Item Information Box (`iinf`) round (BUILD_PLAN §7, 2 new Kotlin modules + 1 new public data class + 43 new JUnit tests)** — pure-data formatters for the `infe` (Item Info Entry) and `iinf` (Item Information Box) FullBoxes per ISO/IEC 14496-12 §8.11.6. Together these declare every item's type and properties inside the `meta` FullBox: `infe` per item (carries `item_ID`, `item_protection_index`, `item_type`, `item_name`, and the `hidden` flag), `iinf` as the count + concatenation of all `infe` entries in the file. For an AVIF still, the engine ships at minimum two `infe` entries — one with `item_type = "av01"` (AV1 image item per ISO/IEC 23008-12 §6.4) for the primary image and (optionally) one with `item_type = "Exif"` for the EXIF metadata item. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC item info table":
  - `ItemInfoEntry.Entry(itemId, itemType, itemName, itemProtectionIndex, contentType, contentEncoding, itemUriType, hidden)` — pure-data carrier for one row of the `iinf` table in the spec's v2/v3 form (the only forms Point & Shoot emits since v0/v1 are deprecated per ISO/IEC 14496-12:2020). `init` validates `0 <= itemId <= MAX_LARGE_ITEM_ID`, `0 <= itemProtectionIndex <= 0xFFFF`, `itemType.length == 4` and printable ASCII, no NUL bytes in any string field (NUL is the wire-format terminator), `contentType` / `contentEncoding` only allowed when `itemType == "mime"`, `itemUriType` only allowed when `itemType == "uri "`. `hidden` surfaces as FullBox `flags` bit 0 (`FLAG_ITEM_HIDDEN = 0x000001`) per §8.11.6.2 — used for AVIF auxiliary alpha layers per ISO/IEC 23008-12 §7.3 / §7.5.
  - `ItemInfoEntry.chooseVersion(itemId)` — picks the minimum FullBox version: `2` for itemIds in `[0, 65535]` (the canonical case), `3` for itemIds in `(65535, 0xFFFFFFFF]`. `computeFlags(entry)` returns `FLAG_ITEM_HIDDEN` when `entry.hidden`, else `0`.
  - `ItemInfoEntry.encodePayload(entry, version)` — emits the FullBox payload: `item_ID (uint16/uint32 BE) + item_protection_index (uint16 BE) + item_type (4 ASCII bytes) + item_name (NUL-terminated UTF-8)`, then the conditional `content_type / content_encoding` (for `mime`) or `item_uri_type` (for `uri `). The caller wraps with `IsobmffBox.encodeFullBox("infe", version, flags, payload)`. `encodeBox(entry)` is the convenience that picks the minimum version + computes flags + encodes the payload + wraps the result in one call.
  - `ItemInfoBox.encodePayload(infeBoxes, version)` — emits the `iinf` FullBox payload: `entry_count (uint16/uint32 BE)` followed by the concatenation of pre-encoded `infe` boxes. Validates that every input box is at least the canonical 8-byte ISOBMFF header. `encodeBox(infeBoxes)` is the convenience that picks the minimum version (`0` for `<= 65535` entries, `1` otherwise) + encodes the payload + wraps with `IsobmffBox.encodeFullBox("iinf", version, 0, payload)`.
  - **Item-type constants shipped**: `ITEM_TYPE_AV01 = "av01"` (AVIF AV1 image item), `ITEM_TYPE_EXIF = "Exif"` (HEIF + AVIF EXIF metadata), `ITEM_TYPE_MIME = "mime"` (generic MIME-typed metadata), `ITEM_TYPE_URI = "uri "` (URI-typed metadata; trailing space is intentional and matches the spec's 4-char fourCC), `ITEM_TYPE_AVC1 = "avc1"` (HEIF-AVC), `ITEM_TYPE_HVC1 = "hvc1"` (HEIF-HEVC; the legacy "HEIC" path).
  - **Pinned constants**: `infe`: `BOX_TYPE = "infe"`, `SCHEMA_VERSION = 1`, `MAX_SMALL_ITEM_ID = 0xFFFF`, `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `MAX_ITEM_PROTECTION_INDEX = 0xFFFF`, `FLAG_ITEM_HIDDEN = 0x000001`. `iinf`: `BOX_TYPE = "iinf"`, `SCHEMA_VERSION = 1`, `MAX_SMALL_ENTRY_COUNT = 0xFFFF`, `MAX_LARGE_ENTRY_COUNT = 0xFFFFFFFF`.
  - **Tests shipped**: `ItemInfoEntryTest` (28 tests covering: constant pins; `Entry` validation rejects negative itemId / over-MAX_LARGE itemId / out-of-range itemProtectionIndex / non-4-char itemType / non-printable-ASCII itemType / NUL in string fields / contentType when not `mime` / itemUriType when not `uri `; boundary itemIds 0 and MAX_LARGE accepted; `chooseVersion` picks 2 for `[0, MAX_SMALL]` and 3 for `(MAX_SMALL, MAX_LARGE]`; `computeFlags` returns 0 by default and `FLAG_ITEM_HIDDEN` when `hidden`; v=2 byte-layout pins for canonical `av01` item with empty name (9-byte payload `0x00 0x01 0x00 0x00 'a' 'v' '0' '1' 0x00`), `av01` with `itemName = "primary"` (16-byte payload), `protection_index = 0x1234`, `mime` itemType with + without `content_encoding`, `uri ` itemType with `item_uri_type`; v=3 emits 4-byte item_ID big-endian for `0x12345678`; `encodePayload` v=2 rejects itemId beyond MAX_SMALL; rejects v=0 / v=1 / v=4; `encodeBox` produces canonical 21-byte FullBox envelope for `av01` itemId=1 with documented `(size=21, type='infe', version=2, flags=0, item_ID=1, payload)`; auto-promotes to v=3 for itemId=100000; surfaces `hidden=true` as flags low byte `0x01`; canonical `Exif` metadata layout with item_type at offset 16..20). `ItemInfoBoxTest` (15 tests covering: constant pins; `chooseVersion` boundary cases; v=0 empty list emits 2 zero bytes; v=1 empty list emits 4 zero bytes; v=0 single-`infe` payload concatenates after 16-bit count; v=0 two-`infe` payload preserves order; v=1 single-`infe` payload uses 4-byte count; rejects invalid version; rejects too-short `infe`; `encodeBox` empty list produces 14-byte v=0 envelope; `encodeBox` two-entry canonical AVIF still (`av01` + `Exif`) produces documented `(size, type='iinf', version=0, flags=0, count=2, infe[0], infe[1])` byte layout).
  - **One offset-arithmetic test fix during gate run**: my first cut of `encodeBox EXIF metadata item produces canonical layout` checked `box.copyOfRange(14, 18)` for the item_type bytes; the actual layout is `header(8) + version+flags(4) + item_ID(2) + protection(2) + item_type(4) + name_NUL(1)`, so item_type is at offset `[16, 20)`. Updated the assertion offsets and added an inline comment documenting the byte-layout breakdown so the next reader doesn't make the same mistake.
  - **62 JUnit suites · 830 tests · 0 failures · 0 errors · 0 skipped** (up from 60 / 787). 43 new tests (28 `ItemInfoEntryTest` + 15 `ItemInfoBoxTest`). APK 33423 KB (+12 KB).
  - **AVIF muxer status (UPDATED)**: with this round, the engine has every host-side primitive needed to mux a fully-tagged AVIF still — every payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) + the canonical box-envelope writer from Round 22 + the item-property association table from Round 23 + the property container & parent boxes from Round 24 + the primary-item declaration from Round 25 + the item-info table (`infe` + `iinf`) from Round 26. The remaining host-side work is `iloc` (Item Location; the table of byte offsets into `mdat`) + the top-level `meta` FullBox (which wraps `hdlr` + `pitm` + `iinf` + `iloc` + `iprp`); after that, the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete.

- **AVIF Primary Item Box (`pitm`) round (BUILD_PLAN §7, 1 new Kotlin module + 27 new JUnit tests)** — pure-data formatter for the AVIF / HEIF Primary Item Box (`pitm`) per ISO/IEC 14496-12 §8.11.4. `pitm` is a `FullBox('pitm', version, 0)` that names the single primary item of the file: for a HEIF / AVIF still image, the primary item is the displayable image. Without `pitm` a HEIF decoder cannot tell which item from `iinf` is the one to present to the user. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC primary-item declaration":
  - `PrimaryItemBox.chooseVersion(itemId): Int` — picks the minimum version that can encode `itemId` without truncation: returns `0` for itemIds in `[0, 65535]` (the canonical 16-bit form, used for every realistic still-image use case the engine ships) and `1` for itemIds in `(65535, 4_294_967_295]` (the spec-completeness 32-bit form). Throws `IllegalArgumentException` for negative or oversized item IDs.
  - `PrimaryItemBox.encodePayload(itemId, version): ByteArray` — emits ONLY the FullBox payload bytes (the bytes after the 4-byte version+flags slot). For `version = 0` produces 2 big-endian bytes; for `version = 1` produces 4 big-endian bytes. Validates that the chosen version's capacity covers the requested itemId and throws otherwise. Caller wraps with `IsobmffBox.encodeFullBox("pitm", version, flags = 0, payload)`.
  - `PrimaryItemBox.decodePayload(bytes, version): Long` — inverse; throws on payload-length mismatch (must be exactly 2 bytes for v=0 and 4 bytes for v=1). Returns the unsigned 16-bit / 32-bit item ID lifted into a `Long` so the v=1 max value `0xFFFFFFFFL` doesn't sign-extend negatively on the JVM int range.
  - `PrimaryItemBox.encodeBox(itemId): ByteArray` — convenience that picks the minimum version via `chooseVersion`, encodes the payload, and wraps with `IsobmffBox.encodeFullBox`. Produces a complete, mux-ready `pitm` box (header + payload) in one call: 14 bytes for v=0 (`size(4) + type(4) + version+flags(4) + payload(2)`) and 16 bytes for v=1 (with 4-byte payload). `flags` is always `0` per spec.
  - **Pinned constants**: `BOX_TYPE = "pitm"`, `SCHEMA_VERSION = 1`, `MAX_SMALL_ITEM_ID = 0xFFFF` (65535), `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `PAYLOAD_SIZE_V0 = 2`, `PAYLOAD_SIZE_V1 = 4`.
  - **Tests shipped**: `PrimaryItemBoxTest` (27 tests covering: constant pins; `chooseVersion` picks 0 for the `[0, 65535]` range incl. boundaries 0/1/255/MAX_SMALL, picks 1 for `(65535, MAX_LARGE]` incl. boundaries 65536/100000/MAX_LARGE, rejects negative + over-MAX_LARGE; `encodePayload` v=0 byte-layout pins for `itemId=1` (`0x00 0x01`), `itemId=MAX_SMALL` (`0xFF 0xFF`), `itemId=0x1234` (`0x12 0x34`), rejects v=0 overflow; v=1 byte-layout pins for `itemId=1` (`0x00 0x00 0x00 0x01`), `itemId=0x12345678` (`0x12 0x34 0x56 0x78`), `itemId=MAX_LARGE` (`0xFF 0xFF 0xFF 0xFF`), rejects v=1 overflow + negative + invalid version; `decodePayload` v=0 round-trips 7 itemIds across the small range, v=1 round-trips 7 itemIds incl. the v=1-max boundary, throws on wrong-length and invalid-version inputs; `encodeBox` produces 14-byte canonical `pitm` envelope for `itemId=1` (with documented size=14, type='pitm', version+flags=0, payload), 16-byte envelope for `itemId=100000` with auto-promoted version=1 and the documented `0x000186A0` big-endian payload, 16-byte envelope for `itemId=MAX_LARGE` with all-FF payload, and stays at version=0 / 14-byte envelope at the `itemId=MAX_SMALL` boundary).
  - **One Kotlin lint fix during gate run**: my first cut of the test name `chooseVersion picks 0 for itemId in [0, 65535]` failed the Kotlin compiler with `Name contains illegal characters: [].` The Kotlin spec rejects `[` and `]` even inside backtick-quoted identifiers; renamed to `chooseVersion picks 0 for itemId in 0 to 65535 inclusive` to keep the same intent without the brackets.
  - **60 JUnit suites · 787 tests · 0 failures · 0 errors · 0 skipped** (up from 59 / 760). 27 new tests in `PrimaryItemBoxTest`. APK 33411 KB (+3 KB).
  - **AVIF muxer status (UPDATED)**: with this round, the engine has every host-side primitive needed to mux a fully-tagged AVIF still — every payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) + the canonical box-envelope writer from Round 22 + the item-property association table from Round 23 + the property container & parent boxes from Round 24 + the primary-item declaration from Round 25. The remaining host-side work is `iinf` (Item Information; FullBox holding `infe` entries) + `iloc` (Item Location; the table of byte offsets into `mdat`) + the top-level `meta` FullBox; after that, the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete.

- **AVIF Item Property Container (`ipco` / `iprp`) round (BUILD_PLAN §7, 1 new Kotlin module + 1 new builder class + 19 new JUnit tests)** — pure-data formatter for the AVIF / HEIF Item Properties parent box (`iprp`) and its mandatory child Item Property Container (`ipco`) per ISO/IEC 14496-12 §8.11.14. Both boxes are plain (non-FullBox) ISOBMFF envelopes, so this round reuses the `IsobmffBox.encodeBox` / `concatPayloads` primitives from Round 22 to compose the `ipco` payload (an ordered concatenation of property child boxes from Rounds 17-20 — `colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) and the `iprp` payload (one `ipco` followed by 1+ `ipma` boxes from Round 23). Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC item property container":
  - `IsobmffItemProperties.encodeIpcoBox(properties: List<ByteArray>): ByteArray` — encodes the `ipco` container box: an ordered concatenation of pre-encoded property child boxes wrapped in the canonical 8-byte ISOBMFF header. Each entry must already include its own 8-byte (or 16-byte large-size) header — the typical call site is `IsobmffBox.encodeBox(type, payload)` for plain property boxes (`colr`, `pasp`, `clap`, etc.) or `IsobmffBox.encodeFullBox(type, version, flags, payload)` for FullBox property boxes. Property order matters per ISO/IEC 14496-12 §8.11.14.3 (`ipma` references properties by their 1-based position in this list), which is why this layer preserves caller-supplied order.
  - `IsobmffItemProperties.encodeIprpBox(ipcoBox: ByteArray, ipmaBoxes: List<ByteArray>): ByteArray` — encodes the `iprp` parent box: exactly one `ipco` (mandatory per spec) followed by 1+ `ipma` boxes. Both arguments must already be pre-encoded box byte buffers; typical call sites are [`IsobmffItemProperties.encodeIpcoBox`] for the `ipco` argument and [`ItemPropertyAssociation.encodeBox`] for each `ipma` argument. Throws `IllegalArgumentException` when `ipmaBoxes` is empty.
  - `IsobmffItemProperties.Builder` — order-preserving builder for the `ipco` payload. Each call to `add(encodedBox)` returns the **1-based** property index per §8.11.14.3 — so the caller can pipeline `val colrIdx = builder.add(...)` straight into `ItemPropertyAssociation.Association(propertyIndex = colrIdx, ...)` without manually counting children. `add` defensively copies the input so a caller mutating the source buffer afterward cannot corrupt the eventual `ipco` payload. `build()` returns the complete `ipco` box; equivalent to `encodeIpcoBox(properties)` where `properties` is the list passed to `add` in order. `size()` returns the running property count.
  - **Pinned constants**: `IPCO_BOX_TYPE = "ipco"`, `IPRP_BOX_TYPE = "iprp"`, `MIN_ENCODED_BOX_SIZE = 8` (the canonical ISOBMFF plain-box header size; rejects malformed children at build time), `SCHEMA_VERSION = 1`.
  - **Tests shipped**: `IsobmffItemPropertiesTest` (19 tests covering: `IPCO_BOX_TYPE` / `IPRP_BOX_TYPE` / `SCHEMA_VERSION` / `MIN_ENCODED_BOX_SIZE` constant pins; `encodeIpcoBox` produces 8-byte header-only box for empty properties + documented byte layout for single `colr` property — `8-byte ipco header + 19-byte colr child = 27 bytes`; `encodeIpcoBox` preserves property order (output differs when `pasp` and `irot` are swapped); `encodeIpcoBox` rejects properties shorter than 8 bytes + accepts exactly-8-byte header-only boxes; `encodeIprpBox` produces documented `iprp + ipco + ipma` layout for canonical AVIF still; `encodeIprpBox` concatenates multiple `ipma` boxes after `ipco` in stable order; `encodeIprpBox` rejects empty `ipmaBoxes` per §8.11.14, rejects too-short `ipco` and too-short `ipma`; `Builder.add` returns 1-based indices in append order (`colrIdx=1, pixiIdx=2, irotIdx=3`); `Builder.size()` returns 0 before any `add`; `Builder.add` rejects too-short boxes; `Builder` defensively copies (mutating the source buffer after `add` does not affect the built `ipco`); `Builder.build` matches `encodeIpcoBox` of the same properties; **end-to-end canonical AVIF still test** that builds the `ipco` Builder for `colr`+`pixi`+`pasp`, calls `ItemPropertyAssociation.encodeBox` with the indices, wraps in `iprp`, and decodes the `ipma` payload back to verify the property indices round-trip cleanly with `[colrIdx, pixiIdx, paspIdx]` and the right essential bits `[true, true, false]`).
  - **AVIF muxer status (UPDATED)**: with this round, the engine has every host-side primitive needed to mux a fully-tagged AVIF still — every payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) + the canonical box-envelope writer from Round 22 + the item-property association table from Round 23 + the property container & parent boxes from Round 24. The remaining host-side work is the top-level `meta` FullBox (which wraps `iprp` plus `iloc` / `iinf` / `pitm` / `iref`); after that, the muxer just feeds `mdat`-encoded image bytes through and the AVIF still file is complete.
  - **59 JUnit suites · 760 tests · 0 failures · 0 errors · 0 skipped** (up from 58 / 741). 19 new tests in `IsobmffItemPropertiesTest`. APK 33408 KB (+3 KB).

- **AVIF Item Property Association (`ipma`) round (BUILD_PLAN §7, 1 new Kotlin module + 2 new public data classes + 40 new JUnit tests)** — pure-data formatter for the `ipma` FullBox per ISO/IEC 23008-12 §9.3.2 / ISO/IEC 14496-12 §8.11.14, the linkage layer that maps each item in a HEIF / AVIF file to the list of property indices in `ipco` that apply to that item. Without `ipma`, every property box from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) lands in the file but no decoder associates them with any image item — `ipma` is the bridge between the property catalog (`ipco`) and the actual items. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC item property association table":
  - `ItemPropertyAssociation.Association(propertyIndex: Int, essential: Boolean)` — pure-data carrier for a single `(essential, propertyIndex)` pair. `propertyIndex` is **1-based** per spec — index `0` is reserved to mean "no property" and would silently corrupt the bitstream; `init` rejects `< 1` and `> MAX_LARGE_PROPERTY_INDEX (32767)`. `essential` per ISO/IEC 14496-12 §8.11.14.3: when set, the property is required for correct rendering and a decoder that cannot understand it MUST refuse to display the item — so mandatory metadata like `colr` and `pixi` should be `essential = true`; optional / informational metadata like `irot` / `imir` is `essential = false` per AVIF spec §6.5.10 / §6.5.12.
  - `ItemPropertyAssociation.Entry(itemId: Long, associations: List<Association>)` — one `(itemId, [associations...])` row of the `ipma` table. `itemId` corresponds to a `iloc` / `iinf` item ID elsewhere in the `meta` box; an AVIF still typically has two items (the primary image item plus its `Exif` metadata item). `init` validates `0 <= itemId <= MAX_LARGE_ITEM_ID (4_294_967_295)`, `associations.isNotEmpty()` (an item with zero properties does not need an `ipma` entry), and `associations.size <= MAX_ASSOCIATION_COUNT (255)` — the spec stores it in a single byte.
  - `ItemPropertyAssociation.chooseVersionAndFlags(entries)` — picks the minimum `(version, flags)` combination that can encode every entry. `version = 0` is selected if every itemId fits in 16 bits; otherwise `version = 1`. `flags = 0` is selected if every property index fits in 7 bits; otherwise `flags = FLAG_LARGE_PROPERTY_INDEX`. For an AVIF still with `<=` 65535 items and `<=` 127 properties in `ipco`, this returns `(0, 0)` — the most compact form per HEIF best practice (saves 2 bytes per entry on item-ID width and 1 byte per association on property-index width).
  - `ItemPropertyAssociation.encodePayload(entries, version, flags): ByteArray` — emits ONLY the FullBox payload (the bytes after the 4-byte version+flags slot); the caller wraps with `IsobmffBox.encodeFullBox("ipma", version, flags, payload)`. Wire format per §8.11.14: `uint32_be entry_count` + per-entry `(uint16/uint32 itemId, uint8 association_count, association_count × (1-byte 7-bit-index or 2-byte 15-bit-index packed with the essential bit in the MSB))`. Validates that every itemId / propertyIndex fits the chosen encoding and throws `IllegalArgumentException` otherwise. `decodePayload(bytes, version, flags)` is the inverse; throws `IllegalArgumentException` on under-4-byte input, truncated `item_ID`, truncated `association_count`, and truncated associations.
  - `ItemPropertyAssociation.encodeBox(entries): ByteArray` — convenience that picks the most compact `(version, flags)` via `chooseVersionAndFlags`, calls `encodePayload`, and wraps with `IsobmffBox.encodeFullBox(BOX_TYPE = "ipma", version, flags, payload)` so the muxer gets a complete, ready-to-write `ipma` box (header + payload) in one call.
  - **Pinned constants**: `BOX_TYPE = "ipma"`, `SCHEMA_VERSION = 1`, `FLAG_LARGE_PROPERTY_INDEX = 0x000001`, `MAX_SMALL_PROPERTY_INDEX = 0x7F` (127), `MAX_LARGE_PROPERTY_INDEX = 0x7FFF` (32767), `MAX_SMALL_ITEM_ID = 0xFFFF` (65535), `MAX_LARGE_ITEM_ID = 0xFFFFFFFF`, `MAX_ASSOCIATION_COUNT = 0xFF` (255).
  - **Tests shipped**: `ItemPropertyAssociationTest` (40 tests covering: `BOX_TYPE` and constant pins; `Association` propertyIndex bounds (`< 1`, `> MAX_LARGE_PROPERTY_INDEX`, boundary 1, boundary 32767); `Entry` itemId bounds (`< 0`, `> MAX_LARGE_ITEM_ID`, empty associations, > 255 associations, boundaries 0 and `MAX_LARGE_ITEM_ID`); `chooseVersionAndFlags` returns `(0, 0)` for canonical AVIF still, `(1, 0)` for itemId > 65535, `(0, 1)` for propertyIndex > 127, `(1, 1)` for both, `(0, 0)` for empty entries; byte-layout pins for `entry_count = 0`, single essential prop=1 (`0x81`), single non-essential prop=1 (`0x01`), max small prop=127 essential (`0xFF`), 3-association canonical AVIF still (`0x81 0x82 0x03`), v=1 4-byte itemId big-endian (`0x12345678`), v=0 f=1 2-byte association big-endian (`0x80 0xC8`); `encodePayload` rejects invalid version, out-of-range flags, itemId overflow, propertyIndex overflow; encode → decode round-trip for canonical AVIF still, v=1 large itemId, f=1 large property index (incl. boundary `MAX_LARGE_PROPERTY_INDEX` essential and non-essential), multiple-entries case; `decodePayload` throws on under-4-byte input + truncated item_ID + truncated association_count + truncated association; `encodeBox` produces 20-byte FullBox envelope for canonical AVIF still with `(size=20, type='ipma', version=0, flags=0, payload=8 bytes)`, auto-promotes version=1 when itemId > 65535, auto-promotes flags=1 when propertyIndex > 127; `Entry` and `Association` equality is by content not reference).
  - **58 JUnit suites · 741 tests · 0 failures · 0 errors · 0 skipped** (up from 57 / 701). 40 new tests in `ItemPropertyAssociationTest`. APK 33405 KB (+11 KB).
  - **AVIF muxer status**: with this round, the engine has every host-side primitive needed to mux a fully-tagged AVIF still — every payload from Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) + the canonical box-envelope writer from Round 22 + the item-property association table from Round 23. The remaining work is the actual orchestration (`MetaBox.kt` for the `meta` FullBox + `IpcoBox.kt` for the `ipco` container + `IprpBox.kt` for the `iprp` parent), which can land any time after Phase 1's image encoder ships.

- **ISOBMFF box envelope writer round (BUILD_PLAN §7, 1 new Kotlin module + 23 new JUnit tests)** — the foundational layer that finally lets the engine *mux* the AVIF metadata payloads built in Rounds 17-20 (`colr`, `mdcv`, `clli`, `pasp`, `clap`, `irot`, `imir`, `pixi`) into a real ISOBMFF bitstream. Until this round, every payload formatter shipped *just* the inner bytes (the spec's "BoxBody"); `IsobmffBox` now wraps any payload in a canonical ISO/IEC 14496-12 §4.2 box envelope so the muxer can stop hand-rolling box headers. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC / JXL bitstream container — pure-data box envelope writer":
  - `IsobmffBox.kt` — pure-data `object` exposing four formatters and one composer. `boxType(type: String): ByteArray` validates the 4-character ASCII restriction (rejects empty / wrong-length / non-ASCII / non-printable types with `IllegalArgumentException` so a typo never lands silently in the bitstream). `writeBox(out, type, payload)` and `encodeBox(type, payload): ByteArray` produce the canonical `(uint32_be size, uint32_be type, payload)` envelope; when the total `8 + payload.size` exceeds `0xFFFFFFFFL`, the writer automatically promotes to the spec's `large_size` escape form (`size=1, type, uint64_be largesize, payload`) per §4.2.2 BoxHeader. `writeFullBox(out, type, version, flags, payload)` and `encodeFullBox(type, version, flags, payload): ByteArray` produce the `FullBox` flavor (`size, type, uint8 version, uint24_be flags, payload`); `version` validated to `[0, 255]` and `flags` to `[0, 0xFFFFFF]`. `concatPayloads(payloads)` is a tiny helper that flattens a list of child boxes into a single byte buffer the muxer can hand to `encodeBox` for a parent container box (e.g. `meta`, `iprp`, `ipco`). Pinned constants: `SCHEMA_VERSION = 1`, `PLAIN_HEADER_SIZE = 8`, `LARGE_HEADER_SIZE = 16`, `FULLBOX_VERSION_FLAGS_SIZE = 4`, `LARGE_SIZE_THRESHOLD = 0xFFFFFFFFL`. The encoder defensively copies the payload before writing (so the muxer can mutate the source buffer afterward without corrupting the encoded box) AND defensively copies on decode read paths.
  - **Tests shipped**: `IsobmffBoxTest` (23 tests covering: `boxType` accepts canonical 4-char ASCII like `"ftyp"` / `"colr"` / `"meta"`, rejects empty / 3-char / 5-char / non-ASCII / non-printable types; `encodeBox("colr", emptyPayload)` produces 8 bytes (`0 0 0 8 c o l r`); `encodeBox` integrates cleanly with `AvifColrPayload.encodeNclxPayload(WorkingSpace.SRGB.cicp)` to produce the documented 19-byte `colr` envelope (`0 0 0 19 c o l r ` + 11-byte sRGB CICP); `writeBox` and `encodeBox` produce identical output; `encodeFullBox("ipma", version=0, flags=0, payload)` produces the documented `(size, type, 00 00 00 00, payload)` shape; `version=255` and `flags=0xFFFFFF` encode exactly; `version=-1` / `version=256` / `flags=-1` / `flags=0x1000000` rejected; `concatPayloads` flattens an empty list to a 0-byte buffer, a single-element list to a copy of the single payload, and a multi-element list (`colr` envelope + `pasp` envelope) into the documented byte sequence; header-size and large-size threshold constants pinned; defensive payload copy verified by mutating the input array after `encodeBox` returns and confirming the encoded buffer is unchanged; schema version pin).
  - **57 JUnit suites · 701 tests · 0 failures · 0 errors · 0 skipped** (up from 56 / 678). 23 new tests in `IsobmffBoxTest`. APK 33394 KB (+4 KB).
  - **AVIF muxer status**: with this round, the engine has *every* host-side primitive needed to mux a fully-tagged AVIF bitstream — every payload from Rounds 17-20 plus the canonical box-envelope writer. The remaining work is the actual orchestration (`MetaBox.kt` + `IpcoBox.kt` + `IprpBox.kt` + the `ItemPropertyAssociation` linkage) which can land any time after Phase 1's image encoder ships.

### Fixed

- **Round 21 — Root Only drawer device-validation bug fixes (2 device-found UX bugs + 2 new pure-data tests + on-device re-screenshot)** — first round with the OnePlus 13 reconnected over ADB after the long device-unavailable streak (Rounds 14-20). Rebuilt + reinstalled the latest debug APK on `adb 8bf09993` and ran the deferred RoundSettingsScreen V&V smoke from Round 14. Two real UX bugs surfaced on the screenshot that JVM tests could not have caught alone, both fixed in the same round with new pure-data tests so a future regression cannot reintroduce them:
  - **Bug A — "Grant Su" button enabled in wrong states**: my Round 14 cut had `enabled = !pending && state != RootCapability.RootState.Granted`, which kept the button enabled in `Unknown`, `NotAvailable`, AND `Denied`. On a stock device (state `NotAvailable`, the screenshot case) the button was a brightly-colored PhotoOrange CTA that, if tapped, would just attempt to fork `su` and silently fail with `Exception: su: not found`. Fix: added `RootState.canRequestGrant` (pure-data) which is true ONLY for `AvailableNotGranted` (the canonical "we found SU paths, escalate now" state) and `Denied` (allow retry after a misclick on the SU manager dialog), and false for the three other states. Wired `RootSettingsScreen` to the new accessor: `Button(enabled = !pending && state.canRequestGrant, ...)`. On the re-validation screenshot the button correctly renders greyed out alongside an enabled "Re-probe" button — exactly the UX the BUILD_PLAN §9 policy ("never silently shells out to `su`") requires.
  - **Bug B — Duplicate fallback line on every disabled feature row**: my Round 14 cut rendered both a clean `Fallback: <text>` line AND the canonical `Requires root. Fallback: <text>` `disabledReason` line on every disabled row, so a stock-device user saw the same fallback summary twice on every single one of the 8 root-only feature rows. Fix: dropped the redundant `disabledReason` line from `RootFeatureRow` — the `DISABLED` chip + `Why:` line + single `Fallback:` line convey everything the user needs. As a side benefit the per-row vertical density tightened up enough that 3 features fit above the fold on a 1080x2400 screen instead of 2.
  - **Tests shipped (Round 21)**: `RootCapabilityTest` adds 2 new pure-data tests (the truth-table pin for `state.canRequestGrant` across all 5 states + a forward-compat sanity check that any future state at least returns a Boolean from the accessor without throwing). The existing 18 RootCapability tests still pass; the existing 18 RootCapabilityProbe tests still pass.
  - **Device validation evidence (Round 21)**: `hfr-runs/rootsettings/rootsettings_20260508_171250.png` (pre-fix; Grant Su button orange-filled despite no SU on device, duplicate `Requires root. Fallback:` lines per row) → `hfr-runs/rootsettings/rootsettings_20260508_171622.png` (post-fix; Grant Su correctly greyed out, single `Fallback:` line per row, 3 features visible above the fold). `hfr-runs/rootsettings/probe_smoke_20260508_171700.png` smoke probe re-run (109,285-char probe report regenerated, no crash, no FATAL EXCEPTION) confirms no regressions from the 7-round metadata-bundle build (Rounds 14-20). `hfr-runs/deep_caps_20260508_171842.json` (161 KB, valid JSON, includes the Round-11 `lensInfo` block for all 5 cameras) confirms the deep-caps probe pipeline is still healthy on hardware.
  - **57 JUnit suites · 677 tests · 0 failures · 0 errors · 0 skipped** (up from 56 / 675). 2 new tests in `RootCapabilityTest`. APK 33390 KB (no size change above noise).
  - **Closes**: BUILD_PLAN §9 "V&V — RootSettingsScreen renders on stock device" + "RootCapabilityProbe.probeStatic returns NotAvailable on stock device" (both now `[x]` after on-device validation; the active-probe-on-rooted-device row is still open until a Magisk / KernelSU device joins the test fleet).

### Added

- **AVIF auxiliary boxes (`irot` / `imir` / `pixi`) round (BUILD_PLAN §7, 1 new Kotlin module + 1 new data class + 2 new enums + 24 new JUnit tests)** — pure-data byte-layout formatters for the three small AVIF auxiliary metadata boxes that complete the AVIF metadata bundle alongside `colr` (Round 17), `mdcv`/`clli` (Round 18), and `pasp`/`clap` (Round 19): image rotation (`irot`, ISO/IEC 23008-12 §6.5.10), image mirror (`imir`, ISO/IEC 23008-12 §6.5.12), and pixel information (`pixi`, ISO/IEC 23008-12 §6.5.6). With these, the engine has every host-side AVIF metadata serializer it needs to mux a fully-tagged AVIF bitstream the moment Phase 1 lands. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC sample-aspect + clean-aperture metadata" plus its sibling rotation / mirror / bit-depth tagging:
  - `AvifAuxiliaryBoxes.Rotation` — enum with `Rot0` / `Rot90` / `Rot180` / `Rot270`. Carries both `degrees` (0/90/180/270, clockwise) and `wireValue` (0..3, the spec's `angle / 90` form). `Rotation.fromDegrees(int)` normalizes any input modulo 360 (so `-90` → `Rot270`, `450` → `Rot90`); rejects non-90-multiple inputs. `Rotation.fromWireValue(int)` rejects values outside `[0, 3]`. Used to express sensor orientation (e.g. portrait capture from a landscape-natural sensor) without re-encoding pixels.
  - `AvifAuxiliaryBoxes.MirrorAxis` — enum with `Horizontal` (top-bottom flip, wire value 0) and `Vertical` (left-right flip, wire value 1). Names are spec-aligned per ISO/IEC 23008-12 — the spec's `axis_horizontal` is the vertical flip a layperson would expect, but we keep spec-aligned names so callers reading the spec directly aren't confused. Used for selfie-mode captures from front-facing sensors.
  - `AvifAuxiliaryBoxes.PixiPayload(bitDepths: IntArray)` — data class carrying the per-channel bit depth list. `init` enforces `bitDepths.size in [1, 255]` and every depth in `[1, 255]`. Custom `equals` / `hashCode` compare arrays by content (Kotlin's data class default would compare by reference). Five pre-computed presets: `MONO_8` (1 ch, `8`), `RGB_8` (3 ch, `8/8/8`), `RGBA_8` (4 ch, `8/8/8/8`), `RGB_10` (3 ch, `10/10/10` — HDR10 / Rec.2020 PQ default), `RGB_12` (3 ch, `12/12/12` — libjxl default for high-bit RAW-derived stills).
  - `AvifAuxiliaryBoxes.encodeIrot(rotation)` produces exactly 1 byte (`wireValue` in the low 2 bits); `decodeIrot(bytes)` is the inverse and silently masks off bits 7..2 for tolerance with pre-spec encoders. `encodeImir(axis)` produces exactly 1 byte (`wireValue` in bit 0); `decodeImir(bytes)` masks off bits 7..1. `encodePixi(pixi)` produces `1 + bitDepths.size` bytes (channel count followed by per-channel depths); `decodePixi(bytes)` validates the declared channel count matches the payload length. All three decoders throw `IllegalArgumentException` on wrong byte lengths. Pinned constants: `IROT_PAYLOAD_LENGTH = 1`, `IMIR_PAYLOAD_LENGTH = 1`, `SCHEMA_VERSION = 1`.
  - **Tests shipped**: `AvifAuxiliaryBoxesTest` (24 tests covering: `Rotation` enum degrees + wireValue mapping, `Rotation.fromDegrees` normalizes negative + over-360 angles + rejects non-90-multiple, `Rotation.fromWireValue` rejects out-of-range, `encodeIrot` produces 1 byte with wireValue, `encode → decode` round-trips every rotation, `decodeIrot` tolerates non-zero reserved bits, `decodeIrot` rejects wrong lengths; `MirrorAxis` wire values pinned, `MirrorAxis.fromWireValue` rejects out-of-range, `encodeImir → decodeImir` round-trips both axes, `decodeImir` tolerates non-zero reserved bits + rejects wrong lengths; `PixiPayload` presets cover MONO/RGB/RGBA/10-bit/12-bit, rejects empty / out-of-range bit depths, `equals` and `hashCode` compare arrays by content, `encodePixi` writes `count, depths...`, RGBA-8 produces `[4, 8, 8, 8, 8]`, RGB-10 produces `[3, 10, 10, 10]`, encode→decode round-trips every preset, `decodePixi` rejects empty + truncated + zero-channel + max-255-channel payloads, schema version pin).
  - **56 JUnit suites · 675 tests · 0 failures · 0 errors · 0 skipped** (up from 55 / 651). 24 new tests in `AvifAuxiliaryBoxesTest`. APK 33390 KB (+11 KB).
  - **AVIF metadata bundle status**: with this round, every spec-required AVIF bitstream-tag box has a pure-data, JVM-tested formatter shipped: `colr` (CICP nclx + ICC prof) from Round 17 + `mdcv` / `clli` (HDR static metadata) from Round 18 + `pasp` / `clap` (pixel aspect + clean aperture) from Round 19 + `irot` / `imir` / `pixi` (rotation + mirror + pixel info) from Round 20. The capture engine's AVIF muxer can now pull every payload out of `WorkingSpace` / `ImagingProfile` and emit a fully-conformant AVIF the moment Phase 1 lands.

- **ISOBMFF `pasp` + `clap` formatter round (BUILD_PLAN §7, 1 new Kotlin module + 2 new public data classes + 20 new JUnit tests)** — pure-data byte-layout formatters for the two ancillary ISOBMFF "transformative" boxes that ride alongside the `colr` (CICP), `mdcv`, and `clli` boxes in AVIF / HEVC samples: pixel aspect ratio (`pasp`, ISO/IEC 14496-12 §12.1.4.3) and clean aperture (`clap`, ISO/IEC 14496-12 §12.1.4.3). Both boxes are mandatory in libavif's strict mode whenever the engine wants to cleanly express a non-square pixel grid or a content rectangle that isn't aligned to the codec's coded-image rectangle. Closes the host-side half of BUILD_PLAN §7 "AVIF / HEVC sample-aspect + clean-aperture metadata":
  - `IsobmffSampleAspect.PaspPayload(hSpacing, vSpacing)` — data class holding the pixel aspect ratio as the integer ratio `hSpacing : vSpacing`. `init` enforces both fields are `>= 1`. `PaspPayload.SQUARE` (`1:1`) is the pre-computed default for every modern phone / DSLR / mirrorless camera (square-pixel sensors).
  - `IsobmffSampleAspect.ClapPayload(widthN, widthD, heightN, heightD, horizOffN, horizOffD, vertOffN, vertOffD)` — data class holding the clean-aperture rectangle as four rationals (width, height, horizontal offset, vertical offset). Width / height numerators + all four denominators must be `>= 1`; offset numerators are signed (the spec's center-form coordinates can be negative). `ClapPayload.centeredCropOf(codedWidth, codedHeight, cropX, cropY, cropW, cropH)` is a convenience constructor that takes a top-left-anchored pixel rectangle and rephrases it into the spec's center-form (centered at `(codedCenter + offN/offD, ...)`); validates that the requested crop lies entirely within the coded image.
  - `IsobmffSampleAspect.encodePasp(pasp)` produces exactly 8 big-endian bytes (`uint32_be hSpacing` + `uint32_be vSpacing`); `decodePasp(bytes)` is the inverse. `encodeClap(clap)` produces exactly 32 big-endian bytes (`uint32_be widthN/widthD/heightN/heightD` + `int32_be horizOffN` + `uint32_be horizOffD` + `int32_be vertOffN` + `uint32_be vertOffD`); `decodeClap(bytes)` is the inverse. Both decoders throw `IllegalArgumentException` on wrong byte lengths and on `uint32 > Int.MAX_VALUE` overflow (we cap to `[0, 2^31 - 1]` because Kotlin/JVM stores the round-trip values as signed `Int`s). Pinned `PASP_PAYLOAD_LENGTH = 8`, `CLAP_PAYLOAD_LENGTH = 32`, `SCHEMA_VERSION = 1`. The capture engine never hand-rolls the bit-packing — it pulls a `PaspPayload.SQUARE` (or a custom ratio for anamorphic captures) and a `ClapPayload.centeredCropOf(...)` from the imaging profile and runs them through `IsobmffSampleAspect.encode*(...)`.
  - **Tests shipped**: `IsobmffSampleAspectTest` (20 tests covering: payload-length constants pinned, `PaspPayload.SQUARE` is `1:1`, `PaspPayload` rejects `< 1` spacings, `encodePasp(40, 33)` produces the documented `0 0 0 40 / 0 0 0 33` byte sequence, `encodePasp → decodePasp` round-trip, decode throws on wrong lengths; `ClapPayload` rejects zero / negative widthD / heightD / horizOffD / vertOffD / widthN / heightN, `encodeClap` produces exactly 32 bytes, signed `horizOffN = -1` encodes as `0xFF 0xFF 0xFF 0xFF` (two's complement), high-byte width / height fields encode in big-endian order, `encodeClap → decodeClap` round-trip including negative offset values, decode throws on wrong lengths; `centeredCropOf` returns identity for full-image crop, computes a centered `1920x1080` crop within `4096x2160` coded image with zero offsets, computes correct `-1120 / -480` half-pixel offsets for a left-edge `800x600` crop within `1920x1080`, rejects crops overflowing coded dimensions, rejects negative or zero arguments; schema version pinned).
  - **55 JUnit suites · 651 tests · 0 failures · 0 errors · 0 skipped** (up from 54 / 631). 20 new tests in `IsobmffSampleAspectTest`. APK 33379 KB (+12 KB).

- **HDR static metadata (`mdcv` + `clli`) round (BUILD_PLAN §7, 1 new Kotlin module + 2 new public data classes + 19 new JUnit tests)** — pure-data byte-layout formatter for the two HDR static-metadata blobs that ride alongside the `colr` (CICP) box in AVIF / HEVC HDR streams: SMPTE ST 2086 mastering-display colour volume (ISOBMFF `mdcv`, HEVC SEI 137 / 144) and CTA-861.3 content light-level information (ISOBMFF `clli`, HEVC SEI 144 § "Content light level information"). Without these, a downstream tone mapper is blind to whether the master was graded for a 1000-nit reference monitor or a 4000-nit one. Closes the host-side half of BUILD_PLAN §7 "HDR encoding side car / SEI metadata":
  - `MasteringDisplayMetadata` — data class carrying RGB primaries (chromaticities, in CIE 1931 `[0, 1]` space), whitepoint chromaticity, and min / max luminance (cd/m^2). `init` validates chromaticity bounds (same wide-gamut tolerances as `ColorSpaceMatrix.Chromaticity`), enforces `y > 0` (chromaticities divide by `y`), and rejects `maxLuminance < minLuminance` or negative luminance. `primariesGbr()` returns the primaries in the spec-mandated G / B / R order so the wire-format conversion is straight-forward (this is a notorious gotcha in ITU-T H.265 §D.2.27 — the bitstream stores G, B, R, NOT R, G, B). Two pre-computed presets: `REC2020_1000_NITS` (HDR10 default, Rec.2020 primaries graded for a 1000-nit monitor) and `DISPLAY_P3_1000_NITS` (Apple HDR Photo grade target, Display P3 primaries at 1000 nits).
  - `ContentLightLevel` — data class carrying `MaxCLL` (peak luminance of any single pixel across the sequence, in cd/m^2) and `MaxFALL` (max frame-average light level). Both fields validated against the 16-bit unsigned wire-format range `[0, 65535]`.
  - `HdrStaticMetadata` — pure-data formatter `object`. `encodeMdcvPayload(meta)` produces exactly 24 bytes per ITU-T H.265 §D.2.27: 6 × `uint16_be` chromaticities (G / B / R order, scaled by 50_000), 2 × `uint16_be` whitepoint chromaticities (scaled by 50_000), 1 × `uint32_be` max luminance (scaled by 10_000), 1 × `uint32_be` min luminance (scaled by 10_000). `decodeMdcvPayload(bytes)` is the inverse. `encodeClliPayload(clli)` produces exactly 4 big-endian bytes (`uint16_be` `MaxCLL` + `uint16_be` `MaxFALL`); `decodeClliPayload(bytes)` is the inverse. Both encoders throw `IllegalArgumentException` on wrong byte lengths. The capture engine never hand-rolls the bit-packing — it pulls a `MasteringDisplayMetadata` / `ContentLightLevel` straight from the imaging profile and runs it through `HdrStaticMetadata.encode*Payload(...)`. Pinned constants: `MDCV_PAYLOAD_LENGTH = 24`, `CLLI_PAYLOAD_LENGTH = 4`, `CHROMATICITY_SCALE = 50000`, `LUMINANCE_SCALE = 10000`. Internal helpers `scaleChromaticity` / `scaleLuminance` round to the nearest integer and clamp to the unsigned wire-format range so the encoder is total over the float input domain.
  - **Tests shipped**: `HdrStaticMetadataTest` (19 tests covering: REC2020_1000_NITS / DISPLAY_P3_1000_NITS preset values match the published Rec.2020 / Display P3 + 1000-nit specs, `primariesGbr()` returns G / B / R in the right order, `MasteringDisplayMetadata` rejects out-of-range chromaticities / `y <= 0` / negative luminance / `max < min`, `encodeMdcvPayload` produces exactly 24 bytes, the Rec.2020 byte sequence has G first (`xG=8500, yG=39850`) then B (`xB=6550, yB=2300`) then R (`xR=35400, yR=14600`) at the right scale (50_000 chromaticity / 10_000 luminance), whitepoint and luminance wire-format scaling are correct (`xW=15635 / yW=16450 / maxLum=10_000_000 / minLum=50`), `encode → decode` round-trips Rec.2020 1000 nits within 2-ULP chromaticity tolerance and 1e-3 luminance tolerance, decode throws on wrong lengths, MDCV constants are pinned, `ContentLightLevel` rejects out-of-range maxCll / maxFall, `encodeClliPayload` produces exactly 4 BE bytes, `encodeClliPayload → decodeClliPayload` round-trips, decode throws on wrong lengths, max-uint16 maxCll / maxFall encode as `0xFF 0xFF 0xFF 0xFF`, CLLI constants are pinned, `scaleChromaticity` / `scaleLuminance` round to nearest and clamp to the uint16 / uint32 ranges, `unscaleChromaticity` inverts within ULP).
  - **54 JUnit suites · 631 tests · 0 failures · 0 errors · 0 skipped** (up from 53 / 612). 19 new tests in `HdrStaticMetadataTest`. APK 33367 KB (+13 KB).

- **AVIF `colr` payload formatter round (BUILD_PLAN §7, 1 new Kotlin module + 19 new JUnit tests)** — pure-data byte-layout formatter for the AVIF (`avif`/`avis`) `colr` box's two shipped colour-type flavors (`nclx` and `prof`), so the capture engine's AVIF muxer has an immediately-usable serializer the moment Phase 1 lands. Closes the host-side half of BUILD_PLAN §7 "Bitstream tagging — AVIF / HEVC / JXL `colr` box" by consuming `Cicp` (Round 16) end-to-end:
  - `AvifColrPayload.kt` — exposes `encodeNclxPayload(cicp: Cicp): ByteArray` (returns exactly 11 bytes per ISO/IEC 14496-12 §8.5.4 `nclx` flavor: 4-byte ASCII colour-type marker + 16-bit big-endian `colour_primaries` / `transfer_characteristics` / `matrix_coefficients` + 1-byte flags where bit 7 is `full_range_flag` and bits 6..0 are reserved zero), `decodeNclxPayload(bytes): Cicp?` (returns null if the marker isn't `nclx`; throws `IllegalArgumentException` for wrong-length payloads; silently drops the reserved bits in the flags byte for tolerance with pre-spec encoders), `encodeProfPayload(iccProfileBytes): ByteArray` (4-byte `prof` ASCII marker + the raw ICC profile blob; rejects empty profiles), `decodeProfPayload(bytes): ByteArray?` (returns null when the marker isn't `prof`; throws for payloads shorter than the 4-byte marker). Total payload (without the BMFF size + box-type header — that's the muxer's job to wrap) is exactly 11 bytes for `nclx`; pinned via the `NCLX_PAYLOAD_LENGTH` constant. The `NCLX_TYPE` and `PROF_TYPE` constants are pre-cooked ASCII byte arrays so the muxer never has to re-encode the marker.
  - **Tests shipped**: `AvifColrPayloadTest` (19 tests covering: every constant has the documented ASCII shape, every `nclx` payload is exactly 11 bytes long and starts with the `nclx` marker, sRGB CICP encodes to the documented byte sequence (`cp=1 / tc=13 / mc=0 / full=0x80`), Rec.2020 PQ CICP encodes to the documented byte sequence (`cp=9 / tc=16 / mc=9 / full=0x80`), `full_range_flag = false` encodes as `0x00`, high-byte CICP fields are emitted as expected (`cp=255 → 0x00 0xFF`), every `WorkingSpace` preset round-trips through `encode → decode` exactly, decode returns null on a non-`nclx` marker, decode throws on wrong lengths, decode silently drops reserved bits in the flags byte, ICC profile bytes round-trip through `encodeProfPayload → decodeProfPayload`, empty ICC profiles are rejected, decode-prof returns null on a wrong marker, decode-prof throws on lengths shorter than 4, every `WorkingSpace.ALL` preset produces a valid 11-byte `nclx` payload, schema version is pinned).
  - **One assertion fix during gate run**: my first cut of the sRGB byte-layout test asserted `mc lo = 0x01` ("BT.709 YCbCr"); the actual `WorkingSpace.SRGB.cicp` ships `matrixCoefficients = 0` (identity / RGB-direct, the correct pick for an sRGB-tagged stream that's stored RGB-direct in the bitstream rather than YCbCr). Updated the assertion to `0x00` and clarified the comment.
  - **53 JUnit suites · 612 tests · 0 failures · 0 errors · 0 skipped** (up from 52 / 593). 19 new tests in `AvifColrPayloadTest`. APK 33354 KB (no size change above noise).

- **Working-space presets + CICP codes round (BUILD_PLAN §7, 1 new Kotlin module + 1 new public data class + 27 new JUnit tests)** — ties together `HdrCurves` (Round 15) and `ColorSpaceMatrix` (Round 15) into a single named-preset API so the capture engine can pass a complete RGB working space (primaries + transfer function + bitstream tagging codes) around as a single value:
  - `WorkingSpace.kt` — `sealed class WorkingSpace` with 6 shipped presets (`SRGB`, `REC709_SDR`, `REC2020_PQ`, `REC2020_HLG`, `DCI_P3`, `ACES_AP1_LINEAR`). Each preset exposes a stable canonical id (lowercase, no spaces; suitable for JSON keys), a human-readable display name, the underlying `ColorSpaceMatrix.Primaries`, `toLinear` (EOTF) and `fromLinear` (OETF) function references, and an `ITU-T H.273 CICP` triple. The companion's `ALL` list is `by lazy` to dodge the sealed-class + nested-objects + companion-list circular static-init dependency. `byId(id)` resolves a preset by canonical id and returns null for unknown ids.
  - `Cicp` — a top-level data class carrying the four ITU-T H.273 fields (`colour_primaries` / `transfer_characteristics` / `matrix_coefficients` + `video_full_range_flag`) downstream containers (AVIF, AV1, HEVC, VVC, MP4, WebP, JXL) need to tag the bitstream so any decoder applies the inverse EOTF and gamut conversion correctly. `init` validates each field is in `[0, 255]`. `describe()` produces a human-readable diagnostic string (e.g. `"BT.709 / sRGB / RGB-identity / full-range"`) for the diagnostics dump.
  - **CICP triples shipped**: sRGB `(1, 13, 0, full)`, Rec.709 SDR `(1, 1, 1, narrow)`, Rec.2020 PQ `(9, 16, 9, full)`, Rec.2020 HLG `(9, 18, 9, full)`, Display P3 `(12, 13, 0, full)`, ACES AP1 linear `(2, 8, 0, full)` (the "unspecified primaries" + "linear transfer" sentinels because no canonical CICP entry exists for AP1 + linear; the engine always converts to one of the displayable spaces above before encoding).
  - **Convenience methods**: `linearToXyz(rgb)` returns the linear-RGB → CIE XYZ vector in the working space's native whitepoint via `ColorSpaceMatrix.primariesToXyz`. `toXyzD65(linearRgb)` Bradford-adapts the result to D65 when the source whitepoint differs (notably ACES AP1 with its D60-ish whitepoint). Both reject wrong-length inputs with `IllegalArgumentException`.
  - **Tests shipped**: `WorkingSpaceTest` (27 tests covering: every preset is in `ALL` in stable order, every preset has a unique id and non-blank displayName, `byId` resolves every shipped preset and returns null for unknown ids, every preset's CICP triple matches its declared spec, every preset's primaries match the corresponding `ColorSpaceMatrix.*_PRIMARIES`, transfer-function round-trip parity for every preset on a 33-point sweep, ACES AP1 linear is identity on both directions, `linearToXyz` on white returns the working-space whitepoint XYZ, `toXyzD65` on a D65 working space matches `linearToXyz` exactly, `toXyzD65` on ACES AP1 differs noticeably from `linearToXyz`, wrong-length input rejection, `Cicp` constructor rejects out-of-range fields, `describe()` formats sRGB / Rec.2020 PQ / Rec.709 SDR correctly + falls back to literal codes for unknown values, every preset's CICP is constructible, schema version pin).
  - **Init-order fix during gate run**: my first cut declared `val ALL: List<WorkingSpace> = listOf(SRGB, REC709_SDR, ...)` as a non-lazy companion val. Kotlin's class init runs the companion `<clinit>` BEFORE the nested `object` declarations are initialized, so `ALL` resolved to a list of `null` references and every test that read `WorkingSpace.ALL` (or any method that traverses it like `byId`) crashed with `NullPointerException`. Switched to `val ALL: List<WorkingSpace> by lazy { listOf(...) }` so the list is built on first read (after every nested object is fully initialized).
  - **52 JUnit suites · 592 tests · 0 failures · 0 errors · 0 skipped** (up from 51 / 565). 27 new tests in `WorkingSpaceTest`. APK 33352 KB (+18 KB).

- **Color-science foundation round (BUILD_PLAN §7, 2 new Kotlin modules + 37 new JUnit tests)** — closes the foundation BUILD_PLAN §7 "Color management, calibration & LUT pipeline" needs to land HDR-aware encoding (10-bit AVIF Rec.2020, 12-bit JXL Rec.2020), color-managed LUT application, and the DNG `ColorMatrix1` / `ForwardMatrix1` derivation flow. Both modules are pure-data Kotlin (no Android imports) so they're JVM-testable and reusable from GLSL shader-generation paths:
  - `HdrCurves.kt` — sRGB (IEC 61966-2-1), Rec.709 (ITU-R BT.709-6), PQ (SMPTE ST 2084) and HLG (ITU-R BT.2100) EOTFs and OETFs. Every curve is a single-arg `Float -> Float` so the same primitives can be lifted across hot per-pixel loops, GLES shader generation (the GLSL form is one-to-one with the Kotlin form), and JVM unit tests. The math is all CIE / IEC / ITU-R / SMPTE standards data — no trademarked source. Constants `PQ_PEAK_NITS = 10000` and `HLG_PEAK_NITS = 1000` are pinned per the standards.
  - `ColorSpaceMatrix.kt` — sRGB / Rec.709 / Rec.2020 / DCI-P3 / ACES AP1 primaries + Bradford D65↔D50 chromatic adaptation. The `linear-RGB → CIE XYZ` matrices are computed on the fly from the chromaticities (Lindbloom's per-primary scaling recipe), so the constants table can be re-verified against any future spec errata without touching the matrix bodies. Standard CIE 1931 illuminants (D50 / D55 / D65 / D75 / A / F2) are exposed via `ColorSpaceMatrix.Illuminants`. Matrix utilities (`multiply`, `multiplyVec`, `invert3x3`) are also exposed for downstream callers; `invert3x3` rejects singular matrices with `IllegalArgumentException`. Above-spectral-locus primaries (notably ACES AP1 red at `(0.713, 0.293)`) are explicitly accepted; only `y > 0` is enforced (chromaticity divides by `y`).
  - **Tests shipped**: `HdrCurvesTest` (19 tests: every direction round-trips on a 257-point sweep, mid-gray / break-point / unity / zero anchors are exact to the published references, Rec.2100 PQ encoded reference at 100 cd/m² lands in `[0.50, 0.52]`, HLG break point at `1/12` linear maps to `0.5` encoded, every curve is monotonically non-decreasing on the unit interval). `ColorSpaceMatrixTest` (18 tests: every published matrix matches against the canonical Lindbloom / BT.2020 / RP 431-2 / ICC reference within 5e-4 ~ 5e-3, Bradford D65↔D50 round-trip identity, Bradford D65→D50 maps the D65 whitepoint to the D50 whitepoint, whitepoint of every working space lands on its declared illuminant, `invert3x3` × original = identity, singular-matrix rejection, chromaticity validation including the wide-gamut ACES AP1 case).
  - **Compile-fix during gate run**: my first cut declared `typealias Matrix3x3 = Array<FloatArray>` inside the `object ColorSpaceMatrix`, which Kotlin rejects with `Nested and local type aliases are not supported`. Removed the typealias and inlined `Array<FloatArray>` everywhere.
  - **Init-order fix during gate run**: `BRADFORD_M` was first declared at the bottom of the `object` and used by `BRADFORD_D65_TO_D50` / `BRADFORD_D50_TO_D65` near the top — Kotlin object init runs top-down, so the references resolved to `null` and the static init threw `NullPointerException`. Moved `BRADFORD_M` to BEFORE the matrices that consume it.
  - **Test-tolerance fix**: my Rec.709 break-point test used a tol of `1e-4f` on the high-side branch which evaluates to `~0.0813` vs the linear-side prediction of `0.081` — within `5e-4` but not `1e-4`. Switched to testing a value clearly below the break (`0.01f`) so the linear branch is unambiguous.
  - **Test-precondition fix**: my first cut of `Chromaticity` rejected `x + y > 1.0001` to mean "outside the spectral locus" — but ACES AP1 red is `(0.713, 0.293)` with sum 1.006, intentionally above the locus. Relaxed to only enforce that `y > 0` (otherwise the `y` divide explodes) and that values are within `[-0.5, 1.5]`.
  - **51 JUnit suites · 565 tests · 0 failures · 0 errors · 0 skipped** (up from 49 / 528). 37 new tests: 19 HdrCurves + 18 ColorSpaceMatrix.

- **Root-only capability + Root Only settings drawer round (BUILD_PLAN §9, 3 new Kotlin modules + 1 new Compose screen + new probe-home tile + new `--es pns_screen rootsettings` alias + 36 new JUnit tests)** — closes the host-side scaffolding for BUILD_PLAN §9 "Root-only enhancements (opt-in; every feature has a non-root fallback)" with a complete state machine + feature gate + drawer that always renders, even on stock devices, so the user can SEE what they would gain by rooting (and what fallbacks Point & Shoot uses to deliver that capability without root):
  - `RootCapability.kt` — pure-data state machine + feature catalog. The `RootState` enum has 5 states (`Unknown`, `NotAvailable`, `AvailableNotGranted`, `Granted`, `Denied`); only `Granted` returns `grantsPrivileged = true`. The `Feature` enum enumerates the 8 shipped root-only enhancements (vendor `setprop`, camera HAL recovery, CPU governor pin during HFR, thermal trip-point read, system-wide logcat ringbuffer, selective vendor-key probing, force-resolution override, backlight brightness read). `FeatureDescriptor` carries a non-blank `purpose` AND a non-blank `fallback` (validated at init time); `disabledReason` always starts with the canonical `"Requires root. Fallback: "` prefix so a single regex can extract the fallback summary from any disabled row. `FEATURE_DESCRIPTORS: Map<Feature, FeatureDescriptor>` is the catalog of every shipped feature; the unit tests assert it covers every enum entry.
  - `RootGate.kt` — pure-data feature gate (sister of `CapabilityGate`). `evaluate(state)` returns one `GateResult` per feature with `enabled` true ONLY when `state.grantsPrivileged` is true; otherwise the canonical `disabledReason` is surfaced. Stable enum-order list so the UI renders consistently. `requireFallbacks()` is a sanity check that every shipped feature has a non-blank purpose + fallback.
  - `RootCapabilityProbe.kt` — two-stage probe per BUILD_PLAN §9. **Stage 1** (`probeStatic()`): walks `CANONICAL_SU_PATHS` (`/system/xbin/su`, `/system/bin/su`, `/sbin/su`, `/system/sbin/su`, `/su/bin/su`, `/system/app/Superuser.apk`, `/data/adb/magisk`, `/data/adb/ksu`, `/data/adb/ksud`) using `java.io.File.exists()` only — **no process is forked**. Returns `NotAvailable` (no SU paths found) or `AvailableNotGranted` (at least one path exists). Safe to run from `Application.onCreate`; never triggers an SU prompt. **Stage 2** (`requestGrant(timeoutSeconds = 5)`): forks `Process.exec("su -c id")` with a 5-second timeout, reads stdout for `uid=0`, returns `Granted` or `Denied`. The block is wrapped in `runCatching` so a misbehaving SU manager cannot crash the calling thread. The static layer is tested via `collateExistence(pathExists: (String) -> Boolean)` which lets unit tests inject any predicate (no need for a real filesystem); the active layer's parsing contract is tested via `parseIdOutput(idOutput: String?)` which lets unit tests pin the `uid=0` detection without forking a process.
  - `RootSettingsScreen.kt` — Compose drawer that always renders. Top section: status banner (state.displayName), "Grant Su" button (enabled only when state is not Granted) + "Re-probe" button. Tip text explains that "Grant Su" runs `su -c id` exactly once and reads the result, with explicit consent of any SU manager prompt. Each feature row shows the display name + ENABLED/DISABLED chip + "Why: <purpose>" + "Fallback: <fallback>" + (when disabled) the canonical disabled-reason. Bottom: the full `CANONICAL_SU_PATHS` list rendered in low-emphasis text so the user can verify which paths Point & Shoot is checking. Reachable from probe home via the new "Root Only" tile and from ADB via `--es pns_screen rootsettings` (no `CAMERA` permission required).
  - `CameraCapabilitiesProbe.kt` — wired the new `SCREEN_ROOT_SETTINGS = "rootsettings"` constant into both launch effects (the early-launch path that does NOT require `CAMERA` permission AND the camera-permission-gated path), the screen-render switch, the `ProbeHomeContent` callback chain, and the new "Root Only" `OutlinedButton` on the Phase 4 / NDK-diag row.
  - **Tests shipped**: `RootCapabilityTest` (18 tests, all green: every Feature has a FEATURE_DESCRIPTOR with non-blank purpose + fallback + displayName, requireFallbacks succeeds on the shipped catalog, disabledReason always starts with the canonical `"Requires root. Fallback: "` prefix, RootState.grantsPrivileged is true ONLY for Granted, RootState.displayName is non-blank for every state, evaluate returns one entry per Feature in stable enum order, evaluate disables every feature for every non-Granted state, evaluate enables every feature when state is Granted, single-feature evaluate accessor matches, FeatureDescriptor rejects blank fields, RootState transitions match the documented state machine, schema version pin, every Feature in catalog). `RootCapabilityProbeTest` (18 tests, all green: CANONICAL_SU_PATHS shape + no duplicates + absolute paths, collateExistence returns NotAvailable when no SU paths exist, AvailableNotGranted when any single path matches across the documented set, ignores non-canonical paths like `/data/local/tmp/su`, parseIdOutput recognizes both `uid=0(root)` and bare `uid=0`, returns Denied for non-root uid / empty / null / SU-error stdout, ACTIVE_PROBE_TIMEOUT_SECONDS pin, probeStatic on the JVM unit-test classpath returns NotAvailable).
  - **BUILD_PLAN sync**: §9 "Root-only enhancements" section added with the policy ("100% of user-visible features work on a stock un-rooted device; root unlocks peak performance / quality / diagnostics ONLY"), settings layout (always-render drawer; "Grant Su" button gates active probe; never silently shells out to `su` on launch), 2-stage detection (static at cold start; active only on user opt-in), and 8 enumerated root-only enhancements each with its purpose + non-root fallback (vendor `setprop`, HAL recovery, CPU governor pin, thermal trip read, system-wide logcat, vendor-key probe, resolution override, backlight read). V&V gate row added (rendering on non-rooted device + active probe on rooted device + revoke handling).
  - **Device validation deferred**: the `[ADB]` rows in §9 V&V (Magisk consent dialog flow, revoke flow) need physical access to the OnePlus 13 (currently unavailable per the user's note); marked as `DEVICE-NEXT` in the round 14 todo list and will run as soon as the device is back.
  - **49 JUnit suites · 528 tests · 0 failures · 0 errors · 0 skipped** (up from 47 / 492). 36 new tests: 18 RootCapability + 18 RootCapabilityProbe. APK 33321 KB (+52 KB).

- **DNG LUT metadata + .spi3d import round (BUILD_PLAN §7, 2 new Kotlin modules + LUT validator + parser extension + 36 new JUnit tests)** — pure-data round that closes the host-side half of two Phase 4 §7 rows ahead of the Phase 1 capture engine:
  - `LutPipeline.parseSpi3d(text)` + `LutImportValidator` routing — Sony Pictures Imageworks `.spi3d` import path. The parser recognizes the canonical SPI 3D LUT shape (`SPILUT 1.0` header, `<inputDim> <outputDim>` line — only `3 3` accepted; 1D variants are explicitly routed away with a `1D variants belong in a separate .spi1d parser` message — then a `<sizeR> <sizeG> <sizeB>` line — only cubic shapes in `Lut3D.SUPPORTED_SIZES` accepted — and finally `size³` body rows of the form `<r_idx> <g_idx> <b_idx> <out_R> <out_G> <out_B>`); body order is recovered by index so cells can appear in any permutation; duplicate cells, missing cells, and out-of-range indices each surface as distinct `IllegalArgumentException` messages so the validator can map them onto the existing `FailureCategory` enum without growing it. `LutImportValidator.sniffFormat(text)` extends to `Format.Spi3d` (the `SPILUT` keyword is sniffed before the `.cube` / `.3dl` keywords); `categorize(...)` maps SPI-specific failure messages onto `FailureCategory.{MalformedHeader, OneDLut, UnsupportedSize, MalformedBody}` so the SAF toast UX stays uniform. **Unblocks** the AMPAS ACES rows + the Blender Filmic `desat65cube.spi3d` row in BUILD_PLAN §7: the user can now drop a `.spi3d` file straight into the import flow and the validator routes it correctly. Implementation detail locked here: the early `rows.size >= 3` (header + dim + size) check fires before the cubic / supported-size validation so a non-cubic shape gets the diagnostic `spi3d only supports cubic LUTs (got <Rx><Gx><B>)` message rather than `spi3d file too short ...`. 16 new tests in `Spi3dParserTest.kt`.
  - `DngLutMetadata.kt` — pure-data formatter for the active LUT identity that gets stamped into a DNG's `Software` (TIFF tag 305) and (optionally) `UniqueCameraModel` (DNG tag 50708) fields per BUILD_PLAN §7 "RAW (DNG): never baked in. The active LUT name + SHA256 are written into the DNG's `UniqueCameraModel` / `Software` tag chain". Two entry points: `formatSoftwareTag(appVersion, activeLut?)` returns `"Point & Shoot v<v>"` when no LUT is active and `"Point & Shoot v<v> / LUT=<name> SHA256=<64-hex>"` when a non-identity LUT is active — desktop tooling can extract the LUT identity with the exposed `MARKER_REGEX = LUT=([^\s]+)\s+SHA256=([a-f0-9]{64})`; `formatUniqueCameraModel(deviceModel, cameraId, activeLut?, includeLutMarkerInUniqueCameraModel = false)` defaults to `"<deviceModel> (cameraId=<id>)"` so per-device profiles in `darktable` / `RawTherapee` still resolve, with an opt-in `includeLutMarkerInUniqueCameraModel = true` for forensic capture where the LUT identity must round-trip even when `Software` gets overwritten by a later editor. Sealed `LutIdentity` adapter (`Bundled(catalogId, sha256)` / `CubeFile(safeFilename, sha256)`) decouples the formatter from `LutSidecar.{BundledRef, CubeFileRef}` so the engine can also pass synthetic test patterns; init-time validation rejects whitespace in marker names AND enforces the 64-lowercase-hex SHA shape so the produced strings are guaranteed to round-trip through `parseLutMarker` (which itself returns null on malformed inputs rather than throwing). 20 new tests in `DngLutMetadataTest.kt` (no-LUT default, bundled + cube-file marker, regex-friendly formatLutMarker, full round-trip parse, parser tolerates leading + trailing tool annotations like `Edited in DesktopTool 2.0; Originally: ... (modified)`, parser returns null on malformed sha + on absent marker, `UniqueCameraModel` defaults + opt-in path, whitespace + sha + blank-input init-time rejections, app name + schema version pins).
  - **Pending engine-side wire-up**: `Dng12Saver.writeStill` is still pending the Phase 1 capture engine; once it lands the saver will call `DngLutMetadata.formatSoftwareTag(BuildConfig.VERSION_NAME, activeLut)` and stamp the result into the DNG's TIFF tag 305 (and optionally tag 50708 when `forensic_capture_mode == true`).
  - **47 JUnit suites · 492 tests · 0 failures · 0 errors · 0 skipped** (up from 45 / 456).

- **Color + metering pipeline round (BUILD_PLAN §4 / §7, 2 new Kotlin modules + 1 reference-target alias + LUT validator extension + 28 new JUnit tests)** — pure-data, JVM-testable closures for the next batch of host-side blockers ahead of the Phase 1 capture engine:
  - `PreviewLumaHistogram.kt` — pure-data luma histogram reducer for the highlight-weighted metering pipeline. `reduceY8(plane, w, h)` for tightly-packed `Y8` buffers; `reduceYuv420Y(plane, w, h, rowStride)` for the camera2 preview surface's stride-padded `YUV_420_888` Y plane (skips the trailing padding bytes per row so we never attribute padding to bin 0); `reduceYuv420YCenterWeighted(plane, w, h, rowStride, centerFrac, centerWeight)` for subject-tracking metering with a configurable centered weighting square (defaults 0.5 / 3x); `pixelCount(hist)` is the sanity helper for the unit tests. 13 new tests in `PreviewLumaHistogramTest.kt` lock the contract (256-bin output, every-pixel-counted, unsigned-byte handling, dimension / buffer validation, rowStride padding skipped, `centerWeight = 1` short-circuit equals base, multiplier applied to the center region, parameter clamping, end-to-end pass through `HighlightMeter.suggestEvCorrection`, default-constants pin). Closes the host-side half of BUILD_PLAN §4 "Highlight-weighted metering — preview-frame histogram pipeline" — engine wiring (where the Y plane comes from in the live preview surface) still pending Phase 1 capture engine.
  - `LutPipeline.parseDl3(text)` + `LutImportValidator` routing — Autodesk Lustre Mesh `.3dl` import path. The parser tolerates optional `#`-comment lines + non-integer header rows (e.g. `Mesh 12 12`), then reads the uniform-ramp shaper line (N integers from 0 to `(2^outputBits) - 1`), then N^3 RGB integer triples with R varying fastest (matches `.cube` body convention); inferred bit depth from the shaper's max value; rejects non-uniform shaper ramps with a 1-step tolerance for fractional `maxValue / (size - 1)`; rejects unsupported sizes (only `Lut3D.SUPPORTED_SIZES = {17, 33, 65}`). `LutImportValidator.sniffFormat(text)` routes between Cube and Dl3 automatically: `.cube` wins on the `LUT_3D_SIZE` / `TITLE` / `DOMAIN_*` / `LUT_1D_SIZE` keywords; `.3dl` wins when the first integer-only line has at least 17 tokens (the minimum supported shaper size); body-only `.cube` payloads with 3-int rows still route to `.cube` so the canonical `MalformedHeader` failure category is preserved for the existing test corpus. New `LutImportValidator.Format.{Cube, Dl3}` enum + `validateForFormat(text, format)` for round-trip tests. 12 new tests in `Dl3ParserTest.kt` (identity 17-cube at 12-bit, identity 33-cube at 16-bit, leading comment + Mesh header tolerated, unsupported shaper size rejected, non-uniform shaper rejected, empty input rejected, malformed body row rejected, validator routes `.3dl` to parseDl3, sniffer picks Cube on `LUT_3D_SIZE`, sniffer picks Dl3 on integer-only first line, validator surfaces UnsupportedSize category, validator surfaces MalformedHeader category for empty `.3dl`). Closes BUILD_PLAN §7 "`.3dl` (Autodesk; publicly documented) — secondary import-only".
  - `BundledReferenceTargets.ColorCheckerPassport24` — pure-data alias for the X-Rite ColorChecker Passport's main 24-patch grid. Reference values are identical to `ColorCheckerClassic24` (the Passport's front-side 24 primaries are the same vendor-published values; values are facts, not copyrightable, so we bundle the math but NOT the trademarked image or chart name as a user-facing string), but the entry carries `id="passport24"`, a Passport-specific `displayName` and `source` string so a user calibrating against a real Passport gets a chart-name match in the calibrate flow and the resulting `CalibrationProfile.targetId` records which physical chart was actually photographed. 3 new tests in `ReferenceTargetTest`: round-trip equality of patch values vs Classic 24, Passport-specific id/source/displayName, `byId("passport24")` resolves, `All` enumeration contains the entry. The Creative Enhancement / portrait-warming patches on the back of the Passport (1/3-stop highlight + shadow + warming + cooling rows) still need to be transcribed from the X-Rite published data sheet; that's tracked as a separate doc TODO.
  - **45 JUnit suites · 456 tests · 0 failures · 0 errors · 0 skipped** (up from 43 / 428).

- **Lens info probe round (BUILD_PLAN §3 / DODGE_PROFILE.md, 2 new Kotlin modules + extension to DeepCapsProbeScreen + extension to CapabilityGate + 18 new JUnit tests + device-validated mapping)** — Phase 0 follow-on that closes BUILD_PLAN §3 "Confirm macro capability + min focus distance / mode-switch behavior" with a typed, JSON-round-trippable lens-info section per cameraId; the previously-unconfirmed macro capability of `cameraId=3` (Samsung S5KJN5 ultra-wide) is now hard-evidenced by a 25-diopter (~4 cm) minimum focus distance read straight from `CameraCharacteristics`:
  - `LensInfoSummary.kt` — pure-data carrier for per-camera lens / sensor info (`availableApertures: List<Float>`, `opticalStabilizationModes: List<String>`, `minimumFocusDistanceDiopters: Float`, `hyperfocalDistanceDiopters: Float`, `availableFocalLengthsMm: List<Float>`, `sensorPhysicalSizeMm: SensorPhysicalSize?`, `sensorActiveArrayPx: SensorActiveArray?`, `sensorOrientationDegrees: Int?`). Inference helpers: `isMacroCapable` flips true when `minimumFocusDistanceDiopters >= MACRO_MIN_DIOPTERS_THRESHOLD = 15f` (~6.7 cm); `hasOpticalStabilization` flips true when any mode is non-`OFF`; `minimumFocusDistanceMeters` inverts diopters when not fixed-focus; `describe()` produces a 1-line summary for the diagnostics dump. Companion `LensInfoSummaryJson` adds a `version: 1` schema with `encode(summary): JSONObject` and `decode(JSONObject): LensInfoSummary` that round-trip every field exactly. The threshold of 15 diopters was tuned UPWARD from an initial 10 specifically because the LYT-808 main wide reports exactly 10 diopters (10 cm — close focus, not super-macro), which would have falsely classified it as macro on the OnePlus 13.
  - `LensInfoExtractor.kt` — Android-side bridge from `CameraCharacteristics` to `LensInfoSummary` and its JSON. Defensive throughout: every framework key read is wrapped in `runCatching { ... }.getOrNull()` so a single missing or vendor-broken key never aborts the rest of the lens-info extraction. `extract(cameraId, cc): LensInfoSummary` and `extractToJson(cameraId, cc): JSONObject` are the two entry points; `lensFacingToken` and `oisModeToken` produce stable string tokens (`FRONT` / `BACK` / `EXTERNAL`, `OFF` / `ON` / `VENDOR(<int>)`).
  - `DeepCapsProbeScreen.kt` — `runDeepCapsProbe` now emits a `lensInfo: { ... }` block per camera in the existing `deep_caps_*.json` artifact (backward-compatible: existing fields untouched). Failures wrap to `lensInfo: { error: "<class>: <msg>" }` so the rest of the JSON is still salvageable.
  - `CapabilityGate.kt` — adds `Feature.OpticalStabilization` (display name "Optical image stabilization") + `HardwareCaps.hasOpticalStabilization: Boolean = false` (default false for backward compat). The gate emits a 9th row that flips with `caps.hasOpticalStabilization`; the disabled reason is the canonical "Optical image stabilization is not advertised by this lens.".
  - **Tests shipped**: `LensInfoSummaryTest` (14 tests, all green: macro inference at every probe-observed distance + the borderline @ 15 diopters case, threshold matches the documented constant, OIS inference flags only non-OFF modes, `minimumFocusDistanceMeters` inverts diopters and is null when fixed-focus, `describe` contains every salient field, JSON round-trip preserves every field exactly, encoder emits `schemaVersion`, decoder rejects schema mismatch + missing cameraId, decoder tolerates absent optional sections + skips blank entries in the OIS modes array, `SensorPhysicalSize` and `SensorActiveArray` reject non-positive dimensions, sensor physical size round-trips, apertures and focal lengths preserve order). `CapabilityGateTest` (4 new tests, all green: OIS gate flips with `hasOpticalStabilization`, OIS feature is included with the canonical display name, `HardwareCaps.hasOpticalStabilization` defaults to false for backward compat, plus the existing 5 tests still pass with the new field).
  - **Device-validated on adb 8bf09993 (OnePlus 13, CPH2655, LineageOS 23 / Android 16)**: `hfr-runs/deep_caps_round11.json` (161903 bytes) decodes through `LensInfoSummaryJson.decode` for every camera. Findings: `cameraId=0` (logical) + `cameraId=2` (LYT-808 main wide) report f/1.60, 6.06 mm focal, 10 diopters min focus (10 cm), 9.18 x 6.88 mm sensor, 90° orientation; `cameraId=1` (front IMX615) reports f/2.40, 3.23 mm focal, fixed-focus, 5.25 x 3.94 mm sensor, 270° orientation; `cameraId=3` (S5KJN5 UW) reports f/2.05, 2.30 mm focal, **25 diopters min focus (4 cm — confirms 🌷 super macro)**, 5.24 x 3.93 mm sensor, 90° orientation; `cameraId=4` (LYT-600 tele) reports f/2.60, 13.85 mm focal, 1.72 diopters (58 cm), 6.55 x 4.92 mm sensor, 90° orientation. **OIS gap discovered**: NO camera advertises a non-`OFF` mode in `LENS_INFO_AVAILABLE_OPTICAL_STABILIZATION`, even though the OnePlus 13 ships hardware OIS on the LYT-808 + LYT-600 — vendor request-key search (`com.oplus.*` / `com.oneplus.*` namespace) is now tracked under DODGE_PROFILE.md "Outstanding Phase 0 follow-ons". `docs/screenshots/smoke_deepcaps_round11.png` shows the deep-caps probe in its done state (`Status: OK saved=...deep_caps_20260508_083111.json`); no `FATAL` / `AndroidRuntime` entries in `adb logcat -d`.
  - Closes BUILD_PLAN §3 "Confirm macro capability + min focus distance / mode-switch behavior" V&V gate (now `[x]`); rewrites DODGE_PROFILE.md "Next probe deltas needed" section into "Lens + sensor info (from probe)" with the actual probed values + "Outstanding Phase 0 follow-ons" tracking the vendor-OIS search.
  - **43 JUnit suites · 428 tests · 0 failures · 0 errors · 0 skipped** (up from 42 / 410).

- **NDK scaffolding round (BUILD_PLAN §4 / NDK_PLAN.md, 3 new Kotlin modules + 1 new Compose screen + 1 new install script + 19 new JUnit tests + device-validated diagnostics)** — Phase 0 turns the "stubs only" `native/` folder into a complete fallback-aware scaffolding so the capture engine can already call into a stable `NativeEncoders` surface and degrade gracefully to JPEG when the .so is absent:
  - `NativeEncoders.kt` — defensive Kotlin facade for the planned `libpns_native.so`. `System.loadLibrary("pns_native")` is wrapped in `try { ... } catch (Throwable)` so a missing library can never crash the camera app at startup. `isAvailable: Boolean`, `lastLoadError: String?`, and `version(): Int` surface the runtime state for diagnostics. `encodeAvif10Hdr(...) -> Result` and `encodeJxl12Rec2020(...) -> Result` short-circuit to `Result.NotAvailable` when the .so is missing AND wrap the underlying `external fun` call in a Throwable-catching block when the .so IS loaded but throws. Sealed `Result` hierarchy: `Success(bytes)` (with `ByteArray` content equality), `NotAvailable` (singleton), `NativeError(code, message?)`. JNI symbols use `@JvmStatic external fun nativeXxx(...)` so the C++ side sees `Java_dev_pointandshoot_NativeEncoders_nativeXxx(JNIEnv*, jclass, ...)` and the linker name doesn't depend on the Kotlin `INSTANCE` singleton lowering.
  - `EncoderRoute.kt` — pure-data router that decides what files the capture pipeline actually writes. `decide(profile, nativeAvailable)` returns `Decision(profile, rawWritten, tonalWritten, fallbackJpeg, downgradeReason)` per `BUILD_PLAN.md` §4 / `FAILURE_MATRIX.md` "Native encoders unavailable": RAW (DNG) is ALWAYS written; the AVIF / JXL container downgrades to JPEG when native is absent; the canonical user-facing message lives at `EncoderRoute.DOWNGRADE_MESSAGE`. Companion `downgradedProfiles(nativeAvailable)` enumerates which profiles in `ImagingProfile.{StandardPro, UltraMax}` will downgrade given the current state, used by the diagnostics screen and the HUD pre-capture banner. `TonalContainer.requiresNativeEncoder` extension property pinned alongside.
  - `NativeDiagnosticsScreen.kt` — Compose screen that surfaces the runtime state of `NativeEncoders` and the resulting `EncoderRoute` decisions per `ImagingProfile`. Reachable from probe home via the new "Native diagnostics" button OR via `--es pns_screen native`; the screen does NOT require `CAMERA` permission (it inspects the .so / Kotlin facade only) so a fresh install can validate the fallback path before any camera prompts. Phase 0 builds always show "Native library: NOT LOADED" + `dlopen failed: library "libpns_native.so" not found` + per-profile rows with "Tonal: DOWNGRADED to JPEG" in the canonical `PnsColors.RecordRed`.
  - `native/pns_native.cpp` — JNI stubs matching the Kotlin facade. `Java_dev_pointandshoot_NativeEncoders_nativeVersion` returns `0`, the encoder stubs return `nullptr`. The legacy `Java_dev_pointandshoot_Native_version` symbol is preserved for backward compatibility with any stale build that might still reference it.
  - `native/CMakeLists.txt` — built-out skeleton with `POSITION_INDEPENDENT_CODE`, host-side JNI include path resolution (so a non-Android `cmake -B build` doesn't fail with "jni.h not found"), commented `FetchContent_Declare` blocks for libavif / libjxl / aom / highway / brotli with the planned `URL_HASH SHA256=` slots, and guarded `find_library(LOG_LIB log) / find_library(JNIGRAPHICS_LIB jnigraphics)` calls under `if(ANDROID)`.
  - `native/THIRD_PARTY.md` — license matrix for the planned upstream dependencies (libavif → BSD-2-Clause, aom → BSD-2 + Alliance for Open Media patent license, libjxl → BSD-3-Clause, highway → Apache-2.0, brotli → MIT). Pre-populated with sourcing rules (pinned tag, pinned SHA-256, license sidecar at `app/src/main/assets/legal/native/<name>/`) so license review can land BEFORE any source archive is fetched.
  - `native/README.md` — rewritten to document the Phase 0 layout, the "how to verify on the JVM (no NDK needed)" procedure, the `--es pns_screen native` ADB recipe, and the `pns_install_ndk.ps1` install path.
  - `scripts/pns_install_ndk.ps1` — non-interactive NDK + CMake installer (host-side helper). Locates the Android SDK (prefers `$env:ANDROID_HOME`, falls back to `%LOCALAPPDATA%\Android\Sdk`), finds `sdkmanager.bat`, and runs `sdkmanager --install "ndk;26.x" "cmake;3.22.1"` with EULA acceptance piped via stdin. Idempotent (`-DryRun` flag prints the planned command without invoking it; bails out cleanly when packages are already present). Prints the exact `$env:ANDROID_NDK_HOME` / `$env:ANDROID_NDK_ROOT` env vars to set so Gradle can pick up the new toolchain. Smoke-tested locally with `-DryRun` (script parses cleanly + exits 1 with helpful instructions when `cmdline-tools` is not yet installed).
  - **Tests shipped**: `NativeEncodersFallbackTest` (10 tests, all green: `isAvailable` is false on the JVM unit-test classpath; `lastLoadError` is non-blank; `version()` returns `VERSION_UNAVAILABLE = 0`; `encodeAvif10Hdr` / `encodeJxl12Rec2020` return `Result.NotAvailable` instead of throwing; empty-input encode does not crash; `Result.Success` `ByteArray` content equality; `Result.NotAvailable` singleton equality; `Result.NativeError` carries opaque code + optional message). `EncoderRouteTest` (9 tests, all green: `decide` writes both files when native is available for both StandardPro and UltraMax; `decide` downgrades to JPEG when native is absent for both profiles; `rawWritten` is preserved regardless of native state; `downgradedProfiles(true)` is empty; `downgradedProfiles(false)` enumerates both AVIF + JXL profiles; every `TonalContainer` requires native in Phase 0; `DOWNGRADE_MESSAGE` is a single non-blank user-facing line). Implementation detail locked here: `EncoderRoute.downgradedProfiles` enumerates `ImagingProfile.{StandardPro, UltraMax}` directly rather than reading `ImagingProfile.all` because `data object` singletons inside a sealed parent can be observed null during early companion init on the JVM.
  - **Device-validated on adb 8bf09993 (OnePlus 13, CPH2655, LineageOS 23 / Android 16)**: `docs/screenshots/smoke_native_diag.png` shows the diagnostics screen rendering the canonical Phase-0 state — "Native library: NOT LOADED" in `PnsColors.RecordRed`, `Version: 0 (unavailable)`, `loadLibrary error: dlopen failed: library "libpns_native.so" not found`, per-profile rows for Standard Pro (RAW: Lossless DNG / Tonal: DOWNGRADED to JPEG) and Ultra-Max (RAW: RAW12 (uncompressed) / Tonal: DOWNGRADED to JPEG), each carrying the canonical downgrade message verbatim. `docs/screenshots/smoke_probe_home_round10.png` shows the probe home rendering the new "Native diagnostics" button alongside the existing tiles. No crashes in logcat across launch + back navigation.
  - Closes BUILD_PLAN §4 "NDK pipeline integration" Phase 0 scaffolding for both libavif and libjxl rows; the parent `[ ]` rows stay open until the actual encoder bodies + `externalNativeBuild` wiring + libavif / libjxl SHA-256 pin land in Phase 1 (which requires the NDK to be installed on dev hosts and CI runners — see "Human action required" in NDK_PLAN.md).
  - **42 JUnit suites · 410 tests · 0 failures · 0 errors · 0 skipped** (up from 39 / 386).

- **GLES live preview LUT round (Phase 4 §7 "Live preview / video", 3 new modules + 17 new JUnit tests + 3 device-validated screenshots)** — promotes the host-side `LutShaderProgram` from "shader template + Kotlin wrapper" to a working end-to-end GLES pipeline with a real `GLSurfaceView`:
  - `TestPattern.kt` — pure-data procedural source for the GLES preview while the Phase 1 capture engine is still pending. 8 vertical color bars (white / yellow / cyan / green / magenta / red / blue / black) over an 11-step grayscale wedge over a smooth grayscale ramp; sRGB-encoded for display via the linear→sRGB EOTF (`linearToSrgb8`). 12 JUnit tests in `TestPatternTest.kt` lock the bar sequence, wedge step values, ramp monotonicity, sRGB endpoint encoding, and the array length contracts so the GLES path can never silently drift from what the unit tests verify.
  - `LutPreviewRenderer.kt` — `GLSurfaceView.Renderer` that owns the program + 2D source texture + 3D LUT texture + full-screen-quad VBO. UI-thread updates flow through two `AtomicReference` slots (`pendingSource`, `pendingLut`); `pendingLut` uses a private `LutUpdate(lut: Lut3D?)` wrapper so "no pending change" (a `null` slot) disambiguates from "user explicitly chose identity / bypass" (a `LutUpdate(null)` slot) — without the wrapper a single `AtomicReference<Lut3D?>` would re-upload the identity bypass on every frame. `BITMAP_VFLIP_QUAD` is a vertically-flipped sibling of `LutShaderProgram.Source.FULL_SCREEN_QUAD` that cancels Android-bitmap row-0-at-top vs OpenGL row-0-at-bottom. Identity LUTs short-circuit via `BypassPolicy.shouldSkipUpload(...)` and free the 3D texture entirely so the per-frame cost stays at "one source-texel sample" when the user picks None. 5 JUnit tests in `LutPreviewRendererQuadTest.kt` lock the V-flip relationship to `Source.FULL_SCREEN_QUAD` so a future refactor cannot accidentally re-introduce the upside-down preview regression.
  - `GLPreviewScreen.kt` — Compose screen with `AndroidView<GLSurfaceView>` + `LutChipRow` + a small banner showing the active LUT's display name + SPDX. `setEGLContextClientVersion(3)` is called explicitly so `sampler3D` works. The `LaunchedEffect(activeLut)` and the `update` lambda both call `renderer.setLut(activeLut.load())` so the GLES preview reflects the user's `HudSettings.stillsLut()` choice the instant they pick a new chip. Reachable from probe home via "Live preview LUT" or `--es pns_screen glpreview`.
  - **Device-validated on adb 8bf09993 (OnePlus 13, CPH2655, LineageOS 23 / Android 16)**: `docs/screenshots/smoke_glpreview_none.png` (identity bypass — all 8 color bars in their original colors, smooth ramp continuous), `docs/screenshots/smoke_glpreview_bw709.png` (B&W BT.709 LUT correctly collapsing the bars to grayscale: white > yellow > cyan > green > magenta > red > blue > black, matching `Y = 0.2126R + 0.7152G + 0.0722B` to the eye), `docs/screenshots/smoke_glpreview_cinematic.png` (PnS Cinematic LUT pulling shadows toward teal `(0.30, 0.55, 0.70)` and highlights toward orange `(1.00, 0.65, 0.35)` exactly as `BuiltInLuts.pnsCinematic` is documented to do — visible on the wedge: black-step → teal-blue, white-step → peach, on the smooth ramp transitioning from teal-shadows to orange-highlights). Logcat `PNS.GLES: GLES program ready (program=3)` confirmed on every launch; no crash, no driver error, no GLES infolog.
  - Closes BUILD_PLAN §7 "Architecture seams" `LutShader.kt` (now `[x]`) and turns the `[ ]` bullet under "Apply path -> Live preview / video" into `[x]` for the GLES seam itself (Camera2 source bind-in stays open for the Phase 1 capture engine).
  - **39 JUnit suites · 386 tests · 0 failures · 0 errors · 0 skipped** (up from 38 / 374).

### Added

- **Color pipeline + branding round (Phase 4 / Part 4 cross-cut, 4 host-side modules + 39 new JUnit tests + on-device evidence)** — closes the host-side scaffolding ahead of the Camera2 ↔ GLES wire-up:
  - `app/src/main/assets/shaders/lut_apply.frag.glsl` + `lut_apply.vert.glsl` — GLES 3.0 fragment + vertex shaders for the `sampler3D`-based LUT apply path. Frag shader does a half-texel inset on the UVW coordinates so we sample at cell centers (not cube edges), and accepts a `uLutEnabled` uniform that the host drives to `0.0` for identity bypass per BUILD_PLAN §7 "Apply path".
  - `LutShaderProgram.kt` — Kotlin wrapper for the GLES program. Pure-data inner objects:
    - `LutShaderProgram.Source` — asset paths, attribute/uniform names, GLSL version directive constant, the canonical `FULL_SCREEN_QUAD` (`FloatArray(16)`, NDC + UV interleaved for `GL_TRIANGLE_STRIP`), and `requiredSymbols()` for the shader-asset contract test.
    - `LutShaderProgram.BypassPolicy` — `lutEnabledUniform(lut)` returns `0f` for null / identity LUTs and `1f` otherwise; `shouldSkipUpload(lut)` short-circuits the 3D-texture upload entirely for identity. Centralizes the bypass policy so the GLES program, the CPU apply path, and the performance-budget gate all agree on what "off" means.
    - `LutShaderProgram` itself — `createFromAssets(context)` compiles + links from the on-disk shaders; `uploadLutTexture(lut)` produces a `GL_RGB16F` 3D texture with `LINEAR` filtering (so trilinear interpolation runs in hardware) and clamp-to-edge wrap on every axis; `bindUniforms(lut)` sets `uSourceTex` to texture unit 0, `uLutTex` to unit 1, `uLutSize` to the grid size, and `uLutEnabled` per the bypass policy.
    - 13 JUnit tests in `LutShaderProgramSourceTest.kt`: shader assets exist at the loader's expected paths, version directive present, fragment shader uses `sampler3D`, every Kotlin-bound symbol appears in the on-disk shader text, `requiredSymbols()` enumerates exactly 6 names with no duplicates, full-screen quad has the documented NDC + UV layout, identity / null / non-identity LUTs drive the bypass policy correctly at every supported size.
  - `LutSidecarWriter.kt` — file-IO wrapper around `LutSidecar.encode` for the engine's capture-write path. `writeBundled(captureFile, ref)` and `writeCube(captureFile, ref)` write atomically via temp file + rename (delete-and-rename on Windows when the target exists, so a power loss between operations leaves no half-formed sidecar). `siblingFor(captureFile, isBundled)` is a pure path helper. `sha256Hex(bytes)` and `sha256ForLut(lut)` (IEEE-754 LE) compute the digests both `BundledRef` and `CubeFileRef` need. 14 JUnit tests in `LutSidecarWriterTest.kt`: sibling resolution preserves the parent dir, `writeBundled` / `writeCube` round-trip through `LutSidecar.decode`, mismatched `captureFilename` rejected, overwrite is atomic with no leaked temp files, parent dirs are created, sha256 of empty-input matches the canonical RFC value, sha256 differs across inputs and is stable, output is always 64 lowercase hex chars, identical LUTs hash identically, different content hashes differently, different grid sizes hash differently, end-to-end "build a bundled sidecar with the LUT's own sha" round-trips perfectly.
  - `DngColorTags.kt` — pure-data converter from `CalibrationProfile` → DNG color tags so desktop processors (`darktable`, `RawTherapee`, Adobe DNG SDK) can honor an in-camera calibration WITHOUT baking it into the RAW. `asShotNeutral(gains)` inverts the WB gains and normalizes the max to 1.0f per the DNG spec. `colorMatrix1(profile)` solves `inverse(diag(wbGains) * sRGBtoXYZ_D65 * CCM)` to map XYZ → camera-native RGB. `forwardMatrix1(profile)` produces `Bradford_D65_to_D50 * sRGBtoXYZ_D65 * CCM` to map (WB-gained) camera RGB → XYZ-D50, the DNG profile connection space. `calibrationIlluminantCode(illuminant)` returns the EXIF LightSource code (D50 → 23, D55 → 20, D65 → 21, StdA → 17, F2 → 14). The published constants (`sRGB → XYZ-D65` linear sRGB / BT.709 primaries; Bradford D65 → D50 CAT) are pinned as private constants — they're standards data, not copyrightable. 12 JUnit tests in `DngColorTagsTest.kt`: identity gains → `(1, 1, 1)`, AsShotNeutral max is exactly `1.0`, AsShotNeutral inverts gains correctly (`1/r, 1/g, 1/b` normalized), identity profile yields `ColorMatrix1 = inverse(sRGB→XYZ_D65)` (validated by `cm1 * sRGB→XYZ_D65 == I`), identity profile `ForwardMatrix1 = Bradford * sRGB→XYZ_D65`, EXIF light-source codes match the DNG 1.7 spec, `forProfile` picks the right code for every `Illuminant`, non-identity CCM yields a different `ColorMatrix1`, WB gains shift `AsShotNeutral` but NOT `ForwardMatrix1` (engineering invariant), `DngColor.equals` compares `FloatArray` contents (not references), `DngColor.init` rejects malformed `AsShotNeutral`.
  - **JetBrains Mono Regular vendored** — Part 4 typography spec ("JetBrains Mono for all technical readouts") closed: `app/src/main/res/font/jetbrainsmono_regular.ttf` (273900 bytes, SHA-256 `a0bf60ef0f83c5ed4d7a75d45838548b1f6873372dfac88f71804491898d138f`) pinned at upstream tag `v2.304` (https://github.com/JetBrains/JetBrainsMono/raw/v2.304/fonts/ttf/JetBrainsMono-Regular.ttf). `PnsTheme.MonoFamily` now resolves to `FontFamily(Font(R.font.jetbrainsmono_regular))` instead of the system `FontFamily.Monospace`. SIL OFL v1.1 license text vendored at `app/src/main/assets/fonts/jetbrainsmono/LICENSE.txt` (SHA-256 `30f0c136e3c88e422d0791acd97238870f9054a9729bc34cf2ff0d4ed8cac4ad`) with `SOURCE.txt` (refresh procedure) and `SHA256.txt` (digest manifest) siblings. `LICENSES.md` "Bundled font assets" section + OFL-1.1 compatibility statement added. **Device-validated on adb 8bf09993**: ProHUD timecode `00:00:00:00`, `FPS 30  ISO 400  1/250`, `STILL LUT B&W BT.709 / VIDEO LUT None` chips, `M H S BKT` command dial, and the `→ Ultra-Max` button all render in JetBrains Mono with its distinctive ligatures (`smoke_jetbrains_mono.png`).
  - **README icon + screenshot section** — `docs/icon.png` (512×512, vector-derived launcher reticle on charcoal) embedded at the top of `README.md`. New `## Screenshots` section between Status and Quickstart references the existing `docs/screenshots/smoke_*.png` artifacts (probe home, Pro HUD + LUT picker, live preview HUD overlay, Calibrate, Import LUT, About live hydration).
  - **Launcher-icon V&V tick (BUILD_PLAN §8)** — `smoke_app_info.png` captured via `adb shell am start -a android.settings.APPLICATION_DETAILS_SETTINGS -d package:dev.pointandshoot` shows the system rendering the adaptive `mipmap/ic_launcher` resource (reticle + crosshair + Hasselblad-orange center dot) correctly on `adb 8bf09993`.
  - **ACES + Filmic LUT blockers documented** — both upstream packages ship `.ocio` configs + Sony Pictures Imageworks `.spi3d` / `.spi1d` files, NOT `.cube`. BUILD_PLAN §7 entries now enumerate the two paths (host-bake to `.cube` with a pinned `ociobakelut` script, OR extend `LutPipeline` with a `.spi3d` parser) and defer the decision until the live-preview GLES path lands.
  - **38 JUnit suites · 374 tests · 0 failures · 0 errors · 0 skipped** (up from 35 / 335).

### Fixed

- **UTF-16 LE encoding regression on `README.md` + `DODGE_PROFILE.md`** — both files were saved as UTF-16 LE (no BOM) at some point during Windows-side editing. Git treated them as binary, which masked diffs in code review. Both converted back to UTF-8 (no BOM); `scripts/pns_verify_toolchain.ps1` now also walks every `*.md` under the project root (excluding `build/`, `.gradle/`, `_gradle_extract/`, `.git/`, `hfr-runs/`, `node_modules/`) and rejects UTF-16 LE so the regression cannot recur. Verify gate now reports `OK: doc encoding (20 *.md files; none UTF-16 LE)`.

- **Compose UI integration round (Phase 4 / Phase 2 cross-cut, 5 device-validated screens)** — closes the on-device interactive surface for calibration, LUT management, live-preview HUD, and live About hydration:
  - `CalibrateScreen.kt` — in-app Calibrate flow (chart-photo flavor): target picker (`BundledReferenceTargets.All`), SAF "Load chart photo…" picker, four-corner tap with live overlay (`Canvas` + `pointerInput.detectTapGestures`), Compute → `CalibrationSampler.sample` → `CalibrationMath.computeWbGains` → `CalibrationMath.computeCcm` chain, Save → `CalibrationProfileStorage.save(...)` (atomic write under `getExternalFilesDir(null)/calibration/`).
  - `BitmapRgbPlane.kt` — converts an Android `Bitmap` to a linear-light, normalized `RgbPlane` (sRGB EOTF, long-edge downsample to 1024 px). 11 JUnit tests in `BitmapRgbPlaneTest.kt` cover the EOTF endpoints (0 → 0, 255 → 1, midtone via 2.4 power law, linear segment), monotonicity across all 256 input values, defensive clamping of out-of-range bytes, and the long-edge downsample arithmetic for landscape + portrait + at-cap inputs.
  - `LutImporterScreen.kt` + `ImportedLutStore.kt` — SAF "Import LUT…" picker with `OpenDocument(arrayOf("*/*"))`, validates via `LutImportValidator.validate(bytes)`, atomic-write to `getExternalFilesDir(null)/luts/imported/<safeName>.cube` plus `<safeName>.cube.sha256.txt` sidecar. Filenames sanitized to `[A-Za-z0-9._-]` (capped at 96 chars, leading dots stripped, run-of-underscores collapsed); collisions resolved via `_2`, `_3`, … suffixes with a SHA-derived fallback at 9999. 9 JUnit tests in `ImportedLutStoreTest.kt` cover the sanitize / collision / sha256 helpers.
  - `LutChipRow.kt` — Pro HUD chip row with separate STILL LUT + VIDEO LUT chips backed by an `AlertDialog` picker that lists every `LutCatalog.forScope(scope)` entry with display name, 1-line description, SPDX badge, and a "Reset to None" affordance. Selection persists immediately to `HudSettings`.
  - `HudSettings` extended with `selectedLutForStills` + `selectedLutForVideo` fields (both `LutCatalog.None.name` by default) plus `stillsLut()` / `videoLut()` resolvers that fall back to None on rename or removal. SharedPreferences keys `selected_lut_stills` / `selected_lut_video`. 5 JUnit tests in `HudSettingsLutResolutionTest.kt`.
  - `ProHudScreen` now hosts the LUT chip row between the command dial and record button.
  - `PreviewEngineScreen.LivePreviewHudOverlay` — the live `TextureView` is now wrapped in a `Box` with the FPS / cameraId readouts, the LUT chip row, the command dial, and an `EyeAfOverlay` placeholder riding on top. Honors `HudSettings` toggles.
  - `CameraCapabilitiesProbe` — new `Calibrate` and `Import LUT` home-screen buttons; new `--es pns_screen` aliases (`about` / `prohud` / `hudsettings` / `calibrate` / `lutimport`) for one-line ADB navigation; live `EncoderAttemptJsonAdapter.loadLatest(...)` → `EncoderResultAggregator.summarize(...)` plumb-through to `AboutScreen(liveSummary = …)`.
  - **Device-validated on adb 8bf09993 (OnePlus 13, CPH2655, LineageOS 23 / Android 16)**: home + ProHUD + LUT picker + LUT persistence (force-stop survives) + About live hydration (180 attempts across 4 cameras, top recipes per camera, Cam2 Hfr 480fps measured 427.8 fps) + Calibrate + LutImporter + PreviewEngine with HUD overlay riding on a 59.3 fps Camera2 preview. Screenshots in `docs/screenshots/`.

### Added

- **Capability probe (Phase 0)** — comprehensive Camera2 enumeration that exports both Markdown (`PROBE_RESULTS.md`) and per-phase JSON artifacts:
  - Deep characteristics, session-configuration matrix, HDR/DCG runtime, capture latency, RAW + dynamic-range exclusivity, burst / AE bracket, logical-vs-physical, exhaustive HFR + encoder matrix, legacy Camera1.
- **Host orchestration** — `scripts/pns_hfr_autorun.ps1` ties build → install → camera permission → probe phase → JSON pull → suite-summary writer → optional Phase 9 thermal snapshot.
- **Toolchain gate** — `scripts/pns_verify_toolchain.ps1`:
  - Runs `assembleDebug`, enforces UTF-8 on Kotlin and PowerShell sources, parses PowerShell scripts.
  - Runs an automated **FOSS dependency audit** (rejects Play Services / Firebase / ML Kit / Play Billing / Ads references in any Gradle file or version catalog).
  - New `-RunTests` switch additionally runs `:app:testDebugUnitTest` so PR authors get the same test coverage CI does.
- **CI**
  - `.github/workflows/toolchain-verify.yml` — Ubuntu; runs the gate, then `:app:testDebugUnitTest`. Uploads `app-debug.apk` as `pns-debug-apk-<sha>` (14-day retention) on success and the JUnit reports on failure (`pns-unit-test-reports-<sha>`, 7-day).
  - `.github/workflows/plan-doc-verify.yml` — skip-Gradle for plan-only changes.
  - `.github/workflows/build-signed.yml` — manual / `v*` tag; runs the gate, decodes a base64 keystore from secrets, builds release APK, verifies signature with `apksigner`, uploads artifact.
  - Dependabot watches GitHub Actions versions weekly and (new) Gradle dependencies, grouped by Compose / Core / CameraX / Kotlin+AGP.
- **Branding & theme**
  - Adaptive launcher icon: vector reticle with Hasselblad-orange shutter dot on charcoal; `mipmap-anydpi-v26/ic_launcher{,_round}.xml` + monochrome layer for Material You.
  - `PnsTheme.kt` (Material 3 wrapper) + `PnsColors` (`PhotoOrange #FF5C00`, `RecordRed #E00000`, `Charcoal #181A1B`, etc.) + `PnsTypography` (monospaced HUD text; `MonoFamily = FontFamily.Monospace` until JetBrains Mono ships as a font asset).
- **Pro HUD scaffolds**
  - `CommandDial.kt` (M / H / S / BKT segmented control, brand-orange selection state).
  - `VideoTallyOverlay.kt` (record-red border, optional pulse; **never vibrates**).
  - `TimecodeOverlay.kt` (`HH:MM:SS:FF` Sony-style with rec / standby dot; `formatTimecode` made defensive — `elapsedMs.coerceAtLeast(0)`, `max(1, fps)`).
  - `EyeAfOverlay.kt` (Compose `Canvas` overlay; green crosshair-decorated rectangles per `EyeMark`).
  - `ProHudScreen.kt` composite host-side smoke harness wiring the above + record toggle + Standard Pro / Ultra-Max profile switcher.
  - `HudSettings` (SharedPreferences `pns_hud_settings`) + `HudSettingsScreen` (`Settings > HUD` granular toggles, reset-to-defaults).
- **Capture pipeline scaffolds**
  - `ImagingProfile.kt` — `StandardPro` (lossless DNG + 10-bit AVIF + Display P3) and `UltraMax` (RAW12 DNG + 12-bit JXL + Rec. 2020) sealed type.
  - `Dng12Saver.kt` — Camera2 `DngCreator` wrapper, profile-aware, EXIF orientation mapping.
  - `CaptureStorage.kt` — MediaStore writer with `IS_PENDING` lifecycle (`Handle.close` clears, `Handle.discard` deletes); per-profile destinations (`Pictures/Point & Shoot/...`); deterministic `pns_<utc>_<profile>_<seq>.<ext>` filenames; `image/x-adobe-dng`, `image/avif`, `image/jxl` MIME types.
  - `CaptureHaptics.kt` — 30 ms post-readout still tick (`VibrationEffect.EFFECT_TICK` on API 29+, 12 ms one-shot fallback); manifest `android.permission.VIBRATE` granted.
  - `BracketPlan.kt` — centered 3 / 5 / 7 EV bracket plans with stable `bracketGroupingId`.
  - `BracketScheduler.kt` — converts EV offsets to integer Camera2 AE compensation values via the camera's `Rational` step + `Range`.
  - `HighlightMeter.kt` — Ricoh GR-style 95th-percentile-luma protection (pure function over a 256-bin histogram, EV-clamped).
  - `TapToShootHandler.kt` — `Modifier.tapToShoot(callbacks)` (down → AF/AE lock, clean up → fire, cancel → release).
  - `VendorKeyGuard.kt` — centralized feature detection (`isCharacteristicKeyAvailable` / `isRequestKeyAvailable` / `isSessionKeyAvailable`); `useIfAvailable {}` enforces gate + logs `PNS.VendorKey present|absent`.
- **Diagnostics**
  - `DiagnosticsMode.kt` — toggle in `pns_diagnostics`; `dump` writes logcat (`PNS.Diagnostics`) + `getExternalFilesDir(null)/diagnostics_<utc>.txt`.
  - Probe home button "Diagnostics dump" enables-and-dumps; Toast confirms artifact path.
- **About / Heritage**
  - `AboutScreen.kt` — heritage tribute (verbatim from `BUILD_PLAN.md` §6) + developer-facing "What works on OnePlus 13 (dodge)" recipes + known-bad paths; reachable from probe home via "About / Heritage".
- **Mapping & UX-safety scaffolds**
  - `CropPlan.kt` — focal-equivalent (`Street35` 1.50x, `Standard50` 2.20x, `Portrait85` 1.16x, `LongTele150` ~2.04x) centered-crop math + engine-facing `MeteringHint` / `AfHint` (covers BUILD_PLAN \u00a73 "crop metadata strategy for 35/50/85/150mm modes"). `LongTele150` is the 12 MP center crop of the 50 MP LYT-600 tele sensor: `sqrt(50/12) \u2248 2.04x` linear, putting effective focal length at `73 mm \u00d7 2.04 \u2248 149 mm` (rounded to 150 mm equivalent).
  - `FaceDetectAdapter.kt` — pure-data mapper from Camera2 face / eye landmarks (active-array pixels) to `EyeMark` previewing-pixel coordinates; handles 0/90/180/270 sensor rotation and front-camera mirroring; bounding-box -> "between the eyes" proxy when face-detect FULL is unavailable.
  - `CapabilityGate.kt` — pure-data feature gate per BUILD_PLAN §9 ("UX safety nets - capability-driven UI"): `evaluate(HardwareCaps)` returns enable-bit + human-readable reason for each `Feature`; `recommendedDefaults` enforces the Standard Pro baseline.
  - `PnsLog.kt` — release-aware logging facade per BUILD_PLAN §9 ("Release builds do not emit verbose debug logs by default"): `v` / `d` muted in non-debuggable builds unless `DiagnosticsMode` is on; `i` / `w` / `e` always emitted. Initialized from `MainActivity.onCreate` via `ApplicationInfo.FLAG_DEBUGGABLE` (no `BuildConfig` dependency).
  - `TrackerState.kt` — Nikon-style "3D tracking" persistence (BUILD_PLAN \u00a74): hysteresis-based per-ID lock/unlock (`acquireFrames` to lock, `keepAliveFrames` of absence before drop). Pure stateful object, no Android types. Drives the engine's "which subjects to follow" decision once the live face/AF feed is wired in Phase 1.
  - `PerfBudget.kt` — machine-checkable mirror of `PERFORMANCE_BUDGETS.md`: `check(label, measured, budget, warnFactor=1.25)` returns OK / WARN / FAIL; `checkHapticTick(measuredMs)` enforces the `30 ms \u00b1 5 ms` post-readout window from the spec. `Defaults` constants pinned by `PerfBudgetTest` so accidental drift between code and the markdown is caught.
  - `EncoderResultAggregator.kt` — pure-data summary of the exhaustive media-encoder probe. Takes `List<EncoderAttempt>` (one row per `cameraId+size+fps+mime` attempt) and returns `EncoderSummary`: `knownGood` (sorted by camera/fps/area), `knownBad` (sorted by canonical-error frequency), `byCamera` roll-up (`bestHfrFps` / `bestRegularFps`), and `canonicalErrors` (errno-collapsed -> count). `bestHfrRecipe(summary, cameraId)` picks the highest-fps known-good row per camera. Closes the analytical half of the open Phase 0 V&V gate "which cameraId+size+fps+mimetype combinations are actually stable for HFR encode".
  - `EncoderAttemptJsonAdapter.kt` — Android-side JSON adapter that finds the newest `exhaustive_probe_*.json` under `getExternalFilesDir(null)`, parses it, and flattens `cameras[].hfrAttempts` + `cameras[].regularAttempts` into a `List<EncoderAttempt>` ready for `EncoderResultAggregator.summarize`. The pure decode step is split out so it can be unit-tested with real `org.json:json` (added as a test-only dep, MIT-style); the file-discovery wrapper returns a `LoadResult` with the source file + `generatedAt` timestamp + `runId` so the UI can surface "hydrated from probe run <ts>".
- **Tests** — JUnit 4 wired (`testImplementation(libs.junit)`); pure-JVM suites:
  - `BracketPlanTest.kt`
  - `BracketSchedulerTest.kt`
  - `HighlightMeterTest.kt`
  - `TimecodeFormatTest.kt`
  - `CaptureStorageFilenameTest.kt`
  - `CropPlanTest.kt`
  - `FaceDetectAdapterTest.kt`
  - `CapabilityGateTest.kt`
  - `PnsLogTest.kt`
  - `TrackerStateTest.kt`
  - `PerfBudgetTest.kt`
  - `EncoderResultAggregatorTest.kt`
  - `EncoderAttemptJsonAdapterTest.kt` (real `org.json` on the test classpath)
  - `Lut3DTest.kt` (8 tests) — identity at every supported size (17/33/65), grid-coordinate exactness, validation errors.
  - `LutPipelineTest.kt` (12 tests) — trilinear apply, NaN-input rejection, swap-channel correctness, allocation-free variant offset, parse/serialize round-trip, non-default DOMAIN_MAX rejection, embedded-quote escaping in TITLE.
  - `BuiltInLutsTest.kt` (11 tests) — Rec.709 identity preserves arbitrary samples; B&W BT.601/709 collapse to gray with the correct luma weights; Cinematic grade pulls deep shadows toward teal + pure highlights toward warm orange + leaves 50% gray near-untouched + cells stay in `[0, 1]`.
  - `LutCatalogTest.kt` (8 tests) — every entry's SPDX is in `ALLOWED_SPDX`; loads at every supported size; B&W entries collapse to gray when loaded from the catalog; `defaultFor(scope)` is `None`.
  - `CalibrationMathTest.kt` (12 tests) — WB gains anchor green at 1, correct a magenta cast, average across multiple gray patches; CCM solver recovers identity, channel-swap, and a synthetic non-trivial mixing matrix to within 1e-3; throws on length-mismatch / too-few-patches / singular system.
  - `CalibrationToLutTest.kt` (8 tests) — identity profile produces identity LUT; full WB+CCM+bias profile reaches < 1 LSB on 8-bit through the LUT vs direct apply; default cube TITLE encodes provenance (illuminant + cameraId + targetId).
  - `ReferenceTargetTest.kt` (15 tests) — `ReferenceTarget` invariants (mismatched patch counts, duplicate (row, col), out-of-range coords); `patchCenter` + `patchHalfSize` math; bundled `ColorCheckerClassic24` (4×6 grid, neutral wedge in row 3 monotonically darkening, row 2 primaries dominate the right channel); bundled `Generic24` (18 hue swatches + 6 grays); `BundledReferenceTargets.byId` lookup + duplicate-id detection; `Patch.equals` uses `contentEquals` on `referenceRgb`.
  - `CalibrationSamplerTest.kt` (14 tests) — `RgbPlane` round-trip + clamping + length-mismatch rejection; `ChartCorners.bilinearMap` exact at the four corners + center is the average; `sampleAt` exact on uniform plane (zero variance), correct mean + variance on two-color stripes (analytic check), ROI clipping to plane bounds, off-plane returns rejected sample, `maxVariance` triggers rejection + accepts under threshold, sample over uniform plane returns identical means for every patch, full `sample(plane, target, corners)` over a synthetic 24-patch chart finds every patch within 1e-3 of reference.
  - `SlantedEdgeMtfTest.kt` (12 tests) — `GrayPlane` build + transpose; oversample / esfBins validation; uniform plane returns null; sharp edge yields higher MTF50 than blurred edge; monotonically decreasing MTF50 across σ ∈ {0.5, 1.0, 1.5, 2.0}; measured value within 50 % of the analytic Gaussian-blur formula `0.187 / σ`; near-horizontal variant agrees with near-vertical within 18 %; `cyclesPerPixelToLpph` multiplies correctly.
  - `LutCreditsBuilderTest.kt` (5 tests) — one row per `LutCatalog` entry; every row has non-empty fields; every SPDX is in `ALLOWED_SPDX`; ordering matches the catalog; well-known entries (None, Cinematic, B&W BT.709) appear.
  - `LutDiagnosticsBuilderTest.kt` (8 tests) — default state reports identity LUT + no calibration; custom state reports active LUT name / SPDX / SHA / calibration id / captured-at; every catalog entry appears in the bundled section; every SPDX is shown; allowed SPDX whitelist enumerates in sorted order; output ends with a trailing newline; null captured-at omits the line; default state matches `LutCatalog.None`.
  - `PerfBudgetTest.kt` gains 3 new tests covering Phase 4 LUT defaults — `LUT_SHADER_PER_FRAME_1080P_MS` (2 ms) and `LUT_CPU_STILL_12MP_MS` (80 ms); shader budget grades 60 fps preview correctly; CPU budget grades 12 MP capture correctly.
  - `ColorMathTest.kt` (22 tests) — sRGB → XYZ primary correctness against the IEC 61966-2-1 published matrix; XYZ → CIE Lab D65 / D50 white-point handling; CIEDE2000 against the published Sharma 2005 reference vectors (rows 1, 2, 3, 14, 22) to within `1e-3`; symmetry, identical-input dE = 0, "tiny perturbation → small dE" sanity, "pure red vs pure green is large dE" sanity, input-length validation.
  - `CalibrationCcmAccuracyTest.kt` (7 tests) — closes BUILD_PLAN §7 V&V "computed CCM produces ≤ 1.0 dE_2000 mean error on synthetic 24-patch fixture" + ".cube round-trip shrinks mean dE_2000 by ≥ 80 %" gates: synthetic ColorChecker24 with ±0.5 % noise lands ≤ 1.0 dE_2000 mean after CCM; noiseless case lands < 0.05; small hue rotation recovers cleanly; Generic24 fixture also passes; WB-only "warm cast" shrinks dE substantially; LUT-vs-direct apply parity within 1 LSB on 8-bit; `.cube` round-trip via `CalibrationToLut.toLut3D` + `LutPipeline.applyTrilinear` shrinks the mean dE_2000 by ≥ 80 %.
  - `CalibrationProfileJsonAdapterTest.kt` (16 tests) — non-trivial profile round-trip + identity profile round-trip; `mtf50Lpph` omitted when null; documented top-level keys present; encoded `version` matches `SCHEMA_VERSION`; decode rejects malformed JSON / unknown version / unknown illuminant / missing wbGains / malformed ccm shape / bad row width / negative capturedAtMs / blank cameraId; filename helper handles every illuminant; encoded JSON is multi-line for diff-friendliness.
  - `LutSidecarTest.kt` (17 tests) — bundled + cube round-trip; encoded sidecar contains documented keys + magic header + schema version; `bundledRefFor` pulls metadata from `LutCatalog`; cube without a title omits the line; sibling filename per flavor; decode rejects missing magic / unknown schema version / malformed key=value lines / unknown kind / missing required fields; decode tolerates blank lines + inline comments; constructor validation rejects malformed sha256 / unsupported lutSize / blank cataloguedAs; Video captureKind round-trips.
  - `EncoderRecipeBuilderTest.kt` (13 tests) — empty summary yields no rows; picks one HFR + one Regular per camera; skips cameras with no successes; recipes deterministic across summary reorderings; mime recovered from note when first-class mime is null; error rows sorted by canonical-error frequency with cap respected; `headlineCounts` is null for null/empty summaries; `okPercent` arithmetic is correct.
  - `LutImportValidatorTest.kt` (15 tests) — accepts identity at every supported size; accepts non-identity within tolerance; bytes overload wraps the string overload; rejects oversized payloads, missing/non-integer `LUT_3D_SIZE`, `LUT_1D_SIZE`, unsupported sizes, non-unit `DOMAIN_MAX`, non-float bodies, wrong number of triples, wildly out-of-range cells; accepts cells in the ±0.001 tolerance band; `Failure.toastMessage()` prefixes the category label; every `FailureCategory` has a non-blank toast label.
  - `MetadataSerializationGoldenTest.kt` (9 tests) — locks the `BracketPlan v1` text projection (3-shot @ 1 EV, 5-shot @ 0.667 EV, 7-shot @ 1 EV) including grouping-id stability across stops + non-collision of two default `build()` calls; locks the `CropPlan v1` projection (Street35, Standard50, Portrait85, LongTele150 on an 8160×6144 active array; locks centered crop rectangle + effective zoom + metering / AF hints).
  - `CalibrationProfileStorageTest.kt` (6 tests) — Context-free surface area: `nowUtcTimestamp` matches the documented `yyyyMMdd_HHmmss` UTC pattern + monotonicity; documented constants stable; load round-trip via JUnit-managed temp dir; load propagates `IllegalArgumentException` on malformed JSON; filename convention parity with the storage `latestFor` key.
- **Phase 4 calibration math + V&V (host-side, all 6 closed)**
  - `ColorMath.kt` — pure-data linear-sRGB → CIE-XYZ (D65 IEC 61966-2-1 matrix) → CIE Lab → CIEDE2000 (full Sharma / Wu / Dalal 2005 implementation including the LCH rotation term and the `kL = kC = kH = 1.0` graphic-arts default). Convenience `deltaE2000FromLinearSrgb(rgb1, rgb2)` for the typical capture-domain comparison. D65 + D50 white-point constants.
  - `CalibrationProfileJsonAdapter.kt` — hand-rolled stable JSON serializer + parser on top of `org.json` (Android-framework type; tests use `testImplementation("org.json:json")`). Schema `version: 1` with documented top-level keys; pretty-printed (2-space) for diff-friendliness. `filenameFor(profile, utc)` enforces the `<illuminant>_<utc>.json` BUILD_PLAN §7 spec. Closes the host-side scaffold for "Profile JSON saved to app-private storage".
  - `LutSidecar.kt` — pure-data writer + parser for the BUILD_PLAN §7 capture-sidecar format. ASCII line-oriented, `# pns-lut-sidecar v1` magic header, `key = value` body, two flavors (`kind = bundled` for catalog references; `kind = cube` for user-imported `.cube` files). `BundledRef` carries the catalog name + SPDX + source + 64-char lowercase SHA256 (regex-validated); `CubeFileRef` carries the relative cube path + optional title + SHA256. Convenience helpers: `bundledRefFor(catalog, ...)` populates SPDX / source from a `LutCatalog` entry; `siblingFilenameFor(captureFilename, isBundled)` enforces the `.lutref.txt` / `.cube.txt` extension convention. Decoder tolerates blank lines + inline comments and emits human-readable errors that pinpoint the offending field.
  - `EncoderRecipeBuilder.kt` — pure-data hydration of the AboutScreen "From the latest probe (live)" subsection. `recipesFromSummary(summary)` selects the best HFR + best regular row per camera (deterministic ordering); `errorRowsFromSummary(summary, max)` surfaces the most-frequent canonical errors; `headlineCounts(summary)` produces the `<totalAttempts>` / `<okPercent>` banner. Android-free so JVM tests can exercise it without a Compose runtime; AboutScreen consumes the output via `LiveRecipeCard` + `LiveErrorCard` Compose adapters.
  - `AboutScreen.kt` — gains a "From the latest probe (live)" section, gated on the optional `liveSummary: EncoderSummary?` parameter (header + recipe cards + top-failure error cards in `RecordRed`). Hidden entirely when no probe artifact is available.
  - `DODGE_PROFILE.md` — gains a 150 mm long-tele row in the "Mode mapping" table; new "Host-side mapping (FocalMode → CropPlan → CapabilityGate)" cross-reference table tying the spec modes to actual `CropPlan` / `CapabilityGate.Feature` constants in code; new "HFR recipes (live)" snapshot table; new "Imaging profile defaults per FocalMode" section; new "Color & LUT pipeline applicability" section (RAW path explicitly excluded); new "Storage filename + GroupingID convention" pointer to `CaptureStorage`.
- **Phase 4 color/LUT pipeline (host-side, pure-data)** — `Lut3D.kt` (size-validated 17/33/65 with companion `identity(size)` builder + `isIdentity(tolerance)` checker), `LutPipeline.kt` (CPU `applyTrilinear` + allocation-free `applyTrilinearInto` for hot loops; `parseCube` rejects non-`[0,1]` domain + 1D LUTs + unsupported sizes; `serializeCube` round-trips), `BuiltInLuts.kt` (code-generated `rec709Identity` + `bwBt601` + `bwBt709` + Apache-2.0 `pnsCinematic` teal-orange grade with all magic numbers pinned as private constants), `LutCatalog.kt` (runtime enum with `ALLOWED_SPDX` whitelist enforced via `LutCatalogTest`), `CalibrationProfile.kt` (data class `{wbGains, ccm, bias, mtf50Lpph?, illuminant, capturedAtMs, cameraId, targetId}` + `Identity` companion sub-types + `Illuminant.{D50, D55, D65, StdA, F2}` enum + an `apply(rgb)` method), `CalibrationMath.kt` (WB-gain solver anchored at `g = 1`; 3×3 CCM via normal-equations + Gaussian elimination with partial pivoting; throws on singular systems at `1e-12`), `CalibrationToLut.kt` (apply WB+CCM+bias to a uniform `[0, 1]` 33³ cube and serialize as `.cube` with provenance-encoded TITLE). Closes BUILD_PLAN §7 architecture-seam rows ✓ Lut3D.kt, ✓ LutPipeline.kt, ✓ LutCatalog.kt, ✓ BuiltInLuts.kt; calibration-math rows ✓ computeWbGains, ✓ computeCcm, ✓ CalibrationProfile data class, ✓ CalibrationToLut.toCube; built-in LUT rows ✓ Rec.709 identity, ✓ B&W BT.601, ✓ B&W BT.709, ✓ Cinematic; the SPDX-whitelist enforcement V&V row ✓; the trilinear self-consistency V&V row ✓; the catalog-validation V&V row ✓.
- **Phase 4 calibration sampler + reference targets + MTF50 (host-side)** — `ReferenceTarget.kt` (chart layout data class with `patches`, `patchCenter(row, col, border)`, `patchHalfSize(border)`, `Patch` + `PatchRole` nested types) + `BundledReferenceTargets.kt` (`ColorCheckerClassic24` with the published reference values converted to linear-light sRGB under D50; `Generic24` with 18 hue swatches + 6-step neutral wedge generated deterministically by `hueToRgb`; `byId` + `All` catalog accessors). `CalibrationSampler.kt` (host-side patch sampler + `RgbPlane` data class + `ChartCorners.bilinearMap` chart→pixel mapping + `PatchSample` with mean / variance / sample count / rejection flag + reason; `sample(plane, target, corners)` returns one `PatchSample` per chart patch; default rejection threshold `DEFAULT_MAX_VARIANCE = 5e-3`). `SlantedEdgeMtf.kt` (classical ISO 12233 MTF50 on a single-channel `GrayPlane`: per-row centroid → linear fit → 4× oversampled 128-bin ESF with empty-bin interpolation → differentiate to LSF → Hamming window → direct DFT → linear-interpolate the 50 % crossing; returns cycles/pixel; `cyclesPerPixelToLpph` converts to BUILD_PLAN's lp/ph flavor; `EdgeOrientation.NearVertical` / `NearHorizontal`). Closes BUILD_PLAN §7 reference-targets rows ✓ Generic 24-patch + ✓ ColorChecker Classic 24; work-items rows ✓ `CalibrationSampler.sample` + ✓ `CalibrationSampler.measureMtf50` (now backed by `SlantedEdgeMtf` instead of a stub).
- **Phase 4 cross-cutting hooks (host-side, all 5 closed)** — `LutCreditsBuilder.kt` walks `LutCatalog.entries` filtering by `ALLOWED_SPDX` so the AboutScreen "Color & LUT credits" sub-block can never drift from what is actually bundled; `LutDiagnosticsBuilder.kt` produces the "Color & LUT" section of `diagnostics_<utc>.txt` with active LUT + calibration state + bundled catalog enumeration + SPDX whitelist (auto-derived); `AboutScreen.kt` renders one `LutCreditCard` per `LutCreditRow`; `DiagnosticsMode.dump(context, colorState)` appends the LUT section to every dump; `PerfBudget.Defaults` gains `LUT_SHADER_PER_FRAME_1080P_MS` (2 ms) + `LUT_CPU_STILL_12MP_MS` (80 ms); `PERFORMANCE_BUDGETS.md`, `CAPTURE_ARCHITECTURE.md`, and `FAILURE_MATRIX.md` each gain a "Color & LUT pipeline (Phase 4)" section. Closes BUILD_PLAN §7 cross-cutting rows ✓ Performance budget, ✓ Capture pipeline architecture, ✓ Failure matrix, ✓ About / Heritage, ✓ Diagnostics dump.
- **Phase 4 file-IO + import validation + asset-LUT infra (host-side)**
  - `LutImportValidator.kt` — pure-data validator wrapping `LutPipeline.parseCube` with a sealed `Result.Success(lut) | Result.Failure(category, message, cause)` taxonomy. `FailureCategory` covers `TooLarge`, `MalformedHeader`, `MalformedBody`, `UnsupportedSize`, `OneDLut`, `NonUnitDomain`, `SizeMismatch`, `OutOfRange`. Hard 16 MB payload cap; sample-value tolerance is ±0.001 around `[0, 1]` so professional LUTs that round-trip through fp32 still pass. `Failure.toastMessage()` formats the user-facing string for the SAF picker.
  - `CalibrationProfileStorage.kt` — Android-side file-IO wrapper around `CalibrationProfileJsonAdapter`. `directory(context)` resolves (and creates) `getExternalFilesDir(null)/calibration/`; `save(context, profile)` writes via temp-file + atomic rename so a crashed write never leaves a half-formed JSON document; `load(file)` round-trips back through the adapter; `list(context)` enumerates saved profiles newest-first; `latestFor(context, illuminant)` returns the most recent profile by illuminant. UTC timestamp generation matches the project-wide `yyyyMMdd_HHmmss` convention.
  - `MetadataSerializationGoldenTest.kt` — closes BUILD_PLAN §9 host-side "Golden-file tests for metadata serialization (GroupingID, crop metadata)". Locks the `BracketPlan v1` and `CropPlan v1` text projections so future plan-class refactors that change the on-disk metadata downstream tools see now require a deliberate version bump + CHANGELOG entry.
  - `app/build.gradle.kts` — registers `:app:downloadBundledLuts` (build-time fetch + SHA-256 verify + atomic copy into `assets/luts/<spdx>/<name>/` with `LICENSE.txt` + `SOURCE.txt` + `SHA256.txt` sidecars; idempotent — cache hit is a no-op) and `:app:downloadBundledLutsDryRun` (prints the URL list without fetching). The URL list (`bundledLutSpecs`) is intentionally empty in this milestone; the Gradle infrastructure is shipped + smoke-tested + ready to wire ACES / Filmic / etc. once their pinned upstream URLs and SHA-256s are finalized. Both tasks were verified `BUILD SUCCESSFUL` against an empty list.
  - `scripts/pns_license_inventory.ps1` — extended with `Test-BundledLutFolders`, which walks `app/src/main/assets/luts/<spdx>/<name>/`, asserts every leaf folder has the three required sidecars, and cross-references each leaf folder name against `LICENSES.md`. When the directory does not exist (the expected state until `downloadBundledLuts` lands a real entry) the walker reports `OK: bundled LUTs - no asset-backed LUTs (folder ... does not exist)` and skips silently. Both the OK path and the FAIL path were smoke-tested against a synthetic temp project root. Closes BUILD_PLAN §7 row "License & sourcing rules" host-side scaffold.
  - `scripts/pns_sbom.ps1` — host-side CycloneDX 1.5 SBOM emitter. Reads `gradle/libs.versions.toml` + the SPDX map (mirrored from `pns_license_inventory.ps1`) and writes a CycloneDX-format JSON SBOM to stdout (or `-OutFile`). Each entry carries a `pkg:maven/...` PURL, an SPDX license, and a CycloneDX `scope` (runtime → `required`, debug/test → `optional`, plugin → `excluded`). `-Verify` re-parses + structural-checks the emitted JSON (bomFormat, specVersion, every component has a PURL + license, serialNumber is `urn:uuid:`-prefixed). `pns_verify_toolchain.ps1` invokes `pns_sbom.ps1 -Verify` on every gate run so SBOM drift is caught immediately. 16 components emitted on the current dependency set. Closes BUILD_PLAN §9 row "SBOM generation".
- **Engineering docs**
  - `STORAGE_STRATEGY.md` — MediaStore vs SAF vs app-private decision per profile.
  - `CAPTURE_ARCHITECTURE.md` — thread lanes, executor names, queue depth, backpressure, cancellation, failure handling.
  - `PERFORMANCE_BUDGETS.md` — cold start, per-mode FPS, still-capture latency, video bitrates, memory pressure.
  - `FAILURE_MATRIX.md` — severity-tagged graceful-degradation table for permissions / camera / app-lifecycle / capture-encode / vendor-tag / thermal failure modes.
  - `NDK_PLAN.md` — concrete plan for the libavif (BSD-2) + libjxl (BSD-3) + highway/brotli native-encoder pipeline: pinned-tag `FetchContent` strategy (no upstream sources committed), Gradle/CMake wiring, `NativeEncoders` JNI surface, fallback to JPEG when `pns_native.so` is unavailable.
  - `LICENSES.md` — first-party (declared) dependency inventory with SPDX ids per coordinate (runtime / debug / test / plugin scopes), license-compatibility statement vs Apache-2.0, and a forward-looking row for the planned NDK pipeline (libavif BSD-2, libjxl BSD-3, highway Apache-2.0, brotli MIT, aom BSD-2). Closes BUILD_PLAN §9 "Security/F-Droid hygiene: dependency/license scan gate (FOSS-only)".
  - `COLOR_PIPELINE.md` — source-of-truth for the end-to-end color flow (sensor → demosaic → WB → CCM → tone curve → LUT → encode); per-stage location table (ISP / engine / encoder); per-output-format table showing what each stage does for RAW / AVIF / JXL / JPEG / MP4; the single-most-important rule "**RAW is never baked through a LUT**" with rationale; pinned-numbers table (luma weights, supported LUT sizes, dE_2000 targets, MTF50 floor, SPDX whitelist) cross-referenced to the actual code so doc/code drift is caught at gate time; calibration-mode flow walkthrough (4-corner tap → patch sample → WB + CCM solve → MTF50 → save profile + `.cube`); license-posture clarifications. Closes the BUILD_PLAN §7 row "documents the reference chain".
  - `scripts/pns_license_inventory.ps1` — host-side drift checker: parses `gradle/libs.versions.toml` (`[libraries]` + `[plugins]` blocks) and cross-references against an embedded license map mirroring `LICENSES.md`. Flags catalog deps missing from the map and stale map entries no longer in the catalog. `-DumpMarkdown` prints a refreshable Markdown table. Wired into `pns_verify_toolchain.ps1` (runs on every gate invocation; also encoding + parse-checked alongside the other PowerShell scripts).
  - `keystore.properties.example` — template for local release signing.

### Changed

- README rewritten as a developer-facing pitch + quickstart + documentation index (replaces the garbled UTF-16 trailing block); index reorganized into Product / Engineering / Releases groups.
- `RELEASE_NOTES_TEMPLATE.md` deduplicated and aligned with the toolchain / signed-build verification flow.
- `MainActivity` now wraps content in `PnsTheme` instead of bare `MaterialTheme`.
- `CameraCapabilitiesProbe` home gains "Pro HUD (preview)", "Settings > HUD", "About / Heritage", and "Diagnostics dump" buttons.
- `BUILD_PLAN.md` gains an authoritative **"Verification protocol"** section (after Conventions): tick-eligibility rules per item tag, partial-completion convention, error-handling rules, audit cadence, and a mandatory pre-tick checklist. The protocol formalizes what "complete" means for `[HOST]` / `[ADB]` / `[CI]` items so future sessions cannot mark a checkbox without producing reproducible evidence. First full audit of every `[x]` item recorded in `PROBE_BUILD_PLAN.md` §5: zero drift, zero reverts (all 13 JUnit suites green; 88 tests; PowerShell scripts parse + UTF-8; `pns_verify_toolchain.ps1 -RunTests` PASSED).
- `BUILD_PLAN.md` gains a new **§7 "Phase 4: Color management, calibration & LUT pipeline"** section: in-app calibration mode against a 24-patch reference chart (manual 4-corner tap → patch sampling → CCM + WB-gain solve → MTF50 baseline → `.cube` export + `ColorMatrix1`/`ForwardMatrix1` DNG tags), plus a built-in LUT library (FOSS-only: ACES sRGB↔ACEScct, Filmic Blender, Rec.709 identity, B&W BT.601/709, in-house "Cinematic"; user-imported `.cube` / `.3dl`). LUTs apply on the GLES preview/video path and on the CPU stills path; RAW (DNG) is never baked through a LUT (sidecar reference only). Trailing sections renumbered §8 (Documentation/CI/CD), §9 (Cross-cutting optimizations), §10 (Scope control gates); all forward-looking `BUILD_PLAN §8` references in `CAPTURE_ARCHITECTURE.md`, `PERFORMANCE_BUDGETS.md`, `FAILURE_MATRIX.md`, `STORAGE_STRATEGY.md`, `NDK_PLAN.md`, `LICENSES.md`, `CHANGELOG.md`, `scripts/pns_license_inventory.ps1`, `VendorKeyGuard.kt`, `DiagnosticsMode.kt`, `CapabilityGate.kt`, `PnsLog.kt` updated to `§9`. Historical `PROBE_BUILD_PLAN.md` §5 audit-trail entries preserved as-is. `LICENSES.md` gains a "Bundled LUTs (planned — Phase 4)" table with the SPDX whitelist + sourcing rules.

### Constraints (FOSS hygiene)

- No `com.google.android.gms`, Firebase, ML Kit, Play Core, Play Billing, or Ads dependencies anywhere in the build graph (audited automatically by `pns_verify_toolchain.ps1`).
- Apache-2.0 license retained on all source.

### Pending (not in this release)

- Wiring the capture-pipeline scaffolds into a live engine (`CameraDevice` + `CaptureSession` driving `Dng12Saver` + `CaptureStorage` + `CaptureHaptics`).
- NDK pipeline integration: `libavif` (10-bit HDR) + `libjxl` (12-bit) — gated by Phase 0 probe results.
- 3D tracking persistence; Super Macro hardware lock; histogram pipeline feeding `HighlightMeter`.
- JetBrains Mono TTF vendoring (currently using `FontFamily.Monospace` system fallback).
- **Phase 4 color/LUT pipeline (engine integration)** — host-side math + data classes + tests + design doc (`COLOR_PIPELINE.md`) shipped in this release (Lut3D / LutPipeline / BuiltInLuts / LutCatalog / CalibrationProfile / CalibrationMath / CalibrationToLut + this release's `ReferenceTarget` / `BundledReferenceTargets` / `CalibrationSampler` / `SlantedEdgeMtf` / `LutCreditsBuilder` / `LutDiagnosticsBuilder`; all JUnit-tested). Still pending until the capture engine arrives: GLES `LutShader` (3D-texture upload + `sampler3D` fragment shader + identity bypass), the manual 4-corner-tap calibration UI in Compose, the `Dng12Saver` extension that writes `ColorMatrix1` / `ForwardMatrix1` from the active `CalibrationProfile`, the Gradle `downloadBundledLuts` task (ACES + Filmic with SHA256 pin), the `pns_license_inventory.ps1` LUT-walker extension, the SAF "Import LUT…" picker, and the `.3dl` parser (deferred — multiple incompatible upstream variants; cube is the dominant interchange format).
- GitLab mirror + GitLab CI (intentionally last per project plan).

---

## Release procedure (reminder)

When tagging a release:

1. Run `.\scripts\pns_verify_toolchain.ps1` (must exit 0).
2. Move all items from **Unreleased** into a new `## <version> - YYYY-MM-DD` section.
3. Annotated git tag, then publish a GitHub Release using `RELEASE_NOTES_TEMPLATE.md`.
4. Add a fresh empty `## Unreleased` section above the new versioned section.
