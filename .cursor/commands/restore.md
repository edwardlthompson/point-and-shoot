# Session state restore

Read `.cursor-session-state` if present (schema: `.cursor-session-state.example`).
Restore milestone, sprint, device notes, and last gate artifacts. Re-read `AGENT_MEMORY.md` for drift.
Delete the session state file after restore.

Begin now.
